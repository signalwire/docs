import { GetListOfAvailableEventsDescription } from "./utils";

- **once**(`event`, `fn`)

Attaches an event handler to the specified event. The handler will fire only once.

#### Parameters

| Name    | Type     | Description                                                                                    |
| :------ | :------- | :--------------------------------------------------------------------------------------------- |
| `event` | `string` | Name of the event. <GetListOfAvailableEventsDescription url={props.url} label={props.label} /> |
| `fn`    | Function | An event handler.                                                                              |
