---
title: label
slug: /swml/methods/label
description: Mark any point of the SWML section with a label.
tags: ['swml']
---

import APIField from "@site/src/components/APIField";

Mark any point of the SWML section with a label so that [`goto`](./goto.mdx) can jump to it.


## **label Parameters**

<APIField
  name="label"
  type="string"
  required={true}
>
  Mark any point of the SWML section with a label so that `goto` can jump to it.
</APIField>

## **Examples**

```yaml andJson
version: 1.0.0
sections:
  main:
    - label: foo
    - play:
        url: 'say: This speech will be repeated 4 times.'
    - goto:
        label: foo
        max: 3
```
