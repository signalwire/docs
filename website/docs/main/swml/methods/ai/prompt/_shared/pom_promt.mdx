import APIField from "@site/src/components/APIField";

[pom]: /swml/methods/ai/prompt/pom

export default function PomPrompts() {
  return (
    <APIField
      name="pom"
      type="object[]"
      required={true}
    >
      An array of objects that defines the prompt object model (POM) for the AI. The POM is a structured data format for organizing and rendering a prompt for the AI agent. This prompt will be used to define the AI's personality, role, and other characteristics. See the [POM technical reference][pom] for more information.
    </APIField>
  );
}
