---
slug: /swml/methods/ai/swaig
title: ai.SWAIG
description: The SignalWire AI Gateway Interface. Allows you to create user-defined functions that can be executed during the dialogue.
tags: ['swml']
---

import APIField from "@site/src/components/APIField";

The SignalWire AI Gateway Interface. Allows you to create user-defined functions that can be executed during the dialogue.

<APIField
  name="SWAIG"
  type="object"
>
  An object that contains the [`SWAIG parameters`](#swaig-parameters).
</APIField>

## **SWAIG Parameters**

<APIField
  name="defaults"
  type="object"
>
  Default settings for all SWAIG functions. If `defaults` is not set, settings may be set in each function object. Default is not set.

  See [`defaults`](/swml/methods/ai/swaig/defaults) for additional details.
</APIField>

<APIField
  name="functions"
  type="object[]"
>
  An array of JSON objects to define functions that can be executed during the interaction with the AI. Default is not set. The fields of this object are the six following.

  See [`functions`](/swml/methods/ai/swaig/functions) for additional details.
</APIField>

<APIField
  name="includes"
  type="object[]"
>
  An array of objects to include remote function signatures. This allows you to include functions that are defined in a remote location.

  See [`includes`](/swml/methods/ai/swaig/includes) for additional details.
</APIField>

<APIField
  name="internal_fillers"
  type="object"
>
  An object containing language-specific arrays of filler phrases. Must specify exactly one language code.

  See [`internal_fillers`](/swml/methods/ai/swaig/internal_fillers) for additional details.
</APIField>

<APIField
  name="native_functions"
  type="string[]"
>
  Prebuilt functions the AI agent is able to call (from [this list of available native functions](./native_functions/index.mdx#available-functions)).

  See [`native_functions`](/swml/methods/ai/swaig/native_functions) for additional details.
</APIField>
