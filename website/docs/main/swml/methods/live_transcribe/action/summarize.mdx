---
title: action.summarize
sidebar_label: action.summarize
slug: /swml/methods/live_transcribe/action/summarize
description: Summarize the live transcription.
tags: ['swml']
---

import APIField from "@site/src/components/APIField";

# action.summarize

Summarizes the conversation.

<APIField
  name="summarize"
  type="object"
  required={true}
>
  An object that contains the [`summarize parameters`](#summarize-parameters).
</APIField>


## **summarize Parameters**

<APIField
  name="webhook"
  type="string"
>
  The webhook URI to be called. Authentication can also be set in the url in the format of `username:password@url`.
</APIField>

<APIField
  name="prompt"
  type="string"
>
  The prompt for summarization.
</APIField>


## **Object Example**

```yaml andJson
live_transcribe:
  action:
    summarize:
      webhook: 'https://example.com/webhook'
      prompt: Summarize the key points of this conversation.
```

## **Default Summarization**

If the `summarize` action is called with a empty object, the default summarization prompt and webhook will be used.

```yaml andJson
live_transcribe:
  action:
    summarize: {}
```

