# dial()

The `dial()` method allows you to dial any number or resource within the Call Fabric.

```javascript
const callSession = await client.dial({
  to: '1234567890',
});
```

## Parameters

- `to`: The number or resource to dial.