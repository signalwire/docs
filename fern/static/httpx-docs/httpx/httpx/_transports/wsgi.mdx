---
layout: overview
slug: httpx/httpx/_transports/wsgi
title: httpx._transports.wsgi
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`WSGIByteStream`](#httpx-_transports-wsgi-WSGIByteStream) | - |
| [`WSGITransport`](#httpx-_transports-wsgi-WSGITransport) | A custom transport that handles sending requests directly to an WSGI app. |

### Functions

| Name | Description |
|------|-------------|
| [`_skip_leading_empty_chunks`](#httpx-_transports-wsgi-_skip_leading_empty_chunks) | - |

### Data

[`_T`](#httpx-_transports-wsgi-_T)

[`__all__`](#httpx-_transports-wsgi-__all__)

### API

<Anchor id="httpx-_transports-wsgi-WSGIByteStream">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._transports.wsgi.WSGIByteStream(
    result: typing.Iterable[bytes]
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [SyncByteStream](/httpx/httpx/_types#httpx-_types-SyncByteStream)

<ParamField path="_close" type="= getattr(result, 'close', None)">
</ParamField>

<ParamField path="_result" type="= _skip_leading_empty_chunks(result)">
</ParamField>
<Anchor id="httpx-_transports-wsgi-WSGIByteStream-__iter__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._transports.wsgi.WSGIByteStream.__iter__() -> typing.Iterator[bytes]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_transports-wsgi-WSGIByteStream-close">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._transports.wsgi.WSGIByteStream.close() -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="httpx-_transports-wsgi-WSGITransport">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._transports.wsgi.WSGITransport(
    app: _typeshed.wsgi.WSGIApplication,
    raise_app_exceptions: bool = True,
    script_name: str = '',
    remote_addr: str = '127.0.0.1',
    wsgi_errors: typing.TextIO | None = None
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [BaseTransport](/httpx/httpx/_transports/base#httpx-_transports-base-BaseTransport)

A custom transport that handles sending requests directly to an WSGI app.
The simplest way to use this functionality is to use the `app` argument.

<CodeBlock showLineNumbers={false}>

```python
client = httpx.Client(app=app)
```

</CodeBlock>

Alternatively, you can setup the transport instance explicitly.
This allows you to include any additional configuration arguments specific
to the WSGITransport class:

<CodeBlock showLineNumbers={false}>

```python
transport = httpx.WSGITransport(
    app=app,
    script_name="/submount",
    remote_addr="1.2.3.4"
)
client = httpx.Client(transport=transport)
```

</CodeBlock>

Arguments:

* `app` - The WSGI application.
* `raise_app_exceptions` - Boolean indicating if exceptions in the application
   should be raised. Default to `True`. Can be set to `False` for use cases
   such as testing the content of a client 500 response.
* `script_name` - The root path on which the WSGI application should be mounted.
* `remote_addr` - A string indicating the client IP of incoming requests.
```


<Anchor id="httpx-_transports-wsgi-WSGITransport-handle_request">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._transports.wsgi.WSGITransport.handle_request(
    request: httpx._models.Request
) -> httpx._models.Response
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="httpx-_transports-wsgi-_skip_leading_empty_chunks">

<CodeBlock links={{"httpx._transports.wsgi._T":"#httpx-_transports-wsgi-_T"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._transports.wsgi._skip_leading_empty_chunks(
    body: typing.Iterable[httpx._transports.wsgi._T]
) -> typing.Iterable[httpx._transports.wsgi._T]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="httpx-_transports-wsgi-_T">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._transports.wsgi._T = typing.TypeVar('_T')
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_transports-wsgi-__all__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._transports.wsgi.__all__ = ['WSGITransport']
```

</CodeBlock>
</Anchor>

