---
layout: overview
slug: httpx/httpx/_auth
title: httpx._auth
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`Auth`](#httpx-_auth-Auth) | Base class for all authentication schemes. |
| [`BasicAuth`](#httpx-_auth-BasicAuth) | Allows the 'auth' argument to be passed as a (username, password) pair, |
| [`DigestAuth`](#httpx-_auth-DigestAuth) | - |
| [`FunctionAuth`](#httpx-_auth-FunctionAuth) | Allows the 'auth' argument to be passed as a simple callable function, |
| [`NetRCAuth`](#httpx-_auth-NetRCAuth) | Use a 'netrc' file to lookup basic auth credentials based on the url host. |
| [`_DigestAuthChallenge`](#httpx-_auth-_DigestAuthChallenge) | - |

### Data

[`__all__`](#httpx-_auth-__all__)

### API

<Anchor id="httpx-_auth-Auth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._auth.Auth()
```

</CodeBlock>
</Anchor>

<Indent>

Base class for all authentication schemes.

To implement a custom authentication scheme, subclass `Auth` and override
the `.auth_flow()` method.

If the authentication scheme does I/O such as disk access or network calls, or uses
synchronization primitives such as locks, you should override `.sync_auth_flow()`
and/or `.async_auth_flow()` instead of `.auth_flow()` to provide specialized
implementations that will be used by `Client` and `AsyncClient` respectively.


<Anchor id="httpx-_auth-Auth-async_auth_flow">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.Auth.async_auth_flow(
    request: httpx._models.Request
) -> typing.AsyncGenerator[httpx._models.Request, httpx._models.Response]
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Execute the authentication flow asynchronously.

By default, this defers to `.auth_flow()`. You should override this method
when the authentication scheme does I/O and/or uses concurrency primitives.


</Indent>
<Anchor id="httpx-_auth-Auth-auth_flow">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.Auth.auth_flow(
    request: httpx._models.Request
) -> typing.Generator[httpx._models.Request, httpx._models.Response, None]
```

</CodeBlock>
</Anchor>

<Indent>

Execute the authentication flow.

To dispatch a request, `yield` it:

<CodeBlock showLineNumbers={false}>

```python
yield request
```

</CodeBlock>

The client will `.send()` the response back into the flow generator. You can
access it like so:

<CodeBlock showLineNumbers={false}>

```python
response = yield request
```

</CodeBlock>

A `return` (or reaching the end of the generator) will result in the
client returning the last response obtained from the server.

You can dispatch as many requests as is necessary.


</Indent>
<Anchor id="httpx-_auth-Auth-sync_auth_flow">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.Auth.sync_auth_flow(
    request: httpx._models.Request
) -> typing.Generator[httpx._models.Request, httpx._models.Response, None]
```

</CodeBlock>
</Anchor>

<Indent>

Execute the authentication flow synchronously.

By default, this defers to `.auth_flow()`. You should override this method
when the authentication scheme does I/O and/or uses concurrency primitives.


</Indent>
</Indent>

<Anchor id="httpx-_auth-BasicAuth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._auth.BasicAuth(
    username: str | bytes,
    password: str | bytes
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [Auth](#httpx-_auth-Auth)

Allows the 'auth' argument to be passed as a (username, password) pair,
and uses HTTP Basic authentication.


<ParamField path="_auth_header" type="= self._build_auth_header(username, password)">
</ParamField>
<Anchor id="httpx-_auth-BasicAuth-_build_auth_header">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.BasicAuth._build_auth_header(
    username: str | bytes,
    password: str | bytes
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_auth-BasicAuth-auth_flow">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.BasicAuth.auth_flow(
    request: httpx._models.Request
) -> typing.Generator[httpx._models.Request, httpx._models.Response, None]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="httpx-_auth-DigestAuth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._auth.DigestAuth(
    username: str | bytes,
    password: str | bytes
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [Auth](#httpx-_auth-Auth)

<ParamField path="_ALGORITHM_TO_HASH_FUNCTION" type="dict[str, Callable[[bytes], _Hash]]">
</ParamField>

<ParamField path="_last_challenge" type="_DigestAuthChallenge | None = None">
</ParamField>

<ParamField path="_nonce_count" type="= 1">
</ParamField>

<ParamField path="_password" type="= to_bytes(password)">
</ParamField>

<ParamField path="_username" type="= to_bytes(username)">
</ParamField>
<Anchor id="httpx-_auth-DigestAuth-_build_auth_header">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._auth._DigestAuthChallenge":"#httpx-_auth-_DigestAuthChallenge"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.DigestAuth._build_auth_header(
    request: httpx._models.Request,
    challenge: httpx._auth._DigestAuthChallenge
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_auth-DigestAuth-_get_client_nonce">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.DigestAuth._get_client_nonce(
    nonce_count: int,
    nonce: bytes
) -> bytes
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_auth-DigestAuth-_get_header_value">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.DigestAuth._get_header_value(
    header_fields: dict[str, bytes]
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_auth-DigestAuth-_parse_challenge">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response","httpx._auth._DigestAuthChallenge":"#httpx-_auth-_DigestAuthChallenge"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.DigestAuth._parse_challenge(
    request: httpx._models.Request,
    response: httpx._models.Response,
    auth_header: str
) -> httpx._auth._DigestAuthChallenge
```

</CodeBlock>
</Anchor>

<Indent>

Returns a challenge from a Digest WWW-Authenticate header.
These take the form of:
`Digest realm="realm@host.com",qop="auth,auth-int",nonce="abc",opaque="xyz"`


</Indent>
<Anchor id="httpx-_auth-DigestAuth-_resolve_qop">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.DigestAuth._resolve_qop(
    qop: bytes | None,
    request: httpx._models.Request
) -> bytes | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_auth-DigestAuth-auth_flow">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.DigestAuth.auth_flow(
    request: httpx._models.Request
) -> typing.Generator[httpx._models.Request, httpx._models.Response, None]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="httpx-_auth-FunctionAuth">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request"}} showLineNumbers={false} wordWrap={true}>

```python
class httpx._auth.FunctionAuth(
    func: typing.Callable[[Request], httpx._models.Request]
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [Auth](#httpx-_auth-Auth)

Allows the 'auth' argument to be passed as a simple callable function,
that takes the request, and returns a new, modified request.


<Anchor id="httpx-_auth-FunctionAuth-auth_flow">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.FunctionAuth.auth_flow(
    request: httpx._models.Request
) -> typing.Generator[httpx._models.Request, httpx._models.Response, None]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="httpx-_auth-NetRCAuth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._auth.NetRCAuth(
    file: str | None = None
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [Auth](#httpx-_auth-Auth)

Use a 'netrc' file to lookup basic auth credentials based on the url host.


<ParamField path="_netrc_info" type="= netrc.netrc(file)">
</ParamField>
<Anchor id="httpx-_auth-NetRCAuth-_build_auth_header">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.NetRCAuth._build_auth_header(
    username: str | bytes,
    password: str | bytes
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_auth-NetRCAuth-auth_flow">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.NetRCAuth.auth_flow(
    request: httpx._models.Request
) -> typing.Generator[httpx._models.Request, httpx._models.Response, None]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="httpx-_auth-_DigestAuthChallenge">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._auth._DigestAuthChallenge()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `NamedTuple`

<ParamField path="algorithm" type="str">
</ParamField>

<ParamField path="nonce" type="bytes">
</ParamField>

<ParamField path="opaque" type="bytes | None">
</ParamField>

<ParamField path="qop" type="bytes | None">
</ParamField>

<ParamField path="realm" type="bytes">
</ParamField>
</Indent>

<Anchor id="httpx-_auth-__all__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._auth.__all__ = ['Auth', 'BasicAuth', 'DigestAuth', 'FunctionAuth', 'NetRCAuth']
```

</CodeBlock>
</Anchor>

