---
layout: overview
slug: httpx/httpx/_exceptions
title: httpx._exceptions
---

Our exception hierarchy:

* HTTPError
  x RequestError
    + TransportError
      - TimeoutException
        · ConnectTimeout
        · ReadTimeout
        · WriteTimeout
        · PoolTimeout
      - NetworkError
        · ConnectError
        · ReadError
        · WriteError
        · CloseError
      - ProtocolError
        · LocalProtocolError
        · RemoteProtocolError
      - ProxyError
      - UnsupportedProtocol
    + DecodingError
    + TooManyRedirects
  x HTTPStatusError
* InvalidURL
* CookieConflict
* StreamError
  x StreamConsumed
  x StreamClosed
  x ResponseNotRead
  x RequestNotRead

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`CloseError`](#httpx-_exceptions-CloseError) | Failed to close a connection. |
| [`ConnectError`](#httpx-_exceptions-ConnectError) | Failed to establish a connection. |
| [`ConnectTimeout`](#httpx-_exceptions-ConnectTimeout) | Timed out while connecting to the host. |
| [`CookieConflict`](#httpx-_exceptions-CookieConflict) | Attempted to lookup a cookie by name, but multiple cookies existed. |
| [`DecodingError`](#httpx-_exceptions-DecodingError) | Decoding of the response failed, due to a malformed encoding. |
| [`HTTPError`](#httpx-_exceptions-HTTPError) | Base class for `RequestError` and `HTTPStatusError`. |
| [`HTTPStatusError`](#httpx-_exceptions-HTTPStatusError) | The response had an error HTTP status of 4xx or 5xx. |
| [`InvalidURL`](#httpx-_exceptions-InvalidURL) | URL is improperly formed or cannot be parsed. |
| [`LocalProtocolError`](#httpx-_exceptions-LocalProtocolError) | A protocol was violated by the client. |
| [`NetworkError`](#httpx-_exceptions-NetworkError) | The base class for network-related errors. |
| [`PoolTimeout`](#httpx-_exceptions-PoolTimeout) | Timed out waiting to acquire a connection from the pool. |
| [`ProtocolError`](#httpx-_exceptions-ProtocolError) | The protocol was violated. |
| [`ProxyError`](#httpx-_exceptions-ProxyError) | An error occurred while establishing a proxy connection. |
| [`ReadError`](#httpx-_exceptions-ReadError) | Failed to receive data from the network. |
| [`ReadTimeout`](#httpx-_exceptions-ReadTimeout) | Timed out while receiving data from the host. |
| [`RemoteProtocolError`](#httpx-_exceptions-RemoteProtocolError) | The protocol was violated by the server. |
| [`RequestError`](#httpx-_exceptions-RequestError) | Base class for all exceptions that may occur when issuing a `.request()`. |
| [`RequestNotRead`](#httpx-_exceptions-RequestNotRead) | Attempted to access streaming request content, without having called `read()`. |
| [`ResponseNotRead`](#httpx-_exceptions-ResponseNotRead) | Attempted to access streaming response content, without having called `read()`. |
| [`StreamClosed`](#httpx-_exceptions-StreamClosed) | Attempted to read or stream response content, but the request has been |
| [`StreamConsumed`](#httpx-_exceptions-StreamConsumed) | Attempted to read or stream content, but the content has already |
| [`StreamError`](#httpx-_exceptions-StreamError) | The base class for stream exceptions. |
| [`TimeoutException`](#httpx-_exceptions-TimeoutException) | The base class for timeout errors. |
| [`TooManyRedirects`](#httpx-_exceptions-TooManyRedirects) | Too many redirects. |
| [`TransportError`](#httpx-_exceptions-TransportError) | Base class for all exceptions that occur at the level of the Transport API. |
| [`UnsupportedProtocol`](#httpx-_exceptions-UnsupportedProtocol) | Attempted to make a request to an unsupported protocol. |
| [`WriteError`](#httpx-_exceptions-WriteError) | Failed to send data through the network. |
| [`WriteTimeout`](#httpx-_exceptions-WriteTimeout) | Timed out while sending data to the host. |

### Functions

| Name | Description |
|------|-------------|
| [`request_context`](#httpx-_exceptions-request_context) | A context manager that can be used to attach the given request context |

### Data

[`__all__`](#httpx-_exceptions-__all__)

### API

<Anchor id="httpx-_exceptions-CloseError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.CloseError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [NetworkError](#httpx-_exceptions-NetworkError)

Failed to close a connection.


</Indent>

<Anchor id="httpx-_exceptions-ConnectError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.ConnectError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [NetworkError](#httpx-_exceptions-NetworkError)

Failed to establish a connection.


</Indent>

<Anchor id="httpx-_exceptions-ConnectTimeout">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.ConnectTimeout()
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** [TimeoutException](#httpx-_exceptions-TimeoutException)

Timed out while connecting to the host.


</Indent>

<Anchor id="httpx-_exceptions-CookieConflict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.CookieConflict(
    message: str
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** `Exception`

Attempted to lookup a cookie by name, but multiple cookies existed.

Can occur when calling `response.cookies.get(...)`.


</Indent>

<Anchor id="httpx-_exceptions-DecodingError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.DecodingError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [RequestError](#httpx-_exceptions-RequestError)

Decoding of the response failed, due to a malformed encoding.


</Indent>

<Anchor id="httpx-_exceptions-HTTPError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.HTTPError(
    message: str
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** `Exception`

Base class for `RequestError` and `HTTPStatusError`.

Useful for `try...except` blocks when issuing a request,
and then calling `.raise_for_status()`.

For example:

<CodeBlock showLineNumbers={false}>

```python
try:
    response = httpx.get("https://www.example.com")
    response.raise_for_status()
except httpx.HTTPError as exc:
    print(f"HTTP Exception for {exc.request.url} - {exc}")
```

</CodeBlock>


<ParamField path="_request" type="Request | None = None">
</ParamField>

<ParamField path="request" type="Request">
</ParamField>
</Indent>

<Anchor id="httpx-_exceptions-HTTPStatusError">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request","httpx._models.Response":"/httpx/httpx/_models#httpx-_models-Response"}} showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.HTTPStatusError(
    message: str,
    request: httpx._models.Request,
    response: httpx._models.Response
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [HTTPError](#httpx-_exceptions-HTTPError)

The response had an error HTTP status of 4xx or 5xx.

May be raised when calling `response.raise_for_status()`


</Indent>

<Anchor id="httpx-_exceptions-InvalidURL">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.InvalidURL(
    message: str
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** `Exception`

URL is improperly formed or cannot be parsed.


</Indent>

<Anchor id="httpx-_exceptions-LocalProtocolError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.LocalProtocolError()
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Protocol</Badge>

**Bases:** [ProtocolError](#httpx-_exceptions-ProtocolError)

A protocol was violated by the client.

For example if the user instantiated a `Request` instance explicitly,
failed to include the mandatory `Host:` header, and then issued it directly
using `client.send()`.


</Indent>

<Anchor id="httpx-_exceptions-NetworkError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.NetworkError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [TransportError](#httpx-_exceptions-TransportError)

The base class for network-related errors.

An error occurred while interacting with the network.


</Indent>

<Anchor id="httpx-_exceptions-PoolTimeout">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.PoolTimeout()
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** [TimeoutException](#httpx-_exceptions-TimeoutException)

Timed out waiting to acquire a connection from the pool.


</Indent>

<Anchor id="httpx-_exceptions-ProtocolError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.ProtocolError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [TransportError](#httpx-_exceptions-TransportError)

The protocol was violated.


</Indent>

<Anchor id="httpx-_exceptions-ProxyError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.ProxyError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [TransportError](#httpx-_exceptions-TransportError)

An error occurred while establishing a proxy connection.


</Indent>

<Anchor id="httpx-_exceptions-ReadError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.ReadError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [NetworkError](#httpx-_exceptions-NetworkError)

Failed to receive data from the network.


</Indent>

<Anchor id="httpx-_exceptions-ReadTimeout">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.ReadTimeout()
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** [TimeoutException](#httpx-_exceptions-TimeoutException)

Timed out while receiving data from the host.


</Indent>

<Anchor id="httpx-_exceptions-RemoteProtocolError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.RemoteProtocolError()
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Protocol</Badge>

**Bases:** [ProtocolError](#httpx-_exceptions-ProtocolError)

The protocol was violated by the server.

For example, returning malformed HTTP.


</Indent>

<Anchor id="httpx-_exceptions-RequestError">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request"}} showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.RequestError(
    message: str,
    request: httpx._models.Request | None = None
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [HTTPError](#httpx-_exceptions-HTTPError)

Base class for all exceptions that may occur when issuing a `.request()`.


</Indent>

<Anchor id="httpx-_exceptions-RequestNotRead">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.RequestNotRead()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [StreamError](#httpx-_exceptions-StreamError)

Attempted to access streaming request content, without having called `read()`.


</Indent>

<Anchor id="httpx-_exceptions-ResponseNotRead">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.ResponseNotRead()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [StreamError](#httpx-_exceptions-StreamError)

Attempted to access streaming response content, without having called `read()`.


</Indent>

<Anchor id="httpx-_exceptions-StreamClosed">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.StreamClosed()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [StreamError](#httpx-_exceptions-StreamError)

Attempted to read or stream response content, but the request has been
closed.


</Indent>

<Anchor id="httpx-_exceptions-StreamConsumed">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.StreamConsumed()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [StreamError](#httpx-_exceptions-StreamError)

Attempted to read or stream content, but the content has already
been streamed.


</Indent>

<Anchor id="httpx-_exceptions-StreamError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.StreamError(
    message: str
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `RuntimeError`

The base class for stream exceptions.

The developer made an error in accessing the request stream in
an invalid way.


</Indent>

<Anchor id="httpx-_exceptions-TimeoutException">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.TimeoutException()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [TransportError](#httpx-_exceptions-TransportError)

The base class for timeout errors.

An operation has timed out.


</Indent>

<Anchor id="httpx-_exceptions-TooManyRedirects">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.TooManyRedirects()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [RequestError](#httpx-_exceptions-RequestError)

Too many redirects.


</Indent>

<Anchor id="httpx-_exceptions-TransportError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.TransportError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [RequestError](#httpx-_exceptions-RequestError)

Base class for all exceptions that occur at the level of the Transport API.


</Indent>

<Anchor id="httpx-_exceptions-UnsupportedProtocol">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.UnsupportedProtocol()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [TransportError](#httpx-_exceptions-TransportError)

Attempted to make a request to an unsupported protocol.

For example issuing a request to `ftp://www.example.com`.


</Indent>

<Anchor id="httpx-_exceptions-WriteError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.WriteError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [NetworkError](#httpx-_exceptions-NetworkError)

Failed to send data through the network.


</Indent>

<Anchor id="httpx-_exceptions-WriteTimeout">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._exceptions.WriteTimeout()
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** [TimeoutException](#httpx-_exceptions-TimeoutException)

Timed out while sending data to the host.


</Indent>

<Anchor id="httpx-_exceptions-request_context">

<CodeBlock links={{"httpx._models.Request":"/httpx/httpx/_models#httpx-_models-Request"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._exceptions.request_context(
    request: httpx._models.Request | None = None
) -> typing.Iterator[None]
```

</CodeBlock>
</Anchor>

<Indent>

A context manager that can be used to attach the given request context
to any `RequestError` exceptions that are raised within the block.


</Indent>

<Anchor id="httpx-_exceptions-__all__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._exceptions.__all__ = ['CloseError', 'ConnectError', 'ConnectTimeout', 'CookieConflict', 'DecodingErro...
```

</CodeBlock>
</Anchor>

