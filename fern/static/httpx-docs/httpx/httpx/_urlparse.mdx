---
layout: overview
slug: httpx/httpx/_urlparse
title: httpx._urlparse
---

An implementation of `urlparse` that provides URL validation and normalization
as described by RFC3986.

We rely on this implementation rather than the one in Python's stdlib, because:

* It provides more complete URL validation.
* It properly differentiates between an empty querystring and an absent querystring,
  to distinguish URLs with a trailing '?'.
* It handles scheme, hostname, port, and path normalization.
* It supports IDNA hostnames, normalizing them to their encoded form.
* The API supports passing individual components, as well as the complete URL string.

Previously we relied on the excellent `rfc3986` package to handle URL parsing and
validation, but this module provides a simpler alternative, with less indirection
required.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`ParseResult`](#httpx-_urlparse-ParseResult) | - |

### Functions

| Name | Description |
|------|-------------|
| [`PERCENT`](#httpx-_urlparse-PERCENT) | - |
| [`encode_host`](#httpx-_urlparse-encode_host) | - |
| [`normalize_path`](#httpx-_urlparse-normalize_path) | Drop "." and ".." segments from a URL path. |
| [`normalize_port`](#httpx-_urlparse-normalize_port) | - |
| [`percent_encoded`](#httpx-_urlparse-percent_encoded) | Use percent-encoding to quote a string. |
| [`quote`](#httpx-_urlparse-quote) | Use percent-encoding to quote a string, omitting existing '%xx' escape sequences. |
| [`urlparse`](#httpx-_urlparse-urlparse) | - |
| [`validate_path`](#httpx-_urlparse-validate_path) | Path validation rules that depend on if the URL contains |

### Data

[`AUTHORITY_REGEX`](#httpx-_urlparse-AUTHORITY_REGEX)

[`COMPONENT_REGEX`](#httpx-_urlparse-COMPONENT_REGEX)

[`FRAG_SAFE`](#httpx-_urlparse-FRAG_SAFE)

[`IPv4_STYLE_HOSTNAME`](#httpx-_urlparse-IPv4_STYLE_HOSTNAME)

[`IPv6_STYLE_HOSTNAME`](#httpx-_urlparse-IPv6_STYLE_HOSTNAME)

[`MAX_URL_LENGTH`](#httpx-_urlparse-MAX_URL_LENGTH)

[`PASSWORD_SAFE`](#httpx-_urlparse-PASSWORD_SAFE)

[`PATH_SAFE`](#httpx-_urlparse-PATH_SAFE)

[`PERCENT_ENCODED_REGEX`](#httpx-_urlparse-PERCENT_ENCODED_REGEX)

[`QUERY_SAFE`](#httpx-_urlparse-QUERY_SAFE)

[`SUB_DELIMS`](#httpx-_urlparse-SUB_DELIMS)

[`UNRESERVED_CHARACTERS`](#httpx-_urlparse-UNRESERVED_CHARACTERS)

[`URL_REGEX`](#httpx-_urlparse-URL_REGEX)

[`USERINFO_SAFE`](#httpx-_urlparse-USERINFO_SAFE)

[`USERNAME_SAFE`](#httpx-_urlparse-USERNAME_SAFE)

### API

<Anchor id="httpx-_urlparse-ParseResult">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class httpx._urlparse.ParseResult()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `NamedTuple`

<ParamField path="authority" type="str">
</ParamField>

<ParamField path="fragment" type="str | None">
</ParamField>

<ParamField path="host" type="str">
</ParamField>

<ParamField path="netloc" type="str">
</ParamField>

<ParamField path="path" type="str">
</ParamField>

<ParamField path="port" type="int | None">
</ParamField>

<ParamField path="query" type="str | None">
</ParamField>

<ParamField path="scheme" type="str">
</ParamField>

<ParamField path="userinfo" type="str">
</ParamField>
<Anchor id="httpx-_urlparse-ParseResult-__str__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.ParseResult.__str__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="httpx-_urlparse-ParseResult-copy_with">

<CodeBlock links={{"httpx._urlparse.ParseResult":"#httpx-_urlparse-ParseResult"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.ParseResult.copy_with(
    kwargs: str | None = {}
) -> httpx._urlparse.ParseResult
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="httpx-_urlparse-PERCENT">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.PERCENT(
    string: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="httpx-_urlparse-encode_host">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.encode_host(
    host: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="httpx-_urlparse-normalize_path">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.normalize_path(
    path: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Drop "." and ".." segments from a URL path.

For example:

    normalize_path("/path/./to/somewhere/..") == "/path/to"


</Indent>

<Anchor id="httpx-_urlparse-normalize_port">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.normalize_port(
    port: str | int | None,
    scheme: str
) -> int | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="httpx-_urlparse-percent_encoded">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.percent_encoded(
    string: str,
    safe: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Use percent-encoding to quote a string.


</Indent>

<Anchor id="httpx-_urlparse-quote">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.quote(
    string: str,
    safe: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Use percent-encoding to quote a string, omitting existing '%xx' escape sequences.

See: https://www.rfc-editor.org/rfc/rfc3986#section-2.1

* `string`: The string to be percent-escaped.
* `safe`: A string containing characters that may be treated as safe, and do not
    need to be escaped. Unreserved characters are always treated as safe.
    See: https://www.rfc-editor.org/rfc/rfc3986#section-2.3


</Indent>

<Anchor id="httpx-_urlparse-urlparse">

<CodeBlock links={{"httpx._urlparse.ParseResult":"#httpx-_urlparse-ParseResult"}} showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.urlparse(
    url: str = '',
    kwargs: str | None = {}
) -> httpx._urlparse.ParseResult
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="httpx-_urlparse-validate_path">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.validate_path(
    path: str,
    has_scheme: bool,
    has_authority: bool
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Path validation rules that depend on if the URL contains
a scheme or authority component.

See https://datatracker.ietf.org/doc/html/rfc3986.html#section-3.3


</Indent>

<Anchor id="httpx-_urlparse-AUTHORITY_REGEX">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.AUTHORITY_REGEX = re.compile('(?:(?P<userinfo>{userinfo})@)?(?P<host>{host}):?(?P<port>{port})?'.f...
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-COMPONENT_REGEX">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.COMPONENT_REGEX = {'scheme': re.compile('([a-zA-Z][a-zA-Z0-9+.-]*)?'), 'authority': re.compile('[^...
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-FRAG_SAFE">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.FRAG_SAFE = ''.join([(chr(i)) for i in (range(32, 127)) if i not in (32, 34, 60, 62, 96)])
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-IPv4_STYLE_HOSTNAME">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.IPv4_STYLE_HOSTNAME = re.compile('^[0-9]+\\.[0-9]+\\.[0-9]+\\.[0-9]+$')
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-IPv6_STYLE_HOSTNAME">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.IPv6_STYLE_HOSTNAME = re.compile('^\\[.*\\]$')
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-MAX_URL_LENGTH">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.MAX_URL_LENGTH = 65536
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-PASSWORD_SAFE">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.PASSWORD_SAFE = ''.join([(chr(i)) for i in (range(32, 127)) if i not in (32, 34, 35, 60, 62) + (...
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-PATH_SAFE">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.PATH_SAFE = ''.join([(chr(i)) for i in (range(32, 127)) if i not in (32, 34, 35, 60, 62) + (...
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-PERCENT_ENCODED_REGEX">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.PERCENT_ENCODED_REGEX = re.compile('%[A-Fa-f0-9]{2}')
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-QUERY_SAFE">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.QUERY_SAFE = ''.join([(chr(i)) for i in (range(32, 127)) if i not in (32, 34, 35, 60, 62)])
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-SUB_DELIMS">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.SUB_DELIMS = "!$&'()*+,;="
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-UNRESERVED_CHARACTERS">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.UNRESERVED_CHARACTERS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-._~'
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-URL_REGEX">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.URL_REGEX = re.compile('(?:(?P<scheme>{scheme}):)?(?://(?P<authority>{authority}))?(?P<path>...
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-USERINFO_SAFE">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.USERINFO_SAFE = ''.join([(chr(i)) for i in (range(32, 127)) if i not in (32, 34, 35, 60, 62) + (...
```

</CodeBlock>
</Anchor>


<Anchor id="httpx-_urlparse-USERNAME_SAFE">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
httpx._urlparse.USERNAME_SAFE = ''.join([(chr(i)) for i in (range(32, 127)) if i not in (32, 34, 35, 60, 62) + (...
```

</CodeBlock>
</Anchor>

