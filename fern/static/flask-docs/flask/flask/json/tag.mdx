---
layout: overview
slug: flask/flask/json/tag
title: flask.json.tag
---

Tagged JSON
~~~~~~~~~~~

A compact representation for lossless serialization of non-standard JSON
types. :class:`~flask.sessions.SecureCookieSessionInterface` uses this
to serialize the session data, but it may be useful in other places. It
can be extended to support other types.

.. autoclass:: TaggedJSONSerializer
    :members:

.. autoclass:: JSONTag
    :members:

Let's see an example that adds support for
:class:`~collections.OrderedDict`. Dicts don't have an order in JSON, so
to handle this we will dump the items as a list of ``[key, value]``
pairs. Subclass :class:`JSONTag` and give it the new key ``' od'`` to
identify the type. The session serializer processes dicts first, so
insert the new tag at the front of the order since ``OrderedDict`` must
be processed before ``dict``.

.. code-block:: python

    from flask.json.tag import JSONTag

    class TagOrderedDict(JSONTag):
        __slots__ = ('serializer',)
        key = ' od'

        def check(self, value):
            return isinstance(value, OrderedDict)

        def to_json(self, value):
            return [[k, self.serializer.tag(v)] for k, v in iteritems(value)]

        def to_python(self, value):
            return OrderedDict(value)

    app.session_interface.serializer.register(TagOrderedDict, index=0)

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`JSONTag`](#flask-json-tag-JSONTag) | Base class for defining type tags for :class:`TaggedJSONSerializer`. |
| [`PassDict`](#flask-json-tag-PassDict) | - |
| [`PassList`](#flask-json-tag-PassList) | - |
| [`TagBytes`](#flask-json-tag-TagBytes) | - |
| [`TagDateTime`](#flask-json-tag-TagDateTime) | - |
| [`TagDict`](#flask-json-tag-TagDict) | Tag for 1-item dicts whose only key matches a registered tag. |
| [`TagMarkup`](#flask-json-tag-TagMarkup) | Serialize anything matching the :class:`~markupsafe.Markup` API by |
| [`TagTuple`](#flask-json-tag-TagTuple) | - |
| [`TagUUID`](#flask-json-tag-TagUUID) | - |
| [`TaggedJSONSerializer`](#flask-json-tag-TaggedJSONSerializer) | Serializer that uses a tag system to compactly represent objects that |

### API

<Anchor id="flask-json-tag-JSONTag">

<CodeBlock links={{"flask.json.tag.TaggedJSONSerializer":"#flask-json-tag-TaggedJSONSerializer"}} showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.JSONTag(
    serializer: flask.json.tag.TaggedJSONSerializer
)
```

</CodeBlock>
</Anchor>

<Indent>

Base class for defining type tags for :class:`TaggedJSONSerializer`.


<ParamField path="__slots__" type="= ('serializer',)">
</ParamField>

<ParamField path="key" type="str = ''">
</ParamField>
<Anchor id="flask-json-tag-JSONTag-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.JSONTag.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check if the given value should be tagged by this tag.


</Indent>
<Anchor id="flask-json-tag-JSONTag-tag">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.JSONTag.tag(
    value: typing.Any
) -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Convert the value to a valid JSON type and add the tag structure
around it.


</Indent>
<Anchor id="flask-json-tag-JSONTag-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.JSONTag.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>

Convert the Python object to an object that is a valid JSON type.
The tag will be added later.


</Indent>
<Anchor id="flask-json-tag-JSONTag-to_python">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.JSONTag.to_python(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>

Convert the JSON representation back to the correct type. The tag
will already be removed.


</Indent>
</Indent>

<Anchor id="flask-json-tag-PassDict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.PassDict()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

<ParamField path="__slots__" type="= ()">
</ParamField>
<Anchor id="flask-json-tag-PassDict-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.PassDict.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-PassDict-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.PassDict.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-PassList">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.PassList()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

<ParamField path="__slots__" type="= ()">
</ParamField>
<Anchor id="flask-json-tag-PassList-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.PassList.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-PassList-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.PassList.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-TagBytes">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.TagBytes()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

<ParamField path="__slots__" type="= ()">
</ParamField>

<ParamField path="key" type="= ' b'">
</ParamField>
<Anchor id="flask-json-tag-TagBytes-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagBytes.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagBytes-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagBytes.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagBytes-to_python">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagBytes.to_python(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-TagDateTime">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.TagDateTime()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

<ParamField path="__slots__" type="= ()">
</ParamField>

<ParamField path="key" type="= ' d'">
</ParamField>
<Anchor id="flask-json-tag-TagDateTime-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagDateTime.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagDateTime-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagDateTime.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagDateTime-to_python">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagDateTime.to_python(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-TagDict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.TagDict()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

Tag for 1-item dicts whose only key matches a registered tag.

Internally, the dict key is suffixed with `__`, and the suffix is removed
when deserializing.


<ParamField path="__slots__" type="= ()">
</ParamField>

<ParamField path="key" type="= ' di'">
</ParamField>
<Anchor id="flask-json-tag-TagDict-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagDict.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagDict-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagDict.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagDict-to_python">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagDict.to_python(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-TagMarkup">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.TagMarkup()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

Serialize anything matching the :class:`~markupsafe.Markup` API by
having a ``__html__`` method to the result of that method. Always
deserializes to an instance of :class:`~markupsafe.Markup`.


<ParamField path="__slots__" type="= ()">
</ParamField>

<ParamField path="key" type="= ' m'">
</ParamField>
<Anchor id="flask-json-tag-TagMarkup-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagMarkup.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagMarkup-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagMarkup.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagMarkup-to_python">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagMarkup.to_python(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-TagTuple">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.TagTuple()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

<ParamField path="__slots__" type="= ()">
</ParamField>

<ParamField path="key" type="= ' t'">
</ParamField>
<Anchor id="flask-json-tag-TagTuple-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagTuple.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagTuple-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagTuple.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagTuple-to_python">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagTuple.to_python(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-TagUUID">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.TagUUID()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [JSONTag](#flask-json-tag-JSONTag)

<ParamField path="__slots__" type="= ()">
</ParamField>

<ParamField path="key" type="= ' u'">
</ParamField>
<Anchor id="flask-json-tag-TagUUID-check">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagUUID.check(
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagUUID-to_json">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagUUID.to_json(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TagUUID-to_python">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TagUUID.to_python(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-json-tag-TaggedJSONSerializer">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.json.tag.TaggedJSONSerializer()
```

</CodeBlock>
</Anchor>

<Indent>

Serializer that uses a tag system to compactly represent objects that
are not JSON types. Passed as the intermediate serializer to
:class:`itsdangerous.Serializer`.

The following extra types are supported:

* :class:`dict`
* :class:`tuple`
* :class:`bytes`
* :class:`~markupsafe.Markup`
* :class:`~uuid.UUID`
* :class:`~datetime.datetime`


<ParamField path="__slots__" type="= ('tags', 'order')">
</ParamField>

<ParamField path="default_tags">
</ParamField>

<ParamField path="order" type="list[JSONTag] = []">
</ParamField>

<ParamField path="tags" type="dict[str, JSONTag] = &#123;&#125;">
</ParamField>
<Anchor id="flask-json-tag-TaggedJSONSerializer-_untag_scan">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TaggedJSONSerializer._untag_scan(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-json-tag-TaggedJSONSerializer-dumps">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TaggedJSONSerializer.dumps(
    value: typing.Any
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Tag the value and dump it to a compact JSON string.


</Indent>
<Anchor id="flask-json-tag-TaggedJSONSerializer-loads">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TaggedJSONSerializer.loads(
    value: str
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>

Load data from a JSON string and deserialized any tagged objects.


</Indent>
<Anchor id="flask-json-tag-TaggedJSONSerializer-register">

<CodeBlock links={{"flask.json.tag.JSONTag":"#flask-json-tag-JSONTag"}} showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TaggedJSONSerializer.register(
    tag_class: type[flask.json.tag.JSONTag],
    force: bool = False,
    index: int | None = None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Register a new tag with this serializer.

:param tag_class: tag class to register. Will be instantiated with this
    serializer instance.
:param force: overwrite an existing tag. If false (default), a
    :exc:`KeyError` is raised.
:param index: index to insert the new tag in the tag order. Useful when
    the new tag is a special case of an existing tag. If ``None``
    (default), the tag is appended to the end of the order.

:raise KeyError: if the tag key is already registered and ``force`` is
    not true.


</Indent>
<Anchor id="flask-json-tag-TaggedJSONSerializer-tag">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TaggedJSONSerializer.tag(
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>

Convert a value to a tagged representation if necessary.


</Indent>
<Anchor id="flask-json-tag-TaggedJSONSerializer-untag">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.json.tag.TaggedJSONSerializer.untag(
    value: dict[str, typing.Any]
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>

Convert a tagged representation back to the original type.


</Indent>
</Indent>
