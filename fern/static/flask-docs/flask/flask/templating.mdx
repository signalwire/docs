---
layout: overview
slug: flask/flask/templating
title: flask.templating
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`DispatchingJinjaLoader`](#flask-templating-DispatchingJinjaLoader) | A loader that looks for templates in the application and all |
| [`Environment`](#flask-templating-Environment) | Works like a regular Jinja environment but has some additional |

### Functions

| Name | Description |
|------|-------------|
| [`_default_template_ctx_processor`](#flask-templating-_default_template_ctx_processor) | Default template context processor.  Replaces the ``request`` and ``g`` |
| [`_render`](#flask-templating-_render) | - |
| [`_stream`](#flask-templating-_stream) | - |
| [`render_template`](#flask-templating-render_template) | Render a template by name with the given context. |
| [`render_template_string`](#flask-templating-render_template_string) | Render a template from the given source string with the given |
| [`stream_template`](#flask-templating-stream_template) | Render a template by name with the given context as a stream. |
| [`stream_template_string`](#flask-templating-stream_template_string) | Render a template from the given source string with the given |

### API

<Anchor id="flask-templating-DispatchingJinjaLoader">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.templating.DispatchingJinjaLoader(
    app: flask.sansio.app.App
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `BaseLoader`

A loader that looks for templates in the application and all
the blueprint folders.


<Anchor id="flask-templating-DispatchingJinjaLoader-_get_source_explained">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.DispatchingJinjaLoader._get_source_explained(
    environment: jinja2.Environment,
    template: str
) -> tuple[str, str | None, typing.Callable[[], bool] | None]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-templating-DispatchingJinjaLoader-_get_source_fast">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.DispatchingJinjaLoader._get_source_fast(
    environment: jinja2.Environment,
    template: str
) -> tuple[str, str | None, typing.Callable[[], bool] | None]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-templating-DispatchingJinjaLoader-_iter_loaders">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.DispatchingJinjaLoader._iter_loaders(
    template: str
) -> typing.Iterator[tuple[flask.sansio.scaffold.Scaffold, jinja2.BaseLoader]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-templating-DispatchingJinjaLoader-get_source">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.DispatchingJinjaLoader.get_source(
    environment: jinja2.Environment,
    template: str
) -> tuple[str, str | None, typing.Callable[[], bool] | None]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-templating-DispatchingJinjaLoader-list_templates">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.DispatchingJinjaLoader.list_templates() -> list[str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-templating-Environment">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.templating.Environment(
    app: flask.sansio.app.App,
    options: typing.Any = {}
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `BaseEnvironment`

Works like a regular Jinja environment but has some additional
knowledge of how Flask's blueprint works so that it can prepend the
name of the blueprint to referenced templates if necessary.


</Indent>

<Anchor id="flask-templating-_default_template_ctx_processor">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating._default_template_ctx_processor() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Default template context processor.  Replaces the ``request`` and ``g``
proxies with their concrete objects for faster access.


</Indent>

<Anchor id="flask-templating-_render">

<CodeBlock links={{"flask.ctx.AppContext":"/flask/flask/ctx#flask-ctx-AppContext"}} showLineNumbers={false} wordWrap={true}>

```python
flask.templating._render(
    ctx: flask.ctx.AppContext,
    template: jinja2.Template,
    context: dict[str, typing.Any]
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-templating-_stream">

<CodeBlock links={{"flask.ctx.AppContext":"/flask/flask/ctx#flask-ctx-AppContext"}} showLineNumbers={false} wordWrap={true}>

```python
flask.templating._stream(
    ctx: flask.ctx.AppContext,
    template: jinja2.Template,
    context: dict[str, typing.Any]
) -> typing.Iterator[str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-templating-render_template">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.render_template(
    template_name_or_list: str | jinja2.Template | list[str | jinja2.Template],
    context: typing.Any = {}
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Render a template by name with the given context.

:param template_name_or_list: The name of the template to render. If
    a list is given, the first name to exist will be rendered.
:param context: The variables to make available in the template.


</Indent>

<Anchor id="flask-templating-render_template_string">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.render_template_string(
    source: str,
    context: typing.Any = {}
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Render a template from the given source string with the given
context.

:param source: The source code of the template to render.
:param context: The variables to make available in the template.


</Indent>

<Anchor id="flask-templating-stream_template">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.stream_template(
    template_name_or_list: str | jinja2.Template | list[str | jinja2.Template],
    context: typing.Any = {}
) -> typing.Iterator[str]
```

</CodeBlock>
</Anchor>

<Indent>

Render a template by name with the given context as a stream.
This returns an iterator of strings, which can be used as a
streaming response from a view.

:param template_name_or_list: The name of the template to render. If
    a list is given, the first name to exist will be rendered.
:param context: The variables to make available in the template.

.. versionadded:: 2.2


</Indent>

<Anchor id="flask-templating-stream_template_string">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.templating.stream_template_string(
    source: str,
    context: typing.Any = {}
) -> typing.Iterator[str]
```

</CodeBlock>
</Anchor>

<Indent>

Render a template from the given source string with the given
context as a stream. This returns an iterator of strings, which can
be used as a streaming response from a view.

:param source: The source code of the template to render.
:param context: The variables to make available in the template.

.. versionadded:: 2.2


</Indent>
