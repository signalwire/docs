---
layout: overview
slug: flask/flask/debughelpers
title: flask.debughelpers
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`DebugFilesKeyError`](#flask-debughelpers-DebugFilesKeyError) | Raised from request.files during debugging.  The idea is that it can |
| [`FormDataRoutingRedirect`](#flask-debughelpers-FormDataRoutingRedirect) | This exception is raised in debug mode if a routing redirect |
| [`UnexpectedUnicodeError`](#flask-debughelpers-UnexpectedUnicodeError) | Raised in places where we want some better error reporting for |

### Functions

| Name | Description |
|------|-------------|
| [`_dump_loader_info`](#flask-debughelpers-_dump_loader_info) | - |
| [`attach_enctype_error_multidict`](#flask-debughelpers-attach_enctype_error_multidict) | Patch ``request.files.__getitem__`` to raise a descriptive error |
| [`explain_template_loading_attempts`](#flask-debughelpers-explain_template_loading_attempts) | This should help developers understand what failed |

### API

<Anchor id="flask-debughelpers-DebugFilesKeyError">

<CodeBlock links={{"flask.wrappers.Request":"/flask/flask/wrappers#flask-wrappers-Request"}} showLineNumbers={false} wordWrap={true}>

```python
class flask.debughelpers.DebugFilesKeyError(
    request: flask.wrappers.Request,
    key: str
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `KeyError`, `AssertionError`

Raised from request.files during debugging.  The idea is that it can
provide a better error message than just a generic KeyError/BadRequest.


<ParamField path="msg" type="= ''.join(buf)">
</ParamField>
<Anchor id="flask-debughelpers-DebugFilesKeyError-__str__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.debughelpers.DebugFilesKeyError.__str__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-debughelpers-FormDataRoutingRedirect">

<CodeBlock links={{"flask.wrappers.Request":"/flask/flask/wrappers#flask-wrappers-Request"}} showLineNumbers={false} wordWrap={true}>

```python
class flask.debughelpers.FormDataRoutingRedirect(
    request: flask.wrappers.Request
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `AssertionError`

This exception is raised in debug mode if a routing redirect
would cause the browser to drop the method or body. This happens
when method is not GET, HEAD or OPTIONS and the status code is not
307 or 308.


</Indent>

<Anchor id="flask-debughelpers-UnexpectedUnicodeError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.debughelpers.UnexpectedUnicodeError()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `AssertionError`, `UnicodeError`

Raised in places where we want some better error reporting for
unexpected unicode or binary data.


</Indent>

<Anchor id="flask-debughelpers-_dump_loader_info">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.debughelpers._dump_loader_info(
    loader: jinja2.loaders.BaseLoader
) -> typing.Iterator[str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-debughelpers-attach_enctype_error_multidict">

<CodeBlock links={{"flask.wrappers.Request":"/flask/flask/wrappers#flask-wrappers-Request"}} showLineNumbers={false} wordWrap={true}>

```python
flask.debughelpers.attach_enctype_error_multidict(
    request: flask.wrappers.Request
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Patch ``request.files.__getitem__`` to raise a descriptive error
about ``enctype=multipart/form-data``.

:param request: The request to patch.
:meta private:


</Indent>

<Anchor id="flask-debughelpers-explain_template_loading_attempts">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.debughelpers.explain_template_loading_attempts(
    app: flask.sansio.app.App,
    template: str,
    attempts: list[tuple[jinja2.loaders.BaseLoader, flask.sansio.scaffold.Scaffold, tuple[str, str | None, typing.Callable[[], bool] | None] | None]]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

This should help developers understand what failed


</Indent>
