---
layout: overview
slug: flask/flask/logging
title: flask.logging
---

## Module Contents

### Functions

| Name | Description |
|------|-------------|
| [`create_logger`](#flask-logging-create_logger) | Get the Flask app's logger and configure it if needed. |
| [`has_level_handler`](#flask-logging-has_level_handler) | Check if there is a handler in the logging chain that will handle the |
| [`wsgi_errors_stream`](#flask-logging-wsgi_errors_stream) | Find the most appropriate error stream for the application. If a request |

### Data

[`default_handler`](#flask-logging-default_handler)

### API

<Anchor id="flask-logging-create_logger">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.logging.create_logger(
    app: flask.sansio.app.App
) -> logging.Logger
```

</CodeBlock>
</Anchor>

<Indent>

Get the Flask app's logger and configure it if needed.

The logger name will be the same as
:attr:`app.import_name &lt;flask.Flask.name&gt;`.

When :attr:`~flask.Flask.debug` is enabled, set the logger level to
:data:`logging.DEBUG` if it is not set.

If there is no handler for the logger's effective level, add a
:class:`~logging.StreamHandler` for
:func:`~flask.logging.wsgi_errors_stream` with a basic format.


</Indent>

<Anchor id="flask-logging-has_level_handler">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.logging.has_level_handler(
    logger: logging.Logger
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check if there is a handler in the logging chain that will handle the
given logger's :meth:`effective level &lt;~logging.Logger.getEffectiveLevel&gt;`.


</Indent>

<Anchor id="flask-logging-wsgi_errors_stream">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.logging.wsgi_errors_stream() -> typing.TextIO
```

</CodeBlock>
</Anchor>

<Indent>

Find the most appropriate error stream for the application. If a request
is active, log to ``wsgi.errors``, otherwise use ``sys.stderr``.

If you configure your own :class:`logging.StreamHandler`, you may want to
use this for the stream. If you are using file or dict configuration and
can't import this directly, you can refer to it as
``ext://flask.logging.wsgi_errors_stream``.


</Indent>

<Anchor id="flask-logging-default_handler">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.logging.default_handler = logging.StreamHandler(wsgi_errors_stream)
```

</CodeBlock>
</Anchor>

