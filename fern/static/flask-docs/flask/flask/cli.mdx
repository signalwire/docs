---
layout: overview
slug: flask/flask/cli
title: flask.cli
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`AppGroup`](#flask-cli-AppGroup) | This works similar to a regular click :class:`~click.Group` but it |
| [`CertParamType`](#flask-cli-CertParamType) | Click option type for the ``--cert`` option. Allows either an |
| [`FlaskGroup`](#flask-cli-FlaskGroup) | Special subclass of the :class:`AppGroup` group that supports |
| [`NoAppException`](#flask-cli-NoAppException) | Raised if an application cannot be found or loaded. |
| [`ScriptInfo`](#flask-cli-ScriptInfo) | Helper object to deal with Flask applications.  This is usually not |
| [`SeparatedPathType`](#flask-cli-SeparatedPathType) | Click option type that accepts a list of values separated by the |

### Functions

| Name | Description |
|------|-------------|
| [`_called_with_wrong_args`](#flask-cli-_called_with_wrong_args) | Check whether calling a function raised a ``TypeError`` because |
| [`_env_file_callback`](#flask-cli-_env_file_callback) | - |
| [`_path_is_ancestor`](#flask-cli-_path_is_ancestor) | Take ``other`` and remove the length of ``path`` from it. Then join it |
| [`_set_app`](#flask-cli-_set_app) | - |
| [`_set_debug`](#flask-cli-_set_debug) | - |
| [`_validate_key`](#flask-cli-_validate_key) | The ``--key`` option must be specified when ``--cert`` is a file. |
| [`find_app_by_string`](#flask-cli-find_app_by_string) | Check if the given string is a variable name or a function. Call |
| [`find_best_app`](#flask-cli-find_best_app) | Given a module instance this tries to find the best possible |
| [`get_version`](#flask-cli-get_version) | - |
| [`load_dotenv`](#flask-cli-load_dotenv) | Load "dotenv" files to set environment variables. A given path takes |
| [`locate_app`](#flask-cli-locate_app) | - |
| [`main`](#flask-cli-main) | - |
| [`prepare_import`](#flask-cli-prepare_import) | Given a filename this will try to calculate the python path, add it |
| [`routes_command`](#flask-cli-routes_command) | Show all registered routes with endpoints and methods. |
| [`run_command`](#flask-cli-run_command) | Run a local development server. |
| [`shell_command`](#flask-cli-shell_command) | Run an interactive Python shell in the context of a given |
| [`show_server_banner`](#flask-cli-show_server_banner) | Show extra startup messages the first time the server is run, |
| [`with_appcontext`](#flask-cli-with_appcontext) | Wraps a callback so that it's guaranteed to be executed with the |

### Data

[`F`](#flask-cli-F)

[`_app_option`](#flask-cli-_app_option)

[`_debug_option`](#flask-cli-_debug_option)

[`_env_file_option`](#flask-cli-_env_file_option)

[`cli`](#flask-cli-cli)

[`pass_script_info`](#flask-cli-pass_script_info)

[`version_option`](#flask-cli-version_option)

### API

<Anchor id="flask-cli-AppGroup">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.cli.AppGroup()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `Group`

This works similar to a regular click :class:`~click.Group` but it
changes the behavior of the :meth:`command` decorator so that it
automatically wraps the functions in :func:`with_appcontext`.

Not to be confused with :class:`FlaskGroup`.


<Anchor id="flask-cli-AppGroup-command">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.AppGroup.command(
    args: typing.Any = (),
    kwargs: typing.Any = {}
) -> typing.Callable[[t.Callable[..., t.Any]], click.Command]
```

</CodeBlock>
</Anchor>

<Indent>

This works exactly like the method of the same name on a regular
:class:`click.Group` but it wraps callbacks in :func:`with_appcontext`
unless it's disabled by passing ``with_appcontext=False``.


</Indent>
<Anchor id="flask-cli-AppGroup-group">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.AppGroup.group(
    args: typing.Any = (),
    kwargs: typing.Any = {}
) -> typing.Callable[[t.Callable[..., t.Any]], click.Group]
```

</CodeBlock>
</Anchor>

<Indent>

This works exactly like the method of the same name on a regular
:class:`click.Group` but it defaults the group class to
:class:`AppGroup`.


</Indent>
</Indent>

<Anchor id="flask-cli-CertParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.cli.CertParamType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `ParamType`

Click option type for the ``--cert`` option. Allows either an
existing file, the string ``'adhoc'``, or an import for a
:class:`~ssl.SSLContext` object.


<ParamField path="name" type="= 'path'">
</ParamField>

<ParamField path="path_type">
</ParamField>
<Anchor id="flask-cli-CertParamType-convert">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.CertParamType.convert(
    value: typing.Any,
    param: click.Parameter | None,
    ctx: click.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-cli-FlaskGroup">

<CodeBlock links={{"flask.app.Flask":"/flask/flask/app#flask-app-Flask"}} showLineNumbers={false} wordWrap={true}>

```python
class flask.cli.FlaskGroup(
    add_default_commands: bool = True,
    create_app: typing.Callable[..., flask.app.Flask] | None = None,
    add_version_option: bool = True,
    load_dotenv: bool = True,
    set_debug_flag: bool = True,
    extra: typing.Any = {}
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [AppGroup](#flask-cli-AppGroup)

Special subclass of the :class:`AppGroup` group that supports
loading more commands from the configured Flask app.  Normally a
developer does not have to interface with this class but there are
some very advanced use cases for which it makes sense to create an
instance of this. see :ref:`custom-scripts`.

:param add_default_commands: if this is True then the default run and
    shell commands will be added.
:param add_version_option: adds the ``--version`` option.
:param create_app: an optional callback that is passed the script info and
    returns the loaded app.
:param load_dotenv: Load the nearest :file:`.env` and :file:`.flaskenv`
    files to set environment variables. Will also change the working
    directory to the directory containing the first file found.
:param set_debug_flag: Set the app's debug flag.

.. versionchanged:: 3.1
    ``-e path`` takes precedence over default ``.env`` and ``.flaskenv`` files.

.. versionchanged:: 2.2
    Added the ``-A/--app``, ``--debug/--no-debug``, ``-e/--env-file`` options.

.. versionchanged:: 2.2
    An app context is pushed when running ``app.cli`` commands, so
    ``@with_appcontext`` is no longer required for those commands.

.. versionchanged:: 1.0
    If installed, python-dotenv will be used to load environment variables
    from :file:`.env` and :file:`.flaskenv` files.


<Anchor id="flask-cli-FlaskGroup-_load_plugin_commands">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.FlaskGroup._load_plugin_commands() -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-cli-FlaskGroup-get_command">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.FlaskGroup.get_command(
    ctx: click.Context,
    name: str
) -> click.Command | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-cli-FlaskGroup-list_commands">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.FlaskGroup.list_commands(
    ctx: click.Context
) -> list[str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-cli-FlaskGroup-make_context">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.FlaskGroup.make_context(
    info_name: str | None,
    args: list[str],
    parent: click.Context | None = None,
    extra: typing.Any = {}
) -> click.Context
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="flask-cli-FlaskGroup-parse_args">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.FlaskGroup.parse_args(
    ctx: click.Context,
    args: list[str]
) -> list[str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-cli-NoAppException">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.cli.NoAppException()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `UsageError`

Raised if an application cannot be found or loaded.


</Indent>

<Anchor id="flask-cli-ScriptInfo">

<CodeBlock links={{"flask.app.Flask":"/flask/flask/app#flask-app-Flask"}} showLineNumbers={false} wordWrap={true}>

```python
class flask.cli.ScriptInfo(
    app_import_path: str | None = None,
    create_app: typing.Callable[..., flask.app.Flask] | None = None,
    set_debug_flag: bool = True,
    load_dotenv_defaults: bool = True
)
```

</CodeBlock>
</Anchor>

<Indent>

Helper object to deal with Flask applications.  This is usually not
necessary to interface with as it's used internally in the dispatching
to click.  In future versions of Flask this object will most likely play
a bigger role.  Typically it's created automatically by the
:class:`FlaskGroup` but you can also manually create it and pass it
onwards as click object.

.. versionchanged:: 3.1
    Added the ``load_dotenv_defaults`` parameter and attribute.


<ParamField path="_loaded_app" type="Flask | None = None">
</ParamField>

<ParamField path="data" type="dict[Any, Any] = &#123;&#125;">
</ParamField>

<ParamField path="load_dotenv_defaults" type="= get_load_dotenv(load_dotenv_defaults)">
Whether default ``.flaskenv`` and ``.env`` files should be loaded.

``ScriptInfo`` doesn't load anything, this is for reference when doing
the load elsewhere during processing.

.. versionadded:: 3.1
</ParamField>
<Anchor id="flask-cli-ScriptInfo-load_app">

<CodeBlock links={{"flask.app.Flask":"/flask/flask/app#flask-app-Flask"}} showLineNumbers={false} wordWrap={true}>

```python
flask.cli.ScriptInfo.load_app() -> flask.app.Flask
```

</CodeBlock>
</Anchor>

<Indent>

Loads the Flask app (if not yet loaded) and returns it.  Calling
this multiple times will just result in the already loaded app to
be returned.


</Indent>
</Indent>

<Anchor id="flask-cli-SeparatedPathType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class flask.cli.SeparatedPathType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `Path`

Click option type that accepts a list of values separated by the
OS's path separator (``:``, ``;`` on Windows). Each value is
validated as a :class:`click.Path` type.


<Anchor id="flask-cli-SeparatedPathType-convert">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.SeparatedPathType.convert(
    value: typing.Any,
    param: click.Parameter | None,
    ctx: click.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="flask-cli-_called_with_wrong_args">

<CodeBlock links={{"flask.app.Flask":"/flask/flask/app#flask-app-Flask"}} showLineNumbers={false} wordWrap={true}>

```python
flask.cli._called_with_wrong_args(
    f: typing.Callable[..., flask.app.Flask]
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check whether calling a function raised a ``TypeError`` because
the call failed or because something in the factory raised the
error.

:param f: The function that was called.
:return: ``True`` if the call failed.


</Indent>

<Anchor id="flask-cli-_env_file_callback">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._env_file_callback(
    ctx: click.Context,
    param: click.Option,
    value: str | None
) -> str | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-cli-_path_is_ancestor">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._path_is_ancestor(
    path: str,
    other: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Take ``other`` and remove the length of ``path`` from it. Then join it
to ``path``. If it is the original value, ``path`` is an ancestor of
``other``.


</Indent>

<Anchor id="flask-cli-_set_app">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._set_app(
    ctx: click.Context,
    param: click.Option,
    value: str | None
) -> str | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-cli-_set_debug">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._set_debug(
    ctx: click.Context,
    param: click.Option,
    value: bool
) -> bool | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-cli-_validate_key">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._validate_key(
    ctx: click.Context,
    param: click.Parameter,
    value: typing.Any
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>

The ``--key`` option must be specified when ``--cert`` is a file.
Modifies the ``cert`` param to be a ``(cert, key)`` pair if needed.


</Indent>

<Anchor id="flask-cli-find_app_by_string">

<CodeBlock links={{"flask.app.Flask":"/flask/flask/app#flask-app-Flask"}} showLineNumbers={false} wordWrap={true}>

```python
flask.cli.find_app_by_string(
    module: types.ModuleType,
    app_name: str
) -> flask.app.Flask
```

</CodeBlock>
</Anchor>

<Indent>

Check if the given string is a variable name or a function. Call
a function to get the app instance, or return the variable directly.


</Indent>

<Anchor id="flask-cli-find_best_app">

<CodeBlock links={{"flask.app.Flask":"/flask/flask/app#flask-app-Flask"}} showLineNumbers={false} wordWrap={true}>

```python
flask.cli.find_best_app(
    module: types.ModuleType
) -> flask.app.Flask
```

</CodeBlock>
</Anchor>

<Indent>

Given a module instance this tries to find the best possible
application in the module or raises an exception.


</Indent>

<Anchor id="flask-cli-get_version">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.get_version(
    ctx: click.Context,
    param: click.Parameter,
    value: typing.Any
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-cli-load_dotenv">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.load_dotenv(
    path: str | os.PathLike[str] | None = None,
    load_defaults: bool = True
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Load "dotenv" files to set environment variables. A given path takes
precedence over ``.env``, which takes precedence over ``.flaskenv``. After
loading and combining these files, values are only set if the key is not
already set in ``os.environ``.

This is a no-op if `python-dotenv`_ is not installed.

.. _python-dotenv: https://github.com/theskumar/python-dotenv#readme

:param path: Load the file at this location.
:param load_defaults: Search for and load the default ``.flaskenv`` and
    ``.env`` files.
:return: ``True`` if at least one env var was loaded.

.. versionchanged:: 3.1
    Added the ``load_defaults`` parameter. A given path takes precedence
    over default files.

.. versionchanged:: 2.0
    The current directory is not changed to the location of the
    loaded file.

.. versionchanged:: 2.0
    When loading the env files, set the default encoding to UTF-8.

.. versionchanged:: 1.1.0
    Returns ``False`` when python-dotenv is not installed, or when
    the given path isn't a file.

.. versionadded:: 1.0


</Indent>

<Anchor id="flask-cli-locate_app">

<CodeBlock links={{"flask.app.Flask":"/flask/flask/app#flask-app-Flask"}} showLineNumbers={false} wordWrap={true}>

```python
flask.cli.locate_app(
    module_name: str,
    app_name: str | None,
    raise_if_not_found: bool = True
) -> flask.app.Flask | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-cli-main">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.main() -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="flask-cli-prepare_import">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.prepare_import(
    path: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Given a filename this will try to calculate the python path, add it
to the search path and return the actual module name that is expected.


</Indent>

<Anchor id="flask-cli-routes_command">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.routes_command(
    sort: str,
    all_methods: bool
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Show all registered routes with endpoints and methods.


</Indent>

<Anchor id="flask-cli-run_command">

<CodeBlock links={{"flask.cli.ScriptInfo":"#flask-cli-ScriptInfo"}} showLineNumbers={false} wordWrap={true}>

```python
flask.cli.run_command(
    info: flask.cli.ScriptInfo,
    host: str,
    port: int,
    reload: bool,
    debugger: bool,
    with_threads: bool,
    cert: ssl.SSLContext | tuple[str, str | None] | typing.Literal['adhoc'] | None,
    extra_files: list[str] | None,
    exclude_patterns: list[str] | None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Run a local development server.

This server is for development purposes only. It does not provide
the stability, security, or performance of production WSGI servers.

The reloader and debugger are enabled by default with the '--debug'
option.


</Indent>

<Anchor id="flask-cli-shell_command">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.shell_command() -> None
```

</CodeBlock>
</Anchor>

<Indent>

Run an interactive Python shell in the context of a given
Flask application.  The application will populate the default
namespace of this shell according to its configuration.

This is useful for executing small snippets of management code
without having to manually configure the application.


</Indent>

<Anchor id="flask-cli-show_server_banner">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.show_server_banner(
    debug: bool,
    app_import_path: str | None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Show extra startup messages the first time the server is run,
ignoring the reloader.


</Indent>

<Anchor id="flask-cli-with_appcontext">

<CodeBlock links={{"flask.cli.F":"#flask-cli-F"}} showLineNumbers={false} wordWrap={true}>

```python
flask.cli.with_appcontext(
    f: flask.cli.F
) -> flask.cli.F
```

</CodeBlock>
</Anchor>

<Indent>

Wraps a callback so that it's guaranteed to be executed with the
script's application context.

Custom commands (and their options) registered under ``app.cli`` or
``blueprint.cli`` will always have an app context available, this
decorator is not required in that case.

.. versionchanged:: 2.2
    The app context is active for subcommands as well as the
    decorated callback. The app context is always available to
    ``app.cli`` command and parameter callbacks.


</Indent>

<Anchor id="flask-cli-F">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.F = t.TypeVar('F', bound=(t.Callable[..., t.Any]))
```

</CodeBlock>
</Anchor>


<Anchor id="flask-cli-_app_option">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._app_option = click.Option(['-A', '--app'], metavar='IMPORT', help="The Flask application or f...
```

</CodeBlock>
</Anchor>


<Anchor id="flask-cli-_debug_option">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._debug_option = click.Option(['--debug/--no-debug'], help='Set debug mode.', expose_value=False,...
```

</CodeBlock>
</Anchor>


<Anchor id="flask-cli-_env_file_option">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli._env_file_option = click.Option(['-e', '--env-file'], type=(click.Path(exists=True, dir_okay=False)...
```

</CodeBlock>
</Anchor>


<Anchor id="flask-cli-cli">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.cli = FlaskGroup(name='flask', help="A general utility script for Flask applications.\...
```

</CodeBlock>
</Anchor>


<Anchor id="flask-cli-pass_script_info">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.pass_script_info = click.make_pass_decorator(ScriptInfo, ensure=True)
```

</CodeBlock>
</Anchor>


<Anchor id="flask-cli-version_option">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
flask.cli.version_option = click.Option(['--version'], help='Show the Flask version.', expose_value=False, ...
```

</CodeBlock>
</Anchor>

