---
layout: overview
slug: click/click/types
title: click.types
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`BoolParamType`](#click-types-BoolParamType) | - |
| [`Choice`](#click-types-Choice) | The choice type allows a value to be checked against a fixed set |
| [`CompositeParamType`](#click-types-CompositeParamType) | - |
| [`DateTime`](#click-types-DateTime) | The DateTime type converts date strings into `datetime` objects. |
| [`File`](#click-types-File) | Declares a parameter to be a file for reading or writing.  The file |
| [`FloatParamType`](#click-types-FloatParamType) | - |
| [`FloatRange`](#click-types-FloatRange) | Restrict a :data:`click.FLOAT` value to a range of accepted |
| [`FuncParamType`](#click-types-FuncParamType) | - |
| [`IntParamType`](#click-types-IntParamType) | - |
| [`IntRange`](#click-types-IntRange) | Restrict an :data:`click.INT` value to a range of accepted |
| [`OptionHelpExtra`](#click-types-OptionHelpExtra) | - |
| [`ParamType`](#click-types-ParamType) | Represents the type of a parameter. Validates and converts values |
| [`Path`](#click-types-Path) | The ``Path`` type is similar to the :class:`File` type, but |
| [`StringParamType`](#click-types-StringParamType) | - |
| [`Tuple`](#click-types-Tuple) | The default behavior of Click is to apply a type on a value directly. |
| [`UUIDParameterType`](#click-types-UUIDParameterType) | - |
| [`UnprocessedParamType`](#click-types-UnprocessedParamType) | - |
| [`_NumberParamTypeBase`](#click-types-_NumberParamTypeBase) | - |
| [`_NumberRangeBase`](#click-types-_NumberRangeBase) | - |

### Functions

| Name | Description |
|------|-------------|
| [`_is_file_like`](#click-types-_is_file_like) | - |
| [`convert_type`](#click-types-convert_type) | Find the most appropriate :class:`ParamType` for the given Python |

### Data

[`BOOL`](#click-types-BOOL)

[`FLOAT`](#click-types-FLOAT)

[`INT`](#click-types-INT)

[`ParamTypeValue`](#click-types-ParamTypeValue)

[`STRING`](#click-types-STRING)

[`UNPROCESSED`](#click-types-UNPROCESSED)

[`UUID`](#click-types-UUID)

### API

<Anchor id="click-types-BoolParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.BoolParamType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

<ParamField path="bool_states" type="dict[str, bool]">
A mapping of string values to boolean states.

Mapping is inspired by :py:attr:`configparser.ConfigParser.BOOLEAN_STATES`
and extends it.

.. caution::
    String values are lower-cased, as the ``str_to_bool`` comparison function
    below is case-insensitive.

.. warning::
    The mapping is not exhaustive, and does not cover all possible boolean strings
    representations. It will remains as it is to avoid endless bikeshedding.

    Future work my be considered to make this mapping user-configurable from public
    API.
</ParamField>

<ParamField path="name" type="= 'boolean'">
</ParamField>
<Anchor id="click-types-BoolParamType-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.BoolParamType.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-BoolParamType-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.BoolParamType.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-BoolParamType-str_to_bool">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.BoolParamType.str_to_bool(
    value: str | bool
) -> bool | None
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>staticmethod</Badge>

Convert a string to a boolean value.

If the value is already a boolean, it is returned as-is. If the value is a
string, it is stripped of whitespaces and lower-cased, then checked against
the known boolean states pre-defined in the `BoolParamType.bool_states` mapping
above.

Returns `None` if the value does not match any known boolean state.


</Indent>
</Indent>

<Anchor id="click-types-Choice">

<CodeBlock links={{"click.types.ParamTypeValue":"#click-types-ParamTypeValue"}} showLineNumbers={false} wordWrap={true}>

```python
class click.types.Choice(
    choices: collections.abc.Iterable[click.types.ParamTypeValue],
    case_sensitive: bool = True
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType), `Generic[ParamTypeValue]`

The choice type allows a value to be checked against a fixed set
of supported values.

You may pass any iterable value which will be converted to a tuple
and thus will only be iterated once.

The resulting value will always be one of the originally passed choices.
See :meth:`normalize_choice` for more info on the mapping of strings
to choices. See :ref:`choice-opts` for an example.

:param case_sensitive: Set to false to make choices case
    insensitive. Defaults to true.

.. versionchanged:: 8.2.0
    Non-``str`` ``choices`` are now supported. It can additionally be any
    iterable. Before you were not recommended to pass anything but a list or
    tuple.

.. versionadded:: 8.2.0
    Choice normalization can be overridden via :meth:`normalize_choice`.


<ParamField path="choices" type="Sequence[ParamTypeValue] = tuple(choices)">
</ParamField>

<ParamField path="name" type="= 'choice'">
</ParamField>
<Anchor id="click-types-Choice-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-Choice-_normalized_mapping">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.types.ParamTypeValue":"#click-types-ParamTypeValue"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice._normalized_mapping(
    ctx: click.core.Context | None = None
) -> collections.abc.Mapping[click.types.ParamTypeValue, str]
```

</CodeBlock>
</Anchor>

<Indent>

Returns mapping where keys are the original choices and the values are
the normalized values that are accepted via the command line.

This is a simple wrapper around :meth:`normalize_choice`, use that
instead which is supported.


</Indent>
<Anchor id="click-types-Choice-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context","click.types.ParamTypeValue":"#click-types-ParamTypeValue"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> click.types.ParamTypeValue
```

</CodeBlock>
</Anchor>

<Indent>

For a given value from the parser, normalize it and find its
matching normalized value in the list of choices. Then return the
matched "original" choice.


</Indent>
<Anchor id="click-types-Choice-get_invalid_choice_message">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.get_invalid_choice_message(
    value: typing.Any,
    ctx: click.core.Context | None
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Get the error message when the given choice is invalid.

:param value: The invalid value.

.. versionadded:: 8.2


</Indent>
<Anchor id="click-types-Choice-get_metavar">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.get_metavar(
    param: click.core.Parameter,
    ctx: click.core.Context
) -> str | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-Choice-get_missing_message">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.get_missing_message(
    param: click.core.Parameter,
    ctx: click.core.Context | None
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Message shown when no choice is passed.

.. versionchanged:: 8.2.0 Added ``ctx`` argument.


</Indent>
<Anchor id="click-types-Choice-normalize_choice">

<CodeBlock links={{"click.types.ParamTypeValue":"#click-types-ParamTypeValue","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.normalize_choice(
    choice: click.types.ParamTypeValue,
    ctx: click.core.Context | None
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Normalize a choice value, used to map a passed string to a choice.
Each choice must have a unique normalized value.

By default uses :meth:`Context.token_normalize_func` and if not case
sensitive, convert it to a casefolded value.

.. versionadded:: 8.2.0


</Indent>
<Anchor id="click-types-Choice-shell_complete">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.core.Parameter":"/click/click/core#click-core-Parameter","click.shell_completion.CompletionItem":"/click/click/shell_completion#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.shell_complete(
    ctx: click.core.Context,
    param: click.core.Parameter,
    incomplete: str
) -> list[click.shell_completion.CompletionItem]
```

</CodeBlock>
</Anchor>

<Indent>

Complete choices that start with the incomplete value.

:param ctx: Invocation context for this command.
:param param: The parameter that is requesting completion.
:param incomplete: Value being completed. May be empty.

.. versionadded:: 8.0


</Indent>
<Anchor id="click-types-Choice-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.Choice.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-CompositeParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.CompositeParamType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

<ParamField path="arity" type="int">
</ParamField>
</Indent>

<Anchor id="click-types-DateTime">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.DateTime(
    formats: collections.abc.Sequence[str] | None = None
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

The DateTime type converts date strings into `datetime` objects.

The format strings which are checked are configurable, but default to some
common (non-timezone aware) ISO 8601 formats.

When specifying *DateTime* formats, you should only pass a list or a tuple.
Other iterables, like generators, may lead to surprising results.

The format strings are processed using ``datetime.strptime``, and this
consequently defines the format strings which are allowed.

Parsing is tried using each format, in order, and the first format which
parses successfully is used.

:param formats: A list or tuple of date format strings, in the order in
                which they should be tried. Defaults to
                ``'%Y-%m-%d'``, ``'%Y-%m-%dT%H:%M:%S'``,
                ``'%Y-%m-%d %H:%M:%S'``.


<ParamField path="formats" type="Sequence[str]">
</ParamField>

<ParamField path="name" type="= 'datetime'">
</ParamField>
<Anchor id="click-types-DateTime-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.DateTime.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-DateTime-_try_to_convert_date">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.DateTime._try_to_convert_date(
    value: typing.Any,
    format: str
) -> datetime.datetime | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-DateTime-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.DateTime.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-DateTime-get_metavar">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.DateTime.get_metavar(
    param: click.core.Parameter,
    ctx: click.core.Context
) -> str | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-DateTime-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.DateTime.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-File">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.File(
    mode: str = 'r',
    encoding: str | None = None,
    errors: str | None = 'strict',
    lazy: bool | None = None,
    atomic: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

Declares a parameter to be a file for reading or writing.  The file
is automatically closed once the context tears down (after the command
finished working).

Files can be opened for reading or writing.  The special value ``-``
indicates stdin or stdout depending on the mode.

By default, the file is opened for reading text data, but it can also be
opened in binary mode or for writing.  The encoding parameter can be used
to force a specific encoding.

The `lazy` flag controls if the file should be opened immediately or upon
first IO. The default is to be non-lazy for standard input and output
streams as well as files opened for reading, `lazy` otherwise. When opening a
file lazily for reading, it is still opened temporarily for validation, but
will not be held open until first IO. lazy is mainly useful when opening
for writing to avoid creating the file until it is needed.

Files can also be opened atomically in which case all writes go into a
separate file in the same folder and upon completion the file will
be moved over to the original location.  This is useful if a file
regularly read by other users is modified.

See :ref:`file-args` for more information.

.. versionchanged:: 2.0
    Added the ``atomic`` parameter.


<ParamField path="envvar_list_splitter" type="str = os.path.pathsep">
</ParamField>

<ParamField path="name" type="= 'filename'">
</ParamField>
<Anchor id="click-types-File-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.File.convert(
    value: str | os.PathLike[str] | typing.IO[typing.Any],
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.IO[typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-File-resolve_lazy_flag">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.File.resolve_lazy_flag(
    value: str | os.PathLike[str]
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-File-shell_complete">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.core.Parameter":"/click/click/core#click-core-Parameter","click.shell_completion.CompletionItem":"/click/click/shell_completion#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.File.shell_complete(
    ctx: click.core.Context,
    param: click.core.Parameter,
    incomplete: str
) -> list[click.shell_completion.CompletionItem]
```

</CodeBlock>
</Anchor>

<Indent>

Return a special completion marker that tells the completion
system to use the shell to provide file path completions.

:param ctx: Invocation context for this command.
:param param: The parameter that is requesting completion.
:param incomplete: Value being completed. May be empty.

.. versionadded:: 8.0


</Indent>
<Anchor id="click-types-File-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.File.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-FloatParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.FloatParamType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [_NumberParamTypeBase](#click-types-_NumberParamTypeBase)

<ParamField path="name" type="= 'float'">
</ParamField>
<Anchor id="click-types-FloatParamType-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.FloatParamType.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-FloatRange">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.FloatRange(
    min: float | None = None,
    max: float | None = None,
    min_open: bool = False,
    max_open: bool = False,
    clamp: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [_NumberRangeBase](#click-types-_NumberRangeBase), [FloatParamType](#click-types-FloatParamType)

Restrict a :data:`click.FLOAT` value to a range of accepted
values. See :ref:`ranges`.

If ``min`` or ``max`` are not passed, any value is accepted in that
direction. If ``min_open`` or ``max_open`` are enabled, the
corresponding boundary is not included in the range.

If ``clamp`` is enabled, a value outside the range is clamped to the
boundary instead of failing. This is not supported if either
boundary is marked ``open``.

.. versionchanged:: 8.0
    Added the ``min_open`` and ``max_open`` parameters.


<ParamField path="name" type="= 'float range'">
</ParamField>
<Anchor id="click-types-FloatRange-_clamp">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.FloatRange._clamp(
    bound: float,
    dir: typing.Literal[1, -1],
    open: bool
) -> float
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-FuncParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.FuncParamType(
    func: typing.Callable[[t.Any], typing.Any]
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

<ParamField path="name" type="str = func.__name__">
</ParamField>
<Anchor id="click-types-FuncParamType-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.FuncParamType.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-FuncParamType-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.FuncParamType.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-IntParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.IntParamType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [_NumberParamTypeBase](#click-types-_NumberParamTypeBase)

<ParamField path="name" type="= 'integer'">
</ParamField>
<Anchor id="click-types-IntParamType-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.IntParamType.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-IntRange">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.IntRange()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [_NumberRangeBase](#click-types-_NumberRangeBase), [IntParamType](#click-types-IntParamType)

Restrict an :data:`click.INT` value to a range of accepted
values. See :ref:`ranges`.

If ``min`` or ``max`` are not passed, any value is accepted in that
direction. If ``min_open`` or ``max_open`` are enabled, the
corresponding boundary is not included in the range.

If ``clamp`` is enabled, a value outside the range is clamped to the
boundary instead of failing.

.. versionchanged:: 8.0
    Added the ``min_open`` and ``max_open`` parameters.


<ParamField path="name" type="= 'integer range'">
</ParamField>
<Anchor id="click-types-IntRange-_clamp">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.IntRange._clamp(
    bound: int,
    dir: typing.Literal[1, -1],
    open: bool
) -> int
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-OptionHelpExtra">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.OptionHelpExtra
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `typing.TypedDict`

<ParamField path="default" type="str">

</ParamField>

<ParamField path="envvars" type="tuple[str, ...]">

</ParamField>

<ParamField path="range" type="str">

</ParamField>

<ParamField path="required" type="str">

</ParamField>

</Indent>

<Anchor id="click-types-ParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.ParamType()
```

</CodeBlock>
</Anchor>

<Indent>

Represents the type of a parameter. Validates and converts values
from the command line or Python into the correct type.

To implement a custom type, subclass and implement at least the
following:

-   The :attr:`name` class attribute must be set.
-   Calling an instance of the type with ``None`` must return
    ``None``. This is already implemented by default.
-   :meth:`convert` must convert string values to the correct type.
-   :meth:`convert` must accept values that are already the correct
    type.
-   It must be able to convert a value if the ``ctx`` and ``param``
    arguments are ``None``. This can occur when converting prompt
    input.


<ParamField path="arity" type="int = 1">
</ParamField>

<ParamField path="envvar_list_splitter" type="str | None = None">
</ParamField>

<ParamField path="is_composite" type="bool = False">
</ParamField>

<ParamField path="name" type="str">
</ParamField>
<Anchor id="click-types-ParamType-__call__">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.__call__(
    value: typing.Any,
    param: click.core.Parameter | None = None,
    ctx: click.core.Context | None = None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-ParamType-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>

Convert the value to the correct type. This is not called if
the value is ``None`` (the missing value).

This must accept string values from the command line, as well as
values that are already the correct type. It may also convert
other compatible types.

The ``param`` and ``ctx`` arguments may be ``None`` in certain
situations, such as when converting prompt input.

If the value cannot be converted, call :meth:`fail` with a
descriptive message.

:param value: The value to convert.
:param param: The parameter that is using this type to convert
    its value. May be ``None``.
:param ctx: The current context that arrived at this value. May
    be ``None``.


</Indent>
<Anchor id="click-types-ParamType-fail">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.fail(
    message: str,
    param: click.core.Parameter | None = None,
    ctx: click.core.Context | None = None
) -> typing.NoReturn
```

</CodeBlock>
</Anchor>

<Indent>

Helper method to fail with an invalid value message.


</Indent>
<Anchor id="click-types-ParamType-get_metavar">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.get_metavar(
    param: click.core.Parameter,
    ctx: click.core.Context
) -> str | None
```

</CodeBlock>
</Anchor>

<Indent>

Returns the metavar default for this param if it provides one.


</Indent>
<Anchor id="click-types-ParamType-get_missing_message">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.get_missing_message(
    param: click.core.Parameter,
    ctx: click.core.Context | None
) -> str | None
```

</CodeBlock>
</Anchor>

<Indent>

Optionally might return extra information about a missing
parameter.

.. versionadded:: 2.0


</Indent>
<Anchor id="click-types-ParamType-shell_complete">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.core.Parameter":"/click/click/core#click-core-Parameter","click.shell_completion.CompletionItem":"/click/click/shell_completion#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.shell_complete(
    ctx: click.core.Context,
    param: click.core.Parameter,
    incomplete: str
) -> list[click.shell_completion.CompletionItem]
```

</CodeBlock>
</Anchor>

<Indent>

Return a list of
:class:`~click.shell_completion.CompletionItem` objects for the
incomplete value. Most types do not provide completions, but
some do, and this allows custom types to provide custom
completions as well.

:param ctx: Invocation context for this command.
:param param: The parameter that is requesting completion.
:param incomplete: Value being completed. May be empty.

.. versionadded:: 8.0


</Indent>
<Anchor id="click-types-ParamType-split_envvar_value">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.split_envvar_value(
    rv: str
) -> collections.abc.Sequence[str]
```

</CodeBlock>
</Anchor>

<Indent>

Given a value from an environment variable this splits it up
into small chunks depending on the defined envvar list splitter.

If the splitter is set to `None`, which means that whitespace splits,
then leading and trailing whitespace is ignored.  Otherwise, leading
and trailing splitters usually lead to empty items being included.


</Indent>
<Anchor id="click-types-ParamType-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamType.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Gather information that could be useful for a tool generating
user-facing documentation.

Use :meth:`click.Context.to_info_dict` to traverse the entire
CLI structure.

.. versionadded:: 8.0


</Indent>
</Indent>

<Anchor id="click-types-Path">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.Path(
    exists: bool = False,
    file_okay: bool = True,
    dir_okay: bool = True,
    writable: bool = False,
    readable: bool = True,
    resolve_path: bool = False,
    allow_dash: bool = False,
    path_type: type[typing.Any] | None = None,
    executable: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

The ``Path`` type is similar to the :class:`File` type, but
returns the filename instead of an open file. Various checks can be
enabled to validate the type of file and permissions.

:param exists: The file or directory needs to exist for the value to
    be valid. If this is not set to ``True``, and the file does not
    exist, then all further checks are silently skipped.
:param file_okay: Allow a file as a value.
:param dir_okay: Allow a directory as a value.
:param readable: if true, a readable check is performed.
:param writable: if true, a writable check is performed.
:param executable: if true, an executable check is performed.
:param resolve_path: Make the value absolute and resolve any
    symlinks. A ``~`` is not expanded, as this is supposed to be
    done by the shell only.
:param allow_dash: Allow a single dash as a value, which indicates
    a standard stream (but does not open it). Use
    :func:`~click.open_file` to handle opening this value.
:param path_type: Convert the incoming path value to this type. If
    ``None``, keep Python's default, which is ``str``. Useful to
    convert to :class:`pathlib.Path`.

.. versionchanged:: 8.1
    Added the ``executable`` parameter.

.. versionchanged:: 8.0
    Allow passing ``path_type=pathlib.Path``.

.. versionchanged:: 6.0
    Added the ``allow_dash`` parameter.


<ParamField path="envvar_list_splitter" type="str = os.path.pathsep">
</ParamField>

<ParamField path="name" type="str = _('file')">
</ParamField>
<Anchor id="click-types-Path-coerce_path_result">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.Path.coerce_path_result(
    value: str | os.PathLike[str]
) -> str | bytes | os.PathLike[str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-Path-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Path.convert(
    value: str | os.PathLike[str],
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> str | bytes | os.PathLike[str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-Path-shell_complete">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.core.Parameter":"/click/click/core#click-core-Parameter","click.shell_completion.CompletionItem":"/click/click/shell_completion#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Path.shell_complete(
    ctx: click.core.Context,
    param: click.core.Parameter,
    incomplete: str
) -> list[click.shell_completion.CompletionItem]
```

</CodeBlock>
</Anchor>

<Indent>

Return a special completion marker that tells the completion
system to use the shell to provide path completions for only
directories or any paths.

:param ctx: Invocation context for this command.
:param param: The parameter that is requesting completion.
:param incomplete: Value being completed. May be empty.

.. versionadded:: 8.0


</Indent>
<Anchor id="click-types-Path-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.Path.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-StringParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.StringParamType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

<ParamField path="name" type="= 'text'">
</ParamField>
<Anchor id="click-types-StringParamType-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.StringParamType.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-StringParamType-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.StringParamType.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-Tuple">

<CodeBlock links={{"click.types.ParamType":"#click-types-ParamType"}} showLineNumbers={false} wordWrap={true}>

```python
class click.types.Tuple(
    types: collections.abc.Sequence[type[typing.Any] | click.types.ParamType]
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [CompositeParamType](#click-types-CompositeParamType)

The default behavior of Click is to apply a type on a value directly.
This works well in most cases, except for when `nargs` is set to a fixed
count and different types should be used for different items.  In this
case the :class:`Tuple` type can be used.  This type can only be used
if `nargs` is set to a fixed number.

For more information see :ref:`tuple-type`.

This can be selected by using a Python tuple literal as a type.

:param types: a list of types that should be used for the tuple items.


<ParamField path="arity" type="int">
</ParamField>

<ParamField path="name" type="str">
</ParamField>

<ParamField path="types" type="Sequence[ParamType] = [(convert_type(ty)) for ty in types]">
</ParamField>
<Anchor id="click-types-Tuple-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.Tuple.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-Tuple-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.Tuple.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-UUIDParameterType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.UUIDParameterType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

<ParamField path="name" type="= 'uuid'">
</ParamField>
<Anchor id="click-types-UUIDParameterType-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.UUIDParameterType.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-UUIDParameterType-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.UUIDParameterType.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-UnprocessedParamType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types.UnprocessedParamType()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

<ParamField path="name" type="= 'text'">
</ParamField>
<Anchor id="click-types-UnprocessedParamType-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.UnprocessedParamType.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-UnprocessedParamType-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.UnprocessedParamType.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-_NumberParamTypeBase">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types._NumberParamTypeBase()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ParamType](#click-types-ParamType)

<ParamField path="_number_class" type="type[Any]">
</ParamField>
<Anchor id="click-types-_NumberParamTypeBase-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types._NumberParamTypeBase.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-_NumberRangeBase">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.types._NumberRangeBase(
    min: float | None = None,
    max: float | None = None,
    min_open: bool = False,
    max_open: bool = False,
    clamp: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [_NumberParamTypeBase](#click-types-_NumberParamTypeBase)

<Anchor id="click-types-_NumberRangeBase-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types._NumberRangeBase.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-_NumberRangeBase-_clamp">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types._NumberRangeBase._clamp(
    bound: float,
    dir: typing.Literal[1, -1],
    open: bool
) -> float
```

</CodeBlock>
</Anchor>

<Indent>

Find the valid value to clamp to bound in the given
direction.

:param bound: The boundary value.
:param dir: 1 or -1 indicating the direction to move.
:param open: If true, the range does not include the bound.


</Indent>
<Anchor id="click-types-_NumberRangeBase-_describe_range">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types._NumberRangeBase._describe_range() -> str
```

</CodeBlock>
</Anchor>

<Indent>

Describe the range for use in help text.


</Indent>
<Anchor id="click-types-_NumberRangeBase-convert">

<CodeBlock links={{"click.core.Parameter":"/click/click/core#click-core-Parameter","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.types._NumberRangeBase.convert(
    value: typing.Any,
    param: click.core.Parameter | None,
    ctx: click.core.Context | None
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-types-_NumberRangeBase-to_info_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types._NumberRangeBase.to_info_dict() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-types-_is_file_like">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types._is_file_like(
    value: typing.Any
) -> typing_extensions.TypeGuard[typing.IO[typing.Any]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="click-types-convert_type">

<CodeBlock links={{"click.types.ParamType":"#click-types-ParamType"}} showLineNumbers={false} wordWrap={true}>

```python
click.types.convert_type(
    ty: typing.Any | None,
    default: typing.Any | None = None
) -> click.types.ParamType
```

</CodeBlock>
</Anchor>

<Indent>

Find the most appropriate :class:`ParamType` for the given Python
type. If the type isn't provided, it can be inferred from a default
value.


</Indent>

<Anchor id="click-types-BOOL">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.BOOL = BoolParamType()
```

</CodeBlock>
</Anchor>


<Anchor id="click-types-FLOAT">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.FLOAT = FloatParamType()
```

</CodeBlock>
</Anchor>


<Anchor id="click-types-INT">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.INT = IntParamType()
```

</CodeBlock>
</Anchor>


<Anchor id="click-types-ParamTypeValue">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.ParamTypeValue = t.TypeVar('ParamTypeValue')
```

</CodeBlock>
</Anchor>


<Anchor id="click-types-STRING">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.STRING = StringParamType()
```

</CodeBlock>
</Anchor>


<Anchor id="click-types-UNPROCESSED">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.UNPROCESSED = UnprocessedParamType()
```

</CodeBlock>
</Anchor>


<Anchor id="click-types-UUID">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.types.UUID = UUIDParameterType()
```

</CodeBlock>
</Anchor>

