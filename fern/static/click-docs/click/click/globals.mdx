---
layout: overview
slug: click/click/globals
title: click.globals
---

## Module Contents

### Functions

| Name | Description |
|------|-------------|
| [`get_current_context`](#click-globals-get_current_context) | Returns the current click context.  This can be used as a way to |
| [`pop_context`](#click-globals-pop_context) | Removes the top level from the stack. |
| [`push_context`](#click-globals-push_context) | Pushes a new context to the current stack. |
| [`resolve_color_default`](#click-globals-resolve_color_default) | Internal helper to get the default value of the color flag.  If a |

### Data

[`_local`](#click-globals-_local)

### API

<Anchor id="click-globals-get_current_context">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.globals.get_current_context(
    silent: bool = False
) -> click.core.Context | None
```

</CodeBlock>
</Anchor>

<Indent>

Returns the current click context.  This can be used as a way to
access the current context object from anywhere.  This is a more implicit
alternative to the :func:`pass_context` decorator.  This function is
primarily useful for helpers such as :func:`echo` which might be
interested in changing its behavior based on the current context.

To push the current context, :meth:`Context.scope` can be used.

.. versionadded:: 5.0

:param silent: if set to `True` the return value is `None` if no context
               is available.  The default behavior is to raise a
               :exc:`RuntimeError`.


</Indent>

<Anchor id="click-globals-pop_context">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.globals.pop_context() -> None
```

</CodeBlock>
</Anchor>

<Indent>

Removes the top level from the stack.


</Indent>

<Anchor id="click-globals-push_context">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.globals.push_context(
    ctx: click.core.Context
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Pushes a new context to the current stack.


</Indent>

<Anchor id="click-globals-resolve_color_default">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.globals.resolve_color_default(
    color: bool | None = None
) -> bool | None
```

</CodeBlock>
</Anchor>

<Indent>

Internal helper to get the default value of the color flag.  If a
value is passed it's returned unchanged, otherwise it's looked up from
the current context.


</Indent>

<Anchor id="click-globals-_local">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.globals._local = local()
```

</CodeBlock>
</Anchor>

