---
layout: overview
slug: click/click/shell_completion
title: click.shell_completion
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`BashComplete`](#click-shell_completion-BashComplete) | Shell completion for Bash. |
| [`CompletionItem`](#click-shell_completion-CompletionItem) | Represents a completion value and metadata about the value. The |
| [`FishComplete`](#click-shell_completion-FishComplete) | Shell completion for Fish. |
| [`ShellComplete`](#click-shell_completion-ShellComplete) | Base class for providing shell completion support. A subclass for |
| [`ZshComplete`](#click-shell_completion-ZshComplete) | Shell completion for Zsh. |

### Functions

| Name | Description |
|------|-------------|
| [`_is_incomplete_argument`](#click-shell_completion-_is_incomplete_argument) | Determine if the given parameter is an argument that can still |
| [`_is_incomplete_option`](#click-shell_completion-_is_incomplete_option) | Determine if the given parameter is an option that needs a value. |
| [`_resolve_context`](#click-shell_completion-_resolve_context) | Produce the context hierarchy starting with the command and |
| [`_resolve_incomplete`](#click-shell_completion-_resolve_incomplete) | Find the Click object that will handle the completion of the |
| [`_start_of_option`](#click-shell_completion-_start_of_option) | Check if the value looks like the start of an option. |
| [`add_completion_class`](#click-shell_completion-add_completion_class) | Register a :class:`ShellComplete` subclass under the given name. |
| [`get_completion_class`](#click-shell_completion-get_completion_class) | Look up a registered :class:`ShellComplete` subclass by the name |
| [`shell_complete`](#click-shell_completion-shell_complete) | Perform shell completion for the given CLI program. |
| [`split_arg_string`](#click-shell_completion-split_arg_string) | Split an argument string as with :func:`shlex.split`, but don't |

### Data

[`ShellCompleteType`](#click-shell_completion-ShellCompleteType)

[`_SOURCE_BASH`](#click-shell_completion-_SOURCE_BASH)

[`_SOURCE_FISH`](#click-shell_completion-_SOURCE_FISH)

[`_SOURCE_ZSH`](#click-shell_completion-_SOURCE_ZSH)

[`_available_shells`](#click-shell_completion-_available_shells)

### API

<Anchor id="click-shell_completion-BashComplete">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.shell_completion.BashComplete()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ShellComplete](#click-shell_completion-ShellComplete)

Shell completion for Bash.


<ParamField path="name" type="= 'bash'">
</ParamField>
<Anchor id="click-shell_completion-BashComplete-_check_version">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.BashComplete._check_version() -> None
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>staticmethod</Badge>


</Indent>
<Anchor id="click-shell_completion-BashComplete-format_completion">

<CodeBlock links={{"click.shell_completion.CompletionItem":"#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.BashComplete.format_completion(
    item: click.shell_completion.CompletionItem
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-shell_completion-BashComplete-get_completion_args">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.BashComplete.get_completion_args() -> tuple[list[str], str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-shell_completion-BashComplete-source">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.BashComplete.source() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-shell_completion-CompletionItem">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.shell_completion.CompletionItem(
    value: typing.Any,
    type: str = 'plain',
    help: str | None = None,
    kwargs: typing.Any = {}
)
```

</CodeBlock>
</Anchor>

<Indent>

Represents a completion value and metadata about the value. The
default metadata is ``type`` to indicate special shell handling,
and ``help`` if a shell supports showing a help string next to the
value.

Arbitrary parameters can be passed when creating the object, and
accessed using ``item.attr``. If an attribute wasn't passed,
accessing it returns ``None``.

:param value: The completion suggestion.
:param type: Tells the shell script to provide special completion
    support for the type. Click uses ``"dir"`` and ``"file"``.
:param help: String shown next to the value if supported.
:param kwargs: Arbitrary metadata. The built-in implementations
    don't use this, but custom type completions paired with custom
    shell support could use it.


<ParamField path="__slots__" type="= ('value', 'type', 'help', '_info')">
</ParamField>
<Anchor id="click-shell_completion-CompletionItem-__getattr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.CompletionItem.__getattr__(
    name: str
) -> typing.Any
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-shell_completion-FishComplete">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.shell_completion.FishComplete()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ShellComplete](#click-shell_completion-ShellComplete)

Shell completion for Fish.


<ParamField path="name" type="= 'fish'">
</ParamField>
<Anchor id="click-shell_completion-FishComplete-format_completion">

<CodeBlock links={{"click.shell_completion.CompletionItem":"#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.FishComplete.format_completion(
    item: click.shell_completion.CompletionItem
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-shell_completion-FishComplete-get_completion_args">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.FishComplete.get_completion_args() -> tuple[list[str], str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-shell_completion-ShellComplete">

<CodeBlock links={{"click.core.Command":"/click/click/core#click-core-Command"}} showLineNumbers={false} wordWrap={true}>

```python
class click.shell_completion.ShellComplete(
    cli: click.core.Command,
    ctx_args: collections.abc.MutableMapping[str, typing.Any],
    prog_name: str,
    complete_var: str
)
```

</CodeBlock>
</Anchor>

<Indent>

Base class for providing shell completion support. A subclass for
a given shell will override attributes and methods to implement the
completion instructions (``source`` and ``complete``).

:param cli: Command being called.
:param prog_name: Name of the executable in the shell.
:param complete_var: Name of the environment variable that holds
    the completion instruction.

.. versionadded:: 8.0


<ParamField path="func_name" type="str">
The name of the shell function defined by the completion
script.
</ParamField>

<ParamField path="name" type="str">
Name to register the shell as with :func:`add_completion_class`.
This is used in completion instructions (``&#123;name&#125;_source`` and
``&#123;name&#125;_complete``).
</ParamField>

<ParamField path="source_template" type="str">
Completion script template formatted by :meth:`source`. This must
be provided by subclasses.
</ParamField>
<Anchor id="click-shell_completion-ShellComplete-complete">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ShellComplete.complete() -> str
```

</CodeBlock>
</Anchor>

<Indent>

Produce the completion data to send back to the shell.

By default this calls :meth:`get_completion_args`, gets the
completions, then calls :meth:`format_completion` for each
completion.


</Indent>
<Anchor id="click-shell_completion-ShellComplete-format_completion">

<CodeBlock links={{"click.shell_completion.CompletionItem":"#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ShellComplete.format_completion(
    item: click.shell_completion.CompletionItem
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Format a completion item into the form recognized by the
shell script. This must be implemented by subclasses.

:param item: Completion item to format.


</Indent>
<Anchor id="click-shell_completion-ShellComplete-get_completion_args">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ShellComplete.get_completion_args() -> tuple[list[str], str]
```

</CodeBlock>
</Anchor>

<Indent>

Use the env vars defined by the shell script to return a
tuple of ``args, incomplete``. This must be implemented by
subclasses.


</Indent>
<Anchor id="click-shell_completion-ShellComplete-get_completions">

<CodeBlock links={{"click.shell_completion.CompletionItem":"#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ShellComplete.get_completions(
    args: list[str],
    incomplete: str
) -> list[click.shell_completion.CompletionItem]
```

</CodeBlock>
</Anchor>

<Indent>

Determine the context and last complete command or parameter
from the complete args. Call that object's ``shell_complete``
method to get the completions for the incomplete value.

:param args: List of complete args before the incomplete value.
:param incomplete: Value being completed. May be empty.


</Indent>
<Anchor id="click-shell_completion-ShellComplete-source">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ShellComplete.source() -> str
```

</CodeBlock>
</Anchor>

<Indent>

Produce the shell script that defines the completion
function. By default this ``%``-style formats
:attr:`source_template` with the dict returned by
:meth:`source_vars`.


</Indent>
<Anchor id="click-shell_completion-ShellComplete-source_vars">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ShellComplete.source_vars() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Vars for formatting :attr:`source_template`.

By default this provides ``complete_func``, ``complete_var``,
and ``prog_name``.


</Indent>
</Indent>

<Anchor id="click-shell_completion-ZshComplete">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class click.shell_completion.ZshComplete()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ShellComplete](#click-shell_completion-ShellComplete)

Shell completion for Zsh.


<ParamField path="name" type="= 'zsh'">
</ParamField>
<Anchor id="click-shell_completion-ZshComplete-format_completion">

<CodeBlock links={{"click.shell_completion.CompletionItem":"#click-shell_completion-CompletionItem"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ZshComplete.format_completion(
    item: click.shell_completion.CompletionItem
) -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="click-shell_completion-ZshComplete-get_completion_args">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ZshComplete.get_completion_args() -> tuple[list[str], str]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="click-shell_completion-_is_incomplete_argument">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.core.Parameter":"/click/click/core#click-core-Parameter"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._is_incomplete_argument(
    ctx: click.core.Context,
    param: click.core.Parameter
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Determine if the given parameter is an argument that can still
accept values.

:param ctx: Invocation context for the command represented by the
    parsed complete args.
:param param: Argument object being checked.


</Indent>

<Anchor id="click-shell_completion-_is_incomplete_option">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.core.Parameter":"/click/click/core#click-core-Parameter"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._is_incomplete_option(
    ctx: click.core.Context,
    args: list[str],
    param: click.core.Parameter
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Determine if the given parameter is an option that needs a value.

:param args: List of complete args before the incomplete value.
:param param: Option object being checked.


</Indent>

<Anchor id="click-shell_completion-_resolve_context">

<CodeBlock links={{"click.core.Command":"/click/click/core#click-core-Command","click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._resolve_context(
    cli: click.core.Command,
    ctx_args: collections.abc.MutableMapping[str, typing.Any],
    prog_name: str,
    args: list[str]
) -> click.core.Context
```

</CodeBlock>
</Anchor>

<Indent>

Produce the context hierarchy starting with the command and
traversing the complete arguments. This only follows the commands,
it doesn't trigger input prompts or callbacks.

:param cli: Command being called.
:param prog_name: Name of the executable in the shell.
:param args: List of complete args before the incomplete value.


</Indent>

<Anchor id="click-shell_completion-_resolve_incomplete">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context","click.core.Command":"/click/click/core#click-core-Command","click.core.Parameter":"/click/click/core#click-core-Parameter"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._resolve_incomplete(
    ctx: click.core.Context,
    args: list[str],
    incomplete: str
) -> tuple[click.core.Command | click.core.Parameter, str]
```

</CodeBlock>
</Anchor>

<Indent>

Find the Click object that will handle the completion of the
incomplete value. Return the object and the incomplete value.

:param ctx: Invocation context for the command represented by
    the parsed complete args.
:param args: List of complete args before the incomplete value.
:param incomplete: Value being completed. May be empty.


</Indent>

<Anchor id="click-shell_completion-_start_of_option">

<CodeBlock links={{"click.core.Context":"/click/click/core#click-core-Context"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._start_of_option(
    ctx: click.core.Context,
    value: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check if the value looks like the start of an option.


</Indent>

<Anchor id="click-shell_completion-add_completion_class">

<CodeBlock links={{"click.shell_completion.ShellCompleteType":"#click-shell_completion-ShellCompleteType"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.add_completion_class(
    cls: click.shell_completion.ShellCompleteType,
    name: str | None = None
) -> click.shell_completion.ShellCompleteType
```

</CodeBlock>
</Anchor>

<Indent>

Register a :class:`ShellComplete` subclass under the given name.
The name will be provided by the completion instruction environment
variable during completion.

:param cls: The completion class that will handle completion for the
    shell.
:param name: Name to register the class under. Defaults to the
    class's ``name`` attribute.


</Indent>

<Anchor id="click-shell_completion-get_completion_class">

<CodeBlock links={{"click.shell_completion.ShellComplete":"#click-shell_completion-ShellComplete"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.get_completion_class(
    shell: str
) -> type[click.shell_completion.ShellComplete] | None
```

</CodeBlock>
</Anchor>

<Indent>

Look up a registered :class:`ShellComplete` subclass by the name
provided by the completion instruction environment variable. If the
name isn't registered, returns ``None``.

:param shell: Name the class is registered under.


</Indent>

<Anchor id="click-shell_completion-shell_complete">

<CodeBlock links={{"click.core.Command":"/click/click/core#click-core-Command"}} showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.shell_complete(
    cli: click.core.Command,
    ctx_args: collections.abc.MutableMapping[str, typing.Any],
    prog_name: str,
    complete_var: str,
    instruction: str
) -> int
```

</CodeBlock>
</Anchor>

<Indent>

Perform shell completion for the given CLI program.

:param cli: Command being called.
:param ctx_args: Extra arguments to pass to
    ``cli.make_context``.
:param prog_name: Name of the executable in the shell.
:param complete_var: Name of the environment variable that holds
    the completion instruction.
:param instruction: Value of ``complete_var`` with the completion
    instruction and shell, in the form ``instruction_shell``.
:return: Status code to exit with.


</Indent>

<Anchor id="click-shell_completion-split_arg_string">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.split_arg_string(
    string: str
) -> list[str]
```

</CodeBlock>
</Anchor>

<Indent>

Split an argument string as with :func:`shlex.split`, but don't
fail if the string is incomplete. Ignores a missing closing quote or
incomplete escape sequence and uses the partial token as-is.

.. code-block:: python

    split_arg_string("example 'my file")
    ["example", "my file"]

    split_arg_string("example my\")
    ["example", "my"]

:param string: String to split.

.. versionchanged:: 8.2
    Moved to ``shell_completion`` from ``parser``.


</Indent>

<Anchor id="click-shell_completion-ShellCompleteType">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion.ShellCompleteType = t.TypeVar('ShellCompleteType', bound='type[ShellComplete]')
```

</CodeBlock>
</Anchor>


<Anchor id="click-shell_completion-_SOURCE_BASH">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._SOURCE_BASH = '%(complete_func)s() {\n    local IFS=$\'\\n\'\n    local response\n\n    respon...
```

</CodeBlock>
</Anchor>


<Anchor id="click-shell_completion-_SOURCE_FISH">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._SOURCE_FISH = 'function %(complete_func)s;\n    set -l response (env %(complete_var)s=fish_com...
```

</CodeBlock>
</Anchor>


<Anchor id="click-shell_completion-_SOURCE_ZSH">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._SOURCE_ZSH = '#compdef %(prog_name)s\n\n%(complete_func)s() {\n    local -a completions\n    ...
```

</CodeBlock>
</Anchor>


<Anchor id="click-shell_completion-_available_shells">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
click.shell_completion._available_shells: dict[str, type[ShellComplete]] = {'bash': BashComplete, 'fish': FishComplete, 'zsh': ZshComplete}
```

</CodeBlock>
</Anchor>

