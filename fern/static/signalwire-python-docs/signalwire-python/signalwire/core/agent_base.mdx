---
layout: overview
slug: signalwire-python/signalwire/core/agent_base
title: signalwire.core.agent_base
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`AgentBase`](#signalwire-core-agent_base-AgentBase) | Base class for all SignalWire AI Agents. |

### Data

[`logger`](#signalwire-core-agent_base-logger)

### API

<Anchor id="signalwire-core-agent_base-AgentBase">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.agent_base.AgentBase(
    name: str,
    route: str = '/',
    host: str = '0.0.0.0',
    port: typing.Optional[int] = None,
    basic_auth: typing.Optional[typing.Tuple[str, str]] = None,
    use_pom: bool = True,
    token_expiry_secs: int = 3600,
    auto_answer: bool = True,
    record_call: bool = False,
    record_format: str = 'mp4',
    record_stereo: bool = True,
    default_webhook_url: typing.Optional[str] = None,
    agent_id: typing.Optional[str] = None,
    native_functions: typing.Optional[typing.List[str]] = None,
    schema_path: typing.Optional[str] = None,
    suppress_logs: bool = False,
    enable_post_prompt_override: bool = False,
    check_for_input_override: bool = False,
    config_file: typing.Optional[str] = None,
    schema_validation: bool = True
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [AuthMixin](/signalwire-python/signalwire/core/mixins/auth_mixin#signalwire-core-mixins-auth_mixin-AuthMixin), [WebMixin](/signalwire-python/signalwire/core/mixins/web_mixin#signalwire-core-mixins-web_mixin-WebMixin), [SWMLService](/signalwire-python/signalwire/core/swml_service#signalwire-core-swml_service-SWMLService), [PromptMixin](/signalwire-python/signalwire/core/mixins/prompt_mixin#signalwire-core-mixins-prompt_mixin-PromptMixin), [ToolMixin](/signalwire-python/signalwire/core/mixins/tool_mixin#signalwire-core-mixins-tool_mixin-ToolMixin), [SkillMixin](/signalwire-python/signalwire/core/mixins/skill_mixin#signalwire-core-mixins-skill_mixin-SkillMixin), [AIConfigMixin](/signalwire-python/signalwire/core/mixins/ai_config_mixin#signalwire-core-mixins-ai_config_mixin-AIConfigMixin), [ServerlessMixin](/signalwire-python/signalwire/core/mixins/serverless_mixin#signalwire-core-mixins-serverless_mixin-ServerlessMixin), [StateMixin](/signalwire-python/signalwire/core/mixins/state_mixin#signalwire-core-mixins-state_mixin-StateMixin), [MCPServerMixin](/signalwire-python/signalwire/core/mixins/mcp_server_mixin#signalwire-core-mixins-mcp_server_mixin-MCPServerMixin)

Base class for all SignalWire AI Agents.

This class extends SWMLService and provides enhanced functionality for building agents including:
- Prompt building and customization
- SWML rendering
- SWAIG function definition and execution
- Web service for serving SWML and handling webhooks
- Security and session management

Subclassing options:
1. Simple override of get_prompt() for raw text
2. Using prompt_* methods for structured prompts
3. Declarative PROMPT_SECTIONS class attribute


<ParamField path="_AUTO_ANSWER_VERBS" type="= &#123;'play', 'connect'&#125;">
</ParamField>

<ParamField path="_PRE_ANSWER_SAFE_VERBS">
</ParamField>

<ParamField path="_answer_config" type="= &#123;&#125;">
</ParamField>

<ParamField path="_debug_events_level" type="= 1">
</ParamField>

<ParamField path="_function_includes" type="= []">
</ParamField>

<ParamField path="_global_data" type="= &#123;&#125;">
</ParamField>

<ParamField path="_hints" type="= []">
</ParamField>

<ParamField path="_languages" type="= []">
</ParamField>

<ParamField path="_mcp_servers" type="= []">
</ParamField>

<ParamField path="_params" type="= &#123;&#125;">
</ParamField>

<ParamField path="_post_ai_verbs" type="= []">
</ParamField>

<ParamField path="_post_answer_verbs" type="= []">
</ParamField>

<ParamField path="_post_prompt_llm_params" type="= &#123;&#125;">
</ParamField>

<ParamField path="_pre_answer_verbs" type="= []">
</ParamField>

<ParamField path="_prompt_llm_params" type="= &#123;&#125;">
</ParamField>

<ParamField path="_prompt_manager" type="= PromptManager(self)">
</ParamField>

<ParamField path="_pronounce" type="= []">
</ParamField>

<ParamField path="_proxy_url_base" type="= os.environ.get('SWML_PROXY_URL_BASE')">
</ParamField>

<ParamField path="_session_manager">
</ParamField>

<ParamField path="_swaig_query_params" type="= &#123;&#125;">
</ParamField>

<ParamField path="_tool_registry" type="= ToolRegistry(self)">
</ParamField>

<ParamField path="agent_id" type="= agent_id or str(uuid.uuid4())">
</ParamField>

<ParamField path="log" type="= logger.bind(agent=name)">
</ParamField>

<ParamField path="native_functions" type="= native_functions or []">
</ParamField>

<ParamField path="pom" type="= PromptObjectModel()">
</ParamField>

<ParamField path="skill_manager" type="= SkillManager(self)">
</ParamField>

<ParamField path="tool" type="= self._tool_decorator">
</ParamField>
<Anchor id="signalwire-core-agent_base-AgentBase-_build_webhook_url">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase._build_webhook_url(
    endpoint: str,
    query_params: typing.Optional[typing.Dict[str, str]] = None
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Helper method to build webhook URLs consistently

**Parameters:**

<ParamField path="endpoint" type="str">
The endpoint path (e.g., "swaig", "post_prompt")
</ParamField>

<ParamField path="query_params" type="Optional[Dict[str, str]]" default="None">
Optional query parameters to append
</ParamField>

**Returns:** `str`

Fully constructed webhook URL


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-_create_ephemeral_copy">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase._create_ephemeral_copy()
```

</CodeBlock>
</Anchor>

<Indent>

Create a lightweight copy of this agent for ephemeral configuration.

This creates a partial copy that shares most resources but has independent
configuration for SWML generation. Used when dynamic configuration callbacks
need to modify the agent without affecting the persistent state.

**Returns:**

A lightweight copy of the agent suitable for ephemeral modifications


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-_find_summary_in_post_data">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase._find_summary_in_post_data(
    body,
    logger
)
```

</CodeBlock>
</Anchor>

<Indent>

Attempt to find a summary in the post-prompt response data

**Parameters:**

<ParamField path="body">
The request body
</ParamField>

<ParamField path="logger">
Logger instance
</ParamField>

**Returns:**

Summary data or None if not found


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-_handle_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase._handle_request(
    request: fastapi.Request,
    response: fastapi.Response
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Override SWMLService's _handle_request to use AgentBase's _render_swml

This ensures that when routes are handled by SWMLService's router,
they still use AgentBase's SWML rendering logic.


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-_load_service_config">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase._load_service_config(
    config_file: typing.Optional[str],
    service_name: str
) -> dict
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>staticmethod</Badge>

Load service configuration from config file if available


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-_render_swml">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase._render_swml(
    call_id: str = None,
    modifications: typing.Optional[dict] = None
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Render the complete SWML document using SWMLService methods

**Parameters:**

<ParamField path="call_id" type="str" default="None">
Optional call ID for session-specific tokens
</ParamField>

<ParamField path="modifications" type="Optional[dict]" default="None">
Optional dict of modifications to apply to the SWML
</ParamField>

**Returns:** `str`

SWML document as a string


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-add_answer_verb">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.add_answer_verb(
    config: typing.Optional[typing.Dict[str, typing.Any]] = None
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Configure the answer verb.

The answer verb connects the call. Use this method to customize
answer behavior, such as setting max_duration.

**Parameters:**

<ParamField path="config" type="Optional[Dict[str, Any]]" default="None">
Optional answer verb configuration (e.g., &#123;"max_duration": 3600&#125;)
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-add_post_ai_verb">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.add_post_ai_verb(
    verb_name: str,
    config: typing.Dict[str, typing.Any]
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Add a verb to run after the AI conversation ends.

Post-AI verbs run when the AI completes its conversation. Common uses
include clean disconnects, transfers, and logging.

**Parameters:**

<ParamField path="verb_name" type="str">
The SWML verb name (e.g., "hangup", "transfer", "request")
</ParamField>

<ParamField path="config" type="Dict[str, Any]">
Verb configuration dictionary
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-add_post_answer_verb">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.add_post_answer_verb(
    verb_name: str,
    config: typing.Dict[str, typing.Any]
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Add a verb to run after the call is answered but before the AI starts.

Post-answer verbs run after the call is connected. Common uses include
welcome messages, legal disclaimers, and hold music.

**Parameters:**

<ParamField path="verb_name" type="str">
The SWML verb name (e.g., "play", "sleep")
</ParamField>

<ParamField path="config" type="Dict[str, Any]">
Verb configuration dictionary
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-add_pre_answer_verb">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.add_pre_answer_verb(
    verb_name: str,
    config: typing.Dict[str, typing.Any]
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Add a verb to run before the call is answered.

Pre-answer verbs execute while the call is still ringing. Only certain
verbs are safe to use before answering:

Verbs with auto_answer option (play, connect): Must include
"auto_answer": False in config to prevent automatic answering.

**Parameters:**

<ParamField path="verb_name" type="str">
The SWML verb name (e.g., "play", "sleep", "request")
</ParamField>

<ParamField path="config" type="Dict[str, Any]">
Verb configuration dictionary
</ParamField>

**Returns:** `AgentBase`

Self for method chaining

**Raises:**

- `ValueError`: If verb is not safe for pre-answer use


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-add_swaig_query_params">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.add_swaig_query_params(
    params: typing.Dict[str, str]
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Add query parameters that will be included in all SWAIG webhook URLs

This is particularly useful for preserving dynamic configuration state
across SWAIG callbacks. For example, if your dynamic config adds skills
based on query parameters, you can pass those same parameters through
to the SWAIG webhook so the same configuration is applied.

**Parameters:**

<ParamField path="params" type="Dict[str, str]">
Dictionary of query parameters to add to SWAIG URLs
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-auto_map_sip_usernames">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.auto_map_sip_usernames() -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Automatically register common SIP usernames based on this agent's 
name and route

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-clear_post_ai_verbs">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.clear_post_ai_verbs() -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Remove all post-AI verbs.

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-clear_post_answer_verbs">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.clear_post_answer_verbs() -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Remove all post-answer verbs.

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-clear_pre_answer_verbs">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.clear_pre_answer_verbs() -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Remove all pre-answer verbs.

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-clear_swaig_query_params">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.clear_swaig_query_params() -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Clear all SWAIG query parameters

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-enable_sip_routing">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.enable_sip_routing(
    auto_map: bool = True,
    path: str = '/sip'
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Enable SIP-based routing for this agent

This allows the agent to automatically route SIP requests based on SIP usernames.
When enabled, an endpoint at the specified path is automatically created
that will handle SIP requests and deliver them to this agent.

**Parameters:**

<ParamField path="auto_map" type="bool" default="True">
Whether to automatically map common SIP usernames to this agent
     (based on the agent name and route path)
</ParamField>

<ParamField path="path" type="str" default="'/sip'">
The path to register the SIP routing endpoint (default: "/sip")
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-get_full_url">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.get_full_url(
    include_auth: bool = False
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Get the full URL for this agent's endpoint

**Parameters:**

<ParamField path="include_auth" type="bool" default="False">
Whether to include authentication credentials in the URL
</ParamField>

**Returns:** `str`

Full URL including host, port, and route (with auth if requested)


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-get_name">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.get_name() -> str
```

</CodeBlock>
</Anchor>

<Indent>

Get agent name

**Returns:** `str`

Agent name


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-on_debug_event">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.on_debug_event(
    handler: typing.Callable
) -> typing.Callable
```

</CodeBlock>
</Anchor>

<Indent>

Register a handler for debug webhook events.

Use as a decorator to receive real-time debug events from the AI module
during calls. Requires enable_debug_events() to be called first.

The handler may be sync or async.

**Parameters:**

<ParamField path="handler" type="Callable">
Callback function with signature (event_type: str, data: dict)
</ParamField>

**Returns:** `Callable`

The handler function (unchanged), for use as a decorator


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-on_summary">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.on_summary(
    summary: typing.Optional[typing.Dict[str, typing.Any]],
    raw_data: typing.Optional[typing.Dict[str, typing.Any]] = None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Called when a post-prompt summary is received

**Parameters:**

<ParamField path="summary" type="Optional[Dict[str, Any]]">
The summary object or None if no summary was found
</ParamField>

<ParamField path="raw_data" type="Optional[Dict[str, Any]]" default="None">
The complete raw POST data from the request
</ParamField>


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-register_sip_username">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.register_sip_username(
    sip_username: str
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Register a SIP username that should be routed to this agent

**Parameters:**

<ParamField path="sip_username" type="str">
SIP username to register
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-set_post_prompt_url">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.set_post_prompt_url(
    url: str
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Override the default post_prompt_url with a supplied URL string

**Parameters:**

<ParamField path="url" type="str">
The URL to use for post-prompt summary delivery
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-agent_base-AgentBase-set_web_hook_url">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.AgentBase.set_web_hook_url(
    url: str
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Override the default web_hook_url with a supplied URL string

**Parameters:**

<ParamField path="url" type="str">
The URL to use for SWAIG function webhooks
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
</Indent>

<Anchor id="signalwire-core-agent_base-logger">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.agent_base.logger = get_logger('agent_base')
```

</CodeBlock>
</Anchor>

