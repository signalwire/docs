---
layout: overview
slug: signalwire-python/signalwire/core/swml_handler
title: signalwire.core.swml_handler
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`AIVerbHandler`](#signalwire-core-swml_handler-AIVerbHandler) | Handler for the SWML 'ai' verb |
| [`SWMLVerbHandler`](#signalwire-core-swml_handler-SWMLVerbHandler) | Base interface for SWML verb handlers |
| [`VerbHandlerRegistry`](#signalwire-core-swml_handler-VerbHandlerRegistry) | Registry for SWML verb handlers |

### API

<Anchor id="signalwire-core-swml_handler-AIVerbHandler">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.swml_handler.AIVerbHandler()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [SWMLVerbHandler](#signalwire-core-swml_handler-SWMLVerbHandler)

Handler for the SWML 'ai' verb

The 'ai' verb is complex and requires specialized handling, particularly
for managing prompts, SWAIG functions, and AI configurations.


<Anchor id="signalwire-core-swml_handler-AIVerbHandler-build_config">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.AIVerbHandler.build_config(
    prompt_text: typing.Optional[str] = None,
    prompt_pom: typing.Optional[typing.List[typing.Dict[str, typing.Any]]] = None,
    contexts: typing.Optional[typing.Dict[str, typing.Any]] = None,
    post_prompt: typing.Optional[str] = None,
    post_prompt_url: typing.Optional[str] = None,
    swaig: typing.Optional[typing.Dict[str, typing.Any]] = None,
    kwargs = {}
) -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Build a configuration for the AI verb

**Parameters:**

<ParamField path="prompt_text" type="Optional[str]" default="None">
Text prompt for the AI (mutually exclusive with prompt_pom)
</ParamField>

<ParamField path="prompt_pom" type="Optional[List[Dict[str, Any]]]" default="None">
POM structure for the AI prompt (mutually exclusive with prompt_text)
</ParamField>

<ParamField path="contexts" type="Optional[Dict[str, Any]]" default="None">
Optional contexts and steps configuration (can be combined with text or pom)
</ParamField>

<ParamField path="post_prompt" type="Optional[str]" default="None">
Optional post-prompt text
</ParamField>

<ParamField path="post_prompt_url" type="Optional[str]" default="None">
Optional URL for post-prompt processing
</ParamField>

<ParamField path="swaig" type="Optional[Dict[str, Any]]" default="None">
Optional SWAIG configuration
</ParamField>

<ParamField path="**kwargs" default="&#123;&#125;">
Additional AI parameters
</ParamField>

**Returns:** `Dict[str, Any]`

AI verb configuration dictionary


</Indent>
<Anchor id="signalwire-core-swml_handler-AIVerbHandler-get_verb_name">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.AIVerbHandler.get_verb_name() -> str
```

</CodeBlock>
</Anchor>

<Indent>

Get the name of the verb this handler handles

**Returns:** `str`

"ai" as the verb name


</Indent>
<Anchor id="signalwire-core-swml_handler-AIVerbHandler-validate_config">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.AIVerbHandler.validate_config(
    config: typing.Dict[str, typing.Any]
) -> typing.Tuple[bool, typing.List[str]]
```

</CodeBlock>
</Anchor>

<Indent>

Validate the configuration for the AI verb

**Parameters:**

<ParamField path="config" type="Dict[str, Any]">
The configuration dictionary for the AI verb
</ParamField>

**Returns:** `Tuple[bool, List[str]]`

(is_valid, error_messages) tuple


</Indent>
</Indent>

<Anchor id="signalwire-core-swml_handler-SWMLVerbHandler">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.swml_handler.SWMLVerbHandler()
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Abstract</Badge>

Base interface for SWML verb handlers

This abstract class defines the interface that all SWML verb handlers
must implement. Verb handlers provide specialized logic for complex
SWML verbs that cannot be handled generically.


<Anchor id="signalwire-core-swml_handler-SWMLVerbHandler-build_config">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.SWMLVerbHandler.build_config(
    kwargs = {}
) -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>abstract</Badge>

Build a configuration for this verb from the provided arguments

**Parameters:**

<ParamField path="**kwargs" default="&#123;&#125;">
Keyword arguments specific to this verb
</ParamField>

**Returns:** `Dict[str, Any]`

Configuration dictionary


</Indent>
<Anchor id="signalwire-core-swml_handler-SWMLVerbHandler-get_verb_name">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.SWMLVerbHandler.get_verb_name() -> str
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>abstract</Badge>

Get the name of the verb this handler handles

**Returns:** `str`

The verb name as a string


</Indent>
<Anchor id="signalwire-core-swml_handler-SWMLVerbHandler-validate_config">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.SWMLVerbHandler.validate_config(
    config: typing.Dict[str, typing.Any]
) -> typing.Tuple[bool, typing.List[str]]
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>abstract</Badge>

Validate the configuration for this verb

**Parameters:**

<ParamField path="config" type="Dict[str, Any]">
The configuration dictionary for this verb
</ParamField>

**Returns:** `Tuple[bool, List[str]]`

(is_valid, error_messages) tuple


</Indent>
</Indent>

<Anchor id="signalwire-core-swml_handler-VerbHandlerRegistry">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.swml_handler.VerbHandlerRegistry()
```

</CodeBlock>
</Anchor>

<Indent>

Registry for SWML verb handlers

This class maintains a registry of handlers for special SWML verbs
and provides methods for accessing and using them.


<ParamField path="_handlers" type="= &#123;&#125;">
</ParamField>
<Anchor id="signalwire-core-swml_handler-VerbHandlerRegistry-get_handler">

<CodeBlock links={{"signalwire.core.swml_handler.SWMLVerbHandler":"#signalwire-core-swml_handler-SWMLVerbHandler"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.VerbHandlerRegistry.get_handler(
    verb_name: str
) -> typing.Optional[signalwire.core.swml_handler.SWMLVerbHandler]
```

</CodeBlock>
</Anchor>

<Indent>

Get the handler for a specific verb

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb
</ParamField>

**Returns:** `Optional[SWMLVerbHandler]`

The handler if found, None otherwise


</Indent>
<Anchor id="signalwire-core-swml_handler-VerbHandlerRegistry-has_handler">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.VerbHandlerRegistry.has_handler(
    verb_name: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check if a handler exists for a specific verb

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb
</ParamField>

**Returns:** `bool`

True if a handler exists, False otherwise


</Indent>
<Anchor id="signalwire-core-swml_handler-VerbHandlerRegistry-register_handler">

<CodeBlock links={{"signalwire.core.swml_handler.SWMLVerbHandler":"#signalwire-core-swml_handler-SWMLVerbHandler"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.swml_handler.VerbHandlerRegistry.register_handler(
    handler: signalwire.core.swml_handler.SWMLVerbHandler
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Register a new verb handler

**Parameters:**

<ParamField path="handler" type="SWMLVerbHandler">
The handler to register
</ParamField>


</Indent>
</Indent>
