---
layout: overview
slug: signalwire-python/signalwire/core/function_result
title: signalwire.core.function_result
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`FunctionResult`](#signalwire-core-function_result-FunctionResult) | Wrapper around SWAIG function responses that handles proper formatting |

### API

<Anchor id="signalwire-core-function_result-FunctionResult">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.function_result.FunctionResult(
    response: typing.Optional[str] = None,
    post_process: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

Wrapper around SWAIG function responses that handles proper formatting
of response text and actions.

The result object has three main components:
1. response: Text the AI should say back to the user
2. action: List of structured actions to execute 
3. post_process: Whether to let AI take another turn before executing actions

Post-processing behavior:
- post_process=False (default): Execute actions immediately after AI response
- post_process=True: Let AI respond to user one more time, then execute actions

This is useful for confirmation workflows like:
"I'll transfer you to sales. Do you have any other questions first?"
(AI can handle follow-up, then execute the transfer)


<ParamField path="action" type="List[Dict[str, Any]] = []">
</ParamField>

<ParamField path="response" type="= response if response is not None else ''">
</ParamField>
<Anchor id="signalwire-core-function_result-FunctionResult-add_action">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.add_action(
    name: str,
    data: typing.Any
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Add a structured action to the response

**Parameters:**

<ParamField path="name" type="str">
The name/type of the action (e.g., "play", "transfer")
</ParamField>

<ParamField path="data" type="Any">
The data for the action - can be a string, boolean, object, or array
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-add_actions">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.add_actions(
    actions: typing.List[typing.Dict[str, typing.Any]]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Add multiple structured actions to the response

**Parameters:**

<ParamField path="actions" type="List[Dict[str, Any]]">
List of action objects to add to the response
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-add_dynamic_hints">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.add_dynamic_hints(
    hints: typing.List[typing.Union[str, typing.Dict[str, typing.Any]]]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Add dynamic speech recognition hints during a call.

Hints improve speech recognition accuracy for domain-specific terms.
Each hint can be a simple string or a pronunciation pattern object.

**Parameters:**

<ParamField path="hints" type="List[Union[str, Dict[str, Any]]]">
List of hints, where each entry is either:
- A string: Simple hint word/phrase (e.g., "Cabby")
- A dict with pronunciation pattern:
    - "pattern": Regex pattern to match in recognized speech
    - "replace": Replacement text when pattern matches
    - "ignore_case": Optional bool for case-insensitive matching
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-clear_dynamic_hints">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.clear_dynamic_hints() -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Clear all dynamic speech recognition hints.

Removes all hints previously added via add_dynamic_hints().

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-connect">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.connect(
    destination: str,
    final: bool = True,
    from_addr: typing.Optional[str] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Add a connect action to transfer/connect the call to another destination.

This is a convenience method that abstracts the SWML connect verb, so users
don't need to manually construct SWML documents.

Transfer behavior:
- final=True: Permanent transfer - call exits the agent completely, 
             SWML replaces the agent and call continues there
- final=False: Temporary transfer - if far end hangs up, call returns 
              to the agent to continue the conversation

**Parameters:**

<ParamField path="destination" type="str">
Where to connect the call (phone number, SIP address, etc.)
</ParamField>

<ParamField path="final" type="bool" default="True">
Whether this is a permanent transfer (True) or temporary (False).
  Defaults to True for permanent transfers.
</ParamField>

<ParamField path="from_addr" type="Optional[str]" default="None">
Optional caller ID override (phone number or SIP address).
      If None, uses the current call's from address.
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-create_payment_action">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.create_payment_action(
    action_type: str,
    phrase: str
) -> typing.Dict[str, str]
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>staticmethod</Badge>

Create a payment action for use in payment prompts.

**Parameters:**

<ParamField path="action_type" type="str">
"Say" for text-to-speech or "Play" for audio file
</ParamField>

<ParamField path="phrase" type="str">
Sentence to say or URL to play
</ParamField>

**Returns:** `Dict[str, str]`

Dictionary representing the action


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-create_payment_parameter">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.create_payment_parameter(
    name: str,
    value: str
) -> typing.Dict[str, str]
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>staticmethod</Badge>

Create a payment parameter for use with pay() method.

**Parameters:**

<ParamField path="name" type="str">
Parameter name
</ParamField>

<ParamField path="value" type="str">
Parameter value
</ParamField>

**Returns:** `Dict[str, str]`

Dictionary representing the parameter


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-create_payment_prompt">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.create_payment_prompt(
    for_situation: str,
    actions: typing.List[typing.Dict[str, str]],
    card_type: typing.Optional[str] = None,
    error_type: typing.Optional[str] = None
) -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>staticmethod</Badge>

Create a payment prompt structure for use with pay() method.

**Parameters:**

<ParamField path="for_situation" type="str">
Situation to use prompt for (e.g., "payment-card-number")
</ParamField>

<ParamField path="actions" type="List[Dict[str, str]]">
List of actions with 'type' and 'phrase' keys
</ParamField>

<ParamField path="card_type" type="Optional[str]" default="None">
Space-separated card types for this prompt
</ParamField>

<ParamField path="error_type" type="Optional[str]" default="None">
Space-separated error types for this prompt
</ParamField>

**Returns:** `Dict[str, Any]`

Dictionary representing the prompt structure


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-enable_extensive_data">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.enable_extensive_data(
    enabled: bool = True
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Send full data to LLM for this turn only, then use smaller replacement
in subsequent turns.

**Parameters:**

<ParamField path="enabled" type="bool" default="True">
Whether to send extensive data this turn only
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-enable_functions_on_timeout">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.enable_functions_on_timeout(
    enabled: bool = True
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Enable function calls on speaker timeout.

**Parameters:**

<ParamField path="enabled" type="bool" default="True">
Whether to enable functions on timeout
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-execute_rpc">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.execute_rpc(
    method: str,
    params: typing.Optional[typing.Dict[str, typing.Any]] = None,
    call_id: typing.Optional[str] = None,
    node_id: typing.Optional[str] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Execute an RPC method on a call using SWML.

This is a generic helper for executing RPC commands. For common operations,
consider using the specific helpers: rpc_dial(), rpc_ai_message(), rpc_ai_unhold().

**Parameters:**

<ParamField path="method" type="str">
The RPC method to execute (e.g., "dial", "ai_message", "ai_unhold")
</ParamField>

<ParamField path="params" type="Optional[Dict[str, Any]]" default="None">
Parameters for the RPC method (optional)
</ParamField>

<ParamField path="call_id" type="Optional[str]" default="None">
Target call ID for the RPC (optional)
</ParamField>

<ParamField path="node_id" type="Optional[str]" default="None">
Target node ID for the RPC (optional)
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-execute_swml">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.execute_swml(
    swml_content,
    transfer: bool = False
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Execute SWML content with optional transfer behavior.

**Parameters:**

<ParamField path="swml_content">
Can be:
- String: Raw SWML JSON text
- Dict: SWML data structure
- SWML object: SignalWire SWML SDK object with .to_dict() method
</ParamField>

<ParamField path="transfer" type="bool" default="False">
Boolean - whether call should exit agent after execution
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-hangup">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.hangup() -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Terminate the call.

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-hold">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.hold(
    timeout: int = 300
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Put the call on hold with optional timeout.

**Parameters:**

<ParamField path="timeout" type="int" default="300">
Timeout in seconds (max 900, default 300)
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-join_conference">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.join_conference(
    name: str,
    muted: bool = False,
    beep: str = 'true',
    start_on_enter: bool = True,
    end_on_exit: bool = False,
    wait_url: typing.Optional[str] = None,
    max_participants: int = 250,
    record: str = 'do-not-record',
    region: typing.Optional[str] = None,
    trim: str = 'trim-silence',
    coach: typing.Optional[str] = None,
    status_callback_event: typing.Optional[str] = None,
    status_callback: typing.Optional[str] = None,
    status_callback_method: str = 'POST',
    recording_status_callback: typing.Optional[str] = None,
    recording_status_callback_method: str = 'POST',
    recording_status_callback_event: str = 'completed',
    result: typing.Optional[typing.Any] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Join an ad-hoc audio conference with RELAY and CXML calls using SWML.

This is a virtual helper that generates SWML to join audio conferences
with extensive configuration options for call management and recording.

**Parameters:**

<ParamField path="name" type="str">
Name of conference (required)
</ParamField>

<ParamField path="muted" type="bool" default="False">
Whether to join muted (default: False)
</ParamField>

<ParamField path="beep" type="str" default="'true'">
Beep configuration - "true", "false", "onEnter", "onExit" (default: "true")
</ParamField>

<ParamField path="start_on_enter" type="bool" default="True">
Whether conference starts when this participant enters (default: True)
</ParamField>

<ParamField path="end_on_exit" type="bool" default="False">
Whether conference ends when this participant exits (default: False)
</ParamField>

<ParamField path="wait_url" type="Optional[str]" default="None">
SWML URL for hold music (default: None for default hold music)
</ParamField>

<ParamField path="max_participants" type="int" default="250">
Maximum participants &lt;= 250 (default: 250)
</ParamField>

<ParamField path="record" type="str" default="'do-not-record'">
Recording mode - "do-not-record", "record-from-start" (default: "do-not-record")
</ParamField>

<ParamField path="region" type="Optional[str]" default="None">
Conference region (default: None)
</ParamField>

<ParamField path="trim" type="str" default="'trim-silence'">
Trim silence - "trim-silence", "do-not-trim" (default: "trim-silence")
</ParamField>

<ParamField path="coach" type="Optional[str]" default="None">
SWML Call ID or CXML CallSid for coaching (default: None)
</ParamField>

<ParamField path="status_callback_event" type="Optional[str]" default="None">
Events to report - "start end join leave mute hold modify speaker announcement" (default: None)
</ParamField>

<ParamField path="status_callback" type="Optional[str]" default="None">
URL for status callbacks (default: None)
</ParamField>

<ParamField path="status_callback_method" type="str" default="'POST'">
HTTP method - "GET", "POST" (default: "POST")
</ParamField>

<ParamField path="recording_status_callback" type="Optional[str]" default="None">
URL for recording status callbacks (default: None)
</ParamField>

<ParamField path="recording_status_callback_method" type="str" default="'POST'">
HTTP method - "GET", "POST" (default: "POST")
</ParamField>

<ParamField path="recording_status_callback_event" type="str" default="'completed'">
Recording events - "in-progress completed absent" (default: "completed")
</ParamField>

<ParamField path="result" type="Optional[Any]" default="None">
Switch on return_value when object &#123;&#125; or cond when array [] (default: None)
</ParamField>

**Returns:** `FunctionResult`

self for method chaining

**Raises:**

- `ValueError`: If beep value is invalid or max_participants exceeds 250


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-join_room">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.join_room(
    name: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Join a RELAY room using SWML.

This is a virtual helper that generates SWML to join a RELAY room,
which enables multi-party communication and collaboration.

**Parameters:**

<ParamField path="name" type="str">
The name of the room to join (required)
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-pay">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.pay(
    payment_connector_url: str,
    input_method: str = 'dtmf',
    status_url: typing.Optional[str] = None,
    payment_method: str = 'credit-card',
    timeout: int = 5,
    max_attempts: int = 1,
    security_code: bool = True,
    postal_code: typing.Union[bool, str] = True,
    min_postal_code_length: int = 0,
    token_type: str = 'reusable',
    charge_amount: typing.Optional[str] = None,
    currency: str = 'usd',
    language: str = 'en-US',
    voice: str = 'woman',
    description: typing.Optional[str] = None,
    valid_card_types: str = 'visa mastercard amex',
    parameters: typing.Optional[typing.List[typing.Dict[str, str]]] = None,
    prompts: typing.Optional[typing.List[typing.Dict[str, typing.Any]]] = None,
    ai_response: typing.Optional[str] = 'The payment status is ${pa...
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Process payment using SWML pay action.

This is a virtual helper that generates SWML for payment processing.

**Parameters:**

<ParamField path="payment_connector_url" type="str">
URL to make payment requests to (required)
</ParamField>

<ParamField path="input_method" type="str" default="'dtmf'">
Method to collect payment details ("dtmf" or "voice")
</ParamField>

<ParamField path="status_url" type="Optional[str]" default="None">
URL for status change notifications
</ParamField>

<ParamField path="payment_method" type="str" default="'credit-card'">
Payment method ("credit-card" currently supported)
</ParamField>

<ParamField path="timeout" type="int" default="5">
Seconds to wait for next digit (default: 5)
</ParamField>

<ParamField path="max_attempts" type="int" default="1">
Number of retry attempts (default: 1)
</ParamField>

<ParamField path="security_code" type="bool" default="True">
Whether to prompt for security code (default: True)
</ParamField>

<ParamField path="postal_code" type="Union[bool, str]" default="True">
Whether to prompt for postal code, or actual postcode
</ParamField>

<ParamField path="min_postal_code_length" type="int" default="0">
Minimum postal code digits (default: 0)
</ParamField>

<ParamField path="token_type" type="str" default="'reusable'">
Payment type ("one-time" or "reusable", default: "reusable")
</ParamField>

<ParamField path="charge_amount" type="Optional[str]" default="None">
Amount to charge as decimal string
</ParamField>

<ParamField path="currency" type="str" default="'usd'">
Currency code (default: "usd")
</ParamField>

<ParamField path="language" type="str" default="'en-US'">
Language for prompts (default: "en-US")
</ParamField>

<ParamField path="voice" type="str" default="'woman'">
TTS voice to use (default: "woman")
</ParamField>

<ParamField path="description" type="Optional[str]" default="None">
Custom payment description
</ParamField>

<ParamField path="valid_card_types" type="str" default="'visa mastercard amex'">
Space-separated card types (default: "visa mastercard amex")
</ParamField>

<ParamField path="parameters" type="Optional[List[Dict[str, str]]]" default="None">
Array of name/value pairs for payment connector
</ParamField>

<ParamField path="prompts" type="Optional[List[Dict[str, Any]]]" default="None">
Array of custom prompt configurations
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-play_background_file">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.play_background_file(
    filename: str,
    wait: bool = False
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Play audio or video file in background.

**Parameters:**

<ParamField path="filename" type="str">
Audio/video filename/path
</ParamField>

<ParamField path="wait" type="bool" default="False">
Whether to suppress attention-getting behavior during playback
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-record_call">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.record_call(
    control_id: typing.Optional[str] = None,
    stereo: bool = False,
    format: str = 'wav',
    direction: str = 'both',
    terminators: typing.Optional[str] = None,
    beep: bool = False,
    input_sensitivity: float = 44.0,
    initial_timeout: typing.Optional[float] = None,
    end_silence_timeout: typing.Optional[float] = None,
    max_length: typing.Optional[float] = None,
    status_url: typing.Optional[str] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Start background call recording using SWML.

This is a virtual helper that generates SWML to start recording the call
in the background. Unlike foreground recording, the script continues
executing while recording happens in the background.

**Parameters:**

<ParamField path="control_id" type="Optional[str]" default="None">
Identifier for this recording (for use with stop_record_call)
</ParamField>

<ParamField path="stereo" type="bool" default="False">
Record in stereo (default: False)
</ParamField>

<ParamField path="format" type="str" default="'wav'">
Recording format - "wav" or "mp3" (default: "wav")
</ParamField>

<ParamField path="direction" type="str" default="'both'">
Audio direction - "speak", "listen", or "both" (default: "both")
</ParamField>

<ParamField path="terminators" type="Optional[str]" default="None">
Digits that stop recording when pressed
</ParamField>

<ParamField path="beep" type="bool" default="False">
Play beep before recording (default: False)
</ParamField>

<ParamField path="input_sensitivity" type="float" default="44.0">
Input sensitivity for recording (default: 44.0)
</ParamField>

<ParamField path="initial_timeout" type="Optional[float]" default="None">
Time in seconds to wait for speech start (for voicemail-style recording)
</ParamField>

<ParamField path="end_silence_timeout" type="Optional[float]" default="None">
Time in seconds to wait in silence before ending (for voicemail-style recording)
</ParamField>

<ParamField path="max_length" type="Optional[float]" default="None">
Maximum recording length in seconds
</ParamField>

<ParamField path="status_url" type="Optional[str]" default="None">
URL to send recording status events to
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-remove_global_data">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.remove_global_data(
    keys: typing.Union[str, typing.List[str]]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Remove global agent data variables.

**Parameters:**

<ParamField path="keys" type="Union[str, List[str]]">
Single key string or list of keys to remove
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-remove_metadata">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.remove_metadata(
    keys: typing.Union[str, typing.List[str]]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Remove metadata from current function's meta_data_token scope.

**Parameters:**

<ParamField path="keys" type="Union[str, List[str]]">
Single key string or list of keys to remove
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-replace_in_history">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.replace_in_history(
    text: typing.Union[str, bool] = True
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

After first send, replace tool_call+result pair in conversation history.

**Parameters:**

<ParamField path="text" type="Union[str, bool]" default="True">
String = replace tool_call with an assistant message containing this text.
  True = remove the tool_call+result pair from history entirely.
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-rpc_ai_message">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.rpc_ai_message(
    call_id: str,
    message_text: str,
    role: str = 'system'
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Inject a message into an AI agent on another call using execute_rpc.

This is useful for cross-call communication, such as notifying a held
caller's AI agent about a status change or instructing it to relay
a message.

**Parameters:**

<ParamField path="call_id" type="str">
The call ID of the target call
</ParamField>

<ParamField path="message_text" type="str">
The message text to inject into the AI conversation
</ParamField>

<ParamField path="role" type="str" default="'system'">
The role for the message, typically "system" (default: "system")
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-rpc_ai_unhold">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.rpc_ai_unhold(
    call_id: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Unhold another call using execute_rpc.

This releases a call from hold state, typically used after injecting
a message to the held caller's AI agent.

**Parameters:**

<ParamField path="call_id" type="str">
The call ID of the call to unhold
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-rpc_dial">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.rpc_dial(
    to_number: str,
    from_number: str,
    dest_swml: str,
    device_type: str = 'phone'
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Dial out to a number with a destination SWML URL using execute_rpc.

This is commonly used in call screening scenarios where you place a caller
on hold and dial out to a human, with the dest_swml specifying what agent
handles the outbound leg.

**Parameters:**

<ParamField path="to_number" type="str">
Phone number to dial (E.164 format)
</ParamField>

<ParamField path="from_number" type="str">
Caller ID to use (E.164 format)
</ParamField>

<ParamField path="dest_swml" type="str">
URL to the SWML that handles the outbound call
</ParamField>

<ParamField path="device_type" type="str" default="'phone'">
Device type, typically "phone" (default: "phone")
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-say">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.say(
    text: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Make the agent speak specific text.

**Parameters:**

<ParamField path="text" type="str">
Text for agent to speak
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-send_sms">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.send_sms(
    to_number: str,
    from_number: str,
    body: typing.Optional[str] = None,
    media: typing.Optional[typing.List[str]] = None,
    tags: typing.Optional[typing.List[str]] = None,
    region: typing.Optional[str] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Send a text message to a PSTN phone number using SWML.

This is a virtual helper that generates SWML to send SMS messages.
Either body or media (or both) must be provided.

**Parameters:**

<ParamField path="to_number" type="str">
Phone number in E.164 format to send to
</ParamField>

<ParamField path="from_number" type="str">
Phone number in E.164 format to send from  
</ParamField>

<ParamField path="body" type="Optional[str]" default="None">
Body text of the message (optional if media provided)
</ParamField>

<ParamField path="media" type="Optional[List[str]]" default="None">
Array of URLs to send in the message (optional if body provided)
</ParamField>

<ParamField path="tags" type="Optional[List[str]]" default="None">
Array of tags to associate with the message for UI searching
</ParamField>

<ParamField path="region" type="Optional[str]" default="None">
Region to originate the message from
</ParamField>

**Returns:** `FunctionResult`

self for method chaining

**Raises:**

- `ValueError`: If neither body nor media is provided


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-set_end_of_speech_timeout">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.set_end_of_speech_timeout(
    milliseconds: int
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Adjust end of speech timeout - milliseconds of silence after speaking 
has been detected to finalize speech recognition.

**Parameters:**

<ParamField path="milliseconds" type="int">
Timeout in milliseconds
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-set_metadata">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.set_metadata(
    data: typing.Dict[str, typing.Any]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Set metadata scoped to current function's meta_data_token.

**Parameters:**

<ParamField path="data" type="Dict[str, Any]">
Dictionary of key-value pairs for metadata
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-set_post_process">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.set_post_process(
    post_process: bool
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Set whether to enable post-processing for this result.

Post-processing allows the AI to take one more turn with the user
before executing any actions. This is useful for confirmation workflows.

**Parameters:**

<ParamField path="post_process" type="bool">
True to let AI respond once more before executing actions,
         False to execute actions immediately after the response.
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-set_response">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.set_response(
    response: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Set the natural language response text

**Parameters:**

<ParamField path="response" type="str">
The text the AI should say
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-set_speech_event_timeout">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.set_speech_event_timeout(
    milliseconds: int
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Adjust speech event timeout - milliseconds since last speech detection 
event to finalize recognition. Works better in noisy environments.

**Parameters:**

<ParamField path="milliseconds" type="int">
Timeout in milliseconds
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-simulate_user_input">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.simulate_user_input(
    text: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Queue simulated user input.

**Parameters:**

<ParamField path="text" type="str">
Text to simulate as user input
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-sip_refer">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.sip_refer(
    to_uri: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Send SIP REFER to a SIP call using SWML.

This is a virtual helper that generates SWML to send a SIP REFER
message, which is used for call transfer in SIP environments.

**Parameters:**

<ParamField path="to_uri" type="str">
The SIP URI to send the REFER to (required)
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-stop">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.stop() -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Stop the agent execution.

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-stop_background_file">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.stop_background_file() -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Stop currently playing background file.

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-stop_record_call">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.stop_record_call(
    control_id: typing.Optional[str] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Stop an active background call recording using SWML.

This is a virtual helper that generates SWML to stop a recording that 
was started with record_call().

**Parameters:**

<ParamField path="control_id" type="Optional[str]" default="None">
Identifier for the recording to stop. If not provided,
       the most recent recording will be stopped.
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-stop_tap">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.stop_tap(
    control_id: typing.Optional[str] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Stop an active tap stream using SWML.

This is a virtual helper that generates SWML to stop a tap stream
that was started with tap().

**Parameters:**

<ParamField path="control_id" type="Optional[str]" default="None">
ID of the tap to stop (optional)
       If not set, the last tap started will be stopped
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-switch_context">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.switch_context(
    system_prompt: typing.Optional[str] = None,
    user_prompt: typing.Optional[str] = None,
    consolidate: bool = False,
    full_reset: bool = False
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Change agent context/prompt during conversation.

**Parameters:**

<ParamField path="system_prompt" type="Optional[str]" default="None">
New system prompt
</ParamField>

<ParamField path="user_prompt" type="Optional[str]" default="None">
User message to add
</ParamField>

<ParamField path="consolidate" type="bool" default="False">
Whether to summarize existing conversation
</ParamField>

<ParamField path="full_reset" type="bool" default="False">
Whether to do complete context reset
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-swml_change_context">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.swml_change_context(
    context_name: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Change the conversation context in the AI agent's workflow.

This is a convenience method for switching between different conversation contexts,
useful for agents that handle multiple distinct workflows or service modes.

**Parameters:**

<ParamField path="context_name" type="str">
Name of the context to transition to (e.g., "support", "sales", "technical")
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-swml_change_step">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.swml_change_step(
    step_name: str
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Change the conversation step in the AI agent's workflow.

This is a convenience method for transitioning between conversation steps,
allowing dynamic workflow control based on user interactions or game state.

**Parameters:**

<ParamField path="step_name" type="str">
Name of the step to transition to (e.g., "betting", "playing", "hand_complete")
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-swml_transfer">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.swml_transfer(
    dest: str,
    ai_response: str,
    final: bool = True
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Add a SWML transfer action with AI response setup for when transfer completes.

This is a virtual helper that generates SWML to transfer the call to another
destination and sets up an AI response for when the transfer completes and
control returns to the agent.

For transfers, you typically want to enable post-processing so the AI speaks
the response first before executing the transfer.

**Parameters:**

<ParamField path="dest" type="str">
Destination URL for the transfer (SWML endpoint, SIP address, etc.)
</ParamField>

<ParamField path="ai_response" type="str">
Message the AI should say when transfer completes and control returns
</ParamField>

<ParamField path="final" type="bool" default="True">
Whether this is a permanent transfer (True) or temporary (False).
  Defaults to True for permanent transfers (same as connect method).
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-swml_user_event">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.swml_user_event(
    event_data: typing.Dict[str, typing.Any]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Send a user event through SWML to update the client UI.

This is a convenience method for sending user events to connected clients,
commonly used for real-time UI updates in interactive applications.

**Parameters:**

<ParamField path="event_data" type="Dict[str, Any]">
Dictionary containing the event type and any associated data
       Example: &#123;"type": "cards_dealt", "player_hand": [...], "score": 21&#125;
</ParamField>

**Returns:** `FunctionResult`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-tap">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.tap(
    uri: str,
    control_id: typing.Optional[str] = None,
    direction: str = 'both',
    codec: str = 'PCMU',
    rtp_ptime: int = 20,
    status_url: typing.Optional[str] = None
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Start background call tap using SWML.

This is a virtual helper that generates SWML to start background call tapping.
Media is streamed over Websocket or RTP to customer controlled URI.

**Parameters:**

<ParamField path="uri" type="str">
Destination of tap media stream (required)
 Formats: rtp://IP:port, ws://example.com, or wss://example.com
</ParamField>

<ParamField path="control_id" type="Optional[str]" default="None">
Identifier for this tap to use with stop_tap (optional)
        Default is generated and stored in tap_control_id variable
</ParamField>

<ParamField path="direction" type="str" default="'both'">
Direction of audio to tap (default: "both")
      "speak" = what party says
      "hear" = what party hears  
      "both" = what party hears and says
</ParamField>

<ParamField path="codec" type="str" default="'PCMU'">
Codec for tap media stream - "PCMU" or "PCMA" (default: "PCMU")
</ParamField>

<ParamField path="rtp_ptime" type="int" default="20">
Packetization time in milliseconds for RTP (default: 20)
</ParamField>

<ParamField path="status_url" type="Optional[str]" default="None">
URL for status change requests (optional)
</ParamField>

**Returns:** `FunctionResult`

self for method chaining

**Raises:**

- `ValueError`: If direction or codec values are invalid


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-to_dict">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.to_dict() -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Convert to the JSON structure expected by SWAIG

The result must have at least one of:
- 'response': Text to be spoken by the AI
- 'action': Array of action objects

Optional:
- 'post_process': Boolean controlling when actions execute

**Returns:** `Dict[str, Any]`

Dictionary in SWAIG function response format


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-toggle_functions">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.toggle_functions(
    function_toggles: typing.List[typing.Dict[str, typing.Any]]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Enable/disable specific SWAIG functions.

**Parameters:**

<ParamField path="function_toggles" type="List[Dict[str, Any]]">
List of dicts with 'function' and 'active' keys
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-update_global_data">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.update_global_data(
    data: typing.Dict[str, typing.Any]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Update global agent data variables.

This is a convenience method that abstracts the set_global_data action.
Global data persists across the entire agent session and is available
in prompt variables and can be accessed by all functions.

**Parameters:**

<ParamField path="data" type="Dict[str, Any]">
Dictionary of key-value pairs to set/update in global data
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-update_settings">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.update_settings(
    settings: typing.Dict[str, typing.Any]
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Update agent runtime settings.

Supported settings:
- frequency-penalty: Float (-2.0 to 2.0)
- presence-penalty: Float (-2.0 to 2.0) 
- max-tokens: Integer (0 to 4096)
- top-p: Float (0.0 to 1.0)
- confidence: Float (0.0 to 1.0)
- barge-confidence: Float (0.0 to 1.0)
- temperature: Float (0.0 to 2.0, clamped to 1.5)

**Parameters:**

<ParamField path="settings" type="Dict[str, Any]">
Dictionary of settings to update
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
<Anchor id="signalwire-core-function_result-FunctionResult-wait_for_user">

<CodeBlock links={{"signalwire.core.function_result.FunctionResult":"#signalwire-core-function_result-FunctionResult"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.function_result.FunctionResult.wait_for_user(
    enabled: typing.Optional[bool] = None,
    timeout: typing.Optional[int] = None,
    answer_first: bool = False
) -> signalwire.core.function_result.FunctionResult
```

</CodeBlock>
</Anchor>

<Indent>

Control how agent waits for user input.

**Parameters:**

<ParamField path="enabled" type="Optional[bool]" default="None">
Boolean to enable/disable waiting
</ParamField>

<ParamField path="timeout" type="Optional[int]" default="None">
Number of seconds to wait
</ParamField>

<ParamField path="answer_first" type="bool" default="False">
Special "answer_first" mode
</ParamField>

**Returns:** `FunctionResult`

self for method chaining


</Indent>
</Indent>
