---
layout: overview
slug: signalwire-python/signalwire/core/security/session_manager
title: signalwire.core.security.session_manager
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`SessionManager`](#signalwire-core-security-session_manager-SessionManager) | Manages security tokens for function calls |

### API

<Anchor id="signalwire-core-security-session_manager-SessionManager">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.security.session_manager.SessionManager(
    token_expiry_secs: int = 900,
    secret_key: typing.Optional[str] = None
)
```

</CodeBlock>
</Anchor>

<Indent>

Manages security tokens for function calls

This implementation is completely stateless - it does not track call sessions
or store any information in memory. All validation is done using cryptographic
signatures with the tokens containing all necessary information.


<ParamField path="secret_key" type="= secret_key or secrets.token_hex(32)">
</ParamField>
<Anchor id="signalwire-core-security-session_manager-SessionManager-activate_session">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.activate_session(
    call_id: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Legacy method, does nothing but returns success


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-create_session">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.create_session(
    call_id: typing.Optional[str] = None
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Create a new session ID if one isn't provided

**Parameters:**

<ParamField path="call_id" type="Optional[str]" default="None">
Optional call ID, generated if not provided
</ParamField>

**Returns:** `str`

The call_id for the session


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-create_tool_token">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.create_tool_token(
    function_name: str,
    call_id: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Alias for generate_token to maintain backward compatibility

**Parameters:**

<ParamField path="function_name" type="str">
Name of the function to generate a token for
</ParamField>

<ParamField path="call_id" type="str">
Call session ID
</ParamField>

**Returns:** `str`

A secure token


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-debug_token">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.debug_token(
    token: str
) -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Debug a token without validating it

This method decodes the token and extracts its components for debugging purposes
without performing validation. Requires _debug_mode to be True.

**Parameters:**

<ParamField path="token" type="str">
The token to debug
</ParamField>

**Returns:** `Dict[str, Any]`

Dictionary with token components and analysis


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-end_session">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.end_session(
    call_id: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Legacy method, does nothing but returns success


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-generate_token">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.generate_token(
    function_name: str,
    call_id: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Generate a secure self-contained token for a function call

**Parameters:**

<ParamField path="function_name" type="str">
Name of the function to generate a token for
</ParamField>

<ParamField path="call_id" type="str">
Call session ID
</ParamField>

**Returns:** `str`

A secure token


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-get_session_metadata">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.get_session_metadata(
    call_id: str
) -> typing.Optional[typing.Dict[str, typing.Any]]
```

</CodeBlock>
</Anchor>

<Indent>

Legacy method, always returns empty metadata


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-set_session_metadata">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.set_session_metadata(
    call_id: str,
    key: str,
    value: typing.Any
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Legacy method, does nothing but returns success


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-validate_token">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.validate_token(
    call_id: str,
    function_name: str,
    token: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Validate a function call token

**Parameters:**

<ParamField path="call_id" type="str">
Call session ID
</ParamField>

<ParamField path="function_name" type="str">
Name of the function being called
</ParamField>

<ParamField path="token" type="str">
Token to validate
</ParamField>

**Returns:** `bool`

True if valid, False otherwise


</Indent>
<Anchor id="signalwire-core-security-session_manager-SessionManager-validate_tool_token">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.security.session_manager.SessionManager.validate_tool_token(
    function_name: str,
    token: str,
    call_id: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Alias for validate_token to maintain backward compatibility

**Parameters:**

<ParamField path="function_name" type="str">
Name of the function being called
</ParamField>

<ParamField path="token" type="str">
Token to validate
</ParamField>

<ParamField path="call_id" type="str">
Call session ID
</ParamField>

**Returns:** `bool`

True if valid, False otherwise


</Indent>
</Indent>
