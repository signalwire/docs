---
layout: overview
slug: signalwire-python/signalwire/core/mixins/auth_mixin
title: signalwire.core.mixins.auth_mixin
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`AuthMixin`](#signalwire-core-mixins-auth_mixin-AuthMixin) | Mixin class containing all authentication-related methods for AgentBase |

### API

<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.mixins.auth_mixin.AuthMixin()
```

</CodeBlock>
</Anchor>

<Indent>

Mixin class containing all authentication-related methods for AgentBase


<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_check_azure_function_auth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._check_azure_function_auth(
    req
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check basic auth in Azure Functions mode using request object

**Parameters:**

<ParamField path="req">
Azure Functions request object containing headers
</ParamField>

**Returns:** `bool`

True if auth is valid, False otherwise


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_check_basic_auth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._check_basic_auth(
    request: fastapi.Request
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check basic auth from a request

**Parameters:**

<ParamField path="request" type="Request">
FastAPI request object
</ParamField>

**Returns:** `bool`

True if auth is valid, False otherwise


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_check_cgi_auth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._check_cgi_auth() -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check basic auth in CGI mode using environment variables

**Returns:** `bool`

True if auth is valid, False otherwise


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_check_google_cloud_function_auth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._check_google_cloud_function_auth(
    request
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check basic auth in Google Cloud Functions mode using request headers

**Parameters:**

<ParamField path="request">
Flask request object or similar containing headers
</ParamField>

**Returns:** `bool`

True if auth is valid, False otherwise


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_check_lambda_auth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._check_lambda_auth(
    event
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check basic auth in Lambda mode using event headers

**Parameters:**

<ParamField path="event">
Lambda event object containing headers
</ParamField>

**Returns:** `bool`

True if auth is valid, False otherwise


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_send_azure_function_auth_challenge">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._send_azure_function_auth_challenge()
```

</CodeBlock>
</Anchor>

<Indent>

Send authentication challenge in Azure Functions mode

**Returns:**

Azure Functions response with 401 status and WWW-Authenticate header


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_send_cgi_auth_challenge">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._send_cgi_auth_challenge() -> str
```

</CodeBlock>
</Anchor>

<Indent>

Send authentication challenge in CGI mode

**Returns:** `str`

HTTP response with 401 status and WWW-Authenticate header


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_send_google_cloud_function_auth_challenge">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._send_google_cloud_function_auth_challenge()
```

</CodeBlock>
</Anchor>

<Indent>

Send authentication challenge in Google Cloud Functions mode

**Returns:**

Flask-compatible response with 401 status and WWW-Authenticate header


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-_send_lambda_auth_challenge">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin._send_lambda_auth_challenge() -> dict
```

</CodeBlock>
</Anchor>

<Indent>

Send authentication challenge in Lambda mode

**Returns:** `dict`

Lambda response with 401 status and WWW-Authenticate header


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-get_basic_auth_credentials">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin.get_basic_auth_credentials(
    include_source: bool = False
) -> typing.Union[typing.Tuple[str, str], typing.Tuple[str, str, str]]
```

</CodeBlock>
</Anchor>

<Indent>

Get the basic auth credentials

**Parameters:**

<ParamField path="include_source" type="bool" default="False">
Whether to include the source of the credentials
</ParamField>

**Returns:** `Union[Tuple[str, str], Tuple[str, str, str]]`

If include_source is False:
(username, password) tuple


</Indent>
<Anchor id="signalwire-core-mixins-auth_mixin-AuthMixin-validate_basic_auth">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.auth_mixin.AuthMixin.validate_basic_auth(
    username: str,
    password: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Validate basic auth credentials

This method can be overridden by subclasses.

**Parameters:**

<ParamField path="username" type="str">
Username from request
</ParamField>

<ParamField path="password" type="str">
Password from request
</ParamField>

**Returns:** `bool`

True if valid, False otherwise


</Indent>
</Indent>
