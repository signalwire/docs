---
layout: overview
slug: signalwire-python/signalwire/core/mixins/web_mixin
title: signalwire.core.mixins.web_mixin
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`WebMixin`](#signalwire-core-mixins-web_mixin-WebMixin) | Mixin class containing all web server and routing-related methods for AgentBase |

### Data

[`MAX_REQUEST_BODY_SIZE`](#signalwire-core-mixins-web_mixin-MAX_REQUEST_BODY_SIZE)

[`_request_proxy_url`](#signalwire-core-mixins-web_mixin-_request_proxy_url)

### API

<Anchor id="signalwire-core-mixins-web_mixin-WebMixin">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.core.mixins.web_mixin.WebMixin()
```

</CodeBlock>
</Anchor>

<Indent>

Mixin class containing all web server and routing-related methods for AgentBase


<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_check_content_type">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._check_content_type(
    request: fastapi.Request
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Check if POST request has application/json content type.

Returns True if content type is acceptable, False otherwise.
Skips check for empty bodies.


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_handle_check_for_input_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._handle_check_for_input_request(
    request: fastapi.Request
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Handle GET/POST requests to the check_for_input endpoint


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_handle_debug_events_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._handle_debug_events_request(
    request: fastapi.Request
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Handle POST requests delivering debug webhook events from the AI module


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_handle_debug_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._handle_debug_request(
    request: fastapi.Request
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Handle GET/POST requests to the debug endpoint


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_handle_post_prompt_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._handle_post_prompt_request(
    request: fastapi.Request
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Handle GET/POST requests to the post_prompt endpoint


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_handle_root_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._handle_root_request(
    request: fastapi.Request
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Handle GET/POST requests to the root endpoint


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_handle_swaig_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._handle_swaig_request(
    request: fastapi.Request,
    response: fastapi.Response
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Handle GET/POST requests to the SWAIG endpoint


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_read_body_with_limit">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._read_body_with_limit(
    request: fastapi.Request
) -> bytes
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Read request body with size limit enforcement.

Checks Content-Length header first, then enforces limit on actual body.
Raises ValueError if body exceeds MAX_REQUEST_BODY_SIZE.


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-_register_routes">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin._register_routes(
    router
)
```

</CodeBlock>
</Anchor>

<Indent>

Register routes for this agent

This method ensures proper route registration by handling the routes 
directly in AgentBase rather than inheriting from SWMLService.

**Parameters:**

<ParamField path="router">
FastAPI router to register routes with
</ParamField>


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-as_router">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.as_router() -> fastapi.APIRouter
```

</CodeBlock>
</Anchor>

<Indent>

Get a FastAPI router for this agent

**Returns:** `APIRouter`

FastAPI router


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-enable_debug_routes">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"/signalwire-python/signalwire/core/agent_base#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.enable_debug_routes() -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Enable debug routes for testing and development

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-get_app">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.get_app()
```

</CodeBlock>
</Anchor>

<Indent>

Get the FastAPI application instance for deployment adapters like Lambda/Mangum

This method ensures the FastAPI app is properly initialized and configured,
then returns it for use with deployment adapters like Mangum for AWS Lambda.

**Returns:**

The configured FastAPI application instance


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-manual_set_proxy_url">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"/signalwire-python/signalwire/core/agent_base#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.manual_set_proxy_url(
    proxy_url: str
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Manually set the proxy URL base for webhook callbacks

This can be called at runtime to set or update the proxy URL

**Parameters:**

<ParamField path="proxy_url" type="str">
The base URL to use for webhooks (e.g., https://example.ngrok.io)
</ParamField>

**Returns:** `AgentBase`

Self for method chaining


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-on_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.on_request(
    request_data: typing.Optional[dict] = None,
    callback_path: typing.Optional[str] = None
) -> typing.Optional[dict]
```

</CodeBlock>
</Anchor>

<Indent>

Called when SWML is requested, with request data when available

This method overrides SWMLService's on_request to properly handle SWML generation
for AI Agents.

**Parameters:**

<ParamField path="request_data" type="Optional[dict]" default="None">
Optional dictionary containing the parsed POST body
</ParamField>

<ParamField path="callback_path" type="Optional[str]" default="None">
Optional callback path
</ParamField>

**Returns:** `Optional[dict]`

None to use the default SWML rendering (which will call _render_swml)


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-on_swml_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.on_swml_request(
    request_data: typing.Optional[dict] = None,
    callback_path: typing.Optional[str] = None,
    request: typing.Optional[fastapi.Request] = None
) -> typing.Optional[dict]
```

</CodeBlock>
</Anchor>

<Indent>

Customization point for subclasses to modify SWML based on request data

**Parameters:**

<ParamField path="request_data" type="Optional[dict]" default="None">
Optional dictionary containing the parsed POST body
</ParamField>

<ParamField path="callback_path" type="Optional[str]" default="None">
Optional callback path
</ParamField>

<ParamField path="request" type="Optional[Request]" default="None">
Optional FastAPI Request object for accessing query params, headers, etc.
</ParamField>

**Returns:** `Optional[dict]`

Optional dict with modifications to apply to the SWML document


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-register_routing_callback">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.register_routing_callback(
    callback_fn: typing.Callable[[Request, Dict[str, Any]], typing.Optional[str]],
    path: str = '/sip'
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Register a callback function that will be called to determine routing
based on POST data.

When a routing callback is registered, an endpoint at the specified path is automatically
created that will handle requests. This endpoint will use the callback to
determine if the request should be processed by this service or redirected.

The callback should take a request object and request body dictionary and return:
- A route string if it should be routed to a different endpoint
- None if normal processing should continue

**Parameters:**

<ParamField path="callback_fn" type="Callable[[Request, Dict[str, Any]], Optional[str]]">
The callback function to register
</ParamField>

<ParamField path="path" type="str" default="'/sip'">
The path where this callback should be registered (default: "/sip")
</ParamField>


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-run">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.run(
    event = None,
    context = None,
    force_mode = None,
    host: typing.Optional[str] = None,
    port: typing.Optional[int] = None
)
```

</CodeBlock>
</Anchor>

<Indent>

Smart run method that automatically detects environment and handles accordingly

**Parameters:**

<ParamField path="event" default="None">
Serverless event object (Lambda, Cloud Functions)
</ParamField>

<ParamField path="context" default="None">
Serverless context object (Lambda, Cloud Functions)
</ParamField>

<ParamField path="force_mode" default="None">
Override automatic mode detection for testing
</ParamField>

<ParamField path="host" type="Optional[str]" default="None">
Host override for server mode
</ParamField>

<ParamField path="port" type="Optional[int]" default="None">
Port override for server mode
</ParamField>

**Returns:**

Response for serverless modes, None for server mode


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-serve">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.serve(
    host: typing.Optional[str] = None,
    port: typing.Optional[int] = None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Start a web server for this agent

**Parameters:**

<ParamField path="host" type="Optional[str]" default="None">
Optional host to override the default
</ParamField>

<ParamField path="port" type="Optional[int]" default="None">
Optional port to override the default
</ParamField>


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-set_dynamic_config_callback">

<CodeBlock links={{"signalwire.core.agent_base.AgentBase":"/signalwire-python/signalwire/core/agent_base#signalwire-core-agent_base-AgentBase"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.set_dynamic_config_callback(
    callback: typing.Callable[[dict, dict, dict, AgentBase], None]
) -> signalwire.core.agent_base.AgentBase
```

</CodeBlock>
</Anchor>

<Indent>

Set a callback function for dynamic agent configuration

This callback receives the actual agent instance, allowing you to dynamically
configure ANY aspect of the agent including adding skills, modifying prompts,
changing parameters, etc. based on request data.

**Parameters:**

<ParamField path="callback" type="Callable[[dict, dict, dict, AgentBase], None]">
Function that takes (query_params, body_params, headers, agent)
     and configures the agent using any available methods like:
     - agent.add_skill(...)
     - agent.add_language(...)
     - agent.prompt_add_section(...)
     - agent.set_params(...)
     - agent.set_global_data(...)
     - agent.define_tool(...)
</ParamField>


</Indent>
<Anchor id="signalwire-core-mixins-web_mixin-WebMixin-setup_graceful_shutdown">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.WebMixin.setup_graceful_shutdown() -> None
```

</CodeBlock>
</Anchor>

<Indent>

Setup signal handlers for graceful shutdown (useful for Kubernetes)


</Indent>
</Indent>

<Anchor id="signalwire-core-mixins-web_mixin-MAX_REQUEST_BODY_SIZE">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin.MAX_REQUEST_BODY_SIZE = 10 * 1024 * 1024
```

</CodeBlock>
</Anchor>


<Anchor id="signalwire-core-mixins-web_mixin-_request_proxy_url">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.core.mixins.web_mixin._request_proxy_url = contextvars.ContextVar('_request_proxy_url', default=None)
```

</CodeBlock>
</Anchor>

