---
layout: overview
slug: signalwire-python/signalwire/relay/message
title: signalwire.relay.message
---

Message object — represents an SMS/MMS message in the RELAY messaging namespace.

A Message tracks the lifecycle of a sent or received message via state events.
Outbound messages progress through: queued → initiated → sent → delivered (or
undelivered/failed).  Inbound messages arrive fully formed with state "received".

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`Message`](#signalwire-relay-message-Message) | Represents a single SMS/MMS message. |

### Data

[`logger`](#signalwire-relay-message-logger)

### API

<Anchor id="signalwire-relay-message-Message">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.message.Message(
    message_id: str = '',
    context: str = '',
    direction: str = '',
    from_number: str = '',
    to_number: str = '',
    body: str = '',
    media: typing.Optional[list[str]] = None,
    segments: int = 0,
    state: str = '',
    reason: str = '',
    tags: typing.Optional[list[str]] = None
)
```

</CodeBlock>
</Anchor>

<Indent>

Represents a single SMS/MMS message.

For outbound messages, use ``await message.wait()`` to block until a
terminal state (delivered, undelivered, failed) is reached.


<ParamField path="_done" type="Future[RelayEvent] = asyncio.get_event_loop().create_future()">
</ParamField>

<ParamField path="_listeners" type="list[Callable] = []">
</ParamField>

<ParamField path="_on_completed" type="Optional[Callable[[RelayEvent], Any]] = None">
</ParamField>

<ParamField path="is_done" type="bool">
True if the message has reached a terminal state.
</ParamField>

<ParamField path="media" type="list[str] = media or []">
</ParamField>

<ParamField path="result" type="Optional[RelayEvent]">
The terminal RelayEvent, or None if not yet done.
</ParamField>

<ParamField path="tags" type="list[str] = tags or []">
</ParamField>
<Anchor id="signalwire-relay-message-Message-__repr__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.message.Message.__repr__() -> str
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="signalwire-relay-message-Message-_dispatch_event">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.message.Message._dispatch_event(
    payload: dict[str, typing.Any]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Handle a messaging.state event for this message.


</Indent>
<Anchor id="signalwire-relay-message-Message-_resolve">

<CodeBlock links={{"signalwire.relay.event.RelayEvent":"/signalwire-python/signalwire/relay/event#signalwire-relay-event-RelayEvent"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.message.Message._resolve(
    event: signalwire.relay.event.RelayEvent
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Mark the message as completed and fire the on_completed callback.


</Indent>
<Anchor id="signalwire-relay-message-Message-on">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.message.Message.on(
    handler: typing.Callable
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Register an event listener for state changes on this message.


</Indent>
<Anchor id="signalwire-relay-message-Message-wait">

<CodeBlock links={{"signalwire.relay.event.RelayEvent":"/signalwire-python/signalwire/relay/event#signalwire-relay-event-RelayEvent"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.message.Message.wait(
    timeout: typing.Optional[float] = None
) -> signalwire.relay.event.RelayEvent
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>async</Badge>

Block until the message reaches a terminal state.

Returns the terminal RelayEvent. Raises asyncio.TimeoutError if
timeout is specified and exceeded.


</Indent>
</Indent>

<Anchor id="signalwire-relay-message-logger">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.message.logger = get_logger('relay_message')
```

</CodeBlock>
</Anchor>

