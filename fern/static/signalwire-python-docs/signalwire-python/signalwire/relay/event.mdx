---
layout: overview
slug: signalwire-python/signalwire/relay/event
title: signalwire.relay.event
---

Typed event wrappers for RELAY calling events.

These are convenience dataclasses over raw event dicts. All Call event handlers
also accept the raw dict, so these are optional.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`CallReceiveEvent`](#signalwire-relay-event-CallReceiveEvent) | Event for calling.call.receive — inbound call notification. |
| [`CallStateEvent`](#signalwire-relay-event-CallStateEvent) | Event for calling.call.state. |
| [`CallingErrorEvent`](#signalwire-relay-event-CallingErrorEvent) | Event for calling.error. |
| [`CollectEvent`](#signalwire-relay-event-CollectEvent) | Event for calling.call.collect. |
| [`ConferenceEvent`](#signalwire-relay-event-ConferenceEvent) | Event for calling.conference. |
| [`ConnectEvent`](#signalwire-relay-event-ConnectEvent) | Event for calling.call.connect. |
| [`DenoiseEvent`](#signalwire-relay-event-DenoiseEvent) | Event for calling.call.denoise. |
| [`DetectEvent`](#signalwire-relay-event-DetectEvent) | Event for calling.call.detect. |
| [`DialEvent`](#signalwire-relay-event-DialEvent) | Event for calling.call.dial. |
| [`EchoEvent`](#signalwire-relay-event-EchoEvent) | Event for calling.call.echo. |
| [`FaxEvent`](#signalwire-relay-event-FaxEvent) | Event for calling.call.fax. |
| [`HoldEvent`](#signalwire-relay-event-HoldEvent) | Event for calling.call.hold. |
| [`MessageReceiveEvent`](#signalwire-relay-event-MessageReceiveEvent) | Event for messaging.receive — inbound message notification. |
| [`MessageStateEvent`](#signalwire-relay-event-MessageStateEvent) | Event for messaging.state — outbound message state change. |
| [`PayEvent`](#signalwire-relay-event-PayEvent) | Event for calling.call.pay. |
| [`PlayEvent`](#signalwire-relay-event-PlayEvent) | Event for calling.call.play. |
| [`QueueEvent`](#signalwire-relay-event-QueueEvent) | Event for calling.call.queue. |
| [`RecordEvent`](#signalwire-relay-event-RecordEvent) | Event for calling.call.record. |
| [`ReferEvent`](#signalwire-relay-event-ReferEvent) | Event for calling.call.refer. |
| [`RelayEvent`](#signalwire-relay-event-RelayEvent) | Base event — wraps the raw params dict from a signalwire.event message. |
| [`SendDigitsEvent`](#signalwire-relay-event-SendDigitsEvent) | Event for calling.call.send_digits. |
| [`StreamEvent`](#signalwire-relay-event-StreamEvent) | Event for calling.call.stream. |
| [`TapEvent`](#signalwire-relay-event-TapEvent) | Event for calling.call.tap. |
| [`TranscribeEvent`](#signalwire-relay-event-TranscribeEvent) | Event for calling.call.transcribe. |

### Functions

| Name | Description |
|------|-------------|
| [`parse_event`](#signalwire-relay-event-parse_event) | Parse a raw signalwire.event params dict into a typed event object. |

### Data

[`EVENT_CLASS_MAP`](#signalwire-relay-event-EVENT_CLASS_MAP)

### API

<Anchor id="signalwire-relay-event-CallReceiveEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.CallReceiveEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    call_state: str = '',
    direction: str = '',
    device: dict[str, typing.Any] = dict(),
    node_id: str = '',
    project_id: str = '',
    context: str = '',
    segment_id: str = '',
    tag: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.receive — inbound call notification.


<ParamField path="call_state" type="str = ''">
</ParamField>

<ParamField path="context" type="str = ''">
</ParamField>

<ParamField path="device" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>

<ParamField path="direction" type="str = ''">
</ParamField>

<ParamField path="node_id" type="str = ''">
</ParamField>

<ParamField path="project_id" type="str = ''">
</ParamField>

<ParamField path="segment_id" type="str = ''">
</ParamField>

<ParamField path="tag" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-CallReceiveEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.CallReceiveEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'CallReceiveEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-CallStateEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.CallStateEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    call_state: str = '',
    end_reason: str = '',
    direction: str = '',
    device: dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.state.


<ParamField path="call_state" type="str = ''">
</ParamField>

<ParamField path="device" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>

<ParamField path="direction" type="str = ''">
</ParamField>

<ParamField path="end_reason" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-CallStateEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.CallStateEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'CallStateEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-CallingErrorEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.CallingErrorEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    code: str = '',
    message: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.error.


<ParamField path="code" type="str = ''">
</ParamField>

<ParamField path="message" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-CallingErrorEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.CallingErrorEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'CallingErrorEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-CollectEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.CollectEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = '',
    result: dict[str, typing.Any] = dict(),
    final: typing.Optional[bool] = None
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.collect.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="final" type="Optional[bool] = None">
</ParamField>

<ParamField path="result" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-CollectEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.CollectEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'CollectEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-ConferenceEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.ConferenceEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    conference_id: str = '',
    name: str = '',
    status: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.conference.


<ParamField path="conference_id" type="str = ''">
</ParamField>

<ParamField path="name" type="str = ''">
</ParamField>

<ParamField path="status" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-ConferenceEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.ConferenceEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'ConferenceEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-ConnectEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.ConnectEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    connect_state: str = '',
    peer: dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.connect.


<ParamField path="connect_state" type="str = ''">
</ParamField>

<ParamField path="peer" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>
<Anchor id="signalwire-relay-event-ConnectEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.ConnectEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'ConnectEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-DenoiseEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.DenoiseEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    denoised: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.denoise.


<ParamField path="denoised" type="bool = False">
</ParamField>
<Anchor id="signalwire-relay-event-DenoiseEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.DenoiseEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'DenoiseEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-DetectEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.DetectEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    detect: dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.detect.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="detect" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>
<Anchor id="signalwire-relay-event-DetectEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.DetectEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'DetectEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-DialEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.DialEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    tag: str = '',
    dial_state: str = '',
    call: dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.dial.


<ParamField path="call" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>

<ParamField path="dial_state" type="str = ''">
</ParamField>

<ParamField path="tag" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-DialEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.DialEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'DialEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-EchoEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.EchoEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    state: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.echo.


<ParamField path="state" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-EchoEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.EchoEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'EchoEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-FaxEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.FaxEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    fax: dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.fax.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="fax" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>
<Anchor id="signalwire-relay-event-FaxEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.FaxEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'FaxEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-HoldEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.HoldEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    state: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.hold.


<ParamField path="state" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-HoldEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.HoldEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'HoldEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-MessageReceiveEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.MessageReceiveEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    message_id: str = '',
    context: str = '',
    direction: str = '',
    from_number: str = '',
    to_number: str = '',
    body: str = '',
    media: list[str] = list(),
    segments: int = 0,
    message_state: str = '',
    tags: list[str] = list()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for messaging.receive — inbound message notification.


<ParamField path="body" type="str = ''">
</ParamField>

<ParamField path="context" type="str = ''">
</ParamField>

<ParamField path="direction" type="str = ''">
</ParamField>

<ParamField path="from_number" type="str = ''">
</ParamField>

<ParamField path="media" type="list[str] = field(default_factory=list)">
</ParamField>

<ParamField path="message_id" type="str = ''">
</ParamField>

<ParamField path="message_state" type="str = ''">
</ParamField>

<ParamField path="segments" type="int = 0">
</ParamField>

<ParamField path="tags" type="list[str] = field(default_factory=list)">
</ParamField>

<ParamField path="to_number" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-MessageReceiveEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.MessageReceiveEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'MessageReceiveEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-MessageStateEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.MessageStateEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    message_id: str = '',
    context: str = '',
    direction: str = '',
    from_number: str = '',
    to_number: str = '',
    body: str = '',
    media: list[str] = list(),
    segments: int = 0,
    message_state: str = '',
    reason: str = '',
    tags: list[str] = list()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for messaging.state — outbound message state change.


<ParamField path="body" type="str = ''">
</ParamField>

<ParamField path="context" type="str = ''">
</ParamField>

<ParamField path="direction" type="str = ''">
</ParamField>

<ParamField path="from_number" type="str = ''">
</ParamField>

<ParamField path="media" type="list[str] = field(default_factory=list)">
</ParamField>

<ParamField path="message_id" type="str = ''">
</ParamField>

<ParamField path="message_state" type="str = ''">
</ParamField>

<ParamField path="reason" type="str = ''">
</ParamField>

<ParamField path="segments" type="int = 0">
</ParamField>

<ParamField path="tags" type="list[str] = field(default_factory=list)">
</ParamField>

<ParamField path="to_number" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-MessageStateEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.MessageStateEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'MessageStateEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-PayEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.PayEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.pay.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-PayEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.PayEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'PayEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-PlayEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.PlayEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.play.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-PlayEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.PlayEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'PlayEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-QueueEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.QueueEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    status: str = '',
    queue_id: str = '',
    queue_name: str = '',
    position: int = 0,
    size: int = 0
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.queue.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="position" type="int = 0">
</ParamField>

<ParamField path="queue_id" type="str = ''">
</ParamField>

<ParamField path="queue_name" type="str = ''">
</ParamField>

<ParamField path="size" type="int = 0">
</ParamField>

<ParamField path="status" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-QueueEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.QueueEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'QueueEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-RecordEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.RecordEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = '',
    url: str = '',
    duration: float = 0.0,
    size: int = 0,
    record: dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.record.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="duration" type="float = 0.0">
</ParamField>

<ParamField path="record" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>

<ParamField path="size" type="int = 0">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>

<ParamField path="url" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-RecordEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.RecordEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'RecordEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-ReferEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.ReferEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    state: str = '',
    sip_refer_to: str = '',
    sip_refer_response_code: str = '',
    sip_notify_response_code: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.refer.


<ParamField path="sip_notify_response_code" type="str = ''">
</ParamField>

<ParamField path="sip_refer_response_code" type="str = ''">
</ParamField>

<ParamField path="sip_refer_to" type="str = ''">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-ReferEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.ReferEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'ReferEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-RelayEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.RelayEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

Base event — wraps the raw params dict from a signalwire.event message.


<ParamField path="call_id" type="str = ''">
</ParamField>

<ParamField path="event_type" type="str">
</ParamField>

<ParamField path="params" type="dict[str, Any]">
</ParamField>

<ParamField path="timestamp" type="float = 0.0">
</ParamField>
<Anchor id="signalwire-relay-event-RelayEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.RelayEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'RelayEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-SendDigitsEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.SendDigitsEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.send_digits.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-SendDigitsEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.SendDigitsEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'SendDigitsEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-StreamEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.StreamEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = '',
    url: str = '',
    name: str = ''
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.stream.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="name" type="str = ''">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>

<ParamField path="url" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-StreamEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.StreamEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'StreamEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-TapEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.TapEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = '',
    tap: dict[str, typing.Any] = dict(),
    device: dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.tap.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="device" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>

<ParamField path="tap" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>
<Anchor id="signalwire-relay-event-TapEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.TapEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'TapEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-TranscribeEvent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.relay.event.TranscribeEvent(
    event_type: str,
    params: dict[str, typing.Any],
    call_id: str = '',
    timestamp: float = 0.0,
    control_id: str = '',
    state: str = '',
    url: str = '',
    recording_id: str = '',
    duration: float = 0.0,
    size: int = 0
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [RelayEvent](#signalwire-relay-event-RelayEvent)

Event for calling.call.transcribe.


<ParamField path="control_id" type="str = ''">
</ParamField>

<ParamField path="duration" type="float = 0.0">
</ParamField>

<ParamField path="recording_id" type="str = ''">
</ParamField>

<ParamField path="size" type="int = 0">
</ParamField>

<ParamField path="state" type="str = ''">
</ParamField>

<ParamField path="url" type="str = ''">
</ParamField>
<Anchor id="signalwire-relay-event-TranscribeEvent-from_payload">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.TranscribeEvent.from_payload(
    payload: dict[str, typing.Any]
) -> 'TranscribeEvent'
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>classmethod</Badge>


</Indent>
</Indent>

<Anchor id="signalwire-relay-event-parse_event">

<CodeBlock links={{"signalwire.relay.event.RelayEvent":"#signalwire-relay-event-RelayEvent"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.parse_event(
    payload: dict[str, typing.Any]
) -> signalwire.relay.event.RelayEvent
```

</CodeBlock>
</Anchor>

<Indent>

Parse a raw signalwire.event params dict into a typed event object.


</Indent>

<Anchor id="signalwire-relay-event-EVENT_CLASS_MAP">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.relay.event.EVENT_CLASS_MAP: dict[str, type[RelayEvent]] = {'calling.call.state': CallStateEvent, 'calling.call.receive': CallReceiveEvent,...
```

</CodeBlock>
</Anchor>

