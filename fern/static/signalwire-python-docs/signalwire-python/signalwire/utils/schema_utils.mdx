---
layout: overview
slug: signalwire-python/signalwire/utils/schema_utils
title: signalwire.utils.schema_utils
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`SchemaUtils`](#signalwire-utils-schema_utils-SchemaUtils) | Utility class for loading and working with SWML schemas |
| [`SchemaValidationError`](#signalwire-utils-schema_utils-SchemaValidationError) | Raised when SWML schema validation fails. |

### Data

[`logger`](#signalwire-utils-schema_utils-logger)

### API

<Anchor id="signalwire-utils-schema_utils-SchemaUtils">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.utils.schema_utils.SchemaUtils(
    schema_path: typing.Optional[str] = None,
    schema_validation: bool = True
)
```

</CodeBlock>
</Anchor>

<Indent>

Utility class for loading and working with SWML schemas


<ParamField path="_validation_enabled" type="= schema_validation and not env_skip">
</ParamField>

<ParamField path="full_validation_available" type="bool">
Check if full JSON Schema validation is available.
</ParamField>

<ParamField path="log" type="= logger.bind(component='schema_utils')">
</ParamField>

<ParamField path="schema" type="= self.load_schema()">
</ParamField>

<ParamField path="verbs" type="= self._extract_verb_definitions()">
</ParamField>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-_extract_verb_definitions">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils._extract_verb_definitions() -> typing.Dict[str, typing.Dict[str, typing.Any]]
```

</CodeBlock>
</Anchor>

<Indent>

Extract verb definitions from the schema

**Returns:** `Dict[str, Dict[str, Any]]`

A dictionary mapping verb names to their definitions


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-_get_default_schema_path">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils._get_default_schema_path() -> typing.Optional[str]
```

</CodeBlock>
</Anchor>

<Indent>

Get the default path to the schema file using the same robust logic as SWMLService

**Returns:** `Optional[str]`

Path to the schema file or None if not found


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-_get_type_annotation">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils._get_type_annotation(
    param_def: typing.Dict[str, typing.Any]
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Get the Python type annotation for a parameter

**Parameters:**

<ParamField path="param_def" type="Dict[str, Any]">
Parameter definition from the schema
</ParamField>

**Returns:** `str`

Python type annotation as a string


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-_init_full_validator">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils._init_full_validator() -> None
```

</CodeBlock>
</Anchor>

<Indent>

Initialize the jsonschema-rs validator for full schema validation.


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-_validate_verb_full">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils._validate_verb_full(
    verb_name: str,
    verb_config: typing.Dict[str, typing.Any]
) -> typing.Tuple[bool, typing.List[str]]
```

</CodeBlock>
</Anchor>

<Indent>

Perform full JSON Schema validation using jsonschema-rs.

Wraps the verb in a minimal SWML document structure for validation
since the schema requires the full document context.

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb
</ParamField>

<ParamField path="verb_config" type="Dict[str, Any]">
The configuration for the verb
</ParamField>

**Returns:** `Tuple[bool, List[str]]`

(is_valid, error_messages) tuple


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-_validate_verb_lightweight">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils._validate_verb_lightweight(
    verb_name: str,
    verb_config: typing.Dict[str, typing.Any]
) -> typing.Tuple[bool, typing.List[str]]
```

</CodeBlock>
</Anchor>

<Indent>

Perform lightweight validation (verb existence + required fields only).

This is the fallback when jsonschema-rs is not available.

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb
</ParamField>

<ParamField path="verb_config" type="Dict[str, Any]">
The configuration for the verb
</ParamField>

**Returns:** `Tuple[bool, List[str]]`

(is_valid, error_messages) tuple


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-generate_method_body">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.generate_method_body(
    verb_name: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Generate the method body implementation for a verb

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb
</ParamField>

**Returns:** `str`

The method body as a string


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-generate_method_signature">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.generate_method_signature(
    verb_name: str
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Generate a Python method signature for a verb

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb
</ParamField>

**Returns:** `str`

A Python method signature string


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-get_all_verb_names">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.get_all_verb_names() -> typing.List[str]
```

</CodeBlock>
</Anchor>

<Indent>

Get all verb names defined in the schema

**Returns:** `List[str]`

List of verb names


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-get_verb_parameters">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.get_verb_parameters(
    verb_name: str
) -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Get the parameter definitions for a specific verb

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb (e.g., "ai", "answer", etc.)
</ParamField>

**Returns:** `Dict[str, Any]`

Dictionary mapping parameter names to their definitions


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-get_verb_properties">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.get_verb_properties(
    verb_name: str
) -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Get the properties for a specific verb

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb (e.g., "ai", "answer", etc.)
</ParamField>

**Returns:** `Dict[str, Any]`

The properties for the verb or an empty dict if not found


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-get_verb_required_properties">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.get_verb_required_properties(
    verb_name: str
) -> typing.List[str]
```

</CodeBlock>
</Anchor>

<Indent>

Get the required properties for a specific verb

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb (e.g., "ai", "answer", etc.)
</ParamField>

**Returns:** `List[str]`

List of required property names for the verb or an empty list if not found


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-load_schema">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.load_schema() -> typing.Dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>

Load the JSON schema from the specified path

**Returns:** `Dict[str, Any]`

The schema as a dictionary


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-validate_document">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.validate_document(
    document: typing.Dict[str, typing.Any]
) -> typing.Tuple[bool, typing.List[str]]
```

</CodeBlock>
</Anchor>

<Indent>

Validate a complete SWML document against the schema.

**Parameters:**

<ParamField path="document" type="Dict[str, Any]">
The complete SWML document to validate
</ParamField>

**Returns:** `Tuple[bool, List[str]]`

(is_valid, error_messages) tuple


</Indent>
<Anchor id="signalwire-utils-schema_utils-SchemaUtils-validate_verb">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.SchemaUtils.validate_verb(
    verb_name: str,
    verb_config: typing.Dict[str, typing.Any]
) -> typing.Tuple[bool, typing.List[str]]
```

</CodeBlock>
</Anchor>

<Indent>

Validate a verb configuration against the schema.

Performs full JSON Schema validation including type checking and nested
object validation using jsonschema-rs.

**Parameters:**

<ParamField path="verb_name" type="str">
The name of the verb (e.g., "ai", "answer", etc.)
</ParamField>

<ParamField path="verb_config" type="Dict[str, Any]">
The configuration for the verb
</ParamField>

**Returns:** `Tuple[bool, List[str]]`

(is_valid, error_messages) tuple


</Indent>
</Indent>

<Anchor id="signalwire-utils-schema_utils-SchemaValidationError">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.utils.schema_utils.SchemaValidationError(
    verb_name: str,
    errors: typing.List[str]
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Exception</Badge>

**Bases:** `Exception`

Raised when SWML schema validation fails.


</Indent>

<Anchor id="signalwire-utils-schema_utils-logger">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.utils.schema_utils.logger = get_logger('signalwire.utils.schema_utils')
```

</CodeBlock>
</Anchor>

