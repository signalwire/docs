---
layout: overview
slug: signalwire-python/signalwire/mcp_gateway/session_manager
title: signalwire.mcp_gateway.session_manager
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`Session`](#signalwire-mcp_gateway-session_manager-Session) | Represents an active MCP session |
| [`SessionManager`](#signalwire-mcp_gateway-session_manager-SessionManager) | Manages MCP server sessions with automatic cleanup |

### Data

[`logger`](#signalwire-mcp_gateway-session_manager-logger)

### API

<Anchor id="signalwire-mcp_gateway-session_manager-Session">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.mcp_gateway.session_manager.Session(
    session_id: str,
    service_name: str,
    process: typing.Any,
    created_at: datetime.datetime = datetime.now(),
    last_accessed: datetime.datetime = datetime.now(),
    timeout: int = 300,
    metadata: typing.Dict[str, typing.Any] = dict()
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

Represents an active MCP session


<ParamField path="created_at" type="datetime = field(default_factory=(datetime.now))">
</ParamField>

<ParamField path="is_alive" type="bool">
Check if the underlying MCP client is still running
</ParamField>

<ParamField path="is_expired" type="bool">
Check if session has expired based on timeout
</ParamField>

<ParamField path="last_accessed" type="datetime = field(default_factory=(datetime.now))">
</ParamField>

<ParamField path="metadata" type="Dict[str, Any] = field(default_factory=dict)">
</ParamField>

<ParamField path="service_name" type="str">
</ParamField>

<ParamField path="session_id" type="str">
</ParamField>

<ParamField path="timeout" type="int = 300">
</ParamField>
<Anchor id="signalwire-mcp_gateway-session_manager-Session-touch">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.Session.touch()
```

</CodeBlock>
</Anchor>

<Indent>

Update last accessed time


</Indent>
</Indent>

<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.mcp_gateway.session_manager.SessionManager(
    config: typing.Dict[str, typing.Any],
    max_total_sessions: int = 500
)
```

</CodeBlock>
</Anchor>

<Indent>

Manages MCP server sessions with automatic cleanup


<ParamField path="_shutdown" type="= threading.Event()">
</ParamField>

<ParamField path="cleanup_interval">
</ParamField>

<ParamField path="cleanup_thread">
</ParamField>

<ParamField path="default_timeout">
</ParamField>

<ParamField path="lock" type="= threading.RLock()">
</ParamField>

<ParamField path="max_sessions_per_service">
</ParamField>

<ParamField path="max_total_sessions">
</ParamField>

<ParamField path="sessions" type="Dict[str, Session] = &#123;&#125;">
</ParamField>
<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager-_cleanup_loop">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.SessionManager._cleanup_loop()
```

</CodeBlock>
</Anchor>

<Indent>

Background thread that cleans up expired sessions


</Indent>
<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager-close_session">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.SessionManager.close_session(
    session_id: str
) -> bool
```

</CodeBlock>
</Anchor>

<Indent>

Close and remove a session


</Indent>
<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager-create_session">

<CodeBlock links={{"signalwire.mcp_gateway.session_manager.Session":"#signalwire-mcp_gateway-session_manager-Session"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.SessionManager.create_session(
    session_id: str,
    service_name: str,
    process: typing.Any,
    timeout: typing.Optional[int] = None,
    metadata: typing.Optional[typing.Dict[str, typing.Any]] = None
) -> signalwire.mcp_gateway.session_manager.Session
```

</CodeBlock>
</Anchor>

<Indent>

Create and register a new session


</Indent>
<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager-get_service_session_count">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.SessionManager.get_service_session_count(
    service_name: str
) -> int
```

</CodeBlock>
</Anchor>

<Indent>

Get number of active sessions for a service


</Indent>
<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager-get_session">

<CodeBlock links={{"signalwire.mcp_gateway.session_manager.Session":"#signalwire-mcp_gateway-session_manager-Session"}} showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.SessionManager.get_session(
    session_id: str
) -> typing.Optional[signalwire.mcp_gateway.session_manager.Session]
```

</CodeBlock>
</Anchor>

<Indent>

Get an active session by ID


</Indent>
<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager-list_sessions">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.SessionManager.list_sessions() -> typing.Dict[str, typing.Dict[str, typing.Any]]
```

</CodeBlock>
</Anchor>

<Indent>

List all active sessions with their info


</Indent>
<Anchor id="signalwire-mcp_gateway-session_manager-SessionManager-shutdown">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.SessionManager.shutdown()
```

</CodeBlock>
</Anchor>

<Indent>

Shutdown all sessions and cleanup


</Indent>
</Indent>

<Anchor id="signalwire-mcp_gateway-session_manager-logger">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.mcp_gateway.session_manager.logger = logging.getLogger(__name__)
```

</CodeBlock>
</Anchor>

