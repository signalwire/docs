---
layout: overview
slug: signalwire-python/signalwire/prefabs/info_gatherer
title: signalwire.prefabs.info_gatherer
---

Copyright (c) 2025 SignalWire

This file is part of the SignalWire AI Agents SDK.

Licensed under the MIT License.
See LICENSE file in the project root for full license information.

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`InfoGathererAgent`](#signalwire-prefabs-info_gatherer-InfoGathererAgent) | A prefab agent designed to collect answers to a series of questions. |

### API

<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class signalwire.prefabs.info_gatherer.InfoGathererAgent(
    questions: typing.Optional[typing.List[typing.Dict[str, str]]] = None,
    name: str = 'info_gatherer',
    route: str = '/info_gatherer',
    kwargs = {}
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [AgentBase](/signalwire-python/signalwire/core/agent_base#signalwire-core-agent_base-AgentBase)

A prefab agent designed to collect answers to a series of questions.

This agent will:
1. Ask if the user is ready to begin
2. Ask each question in sequence
3. Store the answers for later use


<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-_build_prompt">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent._build_prompt(
    mode = 'static'
)
```

</CodeBlock>
</Anchor>

<Indent>

Build a minimal prompt with just the objective


</Indent>
<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-_configure_agent_settings">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent._configure_agent_settings()
```

</CodeBlock>
</Anchor>

<Indent>

Configure additional agent settings


</Indent>
<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-_generate_question_instruction">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent._generate_question_instruction(
    question_text: str,
    needs_confirmation: bool,
    is_first_question: bool = False
) -> str
```

</CodeBlock>
</Anchor>

<Indent>

Generate the instruction text for asking a question

**Parameters:**

<ParamField path="question_text" type="str">
The question to ask
</ParamField>

<ParamField path="needs_confirmation" type="bool">
Whether confirmation is required
</ParamField>

<ParamField path="is_first_question" type="bool" default="False">
Whether this is the first question or a subsequent one
</ParamField>

**Returns:** `str`

Formatted instruction text


</Indent>
<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-_validate_questions">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent._validate_questions(
    questions
)
```

</CodeBlock>
</Anchor>

<Indent>

Validate that questions are in the correct format


</Indent>
<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-on_swml_request">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent.on_swml_request(
    request_data = None,
    callback_path = None,
    request = None
)
```

</CodeBlock>
</Anchor>

<Indent>

Handle dynamic configuration using the callback function

This method is called when SWML is requested and allows us to configure
the agent just-in-time using the provided callback.


</Indent>
<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-set_question_callback">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent.set_question_callback(
    callback: typing.Callable[[dict, dict, dict], typing.List[typing.Dict[str, str]]]
)
```

</CodeBlock>
</Anchor>

<Indent>

Set a callback function for dynamic question configuration

**Parameters:**

<ParamField path="callback" type="Callable[[dict, dict, dict], List[Dict[str, str]]]">
Function that takes (query_params, body_params, headers) and returns
     a list of question dictionaries. Each question dict should have:
     - key_name: Identifier for storing the answer
     - question_text: The actual question to ask the user
     - confirm: (Optional) If True, agent will confirm answer before submitting
</ParamField>


</Indent>
<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-start_questions">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent.start_questions(
    args,
    raw_data
)
```

</CodeBlock>
</Anchor>

<Indent>

Start the question sequence by retrieving the first question

This function gets the current question index from global_data
and returns the corresponding question.


</Indent>
<Anchor id="signalwire-prefabs-info_gatherer-InfoGathererAgent-submit_answer">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
signalwire.prefabs.info_gatherer.InfoGathererAgent.submit_answer(
    args,
    raw_data
)
```

</CodeBlock>
</Anchor>

<Indent>

Submit an answer to the current question and move to the next one

This function:
1. Stores the answer in global_data
2. Increments the question index
3. Returns the next question or completion message


</Indent>
</Indent>
