<ParamField path="dest" type="string | object" required={true} toc={true}>
  The destination to transfer the call to. Can be a SIP URI, phone number, or
  an inline SWML object. When a string, the call is transferred directly; when
  an object, the target call flow is executed.
</ParamField>
