<ParamField path="FriendlyName" type="string" toc={true}>
  A human-readable label for the number.
</ParamField>

<ParamField path="VoiceUrl" type="string" toc={true}>
  URL to fetch LAML instructions when a call is received.
</ParamField>

<ParamField path="SmsUrl" type="string" toc={true}>
  URL to fetch LAML instructions when an SMS is received.
</ParamField>

<ParamField path="StatusCallback" type="string" toc={true}>
  URL to receive status webhook events.
</ParamField>

<ParamField path="StatusCallbackMethod" type="string" toc={true}>
  HTTP method for the `StatusCallback`. `"GET"` or `"POST"`.
</ParamField>

<ParamField path="VoiceMethod" type="string" toc={true}>
  HTTP method for the `VoiceUrl`. `"GET"` or `"POST"`.
</ParamField>

<ParamField path="VoiceFallbackUrl" type="string" toc={true}>
  Fallback URL if the primary `VoiceUrl` fails.
</ParamField>

<ParamField path="VoiceFallbackMethod" type="string" toc={true}>
  HTTP method for the `VoiceFallbackUrl`. `"GET"` or `"POST"`.
</ParamField>

<ParamField path="VoiceApplicationSid" type="string" toc={true}>
  The SID of a voice application to handle calls.
</ParamField>

<ParamField path="VoiceReceiveMode" type="string" toc={true}>
  Receive mode for the number. `"voice"` or `"fax"`.
</ParamField>

<ParamField path="SmsMethod" type="string" toc={true}>
  HTTP method for the `SmsUrl`. `"GET"` or `"POST"`.
</ParamField>

<ParamField path="SmsFallbackUrl" type="string" toc={true}>
  Fallback URL if the primary `SmsUrl` fails.
</ParamField>

<ParamField path="SmsFallbackMethod" type="string" toc={true}>
  HTTP method for the `SmsFallbackUrl`. `"GET"` or `"POST"`.
</ParamField>

<ParamField path="SmsApplicationSid" type="string" toc={true}>
  The SID of an SMS application to handle messages.
</ParamField>

<ParamField path="AccountSid" type="string" toc={true}>
  The SID of the account to transfer the number to.
</ParamField>

<ParamField path="EmergencyAddressSid" type="string" toc={true}>
  The SID of the emergency address for E911.
</ParamField>
