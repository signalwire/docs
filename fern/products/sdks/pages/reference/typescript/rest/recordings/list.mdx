---
title: "list"
slug: /reference/typescript/rest/recordings/list
description: List recordings in the project.
max-toc-depth: 3
---

List recordings in the project.

<EndpointSchemaSnippet endpoint="GET /api/relay/rest/recordings" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/relay/rest/recordings" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const result = await client.recordings.list({ pageSize: 20 });
for (const rec of result.data ?? []) {
    console.log(rec.id, rec["duration"], rec["state"]);
}
```
