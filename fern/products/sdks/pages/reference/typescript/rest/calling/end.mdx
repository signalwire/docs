---
title: "end"
slug: /reference/typescript/rest/calling/end
description: End an active call via REST.
max-toc-depth: 3
---

End an active call.

## **Parameters**

<ParamField path="callId" type="string" required={true} toc={true}>
  The ID of the call to end.
</ParamField>

<Markdown src="/snippets/sdks/rest/calling/_end-params.mdx" />

## **Returns**

`Promise<any>` -- The API response confirming the call was ended.

## **Example**

```typescript {9-9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.end("call-id-xxx", { reason: "hangup" });
```
