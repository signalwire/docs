---
title: "playVolume"
slug: /reference/typescript/rest/calling/play-volume
description: Adjust the volume of an active playback on a call via REST.
max-toc-depth: 3
---

[play]: /docs/sdks/reference/typescript/rest/calling/play

Adjust the volume of an active playback.

<EndpointSchemaSnippet endpoint="POST /api/calling/calls" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /api/calling/calls" />

## **Example**

```typescript {9-9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.playVolume("call-id-xxx", { control_id: "ctrl-id", volume: 5.0 });
```
