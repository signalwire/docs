---
title: "playPause"
slug: /reference/typescript/rest/calling/play-pause
description: Pause an active playback on a call via REST.
max-toc-depth: 3
---

[play]: /docs/sdks/reference/typescript/rest/calling/play

Pause an active playback.

<EndpointSchemaSnippet endpoint="POST /api/calling/calls" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /api/calling/calls" />

## **Example**

```typescript {9-9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.playPause("call-id-xxx", { control_id: "ctrl-id" });
```
