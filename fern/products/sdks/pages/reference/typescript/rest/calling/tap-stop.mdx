---
title: "tapStop"
slug: /reference/typescript/rest/calling/tap-stop
description: Stop an active audio tap on a call via REST.
max-toc-depth: 3
---

[tap]: /docs/sdks/reference/typescript/rest/calling/tap

Stop an active audio tap.

## **Parameters**

<ParamField path="callId" type="string" required={true} toc={true}>
  The ID of the call.
</ParamField>

<ParamField path="controlId" type="string" required={true} toc={true}>
  The control ID returned from [`tap()`][tap].
</ParamField>

## **Returns**

`Promise<any>` -- The API response confirming the tap was stopped.

## **Example**

```typescript {9-9}
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.tapStop("call-id-xxx", { controlId: "ctrl-id" });
```
