---
title: "collectStop"
slug: /reference/typescript/rest/calling/collect-stop
description: Stop an active input collection on a call via REST.
max-toc-depth: 3
---

[collect]: /docs/sdks/reference/typescript/rest/calling/collect

Stop an active input collection.

## **Parameters**

<ParamField path="callId" type="string" required={true} toc={true}>
  The ID of the call.
</ParamField>

<ParamField path="controlId" type="string" required={true} toc={true}>
  The control ID returned from [`collect()`][collect].
</ParamField>

## **Returns**

`Promise<any>` -- The API response confirming collection was stopped.

## **Example**

```typescript {9-9}
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.collectStop("call-id-xxx", { controlId: "ctrl-id" });
```
