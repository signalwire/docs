---
title: "phoneNumber"
slug: /reference/typescript/rest/lookup/phone-number
description: "Phone Number Lookup namespace."
max-toc-depth: 3
---

Phone Number Lookup namespace.

## **Parameters**

<ParamField path="e164" type="string">
  The e164 parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.lookup.phoneNumber();
console.log(result);
```
