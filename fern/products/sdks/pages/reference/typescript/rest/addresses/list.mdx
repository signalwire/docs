---
title: "list"
slug: /reference/typescript/rest/addresses/list
description: List addresses in the project.
max-toc-depth: 3
---

List addresses in the project.

## **Parameters**

<ParamField path="params" type="Record&lt;string, string | number | boolean | undefined&gt;" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`Promise<any>` -- JSON response containing a `data` list of address objects.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const result = await client.addresses.list();
for (const addr of result.data ?? []) {
    console.log(addr.friendlyName, addr.street);
}
```
