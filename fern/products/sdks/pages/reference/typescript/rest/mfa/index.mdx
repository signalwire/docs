---
title: "MFA"
slug: /reference/typescript/rest/mfa
description: "Multi-factor authentication via SMS and voice."
max-toc-depth: 3
---

[restclient]: /docs/sdks/reference/typescript/rest/client
[sms]: /docs/sdks/reference/typescript/rest/mfa/sms
[call]: /docs/sdks/reference/typescript/rest/mfa/call
[verify]: /docs/sdks/reference/typescript/rest/mfa/verify

Send and verify multi-factor authentication (MFA) codes via SMS or voice call.
The typical flow is: request a code with `sms()` or `call()`, then confirm it
with `verify()`.

Access via `client.mfa` on a [`RestClient`][restclient] instance.

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const request = await client.mfa.sms({ to: "+15551234567", from: "+15559876543" });
console.log(request.id);
```

## **Methods**

<CardGroup cols={3}>
  <Card title="sms" href="/docs/sdks/reference/typescript/rest/mfa/sms">
    Send an MFA verification code via SMS.
  </Card>
  <Card title="call" href="/docs/sdks/reference/typescript/rest/mfa/call">
    Send an MFA verification code via voice call.
  </Card>
  <Card title="verify" href="/docs/sdks/reference/typescript/rest/mfa/verify">
    Verify an MFA code submitted by the user.
  </Card>
</CardGroup>
