---
title: "update"
slug: /reference/typescript/rest/registry/update
description: "Call the `update` method."
max-toc-depth: 3
---

Call the `update` method.

## **Parameters**

<ParamField path="campaignId" type="string">
  The campaignId parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RegistryBrands } from "@signalwire/sdk";

const registryBrands = new RegistryBrands();
const result = await registryBrands.update();
console.log(result);
```
