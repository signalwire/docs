---
title: "createCampaign"
slug: /reference/typescript/rest/registry/create-campaign
description: "Call the `createCampaign` method."
max-toc-depth: 3
---

Call the `createCampaign` method.

## **Parameters**

<ParamField path="brandId" type="string">
  The brandId parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RegistryBrands } from "@signalwire/sdk";

const registryBrands = new RegistryBrands();
const result = await registryBrands.createCampaign();
console.log(result);
```
