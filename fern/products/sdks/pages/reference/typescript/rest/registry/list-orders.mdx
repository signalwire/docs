---
title: "listOrders"
slug: /reference/typescript/rest/registry/list-orders
description: "Call the `listOrders` method."
max-toc-depth: 3
---

Call the `listOrders` method.

## **Parameters**

<ParamField path="campaignId" type="string">
  The campaignId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RegistryBrands } from "@signalwire/sdk";

const registryBrands = new RegistryBrands();
const result = await registryBrands.listOrders();
console.log(result);
```
