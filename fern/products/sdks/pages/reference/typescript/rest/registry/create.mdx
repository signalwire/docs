---
title: "create"
slug: /reference/typescript/rest/registry/create
description: "Call the `create` method."
max-toc-depth: 3
---

Call the `create` method.

## **Parameters**

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RegistryBrands } from "@signalwire/sdk";

const registryBrands = new RegistryBrands();
const result = await registryBrands.create();
console.log(result);
```
