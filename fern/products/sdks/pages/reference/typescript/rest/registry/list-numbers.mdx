---
title: "listNumbers"
slug: /reference/typescript/rest/registry/list-numbers
description: "Call the `listNumbers` method."
max-toc-depth: 3
---

Call the `listNumbers` method.

## **Parameters**

<ParamField path="campaignId" type="string">
  The campaignId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RegistryBrands } from "@signalwire/sdk";

const registryBrands = new RegistryBrands();
const result = await registryBrands.listNumbers();
console.log(result);
```
