---
title: "list"
slug: /reference/typescript/rest/registry/list
description: "Call the `list` method."
max-toc-depth: 3
---

Call the `list` method.

## **Parameters**

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RegistryBrands } from "@signalwire/sdk";

const registryBrands = new RegistryBrands();
const result = await registryBrands.list();
console.log(result);
```
