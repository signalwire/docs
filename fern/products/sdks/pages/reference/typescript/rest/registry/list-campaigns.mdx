---
title: "listCampaigns"
slug: /reference/typescript/rest/registry/list-campaigns
description: "List 10DLC campaigns for a brand."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="brandId" type="string" required={true}>
  The unique identifier of the brand.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {4}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient();
const result = await client.registry.brands.listCampaigns("brand-id");
console.log(result);
```
