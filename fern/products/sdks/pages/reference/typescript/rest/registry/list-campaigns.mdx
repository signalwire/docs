---
title: "listCampaigns"
slug: /reference/typescript/rest/registry/list-campaigns
description: "Call the `listCampaigns` method."
max-toc-depth: 3
---

Call the `listCampaigns` method.

## **Parameters**

<ParamField path="brandId" type="string">
  The brandId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RegistryBrands } from "@signalwire/sdk";

const registryBrands = new RegistryBrands();
const result = await registryBrands.listCampaigns();
console.log(result);
```
