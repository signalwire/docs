---
title: "createOrder"
slug: /reference/typescript/rest/registry/create-order
description: "Create a number assignment order."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="campaignId" type="string" required={true}>
  The unique identifier of the campaign.
</ParamField>

<ParamField path="body" type="any" required={true}>
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {4}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient();
const result = await client.registry.campaigns.createOrder("campaign-id", { /* order body */ });
console.log(result);
```
