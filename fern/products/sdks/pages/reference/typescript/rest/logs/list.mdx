---
title: "list"
slug: /reference/typescript/rest/logs/list
description: "Call the `list` method."
max-toc-depth: 3
---

Call the `list` method.

## **Parameters**

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { MessageLogs } from "@signalwire/sdk";

const messageLogs = new MessageLogs();
const result = await messageLogs.list();
console.log(result);
```
