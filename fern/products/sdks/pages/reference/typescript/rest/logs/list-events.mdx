---
title: "listEvents"
slug: /reference/typescript/rest/logs/list-events
description: "List events for a session or recording."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="logId" type="string">
  The logId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { MessageLogs } from "signalwire-agents/rest";

const messageLogs = new MessageLogs();
const result = await messageLogs.listEvents();
console.log(result);
```
