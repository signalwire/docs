---
title: "listEvents"
slug: /reference/typescript/rest/logs/list-events
description: "Call the `listEvents` method."
max-toc-depth: 3
---

Call the `listEvents` method.

## **Parameters**

<ParamField path="logId" type="string">
  The logId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { MessageLogs } from "@signalwire/sdk";

const messageLogs = new MessageLogs();
const result = await messageLogs.listEvents();
console.log(result);
```
