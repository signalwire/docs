---
title: "Fax Logs"
slug: /reference/typescript/rest/logs/fax
description: "Query fax log entries."
max-toc-depth: 3
---

[restclient]: /docs/sdks/reference/typescript/rest/client

Query fax log entries. Fax logs provide a read-only view of fax activity
in your project.

Access via `client.logs.fax` on a [`RestClient`][restclient] instance.

```typescript {3}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient();
```

### list

List fax log entries.

<EndpointSchemaSnippet endpoint="GET /api/fax/logs" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fax/logs" />

---

### get

Retrieve a specific fax log entry.

<EndpointSchemaSnippet endpoint="GET /api/fax/logs/{id}" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fax/logs/{id}" />

## **Examples**

### List fax logs

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const faxes = await client.logs.fax.list();
for (const fax of faxes.data ?? []) {
    console.log(fax.from, "->", fax.to, fax.status);
}
```

### Get a specific fax log

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const fax = await client.logs.fax.get("fax-log-id");
console.log(fax.from, "->", fax.to);
```
