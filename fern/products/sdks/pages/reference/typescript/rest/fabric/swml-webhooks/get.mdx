---
title: "get"
slug: /reference/typescript/rest/fabric/swml-webhooks/get
description: Retrieve a single SWML webhook resource.
max-toc-depth: 3
---

Retrieve a single SWML webhook resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the SWML webhook.
</ParamField>

## **Returns**

`Promise<any>` -- The SWML webhook resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const webhook = await client.fabric.swmlWebhooks.get("webhook-id");
console.log("Swml Webhook:", webhook.display_name, "ID:", webhook.id);
```
