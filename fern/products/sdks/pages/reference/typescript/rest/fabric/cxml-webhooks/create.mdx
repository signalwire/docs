---
title: "create"
slug: /reference/typescript/rest/fabric/cxml-webhooks/create
description: Create a new cXML webhook resource.
max-toc-depth: 3
---

Create a new cXML webhook resource.

## **Parameters**

<ParamField path="primary_request_url" type="string" required={true} toc={true}>
  The primary URL for the cXML webhook.
</ParamField>

<ParamField path="name" type="string" toc={true}>
  Display name of the cXML webhook.
</ParamField>

## **Returns**

`Promise<any>` -- The created cXML webhook resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const result = await client.fabric.cxmlWebhooks.create({
  name: "my-webhook",
  primary_request_url: "https://example.com/cxml",
});
console.log("Created:", result.id);
```
