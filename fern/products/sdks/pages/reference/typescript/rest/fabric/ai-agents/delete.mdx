---
title: "delete"
slug: /reference/typescript/rest/fabric/ai-agents/delete
description: Delete an AI agent resource.
max-toc-depth: 3
---

Delete an AI agent resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the AI agent to delete.
</ParamField>

## **Returns**

`Promise<any>` -- Empty object on success (HTTP 204).

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

await client.fabric.aiAgents.delete("agent-id");
console.log("AI Agent deleted");
```
