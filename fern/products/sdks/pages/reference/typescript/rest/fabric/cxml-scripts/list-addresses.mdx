---
title: "listAddresses"
slug: /reference/typescript/rest/fabric/cxml-scripts/list-addresses
description: List addresses associated with a cXML script resource.
max-toc-depth: 3
---

List addresses associated with a cXML script resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the cXML script.
</ParamField>

## **Returns**

`Promise<any>` -- List of addresses assigned to this resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const addresses = await client.fabric.cxmlScripts.listAddresses("resource-id");
for (const addr of addresses.data ?? []) {
  console.log(`${addr.display_name}: ${addr.id}`);
}
```
