---
title: "assignDomainApplication"
slug: /reference/typescript/rest/fabric/assign-domain-application
description: "Call the `assignDomainApplication` method."
max-toc-depth: 3
---

Call the `assignDomainApplication` method.

## **Parameters**

<ParamField path="resourceId" type="string">
  The resourceId parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.assignDomainApplication();
console.log(result);
```
