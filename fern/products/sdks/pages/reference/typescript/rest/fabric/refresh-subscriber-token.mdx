---
title: "refreshSubscriberToken"
slug: /reference/typescript/rest/fabric/refresh-subscriber-token
description: "Call the `refreshSubscriberToken` method."
max-toc-depth: 3
---

Call the `refreshSubscriberToken` method.

## **Parameters**

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.refreshSubscriberToken();
console.log(result);
```
