---
title: "updateSipEndpoint"
slug: /reference/typescript/rest/fabric/update-sip-endpoint
description: "Call the `updateSipEndpoint` method."
max-toc-depth: 3
---

Call the `updateSipEndpoint` method.

## **Parameters**

<ParamField path="subscriberId" type="string">
  The subscriberId parameter.
</ParamField>

<ParamField path="endpointId" type="string">
  The endpointId parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.updateSipEndpoint();
console.log(result);
```
