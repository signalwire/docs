---
title: "listSipEndpoints"
slug: /reference/typescript/rest/fabric/list-sip-endpoints
description: "List SIP endpoints for a subscriber."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="subscriberId" type="string" required={true}>
  The unique identifier of the subscriber.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.fabric.subscribers.listSipEndpoints("subscriber-id");
console.log(result);
```
