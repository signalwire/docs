---
title: "listSipEndpoints"
slug: /reference/typescript/rest/fabric/list-sip-endpoints
description: "Call the `listSipEndpoints` method."
max-toc-depth: 3
---

Call the `listSipEndpoints` method.

## **Parameters**

<ParamField path="subscriberId" type="string">
  The subscriberId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.listSipEndpoints();
console.log(result);
```
