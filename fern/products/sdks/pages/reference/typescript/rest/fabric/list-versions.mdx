---
title: "listVersions"
slug: /reference/typescript/rest/fabric/list-versions
description: "List versions of a call flow."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="resourceId" type="string" required={true}>
  The unique identifier of the resource.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.fabric.callFlows.listVersions("call-flow-id");
console.log(result);
```
