---
title: "createSubscriberToken"
slug: /reference/typescript/rest/fabric/create-subscriber-token
description: "Call the `createSubscriberToken` method."
max-toc-depth: 3
---

Call the `createSubscriberToken` method.

## **Parameters**

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.createSubscriberToken();
console.log(result);
```
