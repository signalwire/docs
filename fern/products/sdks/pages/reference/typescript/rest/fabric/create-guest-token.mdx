---
title: "createGuestToken"
slug: /reference/typescript/rest/fabric/create-guest-token
description: "Create a guest access token."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="body" type="any" default="{}">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.fabric.tokens.createGuestToken();
console.log(result);
```
