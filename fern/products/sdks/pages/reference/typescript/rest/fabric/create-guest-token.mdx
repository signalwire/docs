---
title: "createGuestToken"
slug: /reference/typescript/rest/fabric/create-guest-token
description: "Call the `createGuestToken` method."
max-toc-depth: 3
---

Call the `createGuestToken` method.

## **Parameters**

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.createGuestToken();
console.log(result);
```
