---
title: "createInviteToken"
slug: /reference/typescript/rest/fabric/create-invite-token
description: "Create an invite token for a subscriber."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="body" type="any = {}">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.fabric.resources.createInviteToken();
console.log(result);
```
