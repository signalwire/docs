---
title: "createInviteToken"
slug: /reference/typescript/rest/fabric/create-invite-token
description: "Create an invite token for a subscriber."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="body" type="any" default="{}">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.fabric.tokens.createInviteToken();
console.log(result);
```
