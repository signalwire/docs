---
title: "createInviteToken"
slug: /reference/typescript/rest/fabric/create-invite-token
description: "Call the `createInviteToken` method."
max-toc-depth: 3
---

Call the `createInviteToken` method.

## **Parameters**

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.createInviteToken();
console.log(result);
```
