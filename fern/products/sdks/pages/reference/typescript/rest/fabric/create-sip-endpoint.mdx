---
title: "createSipEndpoint"
slug: /reference/typescript/rest/fabric/create-sip-endpoint
description: "Create a SIP endpoint for a subscriber."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="subscriberId" type="string">
  The unique identifier of the subscriber.
</ParamField>

<ParamField path="body" type="any">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.fabric.resources.createSipEndpoint();
console.log(result);
```
