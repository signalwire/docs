---
title: "createSipEndpoint"
slug: /reference/typescript/rest/fabric/create-sip-endpoint
description: "Call the `createSipEndpoint` method."
max-toc-depth: 3
---

Call the `createSipEndpoint` method.

## **Parameters**

<ParamField path="subscriberId" type="string">
  The subscriberId parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.createSipEndpoint();
console.log(result);
```
