---
title: "createEmbedToken"
slug: /reference/typescript/rest/fabric/create-embed-token
description: "Call the `createEmbedToken` method."
max-toc-depth: 3
---

Call the `createEmbedToken` method.

## **Parameters**

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.createEmbedToken();
console.log(result);
```
