---
title: "listAddresses"
slug: /reference/typescript/rest/fabric/list-addresses
description: "Call the `listAddresses` method."
max-toc-depth: 3
---

Call the `listAddresses` method.

## **Parameters**

<ParamField path="resourceId" type="string">
  The resourceId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { FabricResource } from "@signalwire/sdk";

const fabricResource = new FabricResource();
const result = await fabricResource.listAddresses();
console.log(result);
```
