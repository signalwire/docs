---
title: "listAddresses"
slug: /reference/typescript/rest/fabric/list-addresses
description: "List addresses assigned to a fabric resource."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="resourceId" type="string">
  The unique identifier of the resource.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.fabric.resources.listAddresses();
console.log(result);
```
