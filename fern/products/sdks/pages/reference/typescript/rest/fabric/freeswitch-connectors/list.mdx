---
title: "list"
slug: /reference/typescript/rest/fabric/freeswitch-connectors/list
description: List FreeSWITCH connector resources.
max-toc-depth: 3
---

List FreeSWITCH connector resources in the project.

<EndpointSchemaSnippet endpoint="GET /api/fabric/resources/freeswitch_connectors" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fabric/resources/freeswitch_connectors" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const response = await client.fabric.freeswitchConnectors.list();
for (const connector of response.data ?? []) {
  console.log(`${connector.display_name}: ${connector.id}`);
}
```
