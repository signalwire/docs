---
title: "createToken"
slug: /reference/typescript/rest/pubsub/create-token
description: "PubSub API namespace — token creation."
max-toc-depth: 3
---

PubSub API namespace — token creation.

## **Parameters**

<ParamField path="body" type="any">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.pubsub.createToken();
console.log(result);
```
