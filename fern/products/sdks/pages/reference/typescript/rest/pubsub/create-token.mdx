---
title: "createToken"
slug: /reference/typescript/rest/pubsub/create-token
description: "PubSub API namespace — token creation."
max-toc-depth: 3
---

PubSub API namespace — token creation.

## **Parameters**

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { PubSubResource } from "@signalwire/sdk";

const pubSubResource = new PubSubResource();
const result = await pubSubResource.createToken();
console.log(result);
```
