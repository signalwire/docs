---
title: "update"
slug: /reference/typescript/rest/video/update
description: "Call the `update` method."
max-toc-depth: 3
---

Call the `update` method.

## **Parameters**

<ParamField path="streamId" type="string">
  The streamId parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { VideoRooms } from "@signalwire/sdk";

const videoRooms = new VideoRooms();
const result = await videoRooms.update();
console.log(result);
```
