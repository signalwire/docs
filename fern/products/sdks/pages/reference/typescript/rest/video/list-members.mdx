---
title: "listMembers"
slug: /reference/typescript/rest/video/list-members
description: "List members of a session or queue."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="sessionId" type="string" required={true}>
  The unique identifier of the session.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.video.roomSessions.listMembers("session-id");
console.log(result);
```
