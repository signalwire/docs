---
title: "list"
slug: /reference/typescript/rest/video/list
description: "Call the `list` method."
max-toc-depth: 3
---

Call the `list` method.

## **Parameters**

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { VideoRooms } from "@signalwire/sdk";

const videoRooms = new VideoRooms();
const result = await videoRooms.list();
console.log(result);
```
