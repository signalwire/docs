---
title: "listRecordings"
slug: /reference/typescript/rest/video/list-recordings
description: "List recordings for a session or conference."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="sessionId" type="string" required={true}>
  The unique identifier of the session.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.video.roomSessions.listRecordings("session-id");
console.log(result);
```
