---
title: "listEvents"
slug: /reference/typescript/rest/video/list-events
description: "List events for a session or recording."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="sessionId" type="string">
  The unique identifier of the session.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.video.rooms.listEvents();
console.log(result);
```
