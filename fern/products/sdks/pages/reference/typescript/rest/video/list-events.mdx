---
title: "listEvents"
slug: /reference/typescript/rest/video/list-events
description: "Call the `listEvents` method."
max-toc-depth: 3
---

Call the `listEvents` method.

## **Parameters**

<ParamField path="sessionId" type="string">
  The sessionId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { VideoRooms } from "@signalwire/sdk";

const videoRooms = new VideoRooms();
const result = await videoRooms.listEvents();
console.log(result);
```
