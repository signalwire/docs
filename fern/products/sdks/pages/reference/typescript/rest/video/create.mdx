---
title: "create"
slug: /reference/typescript/rest/video/create
description: "Create a new resource."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="body" type="any" required={true}>
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.video.rooms.create({
  name: "my-room"
});
console.log(result);
```
