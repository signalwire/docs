---
title: "create"
slug: /reference/typescript/rest/video/rooms/create
description: Create a new video room.
max-toc-depth: 3
---

Create a new video room.

## **Parameters**

<Markdown src="/snippets/sdks/rest/video/rooms/_create-params.mdx" />

## **Returns**

`Promise<any>` -- The created video room.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const result = await client.video.rooms.create({
  name: "my_room",
  display_name: "My Room",
  max_members: 10,
  quality: "720p",
});
console.log("Created:", result.id);
```
