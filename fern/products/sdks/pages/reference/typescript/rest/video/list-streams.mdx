---
title: "listStreams"
slug: /reference/typescript/rest/video/list-streams
description: "Call the `listStreams` method."
max-toc-depth: 3
---

Call the `listStreams` method.

## **Parameters**

<ParamField path="roomId" type="string">
  The roomId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { VideoRooms } from "@signalwire/sdk";

const videoRooms = new VideoRooms();
const result = await videoRooms.listStreams();
console.log(result);
```
