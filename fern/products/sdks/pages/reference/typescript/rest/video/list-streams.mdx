---
title: "listStreams"
slug: /reference/typescript/rest/video/list-streams
description: "List streams for a room or conference."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="roomId" type="string">
  The unique identifier of the room.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.video.rooms.listStreams();
console.log(result);
```
