---
title: "reset"
slug: /reference/typescript/rest/video/reset
description: "Reset a conference token."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="tokenId" type="string" required={true}>
  The unique identifier of the token.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.video.conferenceTokens.reset("token-id");
console.log(result);
```
