---
title: "reset"
slug: /reference/typescript/rest/video/reset
description: "Call the `reset` method."
max-toc-depth: 3
---

Call the `reset` method.

## **Parameters**

<ParamField path="tokenId" type="string">
  The tokenId parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { VideoRooms } from "@signalwire/sdk";

const videoRooms = new VideoRooms();
const result = await videoRooms.reset();
console.log(result);
```
