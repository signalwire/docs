---
title: "createStream"
slug: /reference/typescript/rest/video/create-stream
description: "Create a new stream for a room or conference."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="roomId" type="string">
  The unique identifier of the room.
</ParamField>

<ParamField path="body" type="any">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.video.rooms.createStream();
console.log(result);
```
