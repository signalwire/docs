---
title: "createStream"
slug: /reference/typescript/rest/video/create-stream
description: "Call the `createStream` method."
max-toc-depth: 3
---

Call the `createStream` method.

## **Parameters**

<ParamField path="roomId" type="string">
  The roomId parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { VideoRooms } from "@signalwire/sdk";

const videoRooms = new VideoRooms();
const result = await videoRooms.createStream();
console.log(result);
```
