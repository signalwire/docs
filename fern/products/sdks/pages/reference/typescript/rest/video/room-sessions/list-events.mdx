---
title: "listEvents"
slug: /reference/typescript/rest/video/room-sessions/list-events
description: List events for a video room session.
max-toc-depth: 3
---

List events for a particular video room session.

<EndpointSchemaSnippet endpoint="GET /api/video/room_sessions/{id}/events" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/video/room_sessions/{id}/events" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const events = await client.video.roomSessions.listEvents("session-id");
for (const event of events.data ?? []) {
  console.log(`${event.type}: ${event.id}`);
}
```
