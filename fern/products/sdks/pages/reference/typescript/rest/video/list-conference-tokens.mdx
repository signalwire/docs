---
title: "listConferenceTokens"
slug: /reference/typescript/rest/video/list-conference-tokens
description: "Call the `listConferenceTokens` method."
max-toc-depth: 3
---

Call the `listConferenceTokens` method.

## **Parameters**

<ParamField path="conferenceId" type="string">
  The conferenceId parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { VideoRooms } from "@signalwire/sdk";

const videoRooms = new VideoRooms();
const result = await videoRooms.listConferenceTokens();
console.log(result);
```
