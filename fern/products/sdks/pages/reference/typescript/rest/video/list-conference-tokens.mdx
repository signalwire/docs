---
title: "listConferenceTokens"
slug: /reference/typescript/rest/video/list-conference-tokens
description: "List tokens for a video conference."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="conferenceId" type="string">
  The unique identifier of the conference.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.video.rooms.listConferenceTokens();
console.log(result);
```
