---
title: "listConferenceTokens"
slug: /reference/typescript/rest/video/list-conference-tokens
description: "List tokens for a video conference."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="conferenceId" type="string" required={true}>
  The unique identifier of the conference.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.video.conferences.listConferenceTokens("conference-id");
console.log(result);
```
