---
title: "createToken"
slug: /reference/typescript/rest/chat/create-token
description: "Chat API namespace — token creation."
max-toc-depth: 3
---

Chat API namespace — token creation.

## **Parameters**

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { ChatResource } from "@signalwire/sdk";

const chatResource = new ChatResource();
const result = await chatResource.createToken();
console.log(result);
```
