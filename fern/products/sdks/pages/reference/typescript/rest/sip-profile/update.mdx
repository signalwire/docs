---
title: "update"
slug: /reference/typescript/rest/sip-profile/update
description: "SIP Profile namespace — get and update project SIP profile."
max-toc-depth: 3
---

SIP Profile namespace — get and update project SIP profile.

## **Parameters**

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { SipProfileResource } from "@signalwire/sdk";

const sipProfileResource = new SipProfileResource();
const result = await sipProfileResource.update();
console.log(result);
```
