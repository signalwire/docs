---
title: "create"
slug: /reference/typescript/rest/imported-numbers/create
description: "Imported Phone Numbers namespace — create only."
max-toc-depth: 3
---

Imported Phone Numbers namespace — create only.

## **Parameters**

<ParamField path="body" type="any" required={true}>
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {4}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient();
const result = await client.importedNumbers.create({ number: '+15551234567' });
console.log(result);
```
