---
title: "update"
slug: /reference/typescript/rest/compat/laml-bins/update
description: Update a LAML bin.
max-toc-depth: 3
---

Update a LAML bin's content or name. Uses POST (Twilio convention).

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The LAML bin SID.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/laml-bins/_create-update-params.mdx" />

## **Returns**

`Promise<any>` -- The updated LAML bin resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

await client.compat.lamlBins.update("LB...", {
  Name: "Greeting",
  Contents:
    '<?xml version="1.0" encoding="UTF-8"?>' +
    "<Response><Say>Goodbye!</Say></Response>",
});
```
