---
title: "updateParticipant"
slug: /reference/typescript/rest/compat/update-participant
description: "Call the `updateParticipant` method."
max-toc-depth: 3
---

Call the `updateParticipant` method.

## **Parameters**

<ParamField path="conferenceSid" type="string">
  The conferenceSid parameter.
</ParamField>

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.updateParticipant();
console.log(result);
```
