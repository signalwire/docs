---
title: "getParticipant"
slug: /reference/typescript/rest/compat/get-participant
description: "Call the `getParticipant` method."
max-toc-depth: 3
---

Call the `getParticipant` method.

## **Parameters**

<ParamField path="conferenceSid" type="string">
  The conferenceSid parameter.
</ParamField>

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.getParticipant();
console.log(result);
```
