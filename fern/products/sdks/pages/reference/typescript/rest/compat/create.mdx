---
title: "create"
slug: /reference/typescript/rest/compat/create
description: "Call the `create` method."
max-toc-depth: 3
---

Call the `create` method.

## **Parameters**

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.create();
console.log(result);
```
