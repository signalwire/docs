---
title: "update"
slug: /reference/typescript/rest/compat/queues/update
description: Update a queue.
max-toc-depth: 3
---


Update a queue's configuration. Uses POST (Twilio convention).

<EndpointSchemaSnippet endpoint="POST /Accounts/{AccountSid}/Queues/{Sid}" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /Accounts/{AccountSid}/Queues/{Sid}" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.compat.queues.update("QU...", { FriendlyName: "Support Queue", MaxSize: 200 });
```
