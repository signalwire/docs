---
title: "getMedia"
slug: /reference/typescript/rest/compat/get-media
description: "Call the `getMedia` method."
max-toc-depth: 3
---

Call the `getMedia` method.

## **Parameters**

<ParamField path="messageSid" type="string">
  The messageSid parameter.
</ParamField>

<ParamField path="mediaSid" type="string">
  The mediaSid parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.getMedia();
console.log(result);
```
