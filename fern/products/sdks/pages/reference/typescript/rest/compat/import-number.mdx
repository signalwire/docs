---
title: "importNumber"
slug: /reference/typescript/rest/compat/import-number
description: "Call the `importNumber` method."
max-toc-depth: 3
---

Call the `importNumber` method.

## **Parameters**

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.importNumber();
console.log(result);
```
