---
title: "removeParticipant"
slug: /reference/typescript/rest/compat/remove-participant
description: "Call the `removeParticipant` method."
max-toc-depth: 3
---

Call the `removeParticipant` method.

## **Parameters**

<ParamField path="conferenceSid" type="string">
  The conferenceSid parameter.
</ParamField>

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.removeParticipant();
console.log(result);
```
