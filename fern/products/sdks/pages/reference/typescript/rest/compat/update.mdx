---
title: "update"
slug: /reference/typescript/rest/compat/update
description: "Call the `update` method."
max-toc-depth: 3
---

Call the `update` method.

## **Parameters**

<ParamField path="sid" type="string">
  The sid parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.update();
console.log(result);
```
