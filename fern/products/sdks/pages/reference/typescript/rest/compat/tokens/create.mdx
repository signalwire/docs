---
title: "create"
slug: /reference/typescript/rest/compat/tokens/create
description: Create a new API token.
max-toc-depth: 3
---


Create a new API token.

<EndpointSchemaSnippet endpoint="POST /Accounts/{AccountSid}/tokens" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /Accounts/{AccountSid}/tokens" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const token = await client.compat.tokens.create({
  name: "my-token",
  permissions: ["voice", "messaging"],
});
```
