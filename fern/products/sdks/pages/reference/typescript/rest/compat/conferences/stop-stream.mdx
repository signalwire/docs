---
title: "stopStream"
slug: /reference/typescript/rest/compat/conferences/stop-stream
description: Stop a media stream on a conference.
max-toc-depth: 3
---

Stop a media stream on a conference.

## **Parameters**

<ParamField path="conferenceSid" type="string" required={true} toc={true}>
  The SID of the conference.
</ParamField>

<ParamField path="streamSid" type="string" required={true} toc={true}>
  The SID of the stream to stop.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/calls/_stop-stream-params.mdx" />

## **Returns**

`Promise<any>` -- The updated stream resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.compat.conferences.stopStream("CF...", "ST...", { Status: "stopped" });
```
