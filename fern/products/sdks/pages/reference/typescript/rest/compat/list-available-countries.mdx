---
title: "listAvailableCountries"
slug: /reference/typescript/rest/compat/list-available-countries
description: "Call the `listAvailableCountries` method."
max-toc-depth: 3
---

Call the `listAvailableCountries` method.

## **Parameters**

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.listAvailableCountries();
console.log(result);
```
