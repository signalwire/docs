---
title: "update"
slug: /reference/typescript/rest/compat/conferences/update
description: Update a conference (e.g., end it or set an announce URL).
max-toc-depth: 3
---

Update a conference (e.g., end it, or set the announce URL). Uses POST (Twilio convention).

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The conference SID.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/conferences/_update-params.mdx" />

## **Returns**

`Promise<any>` -- The updated conference resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.compat.conferences.update("CF...", { Status: "completed" });
```
