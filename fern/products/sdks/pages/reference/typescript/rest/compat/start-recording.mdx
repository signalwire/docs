---
title: "startRecording"
slug: /reference/typescript/rest/compat/start-recording
description: "Call the `startRecording` method."
max-toc-depth: 3
---

Call the `startRecording` method.

## **Parameters**

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.startRecording();
console.log(result);
```
