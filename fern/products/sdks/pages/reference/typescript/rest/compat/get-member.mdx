---
title: "getMember"
slug: /reference/typescript/rest/compat/get-member
description: "Call the `getMember` method."
max-toc-depth: 3
---

Call the `getMember` method.

## **Parameters**

<ParamField path="queueSid" type="string">
  The queueSid parameter.
</ParamField>

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.getMember();
console.log(result);
```
