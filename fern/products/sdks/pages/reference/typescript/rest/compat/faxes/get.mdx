---
title: "get"
slug: /reference/typescript/rest/compat/faxes/get
description: Retrieve a single fax by SID.
max-toc-depth: 3
---

Retrieve a single fax by SID.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The fax SID.
</ParamField>

## **Returns**

`Promise<any>` -- The fax resource.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const fax = await client.compat.faxes.get("FX...");
```
