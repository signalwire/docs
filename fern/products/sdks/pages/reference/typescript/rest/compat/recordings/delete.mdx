---
title: "delete"
slug: /reference/typescript/rest/compat/recordings/delete
description: Delete a recording.
max-toc-depth: 3
---

Delete a recording.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The recording SID to delete.
</ParamField>

## **Returns**

`Promise<any>` -- Server acknowledgment.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
await client.compat.recordings.delete("RE...");
```
