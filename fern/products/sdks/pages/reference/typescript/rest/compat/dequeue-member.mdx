---
title: "dequeueMember"
slug: /reference/typescript/rest/compat/dequeue-member
description: "Call the `dequeueMember` method."
max-toc-depth: 3
---

Call the `dequeueMember` method.

## **Parameters**

<ParamField path="queueSid" type="string">
  The queueSid parameter.
</ParamField>

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.dequeueMember();
console.log(result);
```
