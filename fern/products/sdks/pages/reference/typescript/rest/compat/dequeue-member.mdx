---
title: "dequeueMember"
slug: /reference/typescript/rest/compat/dequeue-member
description: "Dequeue a member from a queue."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="queueSid" type="string">
  The SID of the queue.
</ParamField>

<ParamField path="callSid" type="string">
  The SID of the call.
</ParamField>

<ParamField path="body" type="any = {}">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk/rest";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const result = await client.compat.queues.dequeueMember(queueSid, callSid, body);
console.log(result);
```
