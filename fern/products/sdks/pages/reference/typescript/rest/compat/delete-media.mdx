---
title: "deleteMedia"
slug: /reference/typescript/rest/compat/delete-media
description: "Call the `deleteMedia` method."
max-toc-depth: 3
---

Call the `deleteMedia` method.

## **Parameters**

<ParamField path="messageSid" type="string">
  The messageSid parameter.
</ParamField>

<ParamField path="mediaSid" type="string">
  The mediaSid parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.deleteMedia();
console.log(result);
```
