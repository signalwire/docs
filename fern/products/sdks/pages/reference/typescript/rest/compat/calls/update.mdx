---
title: "update"
slug: /reference/typescript/rest/compat/calls/update
description: Update an active call.
max-toc-depth: 3
---

Update an active call (e.g., redirect to new LAML, end the call). Uses POST (Twilio convention).

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The call SID.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/calls/_update-params.mdx" />

## **Returns**

`Promise<any>` -- The updated call resource.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

await client.compat.calls.update("CA...", { Status: "completed" });
```
