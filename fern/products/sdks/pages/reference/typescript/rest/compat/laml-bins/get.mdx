---
title: "get"
slug: /reference/typescript/rest/compat/laml-bins/get
description: Retrieve a single LAML bin by SID.
max-toc-depth: 3
---

Retrieve a single LAML bin by SID.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The LAML bin SID.
</ParamField>

## **Returns**

`Promise<any>` -- The LAML bin resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const lamlBin = await client.compat.lamlBins.get("LB...");
```
