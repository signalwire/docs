---
title: "updateRecording"
slug: /reference/typescript/rest/compat/update-recording
description: "Call the `updateRecording` method."
max-toc-depth: 3
---

Call the `updateRecording` method.

## **Parameters**

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

<ParamField path="recordingSid" type="string">
  The recordingSid parameter.
</ParamField>

<ParamField path="body" type="any">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.updateRecording();
console.log(result);
```
