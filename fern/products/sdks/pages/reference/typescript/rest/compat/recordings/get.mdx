---
title: "get"
slug: /reference/typescript/rest/compat/recordings/get
description: Retrieve a single recording by SID.
max-toc-depth: 3
---


Retrieve a single recording by SID.

<EndpointSchemaSnippet endpoint="GET /Accounts/{AccountSid}/Recordings/{Sid}" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /Accounts/{AccountSid}/Recordings/{Sid}" />

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});
const recording = await client.compat.recordings.get("RE...");
```
