---
title: "searchTollFree"
slug: /reference/typescript/rest/compat/search-toll-free
description: "Call the `searchTollFree` method."
max-toc-depth: 3
---

Call the `searchTollFree` method.

## **Parameters**

<ParamField path="country" type="string">
  The country parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.searchTollFree();
console.log(result);
```
