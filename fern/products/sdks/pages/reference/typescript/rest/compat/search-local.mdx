---
title: "searchLocal"
slug: /reference/typescript/rest/compat/search-local
description: "Call the `searchLocal` method."
max-toc-depth: 3
---

Call the `searchLocal` method.

## **Parameters**

<ParamField path="country" type="string">
  The country parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.searchLocal();
console.log(result);
```
