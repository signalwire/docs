---
title: "listRecordings"
slug: /reference/typescript/rest/compat/list-recordings
description: "List recordings for a session or conference."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="conferenceSid" type="string">
  The SID of the conference.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.compat.accounts.listRecordings();
console.log(result);
```
