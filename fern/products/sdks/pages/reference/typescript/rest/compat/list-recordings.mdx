---
title: "listRecordings"
slug: /reference/typescript/rest/compat/list-recordings
description: "Call the `listRecordings` method."
max-toc-depth: 3
---

Call the `listRecordings` method.

## **Parameters**

<ParamField path="conferenceSid" type="string">
  The conferenceSid parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.listRecordings();
console.log(result);
```
