---
title: "get"
slug: /reference/typescript/rest/compat/queues/get
description: Retrieve a single queue by SID.
max-toc-depth: 3
---

Retrieve a single queue by SID.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The queue SID.
</ParamField>

## **Returns**

`Promise<any>` -- The queue resource.

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const queue = await client.compat.queues.get("QU...");
```
