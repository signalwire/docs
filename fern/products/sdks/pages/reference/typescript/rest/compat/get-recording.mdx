---
title: "getRecording"
slug: /reference/typescript/rest/compat/get-recording
description: "Call the `getRecording` method."
max-toc-depth: 3
---

Call the `getRecording` method.

## **Parameters**

<ParamField path="conferenceSid" type="string">
  The conferenceSid parameter.
</ParamField>

<ParamField path="recordingSid" type="string">
  The recordingSid parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.getRecording();
console.log(result);
```
