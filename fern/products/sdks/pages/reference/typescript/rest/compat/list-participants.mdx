---
title: "listParticipants"
slug: /reference/typescript/rest/compat/list-participants
description: "Call the `listParticipants` method."
max-toc-depth: 3
---

Call the `listParticipants` method.

## **Parameters**

<ParamField path="conferenceSid" type="string">
  The conferenceSid parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.listParticipants();
console.log(result);
```
