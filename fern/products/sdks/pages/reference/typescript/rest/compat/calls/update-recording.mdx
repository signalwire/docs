---
title: "updateRecording"
slug: /reference/typescript/rest/compat/calls/update-recording
description: Update a call recording (pause, resume, or stop).
max-toc-depth: 3
---

Update a call recording (e.g., pause, resume, or stop).

## **Parameters**

<ParamField path="call_sid" type="string" required={true} toc={true}>
  The SID of the call.
</ParamField>

<ParamField path="recording_sid" type="string" required={true} toc={true}>
  The SID of the recording.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/calls/_update-recording-params.mdx" />

## **Returns**

`Promise<any>` -- The updated recording resource.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

await client.compat.calls.updateRecording("CA...", "RE...", { Status: "paused" });
```
