---
title: "create"
slug: /reference/typescript/rest/compat/calls/create
description: Initiate a new outbound call.
max-toc-depth: 3
---

Initiate a new outbound call.

## **Parameters**

<Markdown src="/snippets/sdks/rest/compat/calls/_create-params.mdx" />

## **Returns**

`Promise<any>` -- The created call resource including the call SID.

## **Example**

```typescript {8}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const call = await client.compat.calls.create({
  To: "+15559876543",
  From: "+15551234567",
  Url: "https://example.com/voice",
});
console.log(call.sid);
```
