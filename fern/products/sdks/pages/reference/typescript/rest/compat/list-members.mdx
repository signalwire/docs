---
title: "listMembers"
slug: /reference/typescript/rest/compat/list-members
description: "Call the `listMembers` method."
max-toc-depth: 3
---

Call the `listMembers` method.

## **Parameters**

<ParamField path="queueSid" type="string">
  The queueSid parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.listMembers();
console.log(result);
```
