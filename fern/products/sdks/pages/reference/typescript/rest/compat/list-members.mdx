---
title: "listMembers"
slug: /reference/typescript/rest/compat/list-members
description: "List members of a session or queue."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="queueSid" type="string">
  The SID of the queue.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.compat.accounts.listMembers();
console.log(result);
```
