---
title: "listMedia"
slug: /reference/typescript/rest/compat/list-media
description: "List media attachments for a message or fax."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="messageSid" type="string">
  The SID of the message.
</ParamField>

<ParamField path="params" type="QueryParams">
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.compat.accounts.listMedia();
console.log(result);
```
