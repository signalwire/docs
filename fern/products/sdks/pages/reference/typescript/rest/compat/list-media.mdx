---
title: "listMedia"
slug: /reference/typescript/rest/compat/list-media
description: "Call the `listMedia` method."
max-toc-depth: 3
---

Call the `listMedia` method.

## **Parameters**

<ParamField path="messageSid" type="string">
  The messageSid parameter.
</ParamField>

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.listMedia();
console.log(result);
```
