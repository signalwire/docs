---
title: "stopStream"
slug: /reference/typescript/rest/compat/stop-stream
description: "Stop an active audio stream."
max-toc-depth: 3
---


## **Parameters**

<ParamField path="callSid" type="string">
  The SID of the call.
</ParamField>

<ParamField path="streamSid" type="string">
  The SID of the stream.
</ParamField>

<ParamField path="body" type="any = {}">
  The request body containing the resource data.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { RestClient } from "signalwire-agents/rest";

const client = new RestClient();
const result = await client.compat.accounts.stopStream();
console.log(result);
```
