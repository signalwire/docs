---
title: "stopStream"
slug: /reference/typescript/rest/compat/stop-stream
description: "Call the `stopStream` method."
max-toc-depth: 3
---

Call the `stopStream` method.

## **Parameters**

<ParamField path="callSid" type="string">
  The callSid parameter.
</ParamField>

<ParamField path="streamSid" type="string">
  The streamSid parameter.
</ParamField>

<ParamField path="body" type="any = {}">
  The body parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.stopStream();
console.log(result);
```
