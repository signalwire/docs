---
title: "list"
slug: /reference/typescript/rest/compat/list
description: "Call the `list` method."
max-toc-depth: 3
---

Call the `list` method.

## **Parameters**

<ParamField path="params" type="QueryParams">
  The params parameter.
</ParamField>

## **Returns**

`Promise<any>` -- The result of the operation.

## **Example**

```typescript
import { CompatAccounts } from "@signalwire/sdk";

const compatAccounts = new CompatAccounts();
const result = await compatAccounts.list();
console.log(result);
```
