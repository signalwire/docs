---
title: "update"
slug: /reference/typescript/rest/project/update
description: Update an existing API token.
max-toc-depth: 3
---

### tokens.update

Update an existing API token. Uses PATCH.

## **Parameters**

<ParamField path="tokenId" type="string" required={true} toc={true}>
  The token resource ID.
</ParamField>

<ParamField path="**kwargs" type="dict" toc={true}>
  Fields to update (e.g., `name`).
</ParamField>

## **Returns**

`dict` -- The updated token object.

## **Example**

```typescript {9-9}
import { RestClient } from "@signalwire/sdk";

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.project.tokens.update("token-id", name="renamed-token")
```
