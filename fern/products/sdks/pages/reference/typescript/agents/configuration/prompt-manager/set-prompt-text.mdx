---
title: "setPromptText"
slug: /reference/typescript/agents/configuration/prompt-manager/set-prompt-text
description: "PromptManager - Manages agent prompts with POM support."
max-toc-depth: 3
---

PromptManager - Manages agent prompts with POM support.

## **Parameters**

<ParamField path="usePom" type="unknown">
  Whether to use structured POM sections (default true).
</ParamField>

<ParamField path="text" type="string">
  The raw prompt string.
</ParamField>

## **Returns**

`void` -- The result of the operation.

## **Example**

```typescript
import { PromptManager } from "@signalwire/sdk";

const promptManager = new PromptManager();
const result = promptManager.setPromptText();
console.log(result);
```
