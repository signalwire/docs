---
title: "getServerOptions"
slug: /reference/typescript/agents/configuration/security-config/get-server-options
description: "SslConfig - SSL/TLS configuration for HTTPS serving."
max-toc-depth: 3
---

SslConfig - SSL/TLS configuration for HTTPS serving.

## **Parameters**

<ParamField path="opts" type="unknown">
  Optional SSL configuration overrides.
</ParamField>

## **Returns**

`` -- The result of the operation.

## **Example**

```typescript
import { SslConfig } from "@signalwire/sdk";

const sslConfig = new SslConfig();
const result = sslConfig.getServerOptions();
console.log(result);
```
