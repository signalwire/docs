---
title: "generateToken"
slug: /reference/typescript/agents/configuration/session-manager/generate-token
description: "Generate a signed, base64url-encoded token binding a function name to a call ID."
max-toc-depth: 3
---

Generate a signed, base64url-encoded token binding a function name to a call ID.
The token encodes the call ID, function name, expiry timestamp, a random nonce,
and an HMAC-SHA256 signature.

## **Parameters**

<ParamField path="functionName" type="string" required={true} toc={true}>
  The SWAIG function name to bind.
</ParamField>

<ParamField path="callId" type="string" required={true} toc={true}>
  The call ID to bind.
</ParamField>

## **Returns**

`string` -- A base64url-encoded token string.

## **Example**

```typescript {4}
import { SessionManager } from 'signalwire-agents';

const sm = new SessionManager();
const token = sm.generateToken('get_weather', 'call-abc123');
console.log(token);
```
