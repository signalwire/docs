---
title: "validateSslConfig"
slug: /reference/typescript/agents/configuration/security-config/validate-ssl-config
description: Validate that SSL configuration is complete and certificate files exist.
max-toc-depth: 3
---

Validate that SSL configuration is complete and the certificate files exist.

## **Returns**

`[boolean, string | undefined]` -- A tuple of `(is_valid, error_message)`.
Returns `(True, None)` when SSL is disabled or when all required files exist.

## **Example**

```typescript {4}
import { AgentBase } from "@signalwire/sdk";

security = SecurityConfig()
is_valid, error = security.validate_ssl_config()

if not is_valid:
    console.log(`SSL configuration error: ${error}`)
else:
    console.log("SSL configuration is valid")
```
