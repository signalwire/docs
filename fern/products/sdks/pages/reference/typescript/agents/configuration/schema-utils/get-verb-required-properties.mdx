---
title: "getVerbRequiredProperties"
slug: /reference/typescript/agents/configuration/schema-utils/get-verb-required-properties
description: "SchemaUtils - JSON Schema validation for SWML documents."
max-toc-depth: 3
---

SchemaUtils - JSON Schema validation for SWML documents.

## **Parameters**

<ParamField path="opts" type="unknown">
  Optional settings for skipping validation or limiting cache size.
</ParamField>

<ParamField path="verbName" type="string">
  The verb name (e.g. "answer", "tap").
</ParamField>

<ParamField path="verbName" type="string">
  The verb name.
</ParamField>

## **Returns**

`string[]` -- The result of the operation.

## **Example**

```typescript
import { SchemaUtils } from "@signalwire/sdk";

const schemaUtils = new SchemaUtils();
const result = schemaUtils.getVerbRequiredProperties();
console.log(result);
```
