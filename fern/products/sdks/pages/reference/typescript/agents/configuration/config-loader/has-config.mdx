---
title: "hasConfig"
slug: /reference/typescript/agents/configuration/config-loader/has-config
description: Check whether a configuration file was successfully loaded.
max-toc-depth: 3
---

Check whether a configuration file was successfully loaded.

## **Returns**

`bool` -- `True` if a config file was found and loaded.

## **Example**

```typescript {4}
import { AgentBase } from "@signalwire/sdk";

loader = ConfigLoader()
if loader.has_config():
    console.log(`Loaded config from: ${loader.get_config_file()}`)
else:
    console.log("No config file found, using defaults")
```
