---
title: "getConfigFile"
slug: /reference/typescript/agents/configuration/config-loader/get-config-file
description: Get the path of the loaded config file.
max-toc-depth: 3
---

Get the path of the loaded config file.

## **Returns**

`string | undefined` -- The file path, or `undefined` if no config was loaded.

## **Example**

```typescript {4}
import { AgentBase } from "@signalwire/sdk";

loader = ConfigLoader(["config.json", "/etc/swml/config.json"])
path = loader.get_config_file()

if path:
    console.log(`Loaded config from: ${path}`)
else:
    console.log("No config file found")
```
