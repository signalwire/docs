---
title: "getUrlScheme"
slug: /reference/typescript/agents/configuration/security-config/get-url-scheme
description: Get the URL scheme based on SSL configuration.
max-toc-depth: 3
---

Get the URL scheme based on SSL configuration.

## **Returns**

`str` -- `"https"` if SSL is enabled, `"http"` otherwise.

## **Example**

```typescript {4}
import { AgentBase } from "@signalwire/sdk";

security = SecurityConfig()
scheme = security.get_url_scheme()

console.log(`URL scheme: ${scheme}`)  # "http" (default) or "https"
```
