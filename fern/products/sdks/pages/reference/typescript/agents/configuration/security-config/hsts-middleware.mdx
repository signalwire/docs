---
title: "hstsMiddleware"
slug: /reference/typescript/agents/configuration/security-config/hsts-middleware
description: "SslConfig - SSL/TLS configuration for HTTPS serving."
max-toc-depth: 3
---

SslConfig - SSL/TLS configuration for HTTPS serving.

## **Parameters**

<ParamField path="opts" type="unknown">
  Optional SSL configuration overrides.
</ParamField>

## **Returns**

`(c: any, next: () => Promise<void>) => Promise<void>` -- The result of the operation.

## **Example**

```typescript
import { SslConfig } from "@signalwire/sdk";

const sslConfig = new SslConfig();
const result = await sslConfig.hstsMiddleware();
console.log(result);
```
