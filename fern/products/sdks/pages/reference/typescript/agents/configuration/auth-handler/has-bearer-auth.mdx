---
title: "hasBearerAuth"
slug: /reference/typescript/agents/configuration/auth-handler/has-bearer-auth
description: "AuthHandler - Multi-method authentication handler."
max-toc-depth: 3
---

AuthHandler - Multi-method authentication handler.

## **Parameters**

<ParamField path="config" type="unknown">
  Authentication configuration specifying one or more auth methods.
</ParamField>

<ParamField path="headers" type="unknown">
  The request headers as a string-keyed record.
</ParamField>

## **Returns**

`boolean` -- The result of the operation.

## **Example**

```typescript
import { AuthHandler } from "@signalwire/sdk";

const authHandler = new AuthHandler();
const result = authHandler.hasBearerAuth();
console.log(result);
```
