---
title: "createAzureHandler"
slug: /reference/typescript/agents/configuration/serverless-adapter/create-azure-handler
description: "ServerlessAdapter - Adapts AgentBase for serverless platforms."
max-toc-depth: 3
---

ServerlessAdapter - Adapts AgentBase for serverless platforms.

## **Parameters**

<ParamField path="platform" type="unknown">
  Target platform; defaults to 'auto' which detects from environment variables.
</ParamField>

<ParamField path="app" type="unknown">
  A Hono-compatible application with a `fetch` method.
</ParamField>

<ParamField path="event" type="unknown">
  The incoming serverless event to process.
</ParamField>

<ParamField path="opts" type="unknown">
  Optional overrides for region, project, function name, stage, or API ID.
</ParamField>

<ParamField path="app" type="unknown">
  A Hono-compatible application with a `fetch` method.
</ParamField>

<ParamField path="app" type="unknown">
  A Hono-compatible application with a `fetch` method.
</ParamField>

<ParamField path="app" type="unknown">
  A Hono-compatible application with a `fetch` method.
</ParamField>

## **Returns**

`The detected platform identifier; defaults to 'lambda' if no match is found.` -- The result of the operation.

## **Example**

```typescript
import { ServerlessAdapter } from "@signalwire/sdk";

const result = ServerlessAdapter.createAzureHandler();
console.log(result);
```
