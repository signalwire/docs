---
title: "validate"
slug: /reference/typescript/agents/configuration/schema-utils/validate
description: "SchemaUtils - JSON Schema validation for SWML documents."
max-toc-depth: 3
---

SchemaUtils - JSON Schema validation for SWML documents.

## **Parameters**

<ParamField path="opts" type="unknown">
  Optional settings for skipping validation or limiting cache size.
</ParamField>

<ParamField path="verbName" type="unknown">
  The verb name (e.g. "answer", "tap").
</ParamField>

<ParamField path="verbName" type="unknown">
  The verb name.
</ParamField>

<ParamField path="verbName" type="unknown">
  The verb name.
</ParamField>

<ParamField path="verbName" type="unknown">
  The verb name.
</ParamField>

<ParamField path="verbName" type="unknown">
  The verb name.
</ParamField>

<ParamField path="config" type="unknown">
  The verb configuration to validate.
</ParamField>

<ParamField path="swml" type={"string | Record<string, unknown>"}>
  The SWML document as a JSON string or parsed object.
</ParamField>

## **Returns**

`ValidationResult` -- The result of the operation.

## **Example**

```typescript
import { SchemaUtils } from "@signalwire/sdk";

const schemaUtils = new SchemaUtils();
const result = schemaUtils.validate();
console.log(result);
```
