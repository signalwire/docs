---
title: "build"
slug: /reference/typescript/agents/swml-builder/build
description: Build and return the complete SWML document as a dictionary.
max-toc-depth: 3
---

[swmlservice-get-document]: /docs/sdks/reference/typescript/agents/swml-service/get-document

Build and return the complete SWML document as a Python dictionary. Delegates to
[`AgentBase.get_document()`][swmlservice-get-document].

## **Returns**

`Record<string, unknown>` -- The complete SWML document.

## **Example**

```typescript {7}
import { AgentBase } from "@signalwire/sdk";
import { SwmlBuilder } from "@signalwire/sdk";

service = SWMLService(name="demo")
builder = SWMLBuilder(service)

doc = builder.answer().hangup().build()
console.log(doc)
# {"version": "1.0.0", "sections": {"main": [{"answer": {}}, {"hangup": {}}]}}
```
