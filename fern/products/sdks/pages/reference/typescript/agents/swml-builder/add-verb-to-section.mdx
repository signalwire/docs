---
title: "addVerbToSection"
slug: /reference/typescript/agents/swml-builder/add-verb-to-section
description: "SwmlBuilder - Builds SWML (SignalWire Markup Language) documents."
max-toc-depth: 3
---

SwmlBuilder - Builds SWML (SignalWire Markup Language) documents.

## **Parameters**

<ParamField path="verbName" type="string">
  The SWML verb name (e.g., "answer", "ai").
</ParamField>

<ParamField path="config" type="unknown">
  The verb's configuration payload.
</ParamField>

<ParamField path="sectionName" type="string">
  The target section name.
</ParamField>

<ParamField path="verbName" type="string">
  The SWML verb name.
</ParamField>

<ParamField path="config" type="unknown">
  The verb's configuration payload.
</ParamField>

## **Returns**

`void` -- The result of the operation.

## **Example**

```typescript
import { SwmlBuilder } from 'signalwire-agents';

const swmlBuilder = new SwmlBuilder();
const result = swmlBuilder.addVerbToSection();
console.log(result);
```
