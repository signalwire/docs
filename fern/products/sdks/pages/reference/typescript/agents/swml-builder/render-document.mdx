---
title: "renderDocument"
slug: /reference/typescript/agents/swml-builder/render-document
description: "SwmlBuilder - Builds SWML (SignalWire Markup Language) documents."
max-toc-depth: 3
---

SwmlBuilder - Builds SWML (SignalWire Markup Language) documents.

## **Parameters**

<ParamField path="verbName" type="unknown">
  The SWML verb name (e.g., "answer", "ai").
</ParamField>

<ParamField path="config" type="unknown">
  The verb's configuration payload.
</ParamField>

<ParamField path="sectionName" type="unknown">
  The target section name.
</ParamField>

<ParamField path="verbName" type="unknown">
  The SWML verb name.
</ParamField>

<ParamField path="config" type="unknown">
  The verb's configuration payload.
</ParamField>

## **Returns**

`string` -- The result of the operation.

## **Example**

```typescript
import { SwmlBuilder } from "@signalwire/sdk";

const swmlBuilder = new SwmlBuilder();
const result = swmlBuilder.renderDocument();
console.log(result);
```
