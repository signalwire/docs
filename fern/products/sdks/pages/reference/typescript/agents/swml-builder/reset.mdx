---
title: "reset"
slug: /reference/typescript/agents/swml-builder/reset
description: Reset the SWML document to an empty state.
max-toc-depth: 3
---

[swmlservice-reset-document]: /docs/sdks/reference/typescript/agents/swml-service/reset-document

Reset the document to an empty state and return the builder for continued chaining.
Delegates to
[`AgentBase.reset_document()`][swmlservice-reset-document].

## **Returns**

`Self` -- The builder instance for method chaining.

## **Example**

```typescript {12}
import { AgentBase } from "@signalwire/sdk";
import { SwmlBuilder } from "@signalwire/sdk";

service = SWMLService(name="demo")
builder = SWMLBuilder(service)

# Build first document
doc1 = builder.answer().hangup().build()
console.log(doc1)

# Reset and build a different document
builder.reset()
doc2 = builder.answer().play(url="say:Goodbye").hangup().build()
console.log(doc2)
```
