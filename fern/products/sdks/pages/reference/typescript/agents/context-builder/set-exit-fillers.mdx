---
title: "setExitFillers"
slug: /reference/typescript/agents/context-builder/set-exit-fillers
description: "Set filler phrases spoken when exiting a context."
max-toc-depth: 3
---

[context]: /docs/sdks/reference/typescript/agents/context-builder/context

Set filler phrases spoken when exiting this context, keyed by language code.
This is a method on [`Context`][context].

## **Parameters**

<ParamField path="fillers" type="Record<string, string[]>" required={true} toc={true}>
  Map of language codes to arrays of filler phrases.
</ParamField>

## **Returns**

`this` -- The Context instance for chaining.

## **Example**

```typescript {6-8}
import { ContextBuilder } from '@signalwire/sdk';

const builder = new ContextBuilder();
const ctx = builder.addContext('default');

ctx.setExitFillers({
  'en-US': ['Let me transfer you now...', 'One moment...'],
});
ctx.addStep('greet').setText('Greet the caller.');
```
