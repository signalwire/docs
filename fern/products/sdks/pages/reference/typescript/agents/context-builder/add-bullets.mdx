---
title: "addBullets"
slug: /reference/typescript/agents/context-builder/add-bullets
description: "Add a POM section with bullet points to a step or context."
max-toc-depth: 3
---

[step]: /docs/sdks/reference/typescript/agents/context-builder/step
[context]: /docs/sdks/reference/typescript/agents/context-builder/context

Add a POM section with bullet points, mutually exclusive with raw text. This
method exists on both [`Step`][step] and [`Context`][context].

## **Parameters**

<ParamField path="title" type="string" required={true} toc={true}>
  The section heading.
</ParamField>

<ParamField path="bullets" type="string[]" required={true} toc={true}>
  The list of bullet point strings.
</ParamField>

## **Returns**

`this` -- The Step or Context instance for chaining.

## **Example**

```typescript {7}
import { ContextBuilder } from '@signalwire/sdk';

const builder = new ContextBuilder();
const ctx = builder.addContext('default');

ctx.addStep('qualify')
  .addBullets('Process', ['Ask about budget', 'Ask about timeline', 'Ask about requirements']);
```
