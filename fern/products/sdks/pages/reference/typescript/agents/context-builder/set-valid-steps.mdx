---
title: "setValidSteps"
slug: /reference/typescript/agents/context-builder/set-valid-steps
description: "Set which steps the AI may navigate to from a step or context."
max-toc-depth: 3
---

[step]: /docs/sdks/reference/typescript/agents/context-builder/step
[context]: /docs/sdks/reference/typescript/agents/context-builder/context

Set which steps the AI may navigate to. This method exists on both
[`Step`][step] and [`Context`][context].

## **Parameters**

<ParamField path="steps" type="string[]" required={true} toc={true}>
  List of allowed step names.
</ParamField>

## **Returns**

`this` -- The Step or Context instance for chaining.

## **Example**

```typescript {7}
import { ContextBuilder } from '@signalwire/sdk';

const builder = new ContextBuilder();
const ctx = builder.addContext('default');

ctx.addStep('greet')
  .setText('Greet the caller.')
  .setValidSteps(['get_name', 'get_email']);
```
