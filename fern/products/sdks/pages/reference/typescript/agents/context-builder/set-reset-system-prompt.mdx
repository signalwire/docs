---
title: "setResetSystemPrompt"
slug: /reference/typescript/agents/context-builder/set-reset-system-prompt
description: "Set the system prompt to use when resetting context at a step."
max-toc-depth: 3
---

[step]: /docs/sdks/reference/typescript/agents/context-builder/step

Set the system prompt to use when resetting context at this step. This is a
method on [`Step`][step].

## **Parameters**

<ParamField path="systemPrompt" type="string" required={true} toc={true}>
  The replacement system prompt text.
</ParamField>

## **Returns**

`this` -- The Step instance for chaining.

## **Example**

```typescript {7}
import { ContextBuilder } from '@signalwire/sdk';

const builder = new ContextBuilder();
const ctx = builder.addContext('default');

ctx.addStep('reset_point')
  .setText('Reset the conversation context.')
  .setResetSystemPrompt('You are now a billing specialist.');
```
