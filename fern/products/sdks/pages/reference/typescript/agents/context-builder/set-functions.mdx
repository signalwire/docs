---
title: "setFunctions"
slug: /reference/typescript/agents/context-builder/set-functions
description: "Restrict which SWAIG functions are available during a step."
max-toc-depth: 3
---

[step]: /docs/sdks/reference/typescript/agents/context-builder/step

Restrict which SWAIG functions are available during this step. This is a
method on [`Step`][step].

## **Parameters**

<ParamField path="functions" type="string | string[]" required={true} toc={true}>
  A function name, list of names, or `"*"` for all functions.
</ParamField>

## **Returns**

`this` -- The Step instance for chaining.

## **Example**

```typescript {7}
import { ContextBuilder } from '@signalwire/sdk';

const builder = new ContextBuilder();
const ctx = builder.addContext('default');

ctx.addStep('lookup')
  .setText('Look up the customer\'s order.')
  .setFunctions(['lookup_order', 'get_status']);
```
