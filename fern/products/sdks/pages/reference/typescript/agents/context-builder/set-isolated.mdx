---
title: "setIsolated"
slug: /reference/typescript/agents/context-builder/set-isolated
description: "Set whether a context is isolated from other contexts' conversation history."
max-toc-depth: 3
---

[context]: /docs/sdks/reference/typescript/agents/context-builder/context

Set whether this context is isolated from other contexts' conversation history.
This is a method on [`Context`][context].

## **Parameters**

<ParamField path="isolated" type="boolean" required={true} toc={true}>
  Whether to isolate.
</ParamField>

## **Returns**

`this` -- The Context instance for chaining.

## **Example**

```typescript {6}
import { ContextBuilder } from '@signalwire/sdk';

const builder = new ContextBuilder();
const ctx = builder.addContext('default');

ctx.setIsolated(true);
ctx.addStep('greet').setText('Greet the caller.');
```
