---
title: "fallbackOutput"
slug: /reference/typescript/agents/data-map/fallback-output
description: "DataMap - Fluent builder for SWAIG data_map configurations."
max-toc-depth: 3
---

DataMap - Fluent builder for SWAIG data_map configurations.

## **Parameters**

<ParamField path="prefixes" type="unknown">
  Array of prefix strings to allow.
</ParamField>

<ParamField path="functionName" type="unknown">
  The unique name for this data map tool.
</ParamField>

<ParamField path="enabled" type="unknown">
  Whether to enable expansion (defaults to true).
</ParamField>

<ParamField path="prefixes" type="unknown">
  Array of prefix strings to allow.
</ParamField>

<ParamField path="description" type="unknown">
  A human-readable description of the tool.
</ParamField>

<ParamField path="description" type="unknown">
  A human-readable description of the tool.
</ParamField>

<ParamField path="name" type="unknown">
  The parameter name.
</ParamField>

<ParamField path="paramType" type="unknown">
  The JSON Schema type (e.g., "string", "number").
</ParamField>

<ParamField path="description" type="unknown">
  A description of the parameter shown to the AI.
</ParamField>

<ParamField path="opts" type="unknown">
  Optional flags for required and enum constraints.
</ParamField>

<ParamField path="testValue" type="unknown">
  The string or template variable to test.
</ParamField>

<ParamField path="pattern" type="unknown">
  A regex pattern (string or RegExp) to match against.
</ParamField>

<ParamField path="output" type="unknown">
  The result to return when the pattern matches.
</ParamField>

<ParamField path="nomatchOutput" type="unknown">
  Optional result to return when the pattern does not match.
</ParamField>

<ParamField path="method" type="unknown">
  HTTP method (e.g., "GET", "POST").
</ParamField>

<ParamField path="url" type="unknown">
  The webhook URL to call.
</ParamField>

<ParamField path="opts" type="unknown">
  Optional headers, form parameter name, and argument settings.
</ParamField>

<ParamField path="expressions" type="unknown">
  Array of expression objects to evaluate against the webhook response.
</ParamField>

<ParamField path="data" type="unknown">
  The request body object.
</ParamField>

<ParamField path="data" type="unknown">
  The parameters object.
</ParamField>

<ParamField path="config" type="unknown">
  Foreach configuration with input/output keys, append template, and optional max.
</ParamField>

<ParamField path="result" type="FunctionResult">
  The FunctionResult to use as the output template.
</ParamField>

<ParamField path="result" type="FunctionResult">
  The FunctionResult to use as the fallback.
</ParamField>

## **Returns**

`this` -- The result of the operation.

## **Example**

```typescript
import { DataMap } from "@signalwire/sdk";

const dataMap = new DataMap();
const result = dataMap.fallbackOutput();
console.log(result);
```
