---
title: "setAllowedEnvPrefixes"
slug: /reference/typescript/agents/data-map/set-allowed-env-prefixes
description: "DataMap - Fluent builder for SWAIG data_map configurations."
max-toc-depth: 3
---

DataMap - Fluent builder for SWAIG data_map configurations.

## **Parameters**

<ParamField path="prefixes" type="string[]">
  Array of prefix strings to allow.
</ParamField>

<ParamField path="functionName" type="unknown">
  The unique name for this data map tool.
</ParamField>

<ParamField path="enabled" type="unknown">
  Whether to enable expansion (defaults to true).
</ParamField>

<ParamField path="prefixes" type="string[]">
  Array of prefix strings to allow.
</ParamField>

## **Returns**

`void` -- The result of the operation.

## **Example**

```typescript
import { DataMap } from "@signalwire/sdk";

const dataMap = new DataMap();
const result = dataMap.setAllowedEnvPrefixes();
console.log(result);
```
