---
title: "getDocument"
slug: /reference/typescript/agents/swml-service/get-document
description: "Get the current SWML document as a TypeScript dictionary."
max-toc-depth: 3
---

Get the current SWML document as a Python dictionary. The returned structure follows
the standard SWML format with `version` and `sections` keys.

## **Returns**

`Record<string, unknown>` — The current SWML document. Structure:

```json
{
    "version": "1.0.0",
    "sections": {
        "main": [
            {"answer": {}},
            {"ai": {...}}
        ]
    }
}
```

## **Example**

```typescript {7}
import { AgentBase } from "@signalwire/sdk";

service = SWMLService(name="my-service")
service.add_verb("answer", {})
service.add_verb("play", {"url": "https://example.com/audio.mp3"})

doc = service.get_document()
console.log(doc["version"])       # "1.0.0"
console.log(len(doc["sections"]["main"]))  # 2
```
