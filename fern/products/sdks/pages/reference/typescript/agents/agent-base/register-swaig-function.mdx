---
title: "registerSwaigFunction"
slug: /reference/typescript/agents/agent-base/register-swaig-function
description: Register a raw SWAIG function dictionary, typically from a DataMap.
max-toc-depth: 3
---

[datamap]: /docs/sdks/reference/typescript/agents/data-map
[define-tool]: /docs/sdks/reference/typescript/agents/agent-base/define-tool
[ref-agentbase]: /docs/sdks/reference/typescript/agents/agent-base

Register a raw SWAIG function definition dictionary. This is the primary way to
register server-side functions created by
[`DataMap`][datamap], which generates complete
SWAIG function definitions including URL mappings, expressions, and output templates.

Unlike [`defineTool()`][define-tool],
this method does not take a handler callback -- the function executes on the SignalWire
server (for DataMap functions) or at an external webhook URL.

## **Parameters**

<ParamField path="functionDict" type={"Record<string, unknown>"} required={true} toc={true}>
  Complete SWAIG function definition dictionary. Must follow the SWAIG function
  schema, typically generated by `DataMap.to_swaig_function()`.
</ParamField>

## **Returns**

[`AgentBase`][ref-agentbase] -- Returns `this` for method chaining.

## **Example**

```typescript {16}
import { AgentBase, DataMap } from "@signalwire/sdk";

const agent = new AgentBase({ name: "plant-agent", route: "/plants" });
agent.setPromptText("You are a helpful plant care assistant.");

// Create a DataMap that calls a plant API server-side
const plantTool = new DataMap("get_plant_info")
  .description("Get care information for a plant")
  .parameter("plantId", { type: "string", description: "Plant ID", required: true })
  .webhook("GET", "https://api.plants.example.com/plants/${args.plantId}")
  .output({
    response: "The plant ${args.plantId} needs ${response.water_schedule} watering.",
    action: [{ say: "Here is the plant care information." }],
  });

// Register the DataMap as a SWAIG function
agent.registerSwaigFunction(plantTool.toSwaigFunction());
agent.run();
```
