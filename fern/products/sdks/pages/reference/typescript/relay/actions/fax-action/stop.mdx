---
title: "stop"
slug: /reference/typescript/relay/actions/fax-action/stop
description: Stop the fax operation.
max-toc-depth: 3
---

Stop the fax operation.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```typescript {19}
import { RelayClient } from "@signalwire/sdk";

client = RelayClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
async def handle_call(call):
    await call.answer()
    action = await call.sendFax(
        document="https://example.com/invoice.pdf",
        identity="+15559876543",
    )

    # Stop the fax operation
    await action.stop()

client.run()
```
