---
title: "onMessage"
slug: /reference/typescript/relay/client/on-message
description: "RelayClient — WebSocket + JSON-RPC 2.0 protocol + event dispatch."
max-toc-depth: 3
---

RelayClient — WebSocket + JSON-RPC 2.0 protocol + event dispatch.

## **Parameters**

<ParamField path="handler" type="MessageHandler">
  The handler parameter.
</ParamField>

## **Returns**

`void` -- The result of the operation.

## **Example**

```typescript
import { RelayClient } from "@signalwire/sdk";

const relayClient = new RelayClient();
const result = relayClient.onMessage();
console.log(result);
```
