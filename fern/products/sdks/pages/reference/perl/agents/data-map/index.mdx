---
title: "DataMap"
slug: /reference/perl/agents/data-map
description: "Fluent builder for server-side API tools that execute without webhooks."
max-toc-depth: 3
---

[swaigfunction]: /docs/sdks/reference/perl/agents/swaig-function
[functionresult]: /docs/sdks/reference/perl/agents/function-result
[data-map]: /docs/swml/reference/ai/swaig/functions/data-map
[swml-data-map-reference]: /docs/swml/reference/ai/swaig/functions/data-map
[body]: /docs/sdks/reference/perl/agents/data-map/body
[errorkeys]: /docs/sdks/reference/perl/agents/data-map/error-keys
[expression]: /docs/sdks/reference/perl/agents/data-map/expression
[foreach]: /docs/sdks/reference/perl/agents/data-map/foreach
[helper-functions]: /docs/sdks/reference/perl/agents/data-map/helper-functions
[output]: /docs/sdks/reference/perl/agents/data-map/output
[parameter]: /docs/sdks/reference/perl/agents/data-map/parameter
[purpose]: /docs/sdks/reference/perl/agents/data-map/purpose
[toswaigfunction]: /docs/sdks/reference/perl/agents/data-map/to-swaig-function
[webhook]: /docs/sdks/reference/perl/agents/data-map/webhook
[webhookexpressions]: /docs/sdks/reference/perl/agents/data-map/webhook-expressions

DataMap builds SWAIG function definitions that execute REST API calls directly on
SignalWire's infrastructure -- no webhook endpoint required on your server. This
reduces latency, simplifies deployment, and is ideal for straightforward
API-to-response integrations.

Use DataMap when you need to call an external REST API and format the response
with simple variable substitution. For complex business logic, database access,
or multi-step processing, use a standard SWAIG function with a handler instead.

<Info>
See [`SWAIGFunction`][swaigfunction] for
handler-based tool definitions, and
[`SignalWire::SWAIG::FunctionResult`][functionresult] for the
response builder used in DataMap outputs.
</Info>

<Info>
DataMap generates a SWML [`data_map`][data-map] object
within a SWAIG function definition. See the
[SWML data_map reference][swml-data-map-reference] for the
full specification.
</Info>

## **Properties**

<ParamField path="function_name" type="$scalar" required={true} toc={true}>
  Name of the SWAIG function this DataMap will create.
</ParamField>

## **Variable Substitution Patterns**

| Pattern | Description |
|---------|-------------|
| `${args.param}` | Function argument value |
| `${enc:args.param}` | URL-encoded argument (use in webhook URLs) |
| `${lc:args.param}` | Lowercase argument value |
| `${fmt_ph:args.phone}` | Format as phone number |
| `${response.field}` | API response field |
| `${response.arr[0]}` | Array element in response |
| `${global_data.key}` | Global session data |
| `${meta_data.key}` | Call metadata |
| `${this.field}` | Current item in foreach |

Modifiers are applied right-to-left: `${enc:lc:args.param}` lowercases first,
then URL-encodes.

## **Methods**

<CardGroup cols={4}>
  <Card title="body" href="/docs/sdks/reference/perl/agents/data-map/body">
    Set the request body or parameters for a webhook.
  </Card>
  <Card title="error_keys" href="/docs/sdks/reference/perl/agents/data-map/error-keys">
    Set error detection keys for webhook responses.
  </Card>
  <Card title="expression" href="/docs/sdks/reference/perl/agents/data-map/expression">
    Add a pattern-based response without an API call.
  </Card>
  <Card title="foreach" href="/docs/sdks/reference/perl/agents/data-map/foreach">
    Process an array from the webhook response.
  </Card>
  <Card title="Helper Functions" href="/docs/sdks/reference/perl/agents/data-map/helper-functions">
    Convenience functions for creating common DataMap patterns.
  </Card>
  <Card title="output" href="/docs/sdks/reference/perl/agents/data-map/output">
    Set the output template for a webhook or the fallback output.
  </Card>
  <Card title="parameter" href="/docs/sdks/reference/perl/agents/data-map/parameter">
    Add a function parameter to the tool definition.
  </Card>
  <Card title="purpose" href="/docs/sdks/reference/perl/agents/data-map/purpose">
    Set the function description shown to the AI.
  </Card>
  <Card title="to_swaig_function" href="/docs/sdks/reference/perl/agents/data-map/to-swaig-function">
    Convert the DataMap to a SWAIG function definition dictionary.
  </Card>
  <Card title="webhook" href="/docs/sdks/reference/perl/agents/data-map/webhook">
    Add an API call to the DataMap.
  </Card>
  <Card title="webhook_expressions" href="/docs/sdks/reference/perl/agents/data-map/webhook-expressions">
    Add post-processing expressions for a webhook response.
  </Card>
</CardGroup>

---

## **Examples**

### Weather lookup

```perl {11}
use SignalWire::Agent::AgentBase;
use SignalWire::SWAIG::FunctionResult;

package WeatherAgent;
use Moo;
extends 'SignalWire::Agent::AgentBase';

    sub __init__ {
        super().__init__(name => "weather-agent");
        self.add_language("English", "en-US", "rime.spore");
        self.prompt_add_section("Role", "You help users check the weather.");

        my $weather = (
            SignalWire::DataMap->new("get_weather");
            .description("Get current weather for a city");
            .parameter("city", "string", "City name", required => 1);
            .webhook(
                "GET",
                "https://api.weatherapi.com/v1/current.json"
                "?key => YOUR_API_KEY&q=${enc:args.city}";
            )
            .output(SignalWire::SWAIG::FunctionResult->new(
                "Current weather in ${args.city}: "
                "${$response->current->condition->text}, ";
                "${$response->current->temp_f} degrees Fahrenheit";
            ))
            .fallback_output(SignalWire::SWAIG::FunctionResult->new(
                "Sorry, I couldn't get weather data for ${args.city}"
            ))
        )

        self.register_swaig_function(weather.to_swaig_function());

    WeatherAgent().run();
```

### Expression-based control

```perl {5}
use SignalWire::Agent::AgentBase;
use SignalWire::SWAIG::FunctionResult;

my $volume_control = (
    SignalWire::DataMap->new("set_volume");
    .purpose("Control audio volume");
    .parameter("level", "string", "Volume level", required => 1);
    .expression(
        "${args.level}", r"high|loud|up",
        SignalWire::SWAIG::FunctionResult->new("Volume increased");
    )
    .expression(
        "${args.level}", r"low|quiet|down",
        SignalWire::SWAIG::FunctionResult->new("Volume decreased");
    )
    .expression(
        "${args.level}", r"mute|off",
        SignalWire::SWAIG::FunctionResult->new("Audio muted");
    )
)

my $agent = SignalWire::Agent::AgentBase->new(name => "media-agent");
$agent->set_prompt_text("You are a helpful assistant.");
$agent->register_swaig_function(volume_control.to_swaig_function());

    $agent->run();
```

### POST with body and foreach

```perl {5}
use SignalWire::DataMap;
use SignalWire::SWAIG::FunctionResult;

my $search_docs = (
    SignalWire::DataMap->new("search_docs");
    .purpose("Search documentation");
    .parameter("query", "string", "Search query", required => 1);
    .webhook(
        "POST",
        "https://api.docs.example.com/search",
        headers => {"Authorization": "Bearer TOKEN"}
    )
    .body({"query": "${args.query}", "limit": 3});
    .foreach({
        "input_key": "results",
        "output_key": "formatted_results",
        "max": 3,
        "append": "- ${this.title}: ${this.summary}\n"
    })
    .output(SignalWire::SWAIG::FunctionResult->new("Found:\n${formatted_results}"));
    .fallback_output(SignalWire::SWAIG::FunctionResult->new("Search is currently unavailable."));
)

print search_docs.to_swaig_function();
```
