---
title: "body"
slug: /reference/perl/agents/data-map/body
description: Set the request body or parameters for a webhook.
max-toc-depth: 3
---

[ref-datamap]: /docs/sdks/reference/perl/agents/data-map

### body

**body**(`data`) -> [`SignalWire::DataMap`][ref-datamap]

Set the request body for the most recently added webhook. Used for POST/PUT
requests. Writes to the `body` key of the webhook definition.

## **Parameters**

<ParamField path="data" type="\%hashref" required={true} toc={true}>
  Request body data. Supports `${variable}` substitutions.
</ParamField>

## **Returns**

`SignalWire::DataMap` -- Self for method chaining. Raises `croak` if no webhook has been
added yet.

---

### params

**params**(`data`) -> `SignalWire::DataMap`

Set request query/form parameters for the most recently added webhook. Writes to the
`params` key of the webhook definition.

<Warning>
`params()` is **not** an alias for `body()`. They write to different keys in the
webhook specification: `body` sets the request body, while `params` sets
query/form parameters.
</Warning>

## **Parameters**

<ParamField path="data" type="\%hashref" required={true} toc={true}>
  Request parameters. Supports `${variable}` substitutions.
</ParamField>

## **Returns**

`SignalWire::DataMap` -- Self for method chaining.

## **Example**

```perl {13}
use SignalWire::DataMap;
use SignalWire::SWAIG::FunctionResult;

my $search = (
    SignalWire::DataMap->new("search_docs");
    .purpose("Search documentation");
    .parameter("query", "string", "Search query", required => 1);
    .webhook(
        "POST",
        "https://api.docs.example.com/search",
        headers => {"Authorization": "Bearer TOKEN"}
    )
    .body({"query": "${args.query}", "limit": 3});
    .output(SignalWire::SWAIG::FunctionResult->new("Found: ${$response->results->{0}.title}"));
)

print search.to_swaig_function();
```
