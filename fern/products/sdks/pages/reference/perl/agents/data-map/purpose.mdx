---
title: "purpose"
slug: /reference/perl/agents/data-map/purpose
description: Set the function description shown to the AI.
max-toc-depth: 3
---

[ref-datamap]: /docs/sdks/reference/perl/agents/data-map

Set the function description shown to the AI when deciding which tool to call.

## **Parameters**

<ParamField path="description" type="$scalar" required={true} toc={true}>
  Human-readable description of what this function does.
</ParamField>

## **Returns**

[`SignalWire::DataMap`][ref-datamap] -- Self for method chaining.

---

Alias for `purpose()`.

## **Parameters**

<ParamField path="description" type="$scalar" required={true} toc={true}>
  Human-readable description of what this function does.
</ParamField>

## **Returns**

`SignalWire::DataMap` -- Self for method chaining.

## **Example**

```perl {4}
use SignalWire::DataMap;

my $weather = SignalWire::DataMap->new("get_weather");
weather.purpose("Get the current weather for a given city");

print weather.to_swaig_function();
# {'function': 'get_weather', 'description': 'Get the current weather for a given city', ...}
```

The `description()` method is an alias for `purpose()`:

```perl
use SignalWire::DataMap;

my $weather = SignalWire::DataMap->new("get_weather");
weather.description("Get the current weather for a given city");

print weather.to_swaig_function();
```
