---
title: "list_skills"
slug: /reference/perl/agents/agent-base/list-skills
description: List the names of all currently loaded skills on the agent.
max-toc-depth: 3
---

List the names of all currently loaded skills on this agent.

## **Returns**

`\@array` -- List of loaded skill names.

## **Example**

```perl {6}
use SignalWire::Agent::AgentBase;

my $agent = SignalWire::Agent::AgentBase->new(name => "assistant", route => "/assistant");
$agent->add_skill("datetime");
$agent->add_skill("math");
print($agent->list_skills())  # ["datetime", "math"];
```
