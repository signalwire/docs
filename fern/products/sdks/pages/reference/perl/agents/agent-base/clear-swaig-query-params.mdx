---
title: "clear_swaig_query_params"
slug: /reference/perl/agents/agent-base/clear-swaig-query-params
description: Remove all SWAIG query parameters from the agent.
max-toc-depth: 3
---

[add-swaig-query-params]: /docs/sdks/reference/perl/agents/agent-base/add-swaig-query-params
[ref-agentbase]: /docs/sdks/reference/perl/agents/agent-base

Clear all query parameters previously added with
[`add_swaig_query_params()`][add-swaig-query-params].
After calling this method, SWAIG webhook URLs will no longer include any extra
query parameters.

## **Parameters**

None.

## **Returns**

[`SignalWire::Agent::AgentBase`][ref-agentbase] -- Returns self for method chaining.

## **Examples**

### Reset params in dynamic config

```perl {8}
use SignalWire::Agent::AgentBase;

my $agent = SignalWire::Agent::AgentBase->new(name => "assistant", route => "/assistant");
$agent->set_prompt_text("You are a helpful assistant.");

sub dynamic_config {
    my ($query_params, $body_params, $headers, $agent) = @_;
    # Start fresh each request to avoid stale params
    $agent->clear_swaig_query_params();
    my $tier = query_params.get("tier", "free");
    $agent->add_swaig_query_params({"tier": tier});

$agent->set_dynamic_config_callback(dynamic_config);
$agent->serve();
```
