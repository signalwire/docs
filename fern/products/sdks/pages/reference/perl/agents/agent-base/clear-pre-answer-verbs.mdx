---
title: "clear_pre_answer_verbs"
slug: /reference/perl/agents/agent-base/clear-pre-answer-verbs
description: Remove all pre-answer verbs from the call flow.
max-toc-depth: 3
---

[ref-agentbase]: /docs/sdks/reference/perl/agents/agent-base

Remove all pre-answer verbs.

## **Returns**

[`SignalWire::Agent::AgentBase`][ref-agentbase] -- Returns self for method chaining.

## **Example**

```perl {8}
use SignalWire::Agent::AgentBase;

my $agent = SignalWire::Agent::AgentBase->new(name => "support", route => "/support");
$agent->set_prompt_text("You are a helpful assistant.");
$agent->add_pre_answer_verb("play", {
    "urls": ["ring:us"], "auto_answer": 0
})
$agent->clear_pre_answer_verbs();
$agent->serve();
```
