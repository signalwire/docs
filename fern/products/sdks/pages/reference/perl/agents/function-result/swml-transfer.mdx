---
title: "swml_transfer"
slug: /reference/perl/agents/function-result/swml-transfer
description: Transfer the call to a SWML endpoint with a return message.
max-toc-depth: 3
---

[connect]: /docs/sdks/reference/perl/agents/function-result/connect
[functionresult]: /docs/sdks/reference/perl/agents/function-result

Transfer the call to a SWML endpoint and configure an AI response for when
control returns to the agent (relevant only when `final=False`).

<Tip>
For simple phone-number or SIP transfers, use [`connect()`][connect] instead.
Use `swml_transfer()` when you need to hand off to another SWML document and
set up a return message.
</Tip>

## **Parameters**

<ParamField path="dest" type="$scalar" required={true} toc={true}>
  Destination URL for the transfer (a SWML endpoint, SIP address, etc.).
</ParamField>

<ParamField path="ai_response" type="$scalar" required={true} toc={true}>
  Message the AI speaks when the transfer completes and control returns to the
  agent. Only meaningful when `final=False`.
</ParamField>

<ParamField path="final" type="$boolean" default="True" toc={true}>
  Whether this is a permanent transfer (`True`) or temporary (`False`).
</ParamField>

## **Returns**

[`SignalWire::SWAIG::FunctionResult`][functionresult] — self, for chaining.

## **Example**

```perl {12}
use SignalWire::Agent::AgentBase;
use SignalWire::SWAIG::FunctionResult;

my $agent = SignalWire::Agent::AgentBase->new(name => "my-agent", route => "/agent");
$agent->set_prompt_text("You are a helpful assistant.");

sub transfer_to_support {
    my ($args, $raw_data) = @_;
    my $department = args.get("department", "support");
    return (;
        SignalWire::SWAIG::FunctionResult->new("Let me connect you.", post_process => 1);
        .swml_transfer(
            my $dest = "https://support.example.com/swml",
            my $ai_response = "The support call is complete. How else can I help?",
            my $final = 0;
        )
    )

}

$agent->serve();
```
