---
title: "add_section"
slug: /reference/perl/agents/swml-builder/add-section
description: Add a new named section to the SWML document.
max-toc-depth: 3
---

[swmlservice-add-section]: /docs/sdks/reference/perl/agents/swml-service/add-section

Add a new named section to the SWML document. Delegates to
[`SWMLService.add_section()`][swmlservice-add-section].

## **Parameters**

<ParamField path="section_name" type="$scalar" required={true} toc={true}>
  Name of the section to create.
</ParamField>

## **Returns**

`Self` -- The builder instance for method chaining.

## **Example**

```perl {10}
use SignalWire::SWML::Service;
use SignalWire::SWML::Document;

my $service = SignalWire::SWML::Service->new(name => "multi-section");
my $builder = SignalWire::SWML::Document->new(service);

my $doc = (
    builder
    .answer();
    .add_section("greeting");
    .play(url => "say:Hello!");
    .build();
)
print doc;
```
