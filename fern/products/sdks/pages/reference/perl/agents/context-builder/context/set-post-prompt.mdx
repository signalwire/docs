---
title: "set_post_prompt"
slug: /reference/perl/agents/context-builder/context/set-post-prompt
description: Override the post-prompt text while this context is active.
max-toc-depth: 3
---

[ref-context]: /docs/sdks/reference/perl/agents/context-builder/context

Override the post-prompt text while this context is active.

## **Parameters**

<ParamField path="post_prompt" type="$scalar" required={true} toc={true}>
  Post-prompt text for this context.
</ParamField>

## **Returns**

[`Context`][ref-context] -- Self for method chaining.

## **Example**

```perl {9}
use SignalWire::Agent::AgentBase;

my $agent = SignalWire::Agent::AgentBase->new(name => "my-agent", route => "/agent");

my $contexts = $agent->define_contexts();
contexts.add_context("default").add_step("menu").set_text("Ask what the caller needs.");

my $billing = contexts.add_context("billing");
billing.set_post_prompt("Summarize the billing issue and resolution.");
billing.add_step("review").set_text("Review the caller's account and recent charges.");

$agent->serve();
```
