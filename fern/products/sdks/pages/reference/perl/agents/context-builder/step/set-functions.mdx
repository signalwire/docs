---
title: "set_functions"
slug: /reference/perl/agents/context-builder/step/set-functions
description: Set which SWAIG functions are available during this step.
max-toc-depth: 3
---

[ref-step]: /docs/sdks/reference/perl/agents/context-builder/step

Set which SWAIG functions are available during this step. Restricting functions
per step prevents the AI from calling irrelevant tools.

## **Parameters**

<ParamField path="functions" type="$scalar | \@arrayref" required={true} toc={true}>
  Either `"none"` to disable all functions, or a list of function names to allow.
</ParamField>

## **Returns**

[`Step`][ref-step] -- Self for method chaining.

## **Example**

```perl {9,12}
use SignalWire::Agent::AgentBase;

my $agent = SignalWire::Agent::AgentBase->new(name => "my-agent", route => "/agent");

my $contexts = $agent->define_contexts();
my $ctx = contexts.add_context("default");
my $greet = $ctx->add_step("greet");
greet.set_text("Welcome the caller.");
greet.set_functions("none");
my $verify = $ctx->add_step("verify");
verify.set_text("Verify the caller's identity.");
verify.set_functions(["lookup_account", "verify_identity"]);

$agent->serve();
```
