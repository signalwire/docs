---
title: "get"
slug: /reference/perl/rest/recordings/get
description: Retrieve a specific recording.
max-toc-depth: 3
---

Retrieve a specific recording.

## **Parameters**

<ParamField path="recording_id" type="$scalar" required={true} toc={true}>
  The recording resource ID.
</ParamField>

## **Returns**

`dict` -- The recording object, including download URL and metadata.

## **Example**

```perl {9-10}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $rec = $client->recordings->{"recording-id"}
print rec.get("duration"), rec.get("url");
```
