---
title: "create"
slug: /reference/perl/rest/datasphere/create
description: Upload a new document.
max-toc-depth: 3
---

[ref-datasphere]: /docs/sdks/reference/perl/rest/datasphere

Upload a new document to the [Datasphere][ref-datasphere].

## **Parameters**

<ParamField path="name" type="$scalar" toc={true}>
  Document name or title.
</ParamField>

<ParamField path="content" type="$scalar" toc={true}>
  Document content text.
</ParamField>

<ParamField path="**kwargs" type="\%hashref" toc={true}>
  Additional document fields (e.g., `metadata`).
</ParamField>

## **Returns**

`dict` -- The newly created document object.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $doc = $client->datasphere->documents->create(
    name => "FAQ",
    content => "Frequently asked questions and answers...",
)
print "Document ID:", $doc->{"id"};
```
