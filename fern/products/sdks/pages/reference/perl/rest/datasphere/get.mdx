---
title: "get"
slug: /reference/perl/rest/datasphere/get
description: Retrieve a specific document.
max-toc-depth: 3
---

Retrieve a specific document by ID.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The document resource ID.
</ParamField>

## **Returns**

`dict` -- The document object.

## **Example**

```perl {9-10}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $doc = $client->datasphere->documents->{"document-id"}
print $doc->{"name"}, $doc->{"id"};
```
