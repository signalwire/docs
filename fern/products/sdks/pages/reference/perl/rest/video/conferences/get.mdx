---
title: "get"
slug: /reference/perl/rest/video/conferences/get
description: Retrieve a single video conference.
max-toc-depth: 3
---

Retrieve a single video conference by its unique identifier.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The unique identifier of the conference.
</ParamField>

## **Returns**

`dict` -- The conference resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $conf = $client->video->conferences->{"conference-id"}
```
