---
title: "delete"
slug: /reference/perl/rest/addresses/delete
description: Delete an address from the project.
max-toc-depth: 3
---

Delete an address from the project.

## **Parameters**

<ParamField path="address_id" type="$scalar" required={true} toc={true}>
  The address resource ID.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

$client->addresses->delete("address-id");
```
