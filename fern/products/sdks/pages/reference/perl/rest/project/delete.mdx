---
title: "delete"
slug: /reference/perl/rest/project/delete
description: Revoke and delete an API token.
max-toc-depth: 3
---

### tokens.delete

Revoke and delete an API token.

## **Parameters**

<ParamField path="token_id" type="$scalar" required={true} toc={true}>
  The token resource ID.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

$client->project->tokens->delete("token-id");
```
