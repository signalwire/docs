---
title: "list_memberships"
slug: /reference/perl/rest/number-groups/list-memberships
description: List phone numbers that belong to a group.
max-toc-depth: 3
---

List phone numbers that belong to a group.

## **Parameters**

<ParamField path="group_id" type="$scalar" required={true} toc={true}>
  The group resource ID.
</ParamField>

<ParamField path="**params" type="\%hashref" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`dict` -- JSON response containing a `data` list of membership objects.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $memberships = $client->number_groups->list_memberships("group-id");
for my $m (@{memberships.get("data", [])}) {
    print m.get("phone_number_id"), m.get("id");
```
