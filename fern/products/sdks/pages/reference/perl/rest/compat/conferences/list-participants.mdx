---
title: "list_participants"
slug: /reference/perl/rest/compat/conferences/list-participants
description: List participants in a conference.
max-toc-depth: 3
---

List participants in a conference.

## **Parameters**

<ParamField path="conference_sid" type="$scalar" required={true} toc={true}>
  The SID of the conference.
</ParamField>

<ParamField path="Muted" type="$boolean" toc={true}>
  Filter by muted status.
</ParamField>

<ParamField path="Hold" type="$boolean" toc={true}>
  Filter by hold status.
</ParamField>

## **Returns**

`dict` -- List of participant resources.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $participants = $client->compat->conferences->list_participants("CF...");
```
