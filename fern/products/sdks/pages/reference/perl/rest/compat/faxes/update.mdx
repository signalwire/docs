---
title: "update"
slug: /reference/perl/rest/compat/faxes/update
description: Update a fax resource.
max-toc-depth: 3
---

Update a fax resource.

## **Parameters**

<ParamField path="sid" type="$scalar" required={true} toc={true}>
  The fax SID.
</ParamField>

## **Returns**

`dict` -- The updated fax resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

$client->compat->faxes->update("FX...");
```
