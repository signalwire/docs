---
title: "get"
slug: /reference/perl/rest/compat/phone-numbers/get
description: Retrieve a single phone number by SID.
max-toc-depth: 3
---

Retrieve a single phone number by SID.

## **Parameters**

<ParamField path="sid" type="$scalar" required={true} toc={true}>
  The phone number SID.
</ParamField>

## **Returns**

`dict` -- The phone number resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $number = $client->compat->phone_numbers->{"PN..."}
```
