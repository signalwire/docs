---
title: "get"
slug: /reference/perl/rest/compat/messages/get
description: Retrieve a single message by SID.
max-toc-depth: 3
---

Retrieve a single message by SID.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The message SID.
</ParamField>

## **Returns**

`dict` -- The message resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $message = $client->compat->messages->{"SM..."}
```
