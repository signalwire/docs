---
title: "delete"
slug: /reference/perl/rest/compat/transcriptions/delete
description: Delete a transcription.
max-toc-depth: 3
---

Delete a transcription.

## **Parameters**

<ParamField path="sid" type="$scalar" required={true} toc={true}>
  The transcription SID to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

$client->compat->transcriptions->delete("TR...");
```
