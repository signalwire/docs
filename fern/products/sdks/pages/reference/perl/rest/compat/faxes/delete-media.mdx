---
title: "delete_media"
slug: /reference/perl/rest/compat/faxes/delete-media
description: Delete a media item from a fax.
max-toc-depth: 3
---

Delete a media item from a fax.

## **Parameters**

<ParamField path="fax_sid" type="$scalar" required={true} toc={true}>
  The SID of the fax.
</ParamField>

<ParamField path="media_sid" type="$scalar" required={true} toc={true}>
  The SID of the media item to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

$client->compat->faxes->delete_media("FX...", "ME...");
```
