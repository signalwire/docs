---
title: "get"
slug: /reference/perl/rest/compat/transcriptions/get
description: Retrieve a single transcription by SID.
max-toc-depth: 3
---

Retrieve a single transcription by SID.

## **Parameters**

<ParamField path="sid" type="$scalar" required={true} toc={true}>
  The transcription SID.
</ParamField>

## **Returns**

`dict` -- The transcription resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $transcription = $client->compat->transcriptions->{"TR..."}
```
