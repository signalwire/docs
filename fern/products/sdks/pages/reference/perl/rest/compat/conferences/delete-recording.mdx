---
title: "delete_recording"
slug: /reference/perl/rest/compat/conferences/delete-recording
description: Delete a conference recording.
max-toc-depth: 3
---

Delete a conference recording.

## **Parameters**

<ParamField path="conference_sid" type="$scalar" required={true} toc={true}>
  The SID of the conference.
</ParamField>

<ParamField path="recording_sid" type="$scalar" required={true} toc={true}>
  The SID of the recording to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

$client->compat->conferences->delete_recording("CF...", "RE...");
```
