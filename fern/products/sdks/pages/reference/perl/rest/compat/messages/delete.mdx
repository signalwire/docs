---
title: "delete"
slug: /reference/perl/rest/compat/messages/delete
description: Delete a message record.
max-toc-depth: 3
---

Delete a message record.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The message SID to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

$client->compat->messages->delete("SM...");
```
