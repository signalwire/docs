---
title: "get"
slug: /reference/perl/rest/compat/laml-bins/get
description: Retrieve a single LAML bin by SID.
max-toc-depth: 3
---

Retrieve a single LAML bin by SID.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The LAML bin SID.
</ParamField>

## **Returns**

`dict` -- The LAML bin resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $laml_bin = $client->compat->laml_bins->{"LB..."}
```
