---
title: "get"
slug: /reference/perl/rest/compat/recordings/get
description: Retrieve a single recording by SID.
max-toc-depth: 3
---

Retrieve a single recording by SID.

## **Parameters**

<ParamField path="sid" type="$scalar" required={true} toc={true}>
  The recording SID.
</ParamField>

## **Returns**

`dict` -- The recording resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $recording = $client->compat->recordings->{"RE..."}
```
