---
title: "list_recordings"
slug: /reference/perl/rest/compat/conferences/list-recordings
description: List recordings for a conference.
max-toc-depth: 3
---

List recordings for a conference.

## **Parameters**

<ParamField path="conference_sid" type="$scalar" required={true} toc={true}>
  The SID of the conference.
</ParamField>

## **Returns**

`dict` -- List of recording resources.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $recordings = $client->compat->conferences->list_recordings("CF...");
```
