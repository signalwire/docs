---
title: "get"
slug: /reference/perl/rest/compat/applications/get
description: Retrieve a single application by SID.
max-toc-depth: 3
---

Retrieve a single application by SID.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The application SID.
</ParamField>

## **Returns**

`dict` -- The application resource.

## **Example**

```perl {9-9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $app = $client->compat->applications->{"AP..."}
```
