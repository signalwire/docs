---
title: "update"
slug: /reference/perl/rest/queues/update
description: Update a queue.
max-toc-depth: 3
---

Update a queue. Uses PUT.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The queue resource ID.
</ParamField>

<ParamField path="**kwargs" type="\%hashref" toc={true}>
  Fields to update (e.g., `name`, `max_size`).
</ParamField>

## **Returns**

`dict` -- The updated queue object.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $updated = $client->queues->update("queue-id", name => "priority-support");
print "Updated: ${updated['id']}";
```
