---
title: "create"
slug: /reference/perl/rest/fabric/relay-applications/create
description: Create a new RELAY application resource.
max-toc-depth: 3
---

Create a new RELAY application resource.

## **Parameters**

<ParamField path="name" type="$scalar" required={true} toc={true}>
  Display name of the RELAY application.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`dict` -- The created RELAY application resource.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $result = $client->fabric->relay_applications->create(
    name => "my-item",
)
print "Created: ${result['id']}";
```
