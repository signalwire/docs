---
title: "update"
slug: /reference/perl/rest/fabric/sip-endpoints/update
description: Replace a SIP endpoint resource.
max-toc-depth: 3
---

Replace a SIP endpoint resource. Uses PUT for full replacement.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The unique identifier of the SIP endpoint.
</ParamField>

Additional keyword arguments are the full replacement body.

## **Returns**

`dict` -- The updated SIP endpoint resource.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $updated = $client->fabric->sip_endpoints->update("endpoint-id", name => "updated-name");
print "Updated: ${updated['name']}";
```
