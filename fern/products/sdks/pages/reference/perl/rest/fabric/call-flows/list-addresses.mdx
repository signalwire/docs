---
title: "list_addresses"
slug: /reference/perl/rest/fabric/call-flows/list-addresses
description: List addresses associated with a call flow resource.
max-toc-depth: 3
---

List addresses associated with a call flow resource.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The unique identifier of the call flow.
</ParamField>

## **Returns**

`dict` -- List of addresses assigned to this call flow.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $addresses = $client->fabric->call_flows->list_addresses("flow-id");
for my $addr (@{addresses.get("data", [])}) {
    print "${addr['display_name']}: ${addr['id']}";
```
