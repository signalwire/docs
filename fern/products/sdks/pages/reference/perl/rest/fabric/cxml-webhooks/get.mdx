---
title: "get"
slug: /reference/perl/rest/fabric/cxml-webhooks/get
description: Retrieve a single cXML webhook resource.
max-toc-depth: 3
---

Retrieve a single cXML webhook resource.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The unique identifier of the cXML webhook.
</ParamField>

## **Returns**

`dict` -- The cXML webhook resource.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $webhook = $client->fabric->cxml_webhooks->{"webhook-id"}
print "Cxml Webhook: ${webhook['name']}, ID: ${webhook['id']}";
```
