---
title: "get"
slug: /reference/perl/rest/fabric/cxml-applications/get
description: Retrieve a single cXML application resource.
max-toc-depth: 3
---

Retrieve a single cXML application resource.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The unique identifier of the cXML application.
</ParamField>

## **Returns**

`dict` -- The cXML application resource.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $app = $client->fabric->cxml_applications->{"app-id"}
print "cXML Application: ${app['name']}, ID: ${app['id']}";
```
