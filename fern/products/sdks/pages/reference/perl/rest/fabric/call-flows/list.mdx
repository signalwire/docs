---
title: "list"
slug: /reference/perl/rest/fabric/call-flows/list
description: List call flow resources.
max-toc-depth: 3
---

List call flow resources in the project.

## **Parameters**

<ParamField path="page_size" type="$integer" toc={true}>
  Number of results per page.
</ParamField>

<ParamField path="page_token" type="$scalar" toc={true}>
  Pagination token for the next page of results.
</ParamField>

## **Returns**

`dict` -- Paginated response containing call flow resources.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $response = $client->fabric->call_flows->list();
for my $flow (@{$response->{"data"} // []}) {
    print "${flow['name']}: ${flow['id']}";
```
