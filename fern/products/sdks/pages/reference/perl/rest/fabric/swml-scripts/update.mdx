---
title: "update"
slug: /reference/perl/rest/fabric/swml-scripts/update
description: Replace a SWML script resource.
max-toc-depth: 3
---

Replace a SWML script resource. Uses PUT for full replacement.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The unique identifier of the SWML script.
</ParamField>

Additional keyword arguments are the full replacement body.

## **Returns**

`dict` -- The updated SWML script resource.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $updated = $client->fabric->swml_scripts->update("resource-id", name => "updated-name");
print "Updated: ${updated['name']}";
```
