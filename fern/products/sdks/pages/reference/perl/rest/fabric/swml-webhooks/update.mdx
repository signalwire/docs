---
title: "update"
slug: /reference/perl/rest/fabric/swml-webhooks/update
description: Partially update a SWML webhook resource.
max-toc-depth: 3
---

Partially update a SWML webhook resource. Uses PATCH for partial.

## **Parameters**

<ParamField path="resource_id" type="$scalar" required={true} toc={true}>
  The unique identifier of the SWML webhook.
</ParamField>

Additional keyword arguments are the fields to update.

## **Returns**

`dict` -- The updated SWML webhook resource.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $updated = $client->fabric->swml_webhooks->update("webhook-id", name => "updated-name");
print "Updated: ${updated['name']}";
```
