---
title: "create_invite_token"
slug: /reference/perl/rest/fabric/tokens/create-invite-token
description: Create an invite token for a subscriber.
max-toc-depth: 3
---

Create an invite token for a subscriber.

## **Parameters**

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`dict` -- The created invite token.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $token = $client->fabric->tokens->create_invite_token(subscriber_id => "subscriber-id");
print "Invite token: ${token['token']}";
```
