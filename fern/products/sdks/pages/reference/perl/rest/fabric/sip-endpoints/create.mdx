---
title: "create"
slug: /reference/perl/rest/fabric/sip-endpoints/create
description: Create a new SIP endpoint resource.
max-toc-depth: 3
---

Create a new SIP endpoint resource.

## **Parameters**

<ParamField path="name" type="$scalar" required={true} toc={true}>
  Display name of the SIP endpoint.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`dict` -- The created SIP endpoint resource.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $result = $client->fabric->sip_endpoints->create(
    name => "my-endpoint",
)
print "Created: ${result['id']}";
```
