---
title: "list"
slug: /reference/perl/rest/logs/voice/list
description: List voice call log entries.
max-toc-depth: 3
---

List voice call log entries.

## **Parameters**

<ParamField path="**params" type="\%hashref" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`dict` -- JSON response containing a `data` list of voice log objects.

## **Example**

```perl {9}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $calls = $client->logs->voice->list(page_size => 20);
for my $call (@{calls.get("data", [])}) {
    print $call->{"from"}, "->", $call->{"to"}, $call->{"duration"};
```
