---
title: "Message Logs"
slug: /reference/perl/rest/logs/messages
description: "Query message log entries."
max-toc-depth: 3
---

[restclient]: /docs/sdks/reference/perl/rest/client

Query message log entries. Message logs provide a read-only view of SMS/MMS
activity in your project.

Access via `client.logs.messages` on a [`SignalWire::REST::RestClient`][restclient] instance.

```perl
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new();
```

### list

List message log entries.

## **Parameters**

<ParamField path="**params" type="\%hashref" toc={true}>
  Optional query parameters for filtering and pagination (e.g., `page_size`,
  `from`, `to`, `date_created_after`).
</ParamField>

## **Returns**

`dict` -- JSON response containing a `data` list of message log objects.

---

### get

Retrieve a specific message log entry.

## **Parameters**

<ParamField path="log_id" type="$scalar" required={true} toc={true}>
  The message log ID.
</ParamField>

## **Returns**

`dict` -- The message log object.

## **Examples**

### List message logs

```perl
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $messages = $client->logs->messages->list(page_size => 20);
for my $msg (@{messages.get("data", [])}) {
    print msg.get("from"), "->", msg.get("to"), msg.get("status");
```

### Get a specific message log

```perl
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $msg = $client->logs->messages->{"message-log-id"}
print msg.get("from"), "->", msg.get("to");
```
