---
title: "get"
slug: /reference/perl/rest/logs/voice/get
description: Retrieve a specific voice log entry.
max-toc-depth: 3
---

Retrieve a specific voice call log entry.

## **Parameters**

<ParamField path="log_id" type="$scalar" required={true} toc={true}>
  The voice log ID.
</ParamField>

## **Returns**

`dict` -- The voice log object.

## **Example**

```perl {9-10}
use SignalWire::REST::RestClient;

my $client = SignalWire::REST::RestClient->new(
    project => "your-project-id",
    token => "your-api-token",
    host => "your-space.signalwire.com",
)

my $call = $client->logs->voice->{"voice-log-id"}
print $call->{"from"}, "->", $call->{"to"}, $call->{"duration"};
```
