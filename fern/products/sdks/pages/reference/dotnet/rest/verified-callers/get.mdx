---
title: "get"
slug: /reference/dotnet/rest/verified-callers/get
description: Retrieve a specific verified caller ID.
max-toc-depth: 3
---

Retrieve a specific verified caller ID.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The caller ID resource ID.
</ParamField>

## **Returns**

`dict` -- The caller ID object.

## **Example**

```csharp {9-10}
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

caller = client.verified_callers.get("caller-id");
Console.WriteLine(caller["phone_number"], caller["verification_state"]);

```
