---
title: "Fax Logs"
slug: /reference/dotnet/rest/logs/fax
description: "Query fax log entries."
max-toc-depth: 3
---

[restclient]: /docs/sdks/reference/dotnet/rest/client

Query fax log entries. Fax logs provide a read-only view of fax activity
in your project.

Access via `client.logs.fax` on a [`RestClient`][restclient] instance.

```csharp
using SignalWire.REST;

client = RestClient();

```

### list

List fax log entries.

## **Parameters**

<ParamField path="**params" type="dict" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`dict` -- JSON response containing a `data` list of fax log objects.

---

### get

Retrieve a specific fax log entry.

## **Parameters**

<ParamField path="log_id" type="string" required={true} toc={true}>
  The fax log ID.
</ParamField>

## **Returns**

`dict` -- The fax log object.

## **Examples**

### List fax logs

```csharp
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

faxes = client.logs.fax.list();
for fax in faxes.get("data", []):
    Console.WriteLine(fax["from"], "->", fax["to"], fax["status"]);

```

### Get a specific fax log

```csharp
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

fax = client.logs.fax.get("fax-log-id");
Console.WriteLine(fax["from"], "->", fax["to"]);

```
