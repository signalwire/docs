---
title: "get"
slug: /reference/dotnet/rest/compat/applications/get
description: Retrieve a single application by SID.
max-toc-depth: 3
---

Retrieve a single application by SID.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The application SID.
</ParamField>

## **Returns**

`dict` -- The application resource.

## **Example**

```csharp {9-9}
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

app = client.compat.applications.get("AP...");

```
