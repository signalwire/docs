---
title: "get"
slug: /reference/dotnet/rest/compat/recordings/get
description: Retrieve a single recording by SID.
max-toc-depth: 3
---

Retrieve a single recording by SID.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The recording SID.
</ParamField>

## **Returns**

`dict` -- The recording resource.

## **Example**

```csharp {9-9}
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

recording = client.compat.recordings.get("RE...");

```
