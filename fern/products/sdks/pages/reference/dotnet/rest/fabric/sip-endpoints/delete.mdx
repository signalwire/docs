---
title: "delete"
slug: /reference/dotnet/rest/fabric/sip-endpoints/delete
description: Delete a SIP endpoint resource.
max-toc-depth: 3
---

Delete a SIP endpoint resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the SIP endpoint to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success (HTTP 204).

## **Example**

```csharp {9}
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

client.fabric.sip_endpoints.delete("endpoint-id");
Console.WriteLine("Sip Endpoint deleted");

```
