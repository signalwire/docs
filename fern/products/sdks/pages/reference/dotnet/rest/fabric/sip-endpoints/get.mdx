---
title: "get"
slug: /reference/dotnet/rest/fabric/sip-endpoints/get
description: Retrieve a single SIP endpoint resource.
max-toc-depth: 3
---

Retrieve a single SIP endpoint resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the SIP endpoint.
</ParamField>

## **Returns**

`dict` -- The SIP endpoint resource.

## **Example**

```csharp {9}
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

endpoint = client.fabric.sip_endpoints.get("endpoint-id");
Console.WriteLine($"Sip Endpoint: {endpoint['name']}, ID: {endpoint['id']}");

```
