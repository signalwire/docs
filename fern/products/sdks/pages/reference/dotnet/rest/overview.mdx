---
title: "REST Client"
sidebar-title: Overview
subtitle: "DotNet API reference for RestClient and resource namespaces"
slug: /reference/dotnet/rest
description: "HTTP client for SignalWire REST API resource management."
max-toc-depth: 3
position: 0
---

[client]: /docs/sdks/reference/dotnet/rest/client
[phone-numbers]: /docs/sdks/reference/dotnet/rest/phone-numbers
[fabric]: /docs/sdks/reference/dotnet/rest/fabric
[calling]: /docs/sdks/reference/dotnet/rest/calling
[video]: /docs/sdks/reference/dotnet/rest/video
[datasphere]: /docs/sdks/reference/dotnet/rest/datasphere
[logs]: /docs/sdks/reference/dotnet/rest/logs
[registry]: /docs/sdks/reference/dotnet/rest/registry
[compat]: /docs/sdks/reference/dotnet/rest/compat
[mfa]: /docs/sdks/reference/dotnet/rest/mfa

The REST namespace provides a synchronous HTTP client for the SignalWire platform
APIs. It organizes every HTTP endpoint into namespaced resource objects with
standard CRUD operations, letting you manage phone numbers, fabric resources,
call logs, video rooms, datasphere documents, and more from Python.

## Example

Search for available phone numbers, purchase one, and assign it to a fabric
AI agent resource:

```csharp
using SignalWire.REST;

client = RestClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
)

// Search for available numbers in area code 512
available = client.phone_numbers.search(area_code: "512", quantity=3);
for number in available.get("data", []):
    Console.WriteLine($"{number['number']} - {number.get('region')}");

// Purchase the first available number
purchased = client.phone_numbers.create(number=available["data"][0]["number"]);
Console.WriteLine($"Purchased: {purchased['number']}");

// List your AI agent resources
response = client.fabric.ai_agents.list();
for agent in response.get("data", []):
    Console.WriteLine($"Agent: {agent['name']} ({agent['id']})");

// Query recent voice call logs
logs = client.logs.voice.list(page_size=5);
for log in logs.get("data", []):
    Console.WriteLine($"Call from {log.get('from')} to {log.get('to')}");

```

<Note>
All three constructor arguments can also be provided via environment variables:
`SIGNALWIRE_PROJECT_ID`, `SIGNALWIRE_API_TOKEN`, and `SIGNALWIRE_SPACE`.
When those are set, you can instantiate with `RestClient()` and no arguments.
</Note>

## Error Handling

REST errors raise `SignalWireRestError`:

```csharp
using SignalWire.REST;

client = RestClient();

try:
    client.phone_numbers.get("nonexistent-id");
except SignalWireRestError as e:
    Console.WriteLine($"HTTP {e.status_code}: {e.body}");
    Console.WriteLine($"URL: {e.method} {e.url}");

```

## Resources

<CardGroup cols={2}>
  <Card
    title="RestClient"
    href="/docs/sdks/reference/dotnet/rest/client"
  >
    Constructor, authentication, and all namespace properties.
  </Card>
  <Card
    title="Phone Numbers"
    href="/docs/sdks/reference/dotnet/rest/phone-numbers"
  >
    Search, purchase, and manage phone numbers.
  </Card>
  <Card
    title="Fabric"
    href="/docs/sdks/reference/dotnet/rest/fabric"
  >
    AI agents, SWML scripts, subscribers, call flows, and tokens.
  </Card>
  <Card
    title="Calling"
    href="/docs/sdks/reference/dotnet/rest/calling"
  >
    REST-based call control with 37+ commands.
  </Card>
  <Card
    title="Video"
    href="/docs/sdks/reference/dotnet/rest/video"
  >
    Rooms, conferences, sessions, recordings, and streams.
  </Card>
  <Card
    title="Datasphere"
    href="/docs/sdks/reference/dotnet/rest/datasphere"
  >
    Document management and semantic search.
  </Card>
  <Card
    title="Logs"
    href="/docs/sdks/reference/dotnet/rest/logs"
  >
    Message, voice, fax, and conference log queries.
  </Card>
  <Card
    title="Registry"
    href="/docs/sdks/reference/dotnet/rest/registry"
  >
    10DLC brand and campaign registration.
  </Card>
  <Card
    title="Compatibility"
    href="/docs/sdks/reference/dotnet/rest/compat"
  >
    Twilio-compatible LAML API for migration.
  </Card>
  <Card
    title="MFA"
    href="/docs/sdks/reference/dotnet/rest/mfa"
  >
    Multi-factor authentication via SMS and voice.
  </Card>
</CardGroup>
