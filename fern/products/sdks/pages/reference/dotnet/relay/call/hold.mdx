---
title: "hold"
slug: /reference/dotnet/relay/call/hold
description: Put a call on hold.
max-toc-depth: 3
---

[unhold]: /docs/sdks/reference/dotnet/relay/call/unhold
[calling-call-hold]: /docs/sdks/reference/dotnet/relay/call#events
[call-events]: /docs/sdks/reference/dotnet/relay/call#events

Put the call on hold. The remote party hears hold music or silence while the
call is held.

<Info>
Use [`unhold()`][unhold] to release the call from hold.
</Info>

<Info>
This method emits [`calling.call.hold`][calling-call-hold] events. See [Call Events][call-events] for payload details.
</Info>

## **Parameters**

None.

## **Returns**

`dict` -- Server response confirming the hold operation.

## **Example**

```csharp
import asyncio
using SignalWire.Relay;

var client = new RelayClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],;
)

async Task HandleCall(call)
{
    await call.answer();
    await call.play([{"type": "tts", "text": "Please hold while I look that up."}]);

    var result = await call.hold();
    Console.WriteLine($"Hold started: {result}");

    // Do some processing...
    await asyncio.sleep(5);

    // Take the call off hold
    await call.unhold();

await client.RunAsync();


```
