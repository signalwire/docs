---
title: "LeaveRoom"
slug: /reference/dotnet/relay/call/leave-room
description: Leave a video/audio room.
max-toc-depth: 3
---

Leave the current room.

## **Parameters**

None.

## **Returns**

`dict` -- Server response confirming the leave.

## **Example**

```csharp
using SignalWire.Relay;

var client = new RelayClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],;
)

async Task HandleCall(call)
{
    await call.answer();

    // Join a room
    await call.join_room("meeting-room-1");

    // Later, leave the room
    await call.leave_room();
    await call.hangup();

await client.RunAsync();


```
