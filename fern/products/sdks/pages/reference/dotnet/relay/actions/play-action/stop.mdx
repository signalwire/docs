---
title: "stop"
slug: /reference/dotnet/relay/actions/play-action/stop
description: Stop audio playback immediately.
max-toc-depth: 3
---

Stop playback immediately.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```csharp
using SignalWire.Relay;

var client = new RelayClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],;
)

async Task HandleCall(call)
{
    await call.answer();
    var action = await call.play([{"type": "tts", "text": "Hello!"}]);

    // Stop playback early
    await action.stop();

await client.RunAsync();


```
