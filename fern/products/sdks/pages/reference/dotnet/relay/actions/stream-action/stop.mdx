---
title: "stop"
slug: /reference/dotnet/relay/actions/stream-action/stop
description: Stop audio streaming.
max-toc-depth: 3
---

Stop audio streaming.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```csharp
using SignalWire.Relay;

var client = new RelayClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],;
)

async Task HandleCall(call)
{
    await call.answer();
    var action = await call.stream(;
        url: "wss://example.com/audio-stream",
        track: "inbound",
    )

    // Stop the stream when done
    await action.stop();

await client.RunAsync();


```
