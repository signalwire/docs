---
title: "stop"
slug: /reference/dotnet/relay/actions/transcribe-action/stop
description: Stop transcription.
max-toc-depth: 3
---

Stop transcription.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```csharp
using SignalWire.Relay;

var client = new RelayClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],;
)

async Task HandleCall(call)
{
    await call.answer();
    var action = await call.transcribe();

    // Stop transcription early
    await action.stop();

await client.RunAsync();


```
