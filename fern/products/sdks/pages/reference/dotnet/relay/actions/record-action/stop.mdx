---
title: "stop"
slug: /reference/dotnet/relay/actions/record-action/stop
description: Stop recording immediately.
max-toc-depth: 3
---

Stop recording immediately. The recording file becomes available.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```csharp
using SignalWire.Relay;

var client = new RelayClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],;
)

async Task HandleCall(call)
{
    await call.answer();
    var action = await call.record(audio: {"direction": "both", "format": "wav"});

    // Stop recording early
    await action.stop();

await client.RunAsync();


```
