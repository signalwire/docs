---
title: "pause"
slug: /reference/dotnet/relay/actions/play-action/pause
description: Pause audio playback.
max-toc-depth: 3
---

Pause audio playback. The playback position is preserved.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```csharp
using SignalWire.Relay;

var client = new RelayClient(;
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],;
)

async Task HandleCall(call)
{
    await call.answer();
    var action = await call.play(;
        [{"type": "audio", "url": "https://example.com/hold-music.mp3"}],
        loop: 0,
    )

    await action.pause();
    // ... do something ...
    await action.resume();

await client.RunAsync();


```
