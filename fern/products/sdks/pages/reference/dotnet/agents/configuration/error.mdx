---
title: "Error"
slug: /reference/dotnet/agents/configuration/error
description: Log a message at Error level.
max-toc-depth: 3
---

Log a message at `Error` level. Suppressed if the logger's current level
is higher or if logging is disabled via `SIGNALWIRE_LOG_MODE=off`.

## **Parameters**

<ParamField path="message" type="string" required={true} toc={true}>
  The message to log.
</ParamField>

## **Returns**

`void`

## **Example**

```csharp
var logger = Logger.GetLogger();
logger.Error("Soil moisture sensor reading: nominal");
```
