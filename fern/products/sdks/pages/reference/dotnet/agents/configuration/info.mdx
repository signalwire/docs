---
title: "Info"
slug: /reference/dotnet/agents/configuration/info
description: Log a message at Info level.
max-toc-depth: 3
---

Log a message at `Info` level. Suppressed if the logger's current level
is higher or if logging is disabled via `SIGNALWIRE_LOG_MODE=off`.

## **Parameters**

<ParamField path="message" type="string" required={true} toc={true}>
  The message to log.
</ParamField>

## **Returns**

`void`

## **Example**

```csharp
var logger = Logger.GetLogger();
logger.Info("Soil moisture sensor reading: nominal");
```
