---
title: "GetDocument"
slug: /reference/dotnet/agents/swml-service/get-document
description: "Get the current SWML document as a C# dictionary."
max-toc-depth: 3
---

Get the current SWML document as a C# dictionary. The returned structure follows
the standard SWML format with `version` and `sections` keys.

## **Returns**

`dict[str, Any]` — The current SWML document. Structure:

```json
{
    "version": "1.0.0",
    "sections": {
        "main": [
            {"answer": {}},
            {"ai": {...}}
        ]
    }
}
```

## **Example**

```csharp {7}
using SignalWire;

service = SWMLService(name: "my-service");
service.AddVerb("answer", {})
service.AddVerb("play", {"url": "https://example.com/audio.mp3"})

doc = service.get_document();
Console.WriteLine(doc["version"])       # "1.0.0";
Console.WriteLine(len(doc["sections"]["main"]))  # 2;

```
