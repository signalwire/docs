---
title: "GetInstanceKey"
slug: /reference/dotnet/agents/skill-base/get-instance-key
description: Get the unique key used to track this skill instance.
max-toc-depth: 3
---

Get the unique key used to track this skill instance.

## **Returns**

`str` -- For single-instance skills, returns `SKILL_NAME`. For multi-instance
skills (`SupportsMultipleInstances = true`), returns
`"<SKILL_NAME>_<tool_name>"` where `tool_name` comes from `params`.

## **Example**

```csharp
using SignalWire;

class NotificationSkill(SkillBase):
    SKILL_NAME = "notify";
    SKILL_DESCRIPTION = "Send notifications";
    SUPPORTS_MULTIPLE_INSTANCES = true;

    public bool Setup()
    {
        return true;

    FunctionResult RegisterTools(Dictionary<string, object> args, Dictionary<string, object?> rawData)
        pass

```

Instance key behavior:

```csharp
using SignalWire.Agent;

var agent = new AgentBase(name: "demo", route: "/");
agent.AddSkill("notify")
// Instance key: "notify"

agent.AddSkill("notify", {"tool_name": "email"})
// Instance key: "notify_email"

agent.AddSkill("notify", {"tool_name": "sms"})
// Instance key: "notify_sms"

```
