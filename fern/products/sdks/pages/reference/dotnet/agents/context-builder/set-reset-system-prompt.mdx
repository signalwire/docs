---
title: "SetResetSystemPrompt"
slug: /reference/dotnet/agents/context-builder/set-reset-system-prompt
description: Set a replacement system prompt when transitioning to this step.
max-toc-depth: 3
---

Set a replacement system prompt when transitioning to this step.

## **Parameters**

<ParamField path="sp" type="string" required={true} toc={true}>
  The replacement system prompt.
</ParamField>

## **Returns**

`Step` -- Returns self for method chaining.

## **Example**

```csharp
step.SetResetSystemPrompt("You are now a soil specialist.");
```
