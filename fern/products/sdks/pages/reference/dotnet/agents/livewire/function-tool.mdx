---
title: "FunctionTool"
slug: /reference/dotnet/agents/livewire/function-tool
description: "Decorator for wrapping plain C# methods as agent tools."
max-toc-depth: 3
---

[define-tool]: /docs/sdks/reference/dotnet/agents/agent-base/define-tool
[runcontext]: /docs/sdks/reference/dotnet/agents/livewire/run-context

### function_tool

**function_tool**(`func=None`, `name=None`, `description=None`) -> `Callable`

Wraps a plain C# method so it can be passed into
[`Agent(tools=[...])`](/docs/sdks/reference/dotnet/agents/livewire/agent). The decorator
extracts parameter information from type annotations and builds a JSON-Schema-style
parameter dict. The function's XML doc comment is used as the tool description unless
`Description` is provided explicitly.

When the underlying SignalWire agent is built, each decorated function is registered
as a SWAIG function via
[`DefineTool()`][define-tool].

<Note>
Parameters annotated with [`RunContext`][runcontext]
are automatically excluded from the generated JSON schema and injected at call time.
</Note>

#### Parameters

<ParamField path="func" type="Delegate?" default="None" toc={true}>
  The function to decorate. When `@function_tool` is used without parentheses, the
  decorated function is passed here directly.
</ParamField>

<ParamField path="name" type="string?" default="None" toc={true}>
  Override the tool name. Defaults to the function's `Program.Main`.
</ParamField>

<ParamField path="description" type="string?" default="None" toc={true}>
  Override the tool description. Defaults to the function's XML doc comment.
</ParamField>

#### Returns

`Callable` -- The original function with metadata attributes attached:
- `_livewire_tool` (`bool`) -- Always `true`.
- `_tool_name` (`str`) -- The resolved tool name.
- `_tool_description` (`str`) -- The resolved description.
- `_tool_parameters` (`dict`) -- JSON-Schema-style parameter definition.
- `_tool_handler` (`Callable`) -- Reference to the original function.

#### Type Mapping

The decorator maps C# type annotations to JSON Schema types:

| C# type | JSON Schema type |
|-------------|-----------------|
| `str` | `"string"` |
| `int` | `"integer"` |
| `float` | `"number"` |
| `bool` | `"boolean"` |
| *(other / missing)* | `"string"` |

Parameters without a default value are marked as `required` in the schema.

#### Examples

<CodeBlocks>
<CodeBlock title="Without arguments">
```csharp {3}
using SignalWire;

public string GetWeather(city: str)
{
    """Get the current weather for a city."""
    return $"Sunny in {city}.";

```
</CodeBlock>
<CodeBlock title="With explicit name and description">
```csharp {3}
using SignalWire;

public string GetWeather(city: str)
{
    return $"Sunny in {city}.";

```
</CodeBlock>
<CodeBlock title="With RunContext">
```csharp {3}
using SignalWire;

public string SavePreference(ctx: RunContext, key: str, value: str)
{
    """Save a user preference."""
    ctx.userdata[key] = value;
    return $"Saved {key}={value}.";

```
</CodeBlock>
</CodeBlocks>
