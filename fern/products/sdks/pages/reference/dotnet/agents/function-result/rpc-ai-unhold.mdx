---
title: "RpcAiUnhold"
slug: /reference/dotnet/agents/function-result/rpc-ai-unhold
description: Release another call from hold via RPC.
max-toc-depth: 3
---

[rpc-ai-message]: /docs/sdks/reference/dotnet/agents/function-result/rpc-ai-message
[functionresult]: /docs/sdks/reference/dotnet/agents/function-result

Release another call from hold. Typically used after injecting a message into
the held caller's AI agent via [`rpc_ai_message()`][rpc-ai-message].

## **Parameters**

<ParamField path="call_id" type="string" required={true} toc={true}>
  Call ID of the call to release from hold.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```csharp {13}
using SignalWire.Agent;
using SignalWire.SWAIG;

var agent = new AgentBase(name: "my-agent", route: "/agent");
agent.SetPromptText("You are a helpful assistant.")

FunctionResult ReleaseCaller(Dictionary<string, object> args, Dictionary<string, object?> rawData)
    caller_call_id = args["original_call_id"];
    return (;
        FunctionResult("Returning you to the caller.");
        .rpc_ai_message(caller_call_id, "You can take their message now.");
        .rpc_ai_unhold(caller_call_id);
    )

agent.Serve();

```
