---
title: "CreatePaymentPrompt"
slug: /reference/dotnet/agents/function-result/create-payment-prompt
description: Build a payment prompt object for use with pay().
max-toc-depth: 3
---

[pay]: /docs/sdks/reference/dotnet/agents/function-result/pay
[create-payment-action]: /docs/sdks/reference/dotnet/agents/function-result/create-payment-action
[ref-functionresult]: /docs/sdks/reference/dotnet/agents/function-result

**[FunctionResult][ref-functionresult].create_payment_prompt**(`for_situation`, `actions`, `card_type=None`, `error_type=None`) -> `dict[str, Any]`

Static helper that builds a payment prompt object for use with the `prompts`
parameter of [`Pay()`][pay].
Payment prompts customize the TTS messages played during different stages of
payment collection.

## **Parameters**

<ParamField path="for_situation" type="string" required={true} toc={true}>
  The payment stage this prompt applies to (e.g., `"payment-card-number"`,
  `"expiration-date"`, `"security-code"`, `"postal-code"`).
</ParamField>

<ParamField path="actions" type="List<Dictionary<string, string>>" required={true} toc={true}>
  List of prompt actions. Use [`create_payment_action()`][create-payment-action]
  to build each entry.
</ParamField>

<ParamField path="card_type" type="string?" default="None" toc={true}>
  Space-separated card types this prompt applies to (e.g., `"visa mastercard"`).
</ParamField>

<ParamField path="error_type" type="string?" default="None" toc={true}>
  Space-separated error types this prompt handles.
</ParamField>

## **Returns**

`dict[str, Any]` — prompt dictionary to pass in the `prompts` list of [`Pay()`][pay].

## **Example**

```csharp {9}
using SignalWire.Agent;
using SignalWire.SWAIG;

var agent = new AgentBase(name: "my-agent", route: "/agent");
agent.SetPromptText("You are a helpful assistant.")

FunctionResult CollectCard(Dictionary<string, object> args, Dictionary<string, object?> rawData)
    prompt = FunctionResult.create_payment_prompt(;
        for_situation: "payment-card-number",
        actions=[;
            FunctionResult.create_payment_action("Say", "Please enter your credit card number.");
        ]
    )
    return (;
        FunctionResult("I'll collect your payment now.");
        .pay(;
            payment_connector_url: "https://api.example.com/pay",
            charge_amount: "49.99",
            prompts: [prompt];
        )
    )

agent.Serve();

```
