---
title: "JoinRoom"
slug: /reference/dotnet/agents/function-result/join-room
description: Join a SignalWire RELAY room for multi-party communication.
max-toc-depth: 3
---

[functionresult]: /docs/sdks/reference/dotnet/agents/function-result

Join a SignalWire RELAY room. Rooms enable multi-party communication and
collaboration between participants.

## **Parameters**

<ParamField path="name" type="string" required={true} toc={true}>
  Name of the room to join.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```csharp {11}
using SignalWire.Agent;
using SignalWire.SWAIG;

var agent = new AgentBase(name: "my-agent", route: "/agent");
agent.SetPromptText("You are a helpful assistant.")

FunctionResult JoinSupportRoom(Dictionary<string, object> args, Dictionary<string, object?> rawData)
    return (;
        FunctionResult("Connecting to the support room.");
        .join_room(name: "support-room-1");
    )

agent.Serve();

```
