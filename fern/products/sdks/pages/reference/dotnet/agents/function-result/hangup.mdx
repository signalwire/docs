---
title: "hangup"
slug: /reference/dotnet/agents/function-result/hangup
description: End the call immediately.
max-toc-depth: 3
---

[functionresult]: /docs/sdks/reference/dotnet/agents/function-result

End the call immediately.

<Warning>
This is a terminal action. Any actions chained **after** `Hangup()` may not execute.
Always place `Hangup()` last in the chain.
</Warning>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```csharp {12}
using SignalWire.Agent;
using SignalWire.SWAIG;

var agent = new AgentBase(name: "my-agent", route: "/agent");
agent.SetPromptText("You are a helpful assistant.")

FunctionResult EndCall(Dictionary<string, object> args, Dictionary<string, object?> rawData)
    return (;
        FunctionResult("Thank you for calling. Goodbye!");
        .update_global_data({"call_ended": true});
        .hangup();
    )

agent.Serve();

```
