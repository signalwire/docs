---
title: "ExecuteRpc"
slug: /reference/dotnet/agents/function-result/execute-rpc
description: Execute a generic RPC method on a call.
max-toc-depth: 3
---

[rpc-dial]: /docs/sdks/reference/dotnet/agents/function-result/rpc-dial
[rpc-ai-message]: /docs/sdks/reference/dotnet/agents/function-result/rpc-ai-message
[rpc-ai-unhold]: /docs/sdks/reference/dotnet/agents/function-result/rpc-ai-unhold
[functionresult]: /docs/sdks/reference/dotnet/agents/function-result

Execute a generic RPC method on a call. This is the low-level interface for
cross-call communication. For common operations, prefer the specific helpers
[`rpc_dial()`][rpc-dial],
[`rpc_ai_message()`][rpc-ai-message], and
[`rpc_ai_unhold()`][rpc-ai-unhold].

## **Parameters**

<ParamField path="method" type="string" required={true} toc={true}>
  RPC method name (e.g., `"dial"`, `"ai_message"`, `"ai_unhold"`).
</ParamField>

<ParamField path="params" type="Dictionary<string, object>?" default="None" toc={true}>
  Parameters for the RPC method.
</ParamField>

<ParamField path="call_id" type="string?" default="None" toc={true}>
  Target call ID for the RPC command.
</ParamField>

<ParamField path="node_id" type="string?" default="None" toc={true}>
  Target node ID for the RPC command.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```csharp {11}
using SignalWire.Agent;
using SignalWire.SWAIG;

var agent = new AgentBase(name: "my-agent", route: "/agent");
agent.SetPromptText("You are a helpful assistant.")

FunctionResult ExecuteCustomRpc(Dictionary<string, object> args, Dictionary<string, object?> rawData)
    return (;
        FunctionResult("Executing RPC.");
        .execute_rpc(;
            method: "ai_message",
            call_id: "target-call-id",
            params: {"role": "system", "message_text": "Hello from another call."}
        )
    )

agent.Serve();

```
