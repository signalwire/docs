---
title: "AddAction"
slug: /reference/dotnet/agents/function-result/add-action
description: Append a raw action to the FunctionResult action list.
max-toc-depth: 3
---

[connect]: /docs/sdks/reference/dotnet/agents/function-result/connect
[hangup]: /docs/sdks/reference/dotnet/agents/function-result/hangup
[say]: /docs/sdks/reference/dotnet/agents/function-result/say
[functionresult]: /docs/sdks/reference/dotnet/agents/function-result

Append a raw action to the action list. Prefer the named convenience methods
([`Connect()`][connect],
[`Hangup()`][hangup],
[`Say()`][say], etc.)
for common operations.

## **Parameters**

<ParamField path="name" type="string" required={true} toc={true}>
  Action type identifier (e.g., `"hold"`, `"hangup"`, `"set_global_data"`).
</ParamField>

<ParamField path="data" type="object" required={true} toc={true}>
  Action payload. Can be a string, boolean, dict, or list depending on the action type.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```csharp {11-12}
using SignalWire.Agent;
using SignalWire.SWAIG;

var agent = new AgentBase(name: "my-agent", route: "/agent");
agent.SetPromptText("You are a helpful assistant.")

FunctionResult CustomAction(Dictionary<string, object> args, Dictionary<string, object?> rawData)
    return (;
        FunctionResult("Processing your request.");
        .add_action("set_global_data", {"status": "active"});
        .add_action("hold", 60);
    )

agent.Serve();

```
