---
title: "stop"
slug: /reference/java/relay/actions/play-action/stop
description: Stop audio playback immediately.
max-toc-depth: 3
---

Stop playback immediately.

## **Returns**

`Map<String, Object>` -- Server acknowledgment.

## **Example**

```java {16}
import com.signalwire.sdk.relay.RelayClient;

var client = RelayClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .contexts(List.of("default"))
        .build();

async def handle_call(call):
    await call.answer()
    var action = await call.play([{"type": "tts", "text": "Hello!"}]);

    // Stop playback early
    await action.stop()

client.run();


```
