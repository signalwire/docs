---
title: "stop"
slug: /reference/java/relay/actions/ai-action/stop
description: Stop the AI agent session.
max-toc-depth: 3
---

Stop the AI agent session.

## **Returns**

`Map<String, Object>` -- Server acknowledgment.

## **Example**

```java {19}
import com.signalwire.sdk.relay.RelayClient;

var client = RelayClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .contexts(List.of("default"))
        .build();

async def handle_call(call):
    await call.answer()
    var action = await call.ai( {"text": "You are a helpful assistant."}, {"temperature": 0.7}, );

    // Stop the AI session
    await action.stop()

client.run();


```
