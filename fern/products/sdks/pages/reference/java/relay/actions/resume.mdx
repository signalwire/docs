---
title: "resume"
slug: /reference/java/relay/actions/resume
description: Resume a paused action.
max-toc-depth: 3
---

Resume an action that was previously paused.

## Returns

`void`

## Example

```java
call.onAnswer(c -> {
    var action = c.playTTS("A detailed guide to growing ferns...");
    action.pause();
    Thread.sleep(1000);
    action.resume();
});
```
