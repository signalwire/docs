---
title: "stop"
slug: /reference/java/relay/actions/detect-action/stop
description: Stop detection immediately.
max-toc-depth: 3
---

Stop detection immediately.

## **Returns**

`Map<String, Object>` -- Server acknowledgment.

## **Example**

```java {18}
import com.signalwire.sdk.relay.RelayClient;

var client = RelayClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .contexts(List.of("default"))
        .build();

async def handle_call(call):
    await call.answer()
    var action = await call.detect( {"type": "machine", "params": {"initial_timeout": 5.0}} );

    // Stop detection early
    await action.stop()

client.run();


```
