---
title: "stop"
slug: /reference/java/relay/actions/tap-action/stop
description: Stop media interception.
max-toc-depth: 3
---

Stop media interception.

## **Returns**

`Map<String, Object>` -- Server acknowledgment.

## **Example**

```java {19}
import com.signalwire.sdk.relay.RelayClient;

var client = RelayClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .contexts(List.of("default"))
        .build();

async def handle_call(call):
    await call.answer()
    var action = await call.tap( {"type": "audio", "params": {"direction": "both"}}, {"type": "ws", "params": {"uri": "wss://example.com/tap"}}, );

    // Stop the tap when done
    await action.stop()

client.run();


```
