---
title: "stop"
slug: /reference/java/relay/actions/transcribe-action/stop
description: Stop transcription.
max-toc-depth: 3
---

Stop transcription.

## **Returns**

`Map<String, Object>` -- Server acknowledgment.

## **Example**

```java {16}
import com.signalwire.sdk.relay.RelayClient;

var client = RelayClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .contexts(List.of("default"))
        .build();

async def handle_call(call):
    await call.answer()
    var action = await call.transcribe();

    // Stop transcription early
    await action.stop()

client.run();


```
