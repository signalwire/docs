---
title: "get"
slug: /reference/java/rest/compat/queues/get
description: Retrieve a single queue by SID.
max-toc-depth: 3
---

Retrieve a single queue by SID.

## **Parameters**

<ParamField path="resourceId" type="String" required={true} toc={true}>
  The queue SID.
</ParamField>

## **Returns**

`Map<String, Object>` -- The queue resource.

## **Example**

```java {9-9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var queue = client.compat.queues.get("QU...");


```
