---
title: "listParticipants"
slug: /reference/java/rest/compat/conferences/list-participants
description: List participants in a conference.
max-toc-depth: 3
---

List participants in a conference.

## **Parameters**

<ParamField path="conferenceSid" type="String" required={true} toc={true}>
  The SID of the conference.
</ParamField>

<ParamField path="Muted" type="boolean" toc={true}>
  Filter by muted status.
</ParamField>

<ParamField path="Hold" type="boolean" toc={true}>
  Filter by hold status.
</ParamField>

## **Returns**

`Map<String, Object>` -- List of participant resources.

## **Example**

```java {9-9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var participants = client.compat.conferences.list_participants("CF...");


```
