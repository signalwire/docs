---
title: "get"
slug: /reference/java/rest/compat/recordings/get
description: Retrieve a single recording by SID.
max-toc-depth: 3
---

Retrieve a single recording by SID.

## **Parameters**

<ParamField path="sid" type="String" required={true} toc={true}>
  The recording SID.
</ParamField>

## **Returns**

`Map<String, Object>` -- The recording resource.

## **Example**

```java {9-9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var recording = client.compat.recordings.get("RE...");


```
