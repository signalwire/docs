---
title: "listMedia"
slug: /reference/java/rest/compat/messages/list-media
description: List media items attached to a message.
max-toc-depth: 3
---

List media items (images, files) attached to a message.

## **Parameters**

<ParamField path="messageSid" type="String" required={true} toc={true}>
  The SID of the message.
</ParamField>

## **Returns**

`Map<String, Object>` -- List of media resources attached to the message.

## **Example**

```java {9-9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var media = client.compat.messages.list_media("SM...");


```
