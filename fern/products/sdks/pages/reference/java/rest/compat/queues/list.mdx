---
title: "list"
slug: /reference/java/rest/compat/queues/list
description: List queues in the account.
max-toc-depth: 3
---

List queues in the account.

## **Parameters**

<ParamField path="FriendlyName" type="String" toc={true}>
  Filter by friendly name.
</ParamField>

<ParamField path="PageSize" type="int" toc={true}>
  Number of results per page.
</ParamField>

## **Returns**

`Map<String, Object>` -- Paginated response containing queue resources.

## **Example**

```java {9-9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var queues = client.compat.queues.list(20);


```
