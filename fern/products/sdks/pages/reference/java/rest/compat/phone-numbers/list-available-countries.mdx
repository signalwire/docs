---
title: "listAvailableCountries"
slug: /reference/java/rest/compat/phone-numbers/list-available-countries
description: List countries with available phone numbers.
max-toc-depth: 3
---

List countries that have phone numbers available for purchase.

## **Parameters**

No required parameters.

## **Returns**

`Map<String, Object>` -- Response containing available country resources.

## **Example**

```java {9-9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var countries = client.compat.phone_numbers.list_available_countries();


```
