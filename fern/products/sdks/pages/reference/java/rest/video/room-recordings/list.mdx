---
title: "list"
slug: /reference/java/rest/video/room-recordings/list
description: List video room recordings.
max-toc-depth: 3
---

List video room recordings.

## **Parameters**

<ParamField path="pageSize" type="int" toc={true}>
  Number of results per page.
</ParamField>

Additional keyword arguments are passed as query parameters for filtering and pagination.

## **Returns**

`Map<String, Object>` -- Paginated response containing room recordings.

## **Example**

```java {9-9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var recordings = client.video.room_recordings.list();


```
