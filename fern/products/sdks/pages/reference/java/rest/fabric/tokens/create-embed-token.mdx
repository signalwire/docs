---
title: "createEmbedToken"
slug: /reference/java/rest/fabric/tokens/create-embed-token
description: Create an embed authentication token.
max-toc-depth: 3
---

Create an embed authentication token.

## **Parameters**

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`Map<String, Object>` -- The created embed token.

## **Example**

```java {9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var token = client.fabric.tokens.create_embed_token("resource-id");
System.out.println("Embed token: " + token['token']);


```
