---
title: "update"
slug: /reference/java/rest/fabric/sip-endpoints/update
description: Replace a SIP endpoint resource.
max-toc-depth: 3
---

Replace a SIP endpoint resource. Uses PUT for full replacement.

## **Parameters**

<ParamField path="resourceId" type="String" required={true} toc={true}>
  The unique identifier of the SIP endpoint.
</ParamField>

Additional keyword arguments are the full replacement body.

## **Returns**

`Map<String, Object>` -- The updated SIP endpoint resource.

## **Example**

```java {9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var updated = client.fabric.sip_endpoints.update("endpoint-id", "updated-name");
System.out.println("Updated: " + updated['name']);


```
