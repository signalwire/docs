---
title: "update"
slug: /reference/java/rest/fabric/call-flows/update
description: Replace a call flow resource.
max-toc-depth: 3
---

Replace a call flow resource. Uses PUT for full replacement.

## **Parameters**

<ParamField path="resourceId" type="String" required={true} toc={true}>
  The unique identifier of the call flow.
</ParamField>

Additional keyword arguments are the full replacement body.

## **Returns**

`Map<String, Object>` -- The updated call flow resource.

## **Example**

```java {9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var updated = client.fabric.call_flows.update("flow-id", "updated-name");
System.out.println("Updated: " + updated['name']);


```
