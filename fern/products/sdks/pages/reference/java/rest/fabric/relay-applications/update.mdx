---
title: "update"
slug: /reference/java/rest/fabric/relay-applications/update
description: Replace a RELAY application resource.
max-toc-depth: 3
---

Replace a RELAY application resource. Uses PUT for full replacement.

## **Parameters**

<ParamField path="resourceId" type="String" required={true} toc={true}>
  The unique identifier of the RELAY application.
</ParamField>

Additional keyword arguments are the full replacement body.

## **Returns**

`Map<String, Object>` -- The updated RELAY application resource.

## **Example**

```java {9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var updated = client.fabric.relay_applications.update("resource-id", "updated-name");
System.out.println("Updated: " + updated['name']);


```
