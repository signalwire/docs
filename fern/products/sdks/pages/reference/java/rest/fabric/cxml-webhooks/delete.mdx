---
title: "delete"
slug: /reference/java/rest/fabric/cxml-webhooks/delete
description: Delete a cXML webhook resource.
max-toc-depth: 3
---

Delete a cXML webhook resource.

## **Parameters**

<ParamField path="resourceId" type="String" required={true} toc={true}>
  The unique identifier of the cXML webhook to delete.
</ParamField>

## **Returns**

`Map<String, Object>` -- Empty response on success (HTTP 204).

## **Example**

```java {9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

client.fabric.cxml_webhooks.delete("webhook-id")
System.out.println("Cxml Webhook deleted");


```
