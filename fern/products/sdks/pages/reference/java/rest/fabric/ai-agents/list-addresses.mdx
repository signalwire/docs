---
title: "listAddresses"
slug: /reference/java/rest/fabric/ai-agents/list-addresses
description: List addresses associated with an AI agent resource.
max-toc-depth: 3
---

List addresses associated with an AI agent resource.

## **Parameters**

<ParamField path="resourceId" type="String" required={true} toc={true}>
  The unique identifier of the AI agent.
</ParamField>

## **Returns**

`Map<String, Object>` -- List of addresses assigned to this resource.

## **Example**

```java {9}
import com.signalwire.sdk.rest.RestClient;

var client = RestClient.builder()
        .project("your-project-id")
        .token("your-api-token")
        .host("your-space.signalwire.com")
        .build();

var addresses = client.fabric.ai_agents.list_addresses("agent-id");
for addr in addresses.get("data", []):
    System.out.println(addr['display_name'] + ": " + addr['id']);


```
