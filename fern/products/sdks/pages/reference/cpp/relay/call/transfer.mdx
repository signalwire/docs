---
title: "transfer"
slug: /reference/cpp/relay/call/transfer
description: "Transfer call control to another RELAY application or SWML script."
max-toc-depth: 3
---

Transfer control of the call to another RELAY application or SWML script.
The current application loses control of the call after a successful transfer.

## **Parameters**

<ParamField path="dest" type="std::string" required={true} toc={true}>
  The transfer destination. This can be a RELAY context name or a URL pointing
  to a SWML script.
</ParamField>

Additional keyword arguments are forwarded to the RELAY `calling.transfer` request.

## **Returns**

`nlohmann::json` -- Server response confirming the transfer.

## **Example**

```cpp {16}
#include <signalwire/relay/client.hpp>

client = RelayClient(;
);

// @client.on_call
void handle_call(const auto& call) {
    call.answer();
    action = call.play([{"type": "tts", "text": "Transferring you now..."}]);
    action.wait();

    result = call.transfer("support-queue");
    std::cout << "Transfer result: " + std::string(result) << std::endl;

client.run();


```
