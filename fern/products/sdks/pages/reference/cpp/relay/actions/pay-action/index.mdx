---
title: "PayAction"
slug: /reference/cpp/relay/actions/pay-action
description: Action handle for an active payment collection operation.
max-toc-depth: 3
---

[call-pay]: /docs/sdks/reference/cpp/relay/call/pay
[base-action-interface]: /docs/sdks/reference/cpp/relay/actions
[stop]: /docs/sdks/reference/cpp/relay/actions/pay-action/stop

Returned from [`call.pay()`][call-pay]. Tracks
an active payment collection operation. Terminal states: `finished`, `error`.

Inherits all properties and methods from the
[base Action interface][base-action-interface] (`control_id`,
`is_done`, `completed`, `result`, `wait()`).

## **Properties**

No additional properties beyond the [base Action interface][base-action-interface].

## **Methods**

<CardGroup cols={2}>
  <Card title="stop" href="/docs/sdks/reference/cpp/relay/actions/pay-action/stop">
    Stop payment collection.
  </Card>
</CardGroup>

## **Example**

```cpp
#include <signalwire/relay/client.hpp>

client = RelayClient(;
);

// @client.on_call
void handle_call(const auto& call) {
    call.answer();
    action = call.pay(;
        payment_connector_url="https://example.com/payment-connector",
        charge_amount="29.99",
        currency="USD",
    );

    event = action.wait();
    std::cout << "Payment state: " + std::string(event.params.value("state", "")) << std::endl;

client.run();


```
