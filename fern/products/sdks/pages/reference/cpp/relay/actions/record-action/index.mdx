---
title: "RecordAction"
slug: /reference/cpp/relay/actions/record-action
description: Action handle for an active recording operation.
max-toc-depth: 3
---

[call-record]: /docs/sdks/reference/cpp/relay/call/record
[base-action-interface]: /docs/sdks/reference/cpp/relay/actions
[stop]: /docs/sdks/reference/cpp/relay/actions/record-action/stop
[pause]: /docs/sdks/reference/cpp/relay/actions/record-action/pause
[resume]: /docs/sdks/reference/cpp/relay/actions/record-action/resume

Returned from [`call.record()`][call-record]. Tracks
an active recording operation. Terminal states: `finished`, `no_input`.

Inherits all properties and methods from the
[base Action interface][base-action-interface] (`control_id`,
`is_done`, `completed`, `result`, `wait()`).

## **Properties**

No additional properties beyond the [base Action interface][base-action-interface].

## **Methods**

<CardGroup cols={3}>
  <Card title="stop" href="/docs/sdks/reference/cpp/relay/actions/record-action/stop">
    Stop recording immediately.
  </Card>
  <Card title="pause" href="/docs/sdks/reference/cpp/relay/actions/record-action/pause">
    Pause an active recording.
  </Card>
  <Card title="resume" href="/docs/sdks/reference/cpp/relay/actions/record-action/resume">
    Resume a paused recording.
  </Card>
</CardGroup>

## **Example**

```cpp
#include <signalwire/relay/client.hpp>

client = RelayClient(;
);

// @client.on_call
void handle_call(const auto& call) {
    call.answer();
    action = call.record({"direction": "both", "format": "wav"});

    // Pause during sensitive information
    action.pause();
    // ... sensitive exchange ...
    action.resume();

    // Wait for the recording to complete
    event = action.wait();
    std::cout << "Recording URL: " + std::string(event.params.get('record', {) + ").value("url", "")}" << std::endl;

client.run();


```
