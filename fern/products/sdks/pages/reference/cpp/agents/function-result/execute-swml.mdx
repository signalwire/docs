---
title: "execute_swml"
slug: /reference/cpp/agents/function-result/execute-swml
description: Execute a raw SWML document as an action.
max-toc-depth: 3
---

[connect]: /docs/sdks/reference/cpp/agents/function-result/connect
[record-call]: /docs/sdks/reference/cpp/agents/function-result/record-call
[send-sms]: /docs/sdks/reference/cpp/agents/function-result/send-sms
[functionresult]: /docs/sdks/reference/cpp/agents/function-result
[ref-swmlbuilder]: /docs/sdks/reference/cpp/agents/swml-builder

Execute a raw SWML document as an action. This is the escape hatch for advanced
use cases that are not covered by the named convenience methods.

<Note>
Most use cases are covered by the specific action methods
([`connect()`][connect],
[`record_call()`][record-call],
[`send_sms()`][send-sms], etc.).
Use `execute_swml()` only when you need SWML features not available through
convenience methods.
</Note>

## **Parameters**

<ParamField path="swml_content" type="std::variant<std::string, nlohmann::json, SWML object>" required={true} toc={true}>
  SWML content in one of three formats:
  - `std::string` — raw SWML JSON text (parsed internally)
  - `nlohmann::json` — SWML data structure
  - SWML object — any object with a `.to_dict()` method (e.g., a [`SWMLBuilder`][ref-swmlbuilder] instance)
</ParamField>

<ParamField path="transfer" type="bool" default="false" toc={true}>
  When `true`, the call exits the agent after the SWML executes. When `false`,
  the SWML executes inline and the agent continues.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```cpp {20}
#include <signalwire/agent/agent_base.hpp>
#include <signalwire/swaig/function_result.hpp>

agent::AgentBase agent("my-agent", "/agent");
agent.set_prompt_text("You are a helpful assistant.");

void play_announcement(const nlohmann::json& args, const nlohmann::json& raw_data) {
    swml_doc = {
        "version": "1.0.0",
        "sections": {
            "main": [;
                {"play": {{"url", "https://example.com/announcement.mp3"}}},
                {"hangup": {}}
            ];
        }
    }
        return swaig::FunctionResult()
            .execute_swml(swml_doc, false);

}
agent.serve();


```
