---
title: "create_payment_prompt"
slug: /reference/cpp/agents/function-result/create-payment-prompt
description: Build a payment prompt object for use with pay().
max-toc-depth: 3
---

[pay]: /docs/sdks/reference/cpp/agents/function-result/pay
[create-payment-action]: /docs/sdks/reference/cpp/agents/function-result/create-payment-action
[ref-functionresult]: /docs/sdks/reference/cpp/agents/function-result

**[FunctionResult][ref-functionresult].create_payment_prompt**(`for_situation`, `actions`, `card_type=None`, `error_type=None`) -> `dict[str, Any]`

Static helper that builds a payment prompt object for use with the `prompts`
parameter of [`pay()`][pay].
Payment prompts customize the TTS messages played during different stages of
payment collection.

## **Parameters**

<ParamField path="for_situation" type="std::string" required={true} toc={true}>
  The payment stage this prompt applies to (e.g., `"payment-card-number"`,
  `"expiration-date"`, `"security-code"`, `"postal-code"`).
</ParamField>

<ParamField path="actions" type="std::vector<std::map<std::string, std::string>>" required={true} toc={true}>
  List of prompt actions. Use [`create_payment_action()`][create-payment-action]
  to build each entry.
</ParamField>

<ParamField path="card_type" type="std::optional<std::string>" default="std::nullopt" toc={true}>
  Space-separated card types this prompt applies to (e.g., `"visa mastercard"`).
</ParamField>

<ParamField path="error_type" type="std::optional<std::string>" default="std::nullopt" toc={true}>
  Space-separated error types this prompt handles.
</ParamField>

## **Returns**

`dict[str, Any]` — prompt dictionary to pass in the `prompts` list of [`pay()`][pay].

## **Example**

```cpp {9}
#include <signalwire/agent/agent_base.hpp>
#include <signalwire/swaig/function_result.hpp>

agent::AgentBase agent("my-agent", "/agent");
agent.set_prompt_text("You are a helpful assistant.");

void collect_card(const nlohmann::json& args, const nlohmann::json& raw_data) {
    prompt = FunctionResult.create_payment_prompt(;
        for_situation="payment-card-number",
        actions=[;
            FunctionResult.create_payment_action("Say", "Please enter your credit card number.");
        ];
    );
        return swaig::FunctionResult("I'll collect your payment now.")
            .pay("https://api.example.com/pay", "49.99", {prompt});

}
agent.serve();


```
