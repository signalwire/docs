---
title: "purpose"
slug: /reference/cpp/agents/data-map/purpose
description: Set the function description shown to the AI.
max-toc-depth: 3
---

[ref-datamap]: /docs/sdks/reference/cpp/agents/data-map

Set the function description shown to the AI when deciding which tool to call.

## **Parameters**

<ParamField path="description" type="std::string" required={true} toc={true}>
  Human-readable description of what this function does.
</ParamField>

## **Returns**

[`DataMap`][ref-datamap] -- Self for method chaining.

---

Alias for `purpose()`.

## **Parameters**

<ParamField path="description" type="std::string" required={true} toc={true}>
  Human-readable description of what this function does.
</ParamField>

## **Returns**

`DataMap` -- Self for method chaining.

## **Example**

```cpp {4}
#include <signalwire/signalwire.hpp>

datamap::DataMap weather("get_weather");
weather.purpose("Get the current weather for a given city");

std::cout << weather.to_swaig_function() << std::endl;
// {'function': 'get_weather', 'description': 'Get the current weather for a given city', ...}


```

The `description()` method is an alias for `purpose()`:

```cpp
#include <signalwire/signalwire.hpp>

datamap::DataMap weather("get_weather");
weather.description("Get the current weather for a given city");

std::cout << weather.to_swaig_function() << std::endl;


```
