---
title: "parameter"
slug: /reference/cpp/agents/data-map/parameter
description: Add a function parameter to the tool definition.
max-toc-depth: 3
---

[ref-datamap]: /docs/sdks/reference/cpp/agents/data-map

Add a function parameter to the tool definition.

## **Parameters**

<ParamField path="name" type="std::string" required={true} toc={true}>
  Parameter name.
</ParamField>

<ParamField path="param_type" type="std::string" required={true} toc={true}>
  JSON schema type for the parameter.

  - `"string"` -- text value
  - `"integer"` -- whole number value
  - `"number"` -- numeric value including decimals
  - `"boolean"` -- true or false value
  - `"array"` -- list of values
  - `"object"` -- nested key-value structure
</ParamField>

<ParamField path="description" type="std::string" required={true} toc={true}>
  Description of the parameter shown to the AI.
</ParamField>

<ParamField path="required" type="bool" default="false" toc={true}>
  Whether this parameter is required.
</ParamField>

<ParamField path="enum" type="std::vector<std::string>" toc={true}>
  Optional list of allowed values for this parameter.
</ParamField>

## **Returns**

[`DataMap`][ref-datamap] -- Self for method chaining.

## **Example**

```cpp {6-7}
#include <signalwire/signalwire.hpp>

weather = (;
    DataMap("get_weather");
    .purpose("Get current weather for a city");
    .parameter("city", "string", "City name", true);
    .parameter("units", "string", "Temperature units", enum={"fahrenheit", "celsius"});
);

std::cout << weather.to_swaig_function() << std::endl;
// {'function': 'get_weather', 'parameters': {'type': 'object', 'properties': {'city': ..., 'units': ...}, 'required': ['city']}, ...}


```
