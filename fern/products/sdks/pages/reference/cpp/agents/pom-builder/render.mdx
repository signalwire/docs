---
title: "render"
sidebar-title: "render"
slug: /reference/cpp/agents/pom-builder/render
description: Render the POM as Markdown or XML.
max-toc-depth: 3
---

Render the entire POM as a Markdown string. Sections become `##` headings,
subsections become `###` headings, and bullets render as Markdown lists.

## **Returns**

`std::string` -- The rendered Markdown text.

---

Render the entire POM as an XML string. Useful when the AI benefits from
XML-structured prompts.

## **Returns**

`std::string` -- The rendered XML text.

## **Example**

```cpp {7,17}
#include <signalwire/signalwire.hpp>

pom = PomBuilder();
pom.add_section("Role", "You are a helpful assistant.");
pom.add_section("Rules", bullets={"Be concise", "Be accurate"});

// Render as Markdown
md = pom.render_markdown();
std::cout << md << std::endl;
// ## Role
// You are a helpful assistant.
//
// ## Rules
// - Be concise
// - Be accurate

// Render as XML
xml = pom.render_xml();
std::cout << xml << std::endl;


```
