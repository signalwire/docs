---
title: "cleanup"
slug: /reference/cpp/agents/skill-base/cleanup
description: Release resources when the skill is removed or the agent shuts down.
max-toc-depth: 3
---

Called when the skill is removed or the agent shuts down. Override to release
resources, close connections, cancel background tasks, etc.

## **Returns**

`std::nullopt`

## **Example**

```cpp {11}
#include <signalwire/signalwire.hpp>

using namespace signalwire;

class DatabaseSkill : public signalwire::agent::AgentBase {
public:
    SKILL_NAME = "database";
    SKILL_DESCRIPTION = "Database query skill";

    bool setup() {
        connection = create_connection();
        return true;

    void cleanup() {
        if (hasattr(self, "connection")) {
            connection.close();
            logger.info("Database connection closed");

    void register_tools() {
        pass;


```
