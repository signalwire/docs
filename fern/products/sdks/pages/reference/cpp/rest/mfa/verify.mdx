---
title: "verify"
slug: /reference/cpp/rest/mfa/verify
description: Verify an MFA code submitted by the user.
max-toc-depth: 3
---

Verify an MFA code submitted by the user.

## **Parameters**

<ParamField path="request_id" type="std::string" required={true} toc={true}>
  The MFA request ID returned from `sms()` or `call()`.
</ParamField>

<ParamField path="token" type="std::string" required={true} toc={true}>
  The verification code entered by the user.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Additional parameters.
</ParamField>

## **Returns**

`nlohmann::json` -- Verification result indicating whether the code is valid.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.mfa.verify("your-mfa-request-id", token="123456");
if (result{"success"}) {
    std::cout << "Verification successful" << std::endl;
} else {
    std::cout << "Invalid code" << std::endl;


```
