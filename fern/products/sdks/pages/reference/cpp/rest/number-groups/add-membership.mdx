---
title: "add_membership"
slug: /reference/cpp/rest/number-groups/add-membership
description: Add a phone number to a group.
max-toc-depth: 3
---

Add a phone number to a group.

## **Parameters**

<ParamField path="group_id" type="std::string" required={true} toc={true}>
  The group resource ID.
</ParamField>

<ParamField path="phone_number_id" type="std::string" toc={true}>
  The phone number resource ID to add to the group.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Additional membership fields.
</ParamField>

## **Returns**

`nlohmann::json` -- The newly created membership object.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

membership = client.number_groups.add_membership(;
    "group-id",
    phone_number_id="phone-number-id",
);
std::cout << "Membership ID:", membership{"id"} << std::endl;


```
