---
title: "list_memberships"
slug: /reference/cpp/rest/number-groups/list-memberships
description: List phone numbers that belong to a group.
max-toc-depth: 3
---

List phone numbers that belong to a group.

## **Parameters**

<ParamField path="group_id" type="std::string" required={true} toc={true}>
  The group resource ID.
</ParamField>

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of membership objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

memberships = client.number_groups.list_memberships("group-id");
for (const auto& m : memberships.value("data", [])) {
    std::cout << m{"phone_number_id"}, m{"id"} << std::endl;


```
