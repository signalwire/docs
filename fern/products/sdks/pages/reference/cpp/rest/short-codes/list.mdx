---
title: "list"
slug: /reference/cpp/rest/short-codes/list
description: List short codes in the project.
max-toc-depth: 3
---

List short codes in the project.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of short code objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.short_codes.list();
for (const auto& code : result.value("data", [])) {
    std::cout << code{"short_code"}, code{"id"} << std::endl;


```
