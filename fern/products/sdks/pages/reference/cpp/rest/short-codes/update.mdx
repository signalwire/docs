---
title: "update"
slug: /reference/cpp/rest/short-codes/update
description: Update a short code's configuration.
max-toc-depth: 3
---

Update a short code's configuration. Uses PUT.

## **Parameters**

<ParamField path="short_code_id" type="std::string" required={true} toc={true}>
  The short code resource ID.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Fields to update (e.g., `message_handler`, `message_request_url`).
</ParamField>

## **Returns**

`nlohmann::json` -- The updated short code object.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.short_codes.update(;
    "short-code-id",
    message_request_url="https://example.com/sms",
);


```
