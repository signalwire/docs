---
title: "list"
slug: /reference/cpp/rest/phone-numbers/list
description: List phone numbers owned by the project.
max-toc-depth: 3
---

List phone numbers owned by the project.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters to filter and paginate results (e.g., `page_size`,
  `page_token`).
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of phone number objects and
pagination links.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.phone_numbers.list(10);
for (const auto& number : result.value("data", [])) {
    std::cout << number{"number"}, number{"name"} << std::endl;


```
