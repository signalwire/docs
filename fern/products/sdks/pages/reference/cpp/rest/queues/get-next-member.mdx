---
title: "get_next_member"
slug: /reference/cpp/rest/queues/get-next-member
description: Retrieve the next member in the queue.
max-toc-depth: 3
---

Retrieve the next member in the queue (the member at the front).

## **Parameters**

<ParamField path="queue_id" type="std::string" required={true} toc={true}>
  The queue resource ID.
</ParamField>

## **Returns**

`nlohmann::json` -- The next member object.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

next_member = client.queues.get_next_member("queue-id");
std::cout << "Next caller:", next_member{"call_id"} << std::endl;


```
