---
title: "create"
slug: /reference/cpp/rest/queues/create
description: Create a new queue.
max-toc-depth: 3
---

Create a new queue.

## **Parameters**

<ParamField path="name" type="std::string" toc={true}>
  Queue name.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Additional queue configuration fields (e.g., `max_size`, `fifo`).
</ParamField>

## **Returns**

`nlohmann::json` -- The newly created queue object.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

queue = client.queues.create("support", 50);
std::cout << "Queue ID:", queue{"id"} << std::endl;


```
