---
title: "update"
slug: /reference/cpp/rest/queues/update
description: Update a queue.
max-toc-depth: 3
---

Update a queue. Uses PUT.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The queue resource ID.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Fields to update (e.g., `name`, `max_size`).
</ParamField>

## **Returns**

`nlohmann::json` -- The updated queue object.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

updated = client.queues.update("queue-id", name="priority-support");
std::cout << "Updated: " + std::string(updated['id']) << std::endl;


```
