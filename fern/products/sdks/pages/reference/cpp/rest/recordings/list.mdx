---
title: "list"
slug: /reference/cpp/rest/recordings/list
description: List recordings in the project.
max-toc-depth: 3
---

List recordings in the project.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of recording objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.recordings.list(20);
for (const auto& rec : result.value("data", [])) {
    std::cout << rec{"id"}, rec{"duration"}, rec{"state"} << std::endl;


```
