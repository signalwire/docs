---
title: "list"
slug: /reference/cpp/rest/addresses/list
description: List addresses in the project.
max-toc-depth: 3
---

List addresses in the project.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of address objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.addresses.list();
for (const auto& addr : result.value("data", [])) {
    std::cout << addr{"friendly_name"}, addr{"street"} << std::endl;


```
