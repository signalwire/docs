---
title: "update"
slug: /reference/cpp/rest/project/update
description: Update an existing API token.
max-toc-depth: 3
---

### tokens.update

Update an existing API token. Uses PATCH.

## **Parameters**

<ParamField path="token_id" type="std::string" required={true} toc={true}>
  The token resource ID.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Fields to update (e.g., `name`).
</ParamField>

## **Returns**

`nlohmann::json` -- The updated token object.

## **Example**

```cpp {9-9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.project.tokens.update("token-id", name="renamed-token");


```
