---
title: "PubSub"
slug: /reference/cpp/rest/pubsub
description: "Publish-subscribe real-time messaging."
max-toc-depth: 3
---

[restclient]: /docs/sdks/reference/cpp/rest/client

Generate tokens for the SignalWire PubSub (publish-subscribe) real-time messaging
service. PubSub tokens authenticate client-side connections to subscribe to
channels and publish messages.

Access via `client.pubsub` on a [`RestClient`][restclient] instance.

```cpp
#include <signalwire/rest/rest_client.hpp>

rest::RestClient client();


```

---

### create_token

Create a PubSub authentication token.

## **Parameters**

<ParamField path="channels" type="std::vector<std::string>" toc={true}>
  List of channel names the token grants access to.
</ParamField>

<ParamField path="member_id" type="std::string" toc={true}>
  Identifier for the connecting member.
</ParamField>

<ParamField path="ttl" type="int" toc={true}>
  Token time-to-live in seconds.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Additional token parameters (e.g., `state`, `permissions`).
</ParamField>

## **Returns**

`nlohmann::json` -- The token object containing the JWT token string.

## **Example**

```cpp
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

token = client.pubsub.create_token(;
    channels={"notifications", "updates"},
    member_id="user-123",
    ttl=3600,
);
std::cout << "PubSub token:", token{"token"} << std::endl;


```
