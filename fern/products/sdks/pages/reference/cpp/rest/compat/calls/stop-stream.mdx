---
title: "stop_stream"
slug: /reference/cpp/rest/compat/calls/stop-stream
description: Stop a media stream on a call.
max-toc-depth: 3
---

Stop a media stream on a call.

## **Parameters**

<ParamField path="call_sid" type="std::string" required={true} toc={true}>
  The SID of the call.
</ParamField>

<ParamField path="stream_sid" type="std::string" required={true} toc={true}>
  The SID of the stream to stop.
</ParamField>

## **Returns**

`nlohmann::json` -- The updated stream resource.

## **Example**

```cpp {9-9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.compat.calls.stop_stream("CA...", "ST...");


```
