---
title: "create"
slug: /reference/cpp/rest/compat/tokens/create
description: Create a new API token.
max-toc-depth: 3
---

Create a new API token.

## **Parameters**

<ParamField path="Ttl" type="int" toc={true}>
  Time-to-live in seconds for the token.
</ParamField>

## **Returns**

`nlohmann::json` -- The created token resource.

## **Example**

```cpp {9-9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

token = client.compat.tokens.create(3600);


```
