---
title: "create"
slug: /reference/cpp/rest/compat/accounts/create
description: Create a new subproject (sub-account).
max-toc-depth: 3
---

Create a new subproject (sub-account).

## **Parameters**

<ParamField path="FriendlyName" type="std::string" toc={true}>
  A human-readable name for the subproject.
</ParamField>

## **Returns**

`nlohmann::json` -- The created account resource.

## **Example**

```cpp {9-9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

sub = client.compat.accounts.create("Marketing Team");


```
