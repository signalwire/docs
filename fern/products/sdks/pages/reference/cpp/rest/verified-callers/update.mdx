---
title: "update"
slug: /reference/cpp/rest/verified-callers/update
description: Update a verified caller ID.
max-toc-depth: 3
---

Update a verified caller ID. Uses PUT.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The caller ID resource ID.
</ParamField>

<ParamField path="**kwargs" type="nlohmann::json" toc={true}>
  Fields to update (e.g., `friendly_name`).
</ParamField>

## **Returns**

`nlohmann::json` -- The updated caller ID object.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

updated = client.verified_callers.update("caller-id", friendly_name="Main Office");
std::cout << "Updated: " + std::string(updated['id']) << std::endl;


```
