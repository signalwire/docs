---
title: "list"
slug: /reference/cpp/rest/verified-callers/list
description: List verified caller IDs in the project.
max-toc-depth: 3
---

List verified caller IDs in the project.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of verified caller objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.verified_callers.list();
for (const auto& caller : result.value("data", [])) {
    std::cout << caller{"phone_number"}, caller{"verification_state"} << std::endl;


```
