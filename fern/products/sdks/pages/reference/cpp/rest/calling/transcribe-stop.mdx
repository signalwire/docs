---
title: "transcribe_stop"
slug: /reference/cpp/rest/calling/transcribe-stop
description: Stop an active transcription on a call via REST.
max-toc-depth: 3
---

[transcribe]: /docs/sdks/reference/cpp/rest/calling/transcribe

Stop an active transcription.

## **Parameters**

<ParamField path="call_id" type="std::string" required={true} toc={true}>
  The ID of the call.
</ParamField>

<ParamField path="control_id" type="std::string" required={true} toc={true}>
  The control ID returned from [`transcribe()`][transcribe].
</ParamField>

## **Returns**

`nlohmann::json` — The API response confirming transcription was stopped.

## **Example**

```cpp {9-9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.calling.transcribe_stop("call-id-xxx", "ctrl-id");


```
