---
title: "tap_stop"
slug: /reference/cpp/rest/calling/tap-stop
description: Stop an active audio tap on a call via REST.
max-toc-depth: 3
---

[tap]: /docs/sdks/reference/cpp/rest/calling/tap

Stop an active audio tap.

## **Parameters**

<ParamField path="call_id" type="std::string" required={true} toc={true}>
  The ID of the call.
</ParamField>

<ParamField path="control_id" type="std::string" required={true} toc={true}>
  The control ID returned from [`tap()`][tap].
</ParamField>

## **Returns**

`nlohmann::json` — The API response confirming the tap was stopped.

## **Example**

```cpp {9-9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.calling.tap_stop("call-id-xxx", "ctrl-id");


```
