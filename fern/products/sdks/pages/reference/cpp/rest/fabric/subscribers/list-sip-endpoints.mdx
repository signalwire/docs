---
title: "list_sip_endpoints"
slug: /reference/cpp/rest/fabric/subscribers/list-sip-endpoints
description: List SIP endpoints for a subscriber.
max-toc-depth: 3
---

List SIP endpoints associated with a subscriber.

## **Parameters**

<ParamField path="subscriber_id" type="std::string" required={true} toc={true}>
  The unique identifier of the subscriber.
</ParamField>

<ParamField path="page_size" type="int" toc={true}>
  Number of results per page.
</ParamField>

<ParamField path="page_token" type="std::string" toc={true}>
  Pagination token for the next page of results.
</ParamField>

## **Returns**

`nlohmann::json` -- Paginated response containing SIP endpoints for the subscriber.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

response = client.fabric.subscribers.list_sip_endpoints("subscriber-id");
for (const auto& ep : response.value("data", [])) {
    std::cout << std::string(ep['username']) + ": " + std::string(ep['id']) << std::endl;


```
