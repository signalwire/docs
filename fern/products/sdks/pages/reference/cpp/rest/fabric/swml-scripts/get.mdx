---
title: "get"
slug: /reference/cpp/rest/fabric/swml-scripts/get
description: Retrieve a single SWML script resource.
max-toc-depth: 3
---

Retrieve a single SWML script resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the SWML script.
</ParamField>

## **Returns**

`nlohmann::json` -- The SWML script resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

item = client.fabric.swml_scripts.get("resource-id");
std::cout << "Swml Script: " + std::string(item['name']) + ", ID: " + std::string(item['id']) << std::endl;


```
