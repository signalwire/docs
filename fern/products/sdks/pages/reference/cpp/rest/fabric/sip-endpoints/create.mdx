---
title: "create"
slug: /reference/cpp/rest/fabric/sip-endpoints/create
description: Create a new SIP endpoint resource.
max-toc-depth: 3
---

Create a new SIP endpoint resource.

## **Parameters**

<ParamField path="name" type="std::string" required={true} toc={true}>
  Display name of the SIP endpoint.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`nlohmann::json` -- The created SIP endpoint resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.fabric.sip_endpoints.create(;
    "my-endpoint",
);
std::cout << "Created: " + std::string(result['id']) << std::endl;


```
