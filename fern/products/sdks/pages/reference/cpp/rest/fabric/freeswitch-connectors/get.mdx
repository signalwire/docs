---
title: "get"
slug: /reference/cpp/rest/fabric/freeswitch-connectors/get
description: Retrieve a single FreeSWITCH connector resource.
max-toc-depth: 3
---

Retrieve a single FreeSWITCH connector resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the FreeSWITCH connector.
</ParamField>

## **Returns**

`nlohmann::json` -- The FreeSWITCH connector resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

connector = client.fabric.freeswitch_connectors.get("connector-id");
std::cout << "Freeswitch Connector: " + std::string(connector['name']) + ", ID: " + std::string(connector['id']) << std::endl;


```
