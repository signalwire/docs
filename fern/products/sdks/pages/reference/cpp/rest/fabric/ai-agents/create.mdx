---
title: "create"
slug: /reference/cpp/rest/fabric/ai-agents/create
description: Create a new AI agent resource.
max-toc-depth: 3
---

Create a new AI agent resource.

## **Parameters**

<ParamField path="name" type="std::string" required={true} toc={true}>
  Display name of the AI agent.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`nlohmann::json` -- The created AI agent resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.fabric.ai_agents.create(;
    "support-bot", "ai_agent",
);
std::cout << "Created: " + std::string(result['id']) << std::endl;


```
