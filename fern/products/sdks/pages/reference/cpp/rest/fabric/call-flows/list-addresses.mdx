---
title: "list_addresses"
slug: /reference/cpp/rest/fabric/call-flows/list-addresses
description: List addresses associated with a call flow resource.
max-toc-depth: 3
---

List addresses associated with a call flow resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the call flow.
</ParamField>

## **Returns**

`nlohmann::json` -- List of addresses assigned to this call flow.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

addresses = client.fabric.call_flows.list_addresses("flow-id");
for (const auto& addr : addresses.value("data", [])) {
    std::cout << std::string(addr['display_name']) + ": " + std::string(addr['id']) << std::endl;


```
