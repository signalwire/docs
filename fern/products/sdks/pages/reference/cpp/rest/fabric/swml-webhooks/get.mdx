---
title: "get"
slug: /reference/cpp/rest/fabric/swml-webhooks/get
description: Retrieve a single SWML webhook resource.
max-toc-depth: 3
---

Retrieve a single SWML webhook resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the SWML webhook.
</ParamField>

## **Returns**

`nlohmann::json` -- The SWML webhook resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

webhook = client.fabric.swml_webhooks.get("webhook-id");
std::cout << "Swml Webhook: " + std::string(webhook['name']) + ", ID: " + std::string(webhook['id']) << std::endl;


```
