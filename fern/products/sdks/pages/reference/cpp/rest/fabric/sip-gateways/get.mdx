---
title: "get"
slug: /reference/cpp/rest/fabric/sip-gateways/get
description: Retrieve a single SIP gateway resource.
max-toc-depth: 3
---

Retrieve a single SIP gateway resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the SIP gateway.
</ParamField>

## **Returns**

`nlohmann::json` -- The SIP gateway resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

gateway = client.fabric.sip_gateways.get("gateway-id");
std::cout << "Sip Gateway: " + std::string(gateway['name']) + ", ID: " + std::string(gateway['id']) << std::endl;


```
