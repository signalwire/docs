---
title: "create_invite_token"
slug: /reference/cpp/rest/fabric/tokens/create-invite-token
description: Create an invite token for a subscriber.
max-toc-depth: 3
---

Create an invite token for a subscriber.

## **Parameters**

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`nlohmann::json` -- The created invite token.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

token = client.fabric.tokens.create_invite_token("subscriber-id");
std::cout << "Invite token: " + std::string(token['token']) << std::endl;


```
