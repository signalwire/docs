---
title: "get"
slug: /reference/cpp/rest/fabric/cxml-webhooks/get
description: Retrieve a single cXML webhook resource.
max-toc-depth: 3
---

Retrieve a single cXML webhook resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the cXML webhook.
</ParamField>

## **Returns**

`nlohmann::json` -- The cXML webhook resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

webhook = client.fabric.cxml_webhooks.get("webhook-id");
std::cout << "Cxml Webhook: " + std::string(webhook['name']) + ", ID: " + std::string(webhook['id']) << std::endl;


```
