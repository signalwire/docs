---
title: "get"
slug: /reference/cpp/rest/fabric/subscribers/get
description: Retrieve a single subscriber resource.
max-toc-depth: 3
---

Retrieve a single subscriber resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the subscriber.
</ParamField>

## **Returns**

`nlohmann::json` -- The subscriber resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

subscriber = client.fabric.subscribers.get("subscriber-id");
std::cout << "Subscriber: " + std::string(subscriber['name']) + ", ID: " + std::string(subscriber['id']) << std::endl;


```
