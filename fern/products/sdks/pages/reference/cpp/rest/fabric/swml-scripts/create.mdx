---
title: "create"
slug: /reference/cpp/rest/fabric/swml-scripts/create
description: Create a new SWML script resource.
max-toc-depth: 3
---

Create a new SWML script resource.

## **Parameters**

<ParamField path="name" type="std::string" required={true} toc={true}>
  Display name of the SWML script.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`nlohmann::json` -- The created SWML script resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.fabric.swml_scripts.create(;
    "my-item",
);
std::cout << "Created: " + std::string(result['id']) << std::endl;


```
