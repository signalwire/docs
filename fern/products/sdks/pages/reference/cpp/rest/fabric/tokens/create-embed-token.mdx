---
title: "create_embed_token"
slug: /reference/cpp/rest/fabric/tokens/create-embed-token
description: Create an embed authentication token.
max-toc-depth: 3
---

Create an embed authentication token.

## **Parameters**

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`nlohmann::json` -- The created embed token.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

token = client.fabric.tokens.create_embed_token("resource-id");
std::cout << "Embed token: " + std::string(token['token']) << std::endl;


```
