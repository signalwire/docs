---
title: "get"
slug: /reference/cpp/rest/fabric/conference-rooms/get
description: Retrieve a single conference room resource.
max-toc-depth: 3
---

Retrieve a single conference room resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the conference room.
</ParamField>

## **Returns**

`nlohmann::json` -- The conference room resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

room = client.fabric.conference_rooms.get("room-id");
std::cout << "Conference Room: " + std::string(room['name']) + ", ID: " + std::string(room['id']) << std::endl;


```
