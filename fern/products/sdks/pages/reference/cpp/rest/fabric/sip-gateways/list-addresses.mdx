---
title: "list_addresses"
slug: /reference/cpp/rest/fabric/sip-gateways/list-addresses
description: List addresses associated with a SIP gateway resource.
max-toc-depth: 3
---

List addresses associated with a SIP gateway resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the SIP gateway.
</ParamField>

## **Returns**

`nlohmann::json` -- List of addresses assigned to this resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

addresses = client.fabric.sip_gateways.list_addresses("gateway-id");
for (const auto& addr : addresses.value("data", [])) {
    std::cout << std::string(addr['display_name']) + ": " + std::string(addr['id']) << std::endl;


```
