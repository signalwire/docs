---
title: "delete"
slug: /reference/cpp/rest/fabric/sip-endpoints/delete
description: Delete a SIP endpoint resource.
max-toc-depth: 3
---

Delete a SIP endpoint resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the SIP endpoint to delete.
</ParamField>

## **Returns**

`nlohmann::json` -- Empty dict on success (HTTP 204).

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.fabric.sip_endpoints.delete("endpoint-id");
std::cout << "Sip Endpoint deleted" << std::endl;


```
