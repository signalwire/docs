---
title: "get"
slug: /reference/cpp/rest/fabric/relay-applications/get
description: Retrieve a single RELAY application resource.
max-toc-depth: 3
---

Retrieve a single RELAY application resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the RELAY application.
</ParamField>

## **Returns**

`nlohmann::json` -- The RELAY application resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

item = client.fabric.relay_applications.get("resource-id");
std::cout << "Relay Application: " + std::string(item['name']) + ", ID: " + std::string(item['id']) << std::endl;


```
