---
title: "create"
slug: /reference/cpp/rest/fabric/freeswitch-connectors/create
description: Create a new FreeSWITCH connector resource.
max-toc-depth: 3
---

Create a new FreeSWITCH connector resource.

## **Parameters**

<ParamField path="name" type="std::string" required={true} toc={true}>
  Display name of the FreeSWITCH connector.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`nlohmann::json` -- The created FreeSWITCH connector resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.fabric.freeswitch_connectors.create(;
    "my-connector",
);
std::cout << "Created: " + std::string(result['id']) << std::endl;


```
