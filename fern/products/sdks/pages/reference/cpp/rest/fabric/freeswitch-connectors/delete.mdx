---
title: "delete"
slug: /reference/cpp/rest/fabric/freeswitch-connectors/delete
description: Delete a FreeSWITCH connector resource.
max-toc-depth: 3
---

Delete a FreeSWITCH connector resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the FreeSWITCH connector to delete.
</ParamField>

## **Returns**

`nlohmann::json` -- Empty dict on success (HTTP 204).

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.fabric.freeswitch_connectors.delete("connector-id");
std::cout << "Freeswitch Connector deleted" << std::endl;


```
