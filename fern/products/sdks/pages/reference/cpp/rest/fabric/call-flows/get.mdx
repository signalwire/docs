---
title: "get"
slug: /reference/cpp/rest/fabric/call-flows/get
description: Retrieve a single call flow resource.
max-toc-depth: 3
---

Retrieve a single call flow resource.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the call flow.
</ParamField>

## **Returns**

`nlohmann::json` -- The call flow resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

flow = client.fabric.call_flows.get("flow-id");
std::cout << "Call Flow: " + std::string(flow['name']) + ", ID: " + std::string(flow['id']) << std::endl;


```
