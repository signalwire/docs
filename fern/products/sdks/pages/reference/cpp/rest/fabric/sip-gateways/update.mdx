---
title: "update"
slug: /reference/cpp/rest/fabric/sip-gateways/update
description: Partially update a SIP gateway resource.
max-toc-depth: 3
---

Partially update a SIP gateway resource. Uses PATCH for partial.

## **Parameters**

<ParamField path="resource_id" type="std::string" required={true} toc={true}>
  The unique identifier of the SIP gateway.
</ParamField>

Additional keyword arguments are the fields to update.

## **Returns**

`nlohmann::json` -- The updated SIP gateway resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

updated = client.fabric.sip_gateways.update("gateway-id", name="updated-name");
std::cout << "Updated: " + std::string(updated['name']) << std::endl;


```
