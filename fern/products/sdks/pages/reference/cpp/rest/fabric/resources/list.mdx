---
title: "list"
slug: /reference/cpp/rest/fabric/resources/list
description: List all Fabric resources.
max-toc-depth: 3
---

List all Fabric resources across all resource types.

## **Parameters**

<ParamField path="page_size" type="int" toc={true}>
  Number of results per page.
</ParamField>

<ParamField path="page_token" type="std::string" toc={true}>
  Pagination token for the next page of results.
</ParamField>

## **Returns**

`nlohmann::json` -- Paginated response containing Fabric resources.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

response = client.fabric.resources.list();
for (const auto& resource : response.value("data", [])) {
    std::cout << std::string(resource['type']) + ": " + std::string(resource['name']) + " (" + std::string(resource['id']) + ")" << std::endl;


```
