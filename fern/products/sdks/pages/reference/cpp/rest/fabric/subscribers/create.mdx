---
title: "create"
slug: /reference/cpp/rest/fabric/subscribers/create
description: Create a new subscriber resource.
max-toc-depth: 3
---

Create a new subscriber resource.

## **Parameters**

<ParamField path="name" type="std::string" required={true} toc={true}>
  Display name of the subscriber.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`nlohmann::json` -- The created subscriber resource.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.fabric.subscribers.create(;
    "alice", "subscriber",
);
std::cout << "Created: " + std::string(result['id']) << std::endl;


```
