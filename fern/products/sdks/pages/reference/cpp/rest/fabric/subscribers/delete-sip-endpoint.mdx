---
title: "delete_sip_endpoint"
slug: /reference/cpp/rest/fabric/subscribers/delete-sip-endpoint
description: Delete a SIP endpoint for a subscriber.
max-toc-depth: 3
---

Delete a SIP endpoint for a subscriber.

## **Parameters**

<ParamField path="subscriber_id" type="std::string" required={true} toc={true}>
  The unique identifier of the subscriber.
</ParamField>

<ParamField path="endpoint_id" type="std::string" required={true} toc={true}>
  The unique identifier of the SIP endpoint to delete.
</ParamField>

## **Returns**

`nlohmann::json` -- Empty dict on success (HTTP 204).

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

client.fabric.subscribers.delete_sip_endpoint("subscriber-id", "endpoint-id");
std::cout << "SIP endpoint deleted" << std::endl;


```
