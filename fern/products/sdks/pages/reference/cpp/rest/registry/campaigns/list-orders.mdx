---
title: "list_orders"
slug: /reference/cpp/rest/registry/campaigns/list-orders
description: List number assignment orders for a campaign.
max-toc-depth: 3
---

List number assignment orders for a campaign.

## **Parameters**

<ParamField path="campaign_id" type="std::string" required={true} toc={true}>
  The campaign resource ID.
</ParamField>

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of order objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

orders = client.registry.campaigns.list_orders("campaign-id");
for (const auto& order : orders.value("data", [])) {
    std::cout << order{"id"}, order{"status"} << std::endl;


```
