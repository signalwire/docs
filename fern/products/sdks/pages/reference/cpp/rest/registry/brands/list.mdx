---
title: "list"
slug: /reference/cpp/rest/registry/brands/list
description: List registered 10DLC brands.
max-toc-depth: 3
---

List registered 10DLC brands.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of brand objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

brands = client.registry.brands.list();
for (const auto& brand : brands.value("data", [])) {
    std::cout << brand{"display_name"}, brand{"entity_type"} << std::endl;


```
