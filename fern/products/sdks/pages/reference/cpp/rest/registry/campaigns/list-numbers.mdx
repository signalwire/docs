---
title: "list_numbers"
slug: /reference/cpp/rest/registry/campaigns/list-numbers
description: List phone numbers assigned to a campaign.
max-toc-depth: 3
---

List phone numbers assigned to a campaign.

## **Parameters**

<ParamField path="campaign_id" type="std::string" required={true} toc={true}>
  The campaign resource ID.
</ParamField>

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of number objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

numbers = client.registry.campaigns.list_numbers("campaign-id");
for (const auto& n : numbers.value("data", [])) {
    std::cout << n{"number"}, n{"id"} << std::endl;


```
