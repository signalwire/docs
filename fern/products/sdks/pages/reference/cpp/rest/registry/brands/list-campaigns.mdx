---
title: "list_campaigns"
slug: /reference/cpp/rest/registry/brands/list-campaigns
description: List campaigns under a brand.
max-toc-depth: 3
---

List messaging campaigns under a brand.

## **Parameters**

<ParamField path="brand_id" type="std::string" required={true} toc={true}>
  The brand resource ID.
</ParamField>

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of campaign objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

campaigns = client.registry.brands.list_campaigns("brand-id");
for (const auto& c : campaigns.value("data", [])) {
    std::cout << c{"description"}, c{"usecase"} << std::endl;


```
