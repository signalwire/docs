---
title: "list"
slug: /reference/cpp/rest/datasphere/list
description: List documents in the project.
max-toc-depth: 3
---

List documents in the project.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of document objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

result = client.datasphere.documents.list();
for (const auto& doc : result.value("data", [])) {
    std::cout << doc{"name"}, doc{"id"} << std::endl;


```
