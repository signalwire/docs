---
title: "list_events"
slug: /reference/cpp/rest/logs/voice/list-events
description: List events for a specific voice call.
max-toc-depth: 3
---

List events for a specific voice call. Events include state transitions
(ringing, answered, ended), media events, and other call lifecycle data.

## **Parameters**

<ParamField path="log_id" type="std::string" required={true} toc={true}>
  The voice log ID.
</ParamField>

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of event objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

events = client.logs.voice.list_events("voice-log-id");
for (const auto& event : events.value("data", [])) {
    std::cout << event{"type"}, event{"timestamp"} << std::endl;


```
