---
title: "list"
slug: /reference/cpp/rest/logs/voice/list
description: List voice call log entries.
max-toc-depth: 3
---

List voice call log entries.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of voice log objects.

## **Example**

```cpp {9}
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

calls = client.logs.voice.list(20);
for (const auto& call : calls.value("data", [])) {
    std::cout << call{"from"}, "->", call{"to"}, call{"duration"} << std::endl;


```
