---
title: "Conference Logs"
slug: /reference/cpp/rest/logs/conferences
description: "Query conference log entries."
max-toc-depth: 3
---

[restclient]: /docs/sdks/reference/cpp/rest/client

Query conference log entries. Conference logs provide a read-only view of
conference activity in your project.

Access via `client.logs.conferences` on a [`RestClient`][restclient] instance.

```cpp
#include <signalwire/rest/rest_client.hpp>

rest::RestClient client();


```

<Note>
Conference logs only support `list()`. There is no `get()` method for
individual conference entries.
</Note>

### list

List conference log entries.

## **Parameters**

<ParamField path="**params" type="nlohmann::json" toc={true}>
  Optional query parameters for filtering and pagination.
</ParamField>

## **Returns**

`nlohmann::json` -- JSON response containing a `data` list of conference log objects.

## **Example**

```cpp
#include <signalwire/rest/rest_client.hpp>

client = RestClient(;
);

confs = client.logs.conferences.list();
for (const auto& conf : confs.value("data", [])) {
    std::cout << conf{"name"}, conf{"status"} << std::endl;


```
