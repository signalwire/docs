---
title: "renderSwml"
slug: /reference/php/agents/swml-service/render-swml
description: Render the complete SWML response for a request.
max-toc-depth: 3
---

[ref-class]: /docs/sdks/reference/php/agents/swml-service

Render the complete SWML response for a request.

## **Parameters**

<ParamField path="requestBody" type="?array" required={false} toc={true}>
  The incoming HTTP request body.
  Defaults to `null`.
</ParamField>

<ParamField path="headers" type="array" required={false} toc={true}>
  HTTP headers to include.
  Defaults to `[]`.
</ParamField>

## **Returns**

`array` -- The result array.

## **Example**

```php
<?php
use SignalWire\SWML\Service;

$service = new Service(name: "my-service", route: "/swml");

$result = $service->renderSwml(
    requestBody: [],
    headers: ["Content-Type" => "application/json"]
);
```
