---
title: "getRequiredEnvVars"
slug: /reference/php/agents/skill-base/get-required-env-vars
description: Get the required env vars of the skill.
max-toc-depth: 3
---

[ref-class]: /docs/sdks/reference/php/agents/skill-base

Get the required env vars of the skill.

## **Parameters**

This method takes no parameters.

## **Returns**

`array` -- The result array.

## **Example**

```php
<?php
use SignalWire\Skills\SkillBase;

// SkillBase is abstract; extend it to create a custom skill
class PlantSkill extends SkillBase {
    // ...
}
$skillBase = new PlantSkill();

$result = $skillBase->getRequiredEnvVars();
```
