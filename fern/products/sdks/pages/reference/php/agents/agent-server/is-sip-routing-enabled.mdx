---
title: "isSipRoutingEnabled"
slug: /reference/php/agents/agent-server/is-sip-routing-enabled
description: Check whether sip routing enabled is enabled.
max-toc-depth: 3
---

[ref-class]: /docs/sdks/reference/php/agents/agent-server

Check whether sip routing enabled is enabled.

## **Parameters**

This method takes no parameters.

## **Returns**

`bool` -- Whether the condition is true.

## **Example**

```php
<?php
use SignalWire\Server\AgentServer;

$agentServer = new AgentServer();

$result = $agentServer->isSipRoutingEnabled();
```
