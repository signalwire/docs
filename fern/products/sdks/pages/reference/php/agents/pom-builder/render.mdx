---
title: "render"
sidebar-title: "render"
slug: /reference/php/agents/pom-builder/render
description: Render the POM as Markdown or XML.
max-toc-depth: 3
---

Render the entire POM as a Markdown string. Sections become `##` headings,
subsections become `###` headings, and bullets render as Markdown lists.

## **Returns**

`string` -- The rendered Markdown text.

---

Render the entire POM as an XML string. Useful when the AI benefits from
XML-structured prompts.

## **Returns**

`string` -- The rendered XML text.

## **Example**

```php {7,17}
<?php
use SignalWire\Core\PomBuilder\PomBuilder;

$pom = new PomBuilder();
$pom->addSection("Role", body: "You are a helpful assistant.");
$pom->addSection("Rules", bullets: ["Be concise", "Be accurate"]);

// Render as Markdown
$md = $pom->render_markdown();
echo $md;
// ## Role
// You are a helpful assistant.
//
// ## Rules
// - Be concise
// - Be accurate

// Render as XML
$xml = $pom->render_xml();
echo $xml;


```
