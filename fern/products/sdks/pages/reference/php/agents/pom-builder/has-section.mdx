---
title: "hasSection / getSection"
slug: /reference/php/agents/pom-builder/has-section
description: Check if a section exists and retrieve it by title.
max-toc-depth: 3
---

## **has_section**

Check if a section with the given title exists.

### **Parameters**

<ParamField path="title" type="string" required={true} toc={true}>
  Section title to check.
</ParamField>

### **Returns**

`bool` -- `True` if the section exists, `False` otherwise.

---

## **get_section**

Get a section by title for direct manipulation.

### **Parameters**

<ParamField path="title" type="string" required={true} toc={true}>
  Section title to look up.
</ParamField>

### **Returns**

`?Section` -- The POM Section object, or `null` if not found.

## **Example**

```php
<?php
use SignalWire\Core\PomBuilder\PomBuilder;

$pom = new PomBuilder();
$pom->addSection("Role", body: "You are a helpful assistant.");

if ($pom->has_section("Role")) {
    $section = $pom->get_section("Role");
    echo "Found section: {$section->title}";
}

echo $pom->has_section("Missing");


```
