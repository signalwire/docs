---
title: "fallbackOutput"
slug: /reference/php/agents/data-map/fallback-output
description: Set the fallback output when no expressions match.
max-toc-depth: 3
---

[ref-class]: /docs/sdks/reference/php/agents/data-map

Set the fallback output when no expressions match.

## **Parameters**

<ParamField path="result" type="mixed" required={true} toc={true}>
  The fallback result value.
</ParamField>

## **Returns**

`self` -- The the data map instance for method chaining.

## **Example**

```php
<?php
use SignalWire\DataMap\DataMap;

$dataMap = new DataMap("lookup-plant");

$dataMap->fallbackOutput("value");
```
