---
title: "get"
slug: /reference/php/rest/compat/conferences/get
description: Retrieve a single conference by SID.
max-toc-depth: 3
---

Retrieve a single conference by SID.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The conference SID.
</ParamField>

## **Returns**

`dict` -- The conference resource.

## **Example**

```php {9-9}
<?php
use SignalWire\REST\RestClient;

$client = new RestClient(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
);

$conf = $client->compat->conferences->get("CF...");


```
