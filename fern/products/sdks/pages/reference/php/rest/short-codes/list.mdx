---
title: "list"
slug: /reference/php/rest/short-codes/list
description: List short codes in the project.
max-toc-depth: 3
---

List short codes in the project.

## **Parameters**

<ParamField path="**params" type="dict" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`dict` -- JSON response containing a `data` list of short code objects.

## **Example**

```php {9}
<?php
use SignalWire\REST\RestClient;

$client = new RestClient(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
);

$result = $client->shortCodes->list();
foreach ($result["data"] ?? [] as $code) {
    echo $code["short_code"] ?? null, $code["id"] ?? null;
}


```
