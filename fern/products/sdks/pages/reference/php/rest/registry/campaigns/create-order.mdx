---
title: "createOrder"
slug: /reference/php/rest/registry/campaigns/create-order
description: Create a number assignment order for a campaign.
max-toc-depth: 3
---

Create a number assignment order to assign phone numbers to a campaign.

## **Parameters**

<ParamField path="campaign_id" type="string" required={true} toc={true}>
  The campaign resource ID.
</ParamField>

<ParamField path="phone_number_ids" type="array" toc={true}>
  List of phone number resource IDs to assign.
</ParamField>

<ParamField path="**kwargs" type="dict" toc={true}>
  Additional order parameters.
</ParamField>

## **Returns**

`dict` -- The newly created order object.

## **Example**

```php {9}
<?php
use SignalWire\REST\RestClient;

$client = new RestClient(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
);

$order = $client->registry->campaigns->create_order(
    "campaign-id",
    phone_number_ids: ["number-id-1", "number-id-2"],
);
echo "Order ID:", $order["id"] ?? null;


```
