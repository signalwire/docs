---
title: "listAddresses"
slug: /reference/php/rest/fabric/call-flows/list-addresses
description: List addresses associated with a call flow resource.
max-toc-depth: 3
---

List addresses associated with a call flow resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the call flow.
</ParamField>

## **Returns**

`dict` -- List of addresses assigned to this call flow.

## **Example**

```php {9}
<?php
use SignalWire\REST\RestClient;

$client = new RestClient(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
);

$addresses = $client->fabric->call_flows->list_addresses("flow-id");
foreach ($addresses["data"] ?? [] as $addr) {
    echo "{$addr['display_name']}: {$addr['id']}";
}


```
