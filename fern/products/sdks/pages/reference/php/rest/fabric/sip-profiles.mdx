---
title: "sipProfiles"
slug: /reference/php/rest/fabric/sip-profiles
description: Access the SIP Profiles resource.
max-toc-depth: 3
---

[ref-class]: /docs/sdks/reference/php/rest/fabric

Access the SIP Profiles resource.

## **Parameters**

This method takes no parameters.

## **Returns**

`CrudResource` -- A CRUD resource instance for performing operations.

## **Example**

```php
<?php
use SignalWire\REST\Namespaces\Fabric;

// Access via RestClient
$client = new \SignalWire\REST\RestClient(
    space: "your-space",
    projectId: "your-project-id",
    token: "your-api-token"
);
$fabric = $client->fabric();

$result = $fabric->sipProfiles();
```
