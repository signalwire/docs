---
title: "get"
slug: /reference/php/rest/verified-callers/get
description: Retrieve a specific verified caller ID.
max-toc-depth: 3
---

Retrieve a specific verified caller ID.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The caller ID resource ID.
</ParamField>

## **Returns**

`dict` -- The caller ID object.

## **Example**

```php {9-10}
<?php
use SignalWire\REST\RestClient;

$client = new RestClient(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
);

$caller = $client->verifiedCallers->get("caller-id");
echo $caller["phone_number"] ?? null, $caller["verification_state"] ?? null;


```
