---
title: "unhold"
slug: /reference/php/relay/call/unhold
description: Release a call from hold.
max-toc-depth: 3
---

[calling-call-hold]: /docs/sdks/reference/php/relay/call#events
[call-events]: /docs/sdks/reference/php/relay/call#events

Release the call from hold, resuming normal audio between the parties.

<Info>
This method emits [`calling.call.hold`][calling-call-hold] events. See [Call Events][call-events] for payload details.
</Info>

## **Parameters**

None.

## **Returns**

`dict` -- Server response confirming the unhold operation.

## **Example**

```php {21}
<?php
// import asyncio
use SignalWire\Relay\Client;

$client = new Client(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],
);

// Handler: $client->onCall()
function handleCall($call)
{
    $call->answer();
    $call->play([["type" => "tts", "text": "Please hold while I look that up."]]);
}

    // Put the call on hold
    $call->hold();

    // Do some processing...
    $asyncio->sleep(5);

    $result = $await $call->unhold();
    echo "Call resumed from hold: {$result}";
    $call->play([["type" => "tts", "text": "Thanks for holding. I have your answer."]]);

$client->run();


```
