---
title: "stop"
slug: /reference/php/relay/actions/play-action/stop
description: Stop audio playback immediately.
max-toc-depth: 3
---

Stop playback immediately.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```php {16}
<?php
use SignalWire\Relay\Client;

$client = new Client(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],
);

// Handler: $client->onCall()
function handleCall($call)
{
    $call->answer();
    $action = $await $call->play([["type" => "tts", "text": "Hello!"]]);
}

    // Stop playback early
    $action->stop();

$client->run();


```
