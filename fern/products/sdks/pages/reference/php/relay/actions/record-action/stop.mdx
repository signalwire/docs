---
title: "stop"
slug: /reference/php/relay/actions/record-action/stop
description: Stop recording immediately.
max-toc-depth: 3
---

Stop recording immediately. The recording file becomes available.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```php {16}
<?php
use SignalWire\Relay\Client;

$client = new Client(
    project: "your-project-id",
    token: "your-api-token",
    host: "your-space.signalwire.com",
    contexts: ["default"],
);

// Handler: $client->onCall()
function handleCall($call)
{
    $call->answer();
    $action = $await $call->record(audio: ["direction" => "both", "format": "wav"]);
}

    // Stop recording early
    $action->stop();

$client->run();


```
