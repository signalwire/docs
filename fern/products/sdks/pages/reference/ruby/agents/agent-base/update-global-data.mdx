---
title: "update_global_data"
slug: /reference/ruby/agents/agent-base/update-global-data
description: Update the global data dictionary with new values.
max-toc-depth: 3
---

[set-global-data]: /docs/sdks/reference/ruby/agents/agent-base/set-global-data
[ref-agentbase]: /docs/sdks/reference/ruby/agents/agent-base

Update the global data with new values. Functionally identical to
[`set_global_data()`][set-global-data] --
both merge the provided dictionary into the existing global data.

## **Parameters**

<ParamField path="data" type="Hash" required={true} toc={true}>
  Dictionary of key/value pairs to merge into global data.
</ParamField>

## **Returns**

[`AgentBase`][ref-agentbase] -- Returns self for method chaining.

## **Example**

```ruby {10}
require 'signalwire'

agent = SignalWire::AgentBase.new(name: "support", route: "/support")
agent.set_prompt_text("You are a helpful assistant.")

def authenticate(args, raw_data: nil)
    user = {"name": "Alice", "tier": "gold"}  # Replace with your user lookup
    agent.update_global_data({
        "authenticated": true,
        "user_name": user["name"],
        "user_tier": user["tier"]
    })
    return SignalWire::Swaig::FunctionResult.new("Welcome back, #{user['name']}.")

agent.serve()
```
