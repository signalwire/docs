---
title: "add_hints"
slug: /reference/ruby/agents/agent-base/add-hints
description: Add multiple speech recognition hints at once.
max-toc-depth: 3
---

[ref-agentbase]: /docs/sdks/reference/ruby/agents/agent-base

Add multiple speech recognition hints at once.

## **Parameters**

<ParamField path="hints" type="Array\<String\>" required={true} toc={true}>
  A list of words or phrases.
</ParamField>

## **Returns**

[`AgentBase`][ref-agentbase] -- Returns self for method chaining.

## **Example**

```ruby {5}
require 'signalwire'

agent = SignalWire::AgentBase.new(name: "support", route: "/support")
agent.set_prompt_text("You are a helpful assistant.")
agent.add_hints(["SWML", "SWAIG", "FreeSWITCH"])
agent.serve()
```
