---
title: "list"
slug: /reference/ruby/rest/fabric/sip-endpoints/list
description: List SIP endpoint resources.
max-toc-depth: 3
---

List SIP endpoint resources in the project.

## **Parameters**

<ParamField path="page_size" type="Integer" toc={true}>
  Number of results per page.
</ParamField>

<ParamField path="page_token" type="String" toc={true}>
  Pagination token for the next page of results.
</ParamField>

## **Returns**

`Hash` -- Paginated response containing SIP endpoint resources.

## **Example**

```ruby {9}
require 'signalwire/rest/rest_client'

client = SignalWire::REST::RestClient.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

response = client.fabric.sip_endpoints.list()
for endpoint in response.fetch("data", [])
    puts("#{endpoint['name']}: #{endpoint['id']}")
```
