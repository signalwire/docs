---
title: "list_addresses"
slug: /reference/ruby/rest/fabric/call-flows/list-addresses
description: List addresses associated with a call flow resource.
max-toc-depth: 3
---

List addresses associated with a call flow resource.

## **Parameters**

<ParamField path="resource_id" type="String" required={true} toc={true}>
  The unique identifier of the call flow.
</ParamField>

## **Returns**

`Hash` -- List of addresses assigned to this call flow.

## **Example**

```ruby {9}
require 'signalwire/rest/rest_client'

client = SignalWire::REST::RestClient.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

addresses = client.fabric.call_flows.list_addresses("flow-id")
for addr in addresses.fetch("data", [])
    puts("#{addr['display_name']}: #{addr['id']}")
```
