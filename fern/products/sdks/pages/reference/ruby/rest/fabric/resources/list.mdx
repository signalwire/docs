---
title: "list"
slug: /reference/ruby/rest/fabric/resources/list
description: List all Fabric resources.
max-toc-depth: 3
---

List all Fabric resources across all resource types.

## **Parameters**

<ParamField path="page_size" type="Integer" toc={true}>
  Number of results per page.
</ParamField>

<ParamField path="page_token" type="String" toc={true}>
  Pagination token for the next page of results.
</ParamField>

## **Returns**

`Hash` -- Paginated response containing Fabric resources.

## **Example**

```ruby {9}
require 'signalwire/rest/rest_client'

client = SignalWire::REST::RestClient.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

response = client.fabric.resources.list()
for resource in response.fetch("data", [])
    puts("#{resource['type']}: #{resource['name']} (#{resource['id']})")
```
