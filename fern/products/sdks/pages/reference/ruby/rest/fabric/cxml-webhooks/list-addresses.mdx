---
title: "list_addresses"
slug: /reference/ruby/rest/fabric/cxml-webhooks/list-addresses
description: List addresses associated with a cXML webhook resource.
max-toc-depth: 3
---

List addresses associated with a cXML webhook resource.

## **Parameters**

<ParamField path="resource_id" type="String" required={true} toc={true}>
  The unique identifier of the cXML webhook.
</ParamField>

## **Returns**

`Hash` -- List of addresses assigned to this resource.

## **Example**

```ruby {9}
require 'signalwire/rest/rest_client'

client = SignalWire::REST::RestClient.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

addresses = client.fabric.cxml_webhooks.list_addresses("webhook-id")
for addr in addresses.fetch("data", [])
    puts("#{addr['display_name']}: #{addr['id']}")
```
