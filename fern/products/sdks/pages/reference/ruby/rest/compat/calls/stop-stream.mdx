---
title: "stop_stream"
slug: /reference/ruby/rest/compat/calls/stop-stream
description: Stop a media stream on a call.
max-toc-depth: 3
---

Stop a media stream on a call.

## **Parameters**

<ParamField path="call_sid" type="String" required={true} toc={true}>
  The SID of the call.
</ParamField>

<ParamField path="stream_sid" type="String" required={true} toc={true}>
  The SID of the stream to stop.
</ParamField>

## **Returns**

`Hash` -- The updated stream resource.

## **Example**

```ruby {9-9}
require 'signalwire/rest/rest_client'

client = SignalWire::REST::RestClient.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.compat.calls.stop_stream("CA...", "ST...")
```
