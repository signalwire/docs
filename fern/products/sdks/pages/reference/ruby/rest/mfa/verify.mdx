---
title: "verify"
slug: /reference/ruby/rest/mfa/verify
description: Verify an MFA code submitted by the user.
max-toc-depth: 3
---

Verify an MFA code submitted by the user.

## **Parameters**

<ParamField path="request_id" type="String" required={true} toc={true}>
  The MFA request ID returned from `sms()` or `call()`.
</ParamField>

<ParamField path="token" type="String" required={true} toc={true}>
  The verification code entered by the user.
</ParamField>

<ParamField path="**kwargs" type="dict" toc={true}>
  Additional parameters.
</ParamField>

## **Returns**

`Hash` -- Verification result indicating whether the code is valid.

## **Example**

```ruby {9}
require 'signalwire/rest/rest_client'

client = SignalWire::REST::RestClient.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

result = client.mfa.verify("your-mfa-request-id", token: "123456")
if result["success"]
    puts("Verification successful")
else
    puts("Invalid code")
```
