---
title: "stop"
slug: /reference/ruby/relay/actions/stream-action/stop
description: Stop audio streaming.
max-toc-depth: 3
---

Stop audio streaming.

## **Returns**

`Hash` -- Server acknowledgment.

## **Example**

```ruby {19}
require 'signalwire/relay/client'

client = SignalWire::Relay::Client.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
def handle_call(call)
    call.answer()
    action = call.stream(
        url="wss://example.com/audio-stream",
        track="inbound",
    )

    # Stop the stream when done
    action.stop()

client.run()
```
