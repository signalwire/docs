---
title: "pause"
slug: /reference/ruby/relay/actions/play-action/pause
description: Pause audio playback.
max-toc-depth: 3
---

Pause audio playback. The playback position is preserved.

## **Returns**

`Hash` -- Server acknowledgment.

## **Example**

```ruby {18}
require 'signalwire/relay/client'

client = SignalWire::Relay::Client.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
def handle_call(call)
    call.answer()
    action = call.play(
        [{"type": "audio", "url": "https://example.com/hold-music.mp3"}],
        loop=0,
    )

    action.pause()
    # ... do something ...
    action.resume()

client.run()
```
