---
title: "stop"
slug: /reference/ruby/relay/actions/fax-action/stop
description: Stop the fax operation.
max-toc-depth: 3
---

Stop the fax operation.

## **Returns**

`Hash` -- Server acknowledgment.

## **Example**

```ruby {19}
require 'signalwire/relay/client'

client = SignalWire::Relay::Client.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
def handle_call(call)
    call.answer()
    action = call.send_fax(
        document="https://example.com/invoice.pdf",
        identity="+15559876543",
    )

    # Stop the fax operation
    action.stop()

client.run()
```
