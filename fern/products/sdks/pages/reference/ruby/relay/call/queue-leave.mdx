---
title: "queue_leave"
slug: /reference/ruby/relay/call/queue-leave
description: Remove a call from a queue.
max-toc-depth: 3
---

[calling-call-queue]: /docs/sdks/reference/ruby/relay/call#events
[call-events]: /docs/sdks/reference/ruby/relay/call#events

Remove the call from a queue.

<Info>
This method emits [`calling.call.queue`][calling-call-queue] events. See [Call Events][call-events] for payload details.
</Info>

## **Parameters**

<ParamField path="queue_name" type="String" required={true} toc={true}>
  Name of the queue to leave.
</ParamField>

<ParamField path="control_id" type="String, nil" toc={true}>
  Custom control ID. Auto-generated if not provided.
</ParamField>

<ParamField path="queue_id" type="String, nil" toc={true}>
  Specific queue ID to leave (if the call is in multiple queues with the
  same name).
</ParamField>

<ParamField path="status_url" type="String, nil" toc={true}>
  URL to receive queue status webhooks.
</ParamField>

## **Returns**

`Hash` -- Server response confirming the call left the queue.

## **Example**

```ruby {20}
# import asyncio
require 'signalwire/relay/client'

client = SignalWire::Relay::Client.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
def handle_call(call)
    call.answer()

    # Enter the queue
    call.queue_enter(queue_name: "support")

    # Remove from queue after a timeout
    asyncio.sleep(300)  # 5 minute max wait
    call.queue_leave(queue_name: "support")
    call.play([{"type": "tts", "text": "No agents available. Please try again later."}])
    call.hangup()

client.run()
```
