---
title: "receive_fax"
slug: /reference/ruby/relay/call/receive-fax
description: "Receive a fax on a call."
max-toc-depth: 3
---

[faxaction]: /docs/sdks/reference/ruby/relay/actions
[calling-call-fax]: /docs/sdks/reference/ruby/relay/call#events
[call-events]: /docs/sdks/reference/ruby/relay/call#events

Start receiving a fax on the call. Returns a
[`FaxAction`][faxaction] that resolves when the
fax is fully received or an error occurs.

<Info>
This method emits [`calling.call.fax`][calling-call-fax] events. See [Call Events][call-events] for payload details.
</Info>

## **Parameters**

<ParamField path="control_id" type="String, nil" toc={true}>
  Custom control ID. Auto-generated if not provided.
</ParamField>

<ParamField path="on_completed" type="Proc, nil" toc={true}>
  Callback invoked when the fax reception completes. The event contains
  the received document URL and page count.
</ParamField>

## **Returns**

[`FaxAction`][faxaction] -- An action handle with
`stop()` and `wait()` methods.

## **Example**

```ruby {14}
require 'signalwire/relay/client'

client = SignalWire::Relay::Client.new(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
def handle_call(call)
    call.answer()

    action = call.receive_fax()
    event = action.wait()

    fax_result = event.params.fetch("fax", {})
    document_url = fax_result.fetch("document", "")
    pages = fax_result.fetch("pages", 0)
    puts("Received fax: #{pages} pages, document: #{document_url}")
    call.hangup()

client.run()
```
