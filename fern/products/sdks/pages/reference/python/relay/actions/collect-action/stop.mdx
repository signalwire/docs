---
title: "stop"
slug: /reference/python/relay/actions/collect-action/stop
description: Stop the play-and-collect operation.
max-toc-depth: 3
---

Stop the play-and-collect operation.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```python {11-16}
from signalwire.relay import RelayClient

client = RelayClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
async def handle_call(call):
    await call.answer()
    action = await call.play_and_collect(
        media=[{"type": "tts", "text": "Press 1 for sales, 2 for support."}],
        collect={"digits": {"max": 1, "digit_timeout": 5.0}},
    )

    # Cancel the operation
    await action.stop()

client.run()
```
