---
title: "end"
slug: /reference/python/rest/calling/end
description: End an active call via REST.
max-toc-depth: 3
---

End an active call.

## **Parameters**

<ParamField path="call_id" type="str" required={true} toc={true}>
  The ID of the call to end.
</ParamField>

<Markdown src="/snippets/sdks/rest/calling/_end-params.mdx" />

## **Returns**

`dict` — The API response confirming the call was ended.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.calling.end(call_id="call-id-xxx", reason="hangup")
```
