---
title: "user_event"
slug: /reference/python/rest/calling/user-event
description: Send custom user-defined events on an active call via REST.
max-toc-depth: 3
---

Send a custom user-defined event on an active call. User events allow you
to pass arbitrary data between call control applications and event handlers.
Listeners registered for user events on the RELAY side will receive the
payload you send here.

## **Parameters**

<ParamField path="call_id" type="str" required={true} toc={true}>
  The ID of the call to send the event on.
</ParamField>

<Markdown src="/snippets/sdks/rest/calling/_user-event-params.mdx" />

## **Returns**

`dict` — The API response confirming the event was sent.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.calling.user_event(
    call_id="call-id-xxx",
    event={"action": "order.confirmed", "order_id": "ORD-12345", "amount": 49.99},
)
```
