---
title: "record_resume"
slug: /reference/python/rest/calling/record-resume
description: Resume a paused recording on a call via REST.
max-toc-depth: 3
---

[record]: /docs/sdks/reference/python/rest/calling/record

Resume a paused recording.

<EndpointSchemaSnippet endpoint="POST /api/calling/calls" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /api/calling/calls" />

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.calling.record_resume(call_id="call-id-xxx", control_id="ctrl-id")
```
