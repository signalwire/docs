---
title: "get"
slug: /reference/python/rest/recordings/get
description: Retrieve a specific recording.
max-toc-depth: 3
---

Retrieve a specific recording.

## **Parameters**

<ParamField path="recording_id" type="str" required={true} toc={true}>
  The recording resource ID.
</ParamField>

## **Returns**

`dict` -- The recording object, including download URL and metadata.

## **Example**

```python
rec = client.recordings.get("recording-id")
print(rec.get("duration"), rec.get("url"))
```
