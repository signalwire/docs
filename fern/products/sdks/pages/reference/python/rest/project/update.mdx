---
title: "update"
slug: /reference/python/rest/project/update
description: Update an existing API token.
max-toc-depth: 3
---

### tokens.update

Update an existing API token. Uses PATCH.

## **Parameters**

<ParamField path="token_id" type="str" required={true} toc={true}>
  The token resource ID.
</ParamField>

<ParamField path="**kwargs" type="dict" toc={true}>
  Fields to update (e.g., `name`).
</ParamField>

## **Returns**

`dict` -- The updated token object.

## **Example**

```python
client.project.tokens.update("token-id", name="renamed-token")
```
