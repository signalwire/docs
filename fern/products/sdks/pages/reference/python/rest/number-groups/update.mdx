---
title: "update"
slug: /reference/python/rest/number-groups/update
description: Update a number group.
max-toc-depth: 3
---

Update a number group. Uses PUT to replace fields.

## **Parameters**

<ParamField path="resource_id" type="str" required={true} toc={true}>
  The group resource ID.
</ParamField>

<ParamField path="**kwargs" type="dict" toc={true}>
  Fields to update (e.g., `name`).
</ParamField>

## **Returns**

`dict` -- The updated group object.

## **Example**

```python
updated = client.number_groups.update("group-id", name="Support Numbers")
print(updated["name"])
```
