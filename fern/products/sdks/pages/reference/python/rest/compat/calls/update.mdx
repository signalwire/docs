---
title: "update"
slug: /reference/python/rest/compat/calls/update
description: Update an active call.
max-toc-depth: 3
---

Update an active call (e.g., redirect to new LAML, end the call). Uses POST (Twilio convention).

## **Parameters**

<ParamField path="sid" type="str" required={true} toc={true}>
  The call SID.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/calls/_update-params.mdx" />

## **Returns**

`dict` -- The updated call resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.compat.calls.update("CA...", Status="completed")
```
