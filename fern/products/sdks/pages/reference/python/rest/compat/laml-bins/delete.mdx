---
title: "delete"
slug: /reference/python/rest/compat/laml-bins/delete
description: Delete a LAML bin.
max-toc-depth: 3
---

Delete a LAML bin.

## **Parameters**

<ParamField path="resource_id" type="str" required={true} toc={true}>
  The LAML bin SID to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.compat.laml_bins.delete("LB...")
```
