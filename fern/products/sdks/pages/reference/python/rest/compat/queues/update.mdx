---
title: "update"
slug: /reference/python/rest/compat/queues/update
description: Update a queue.
max-toc-depth: 3
---

Update a queue's configuration. Uses POST (Twilio convention).

## **Parameters**

<ParamField path="sid" type="str" required={true} toc={true}>
  The queue SID.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/queues/_create-update-params.mdx" />

## **Returns**

`dict` -- The updated queue resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.compat.queues.update("QU...", FriendlyName="Support Queue", MaxSize=200)
```
