---
title: "update_participant"
slug: /reference/python/rest/compat/conferences/update-participant
description: Update a participant in a conference (mute or hold).
max-toc-depth: 3
---

Update a participant in a conference (e.g., mute or hold).

## **Parameters**

<ParamField path="conference_sid" type="str" required={true} toc={true}>
  The SID of the conference.
</ParamField>

<ParamField path="call_sid" type="str" required={true} toc={true}>
  The call SID of the participant.
</ParamField>

<Markdown src="/snippets/sdks/rest/compat/conferences/_update-participant-params.mdx" />

## **Returns**

`dict` -- The updated participant resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.compat.conferences.update_participant("CF...", "CA...", Muted=True)
```
