---
title: "create"
slug: /reference/python/rest/compat/accounts/create
description: Create a new subproject (sub-account).
max-toc-depth: 3
---

Create a new subproject (sub-account).

## **Parameters**

<Markdown src="/snippets/sdks/rest/compat/accounts/_create-update-params.mdx" />

## **Returns**

`dict` -- The created account resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

sub = client.compat.accounts.create(FriendlyName="Marketing Team")
```
