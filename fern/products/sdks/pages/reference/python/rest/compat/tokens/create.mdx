---
title: "create"
slug: /reference/python/rest/compat/tokens/create
description: Create a new API token.
max-toc-depth: 3
---

Create a new API token.

## **Parameters**

<Markdown src="/snippets/sdks/rest/compat/tokens/_create-params.mdx" />

## **Returns**

`dict` -- The created token resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

token = client.compat.tokens.create(name="my-token", permissions=["voice", "messaging"])
```
