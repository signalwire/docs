---
title: "stop_stream"
slug: /reference/python/rest/compat/conferences/stop-stream
description: Stop a media stream on a conference.
max-toc-depth: 3
---

Stop a media stream on a conference.

## **Parameters**

<ParamField path="conference_sid" type="str" required={true} toc={true}>
  The SID of the conference.
</ParamField>

<ParamField path="stream_sid" type="str" required={true} toc={true}>
  The SID of the stream to stop.
</ParamField>

## **Returns**

`dict` -- The updated stream resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.compat.conferences.stop_stream("CF...", "ST...")
```
