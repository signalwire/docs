---
title: "get"
slug: /reference/python/rest/compat/queues/get
description: Retrieve a single queue by SID.
max-toc-depth: 3
---

Retrieve a single queue by SID.

## **Parameters**

<ParamField path="resource_id" type="str" required={true} toc={true}>
  The queue SID.
</ParamField>

## **Returns**

`dict` -- The queue resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

queue = client.compat.queues.get("QU...")
```
