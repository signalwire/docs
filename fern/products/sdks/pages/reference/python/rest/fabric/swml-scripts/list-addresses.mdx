---
title: "list_addresses"
slug: /reference/python/rest/fabric/swml-scripts/list-addresses
description: List addresses associated with a SWML script resource.
max-toc-depth: 3
---

List addresses associated with a SWML script resource.

## **Parameters**

<ParamField path="resource_id" type="str" required={true} toc={true}>
  The unique identifier of the SWML script.
</ParamField>

## **Returns**

`dict` -- List of addresses assigned to this resource.

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

addresses = client.fabric.swml_scripts.list_addresses("resource-id")
for addr in addresses.get("data", []):
    print(f"{addr['display_name']}: {addr['id']}")
```
