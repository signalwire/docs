---
title: "delete"
slug: /reference/python/rest/fabric/freeswitch-connectors/delete
description: Delete a FreeSWITCH connector resource.
max-toc-depth: 3
---

Delete a FreeSWITCH connector resource.

## **Parameters**

<ParamField path="resource_id" type="str" required={true} toc={true}>
  The unique identifier of the FreeSWITCH connector to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success (HTTP 204).

## **Example**

```python
from signalwire.rest import RestClient

client = RestClient()
client.fabric.freeswitch_connectors.delete("resource-id")
```
