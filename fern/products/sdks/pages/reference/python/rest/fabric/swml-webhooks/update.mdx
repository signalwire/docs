---
title: "update"
slug: /reference/python/rest/fabric/swml-webhooks/update
description: Partially update a SWML webhook resource.
max-toc-depth: 3
---

Partially update a SWML webhook resource. Uses PATCH for partial.

## **Parameters**

<ParamField path="resource_id" type="str" required={true} toc={true}>
  The unique identifier of the SWML webhook.
</ParamField>

Additional keyword arguments are the fields to update.

## **Returns**

`dict` -- The updated SWML webhook resource.

## **Example**

```python
from signalwire.rest import RestClient

client = RestClient()
client.fabric.swml_webhooks.update("resource-id", name="updated-name")
```
