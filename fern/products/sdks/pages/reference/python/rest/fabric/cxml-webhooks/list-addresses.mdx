---
title: "list_addresses"
slug: /reference/python/rest/fabric/cxml-webhooks/list-addresses
description: List addresses associated with a cXML webhook resource.
max-toc-depth: 3
---

List addresses associated with a cXML webhook resource.

## **Parameters**

<ParamField path="resource_id" type="str" required={true} toc={true}>
  The unique identifier of the cXML webhook.
</ParamField>

## **Returns**

`dict` -- List of addresses assigned to this resource.

## **Example**

```python {8-10}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

addresses = client.fabric.cxml_webhooks.list_addresses("webhook-id")
for addr in addresses.get("data", []):
    print(f"{addr['display_name']}: {addr['id']}")
```
