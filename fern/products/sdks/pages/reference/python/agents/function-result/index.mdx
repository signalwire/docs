---
title: "FunctionResult"
slug: /reference/python/agents/function-result
description: Fluent interface for returning responses and actions from SWAIG tool functions.
max-toc-depth: 3
---

`FunctionResult` is the return type for all SWAIG tool functions. It wraps a response
message (text for the AI to speak) and an ordered list of actions (transfers, SMS,
data updates, context switches, and more). Every method returns `self`, so you can
chain calls into a single fluent expression.

Returned from functions defined with the [`@tool()`](/docs/sdks/reference/python/agents/agent-base/tool-decorator) decorator
or [`define_tool()`](/docs/sdks/reference/python/agents/agent-base/define-tool) on
[`AgentBase`](/docs/sdks/reference/python/agents/agent-base).

## **Properties**

<ParamField path="response" type="str" default="''" toc={true}>
  Text the AI speaks back to the caller after the function executes.
</ParamField>

<ParamField path="action" type="list[dict[str, Any]]" default="[]" toc={true}>
  Ordered list of action objects to execute. Actions run sequentially in the
  order they were added.
</ParamField>

<ParamField path="post_process" type="bool" default="False" toc={true}>
  When `True`, the AI speaks the response and takes one more conversational turn
  with the user before executing actions. When `False` (default), actions execute
  immediately after the response.
</ParamField>

## **Example**

```python {8-20}
from signalwire_agents import AgentBase
from signalwire_agents.core.function_result import SwaigFunctionResult

agent = AgentBase(name="my-agent", route="/agent")

@agent.tool(name="transfer_to_billing", description="Transfer the caller to billing")
def transfer_to_billing(args, raw_data):
    return (
        SwaigFunctionResult(
            "I'll transfer you to billing. Anything else first?",
            post_process=True
        )
        .update_global_data({"transferred": True})
        .send_sms(
            to_number="+15551234567",
            from_number="+15559876543",
            body="You are being transferred to billing."
        )
        .connect("+15551234567", final=True)
    )

agent.serve()
```

## **Fluent Chaining Pattern**

Every method on `FunctionResult` returns `self`, so you build complex responses
in a single expression. Actions execute in the order they are added.

<Warning>
Terminal actions like `connect(final=True)` and `hangup()` end the call flow.
Place them **last** in the chain so that preceding actions (data updates, SMS, etc.)
have a chance to execute.
</Warning>

```python {9-18}
from signalwire_agents import AgentBase
from signalwire_agents.core.function_result import SwaigFunctionResult

agent = AgentBase(name="my-agent", route="/agent")

@agent.tool(name="transfer_call", description="Transfer the call")
def transfer_call(args, raw_data):
    # Data update + SMS execute before the terminal transfer
    return (
        SwaigFunctionResult("Transferring you now.")
        .update_global_data({"transferred": True})
        .send_sms(
            to_number="+15551234567",
            from_number="+15559876543",
            body="Your call is being transferred."
        )
        .connect("+15551234567", final=True)  # terminal — goes last
    )

agent.serve()
```

## **Methods**

### Core

- [`set_response()`](/docs/sdks/reference/python/agents/function-result/set-response) — Set or replace the response text.
- [`set_post_process()`](/docs/sdks/reference/python/agents/function-result/set-post-process) — Enable or disable post-processing.
- [`add_action()`](/docs/sdks/reference/python/agents/function-result/add-action) — Append a raw action.
- [`add_actions()`](/docs/sdks/reference/python/agents/function-result/add-actions) — Append multiple raw actions.
- [`to_dict()`](/docs/sdks/reference/python/agents/function-result/to-dict) — Serialize to SWAIG format.

### Call Control

- [`connect()`](/docs/sdks/reference/python/agents/function-result/connect) — Transfer or connect the call.
- [`hangup()`](/docs/sdks/reference/python/agents/function-result/hangup) — End the call.
- [`hold()`](/docs/sdks/reference/python/agents/function-result/hold) — Put the call on hold.
- [`swml_transfer()`](/docs/sdks/reference/python/agents/function-result/swml-transfer) — Transfer to a SWML endpoint.

### Speech

- [`say()`](/docs/sdks/reference/python/agents/function-result/say) — Speak text immediately.
- [`wait_for_user()`](/docs/sdks/reference/python/agents/function-result/wait-for-user) — Control waiting for user input.
- [`stop()`](/docs/sdks/reference/python/agents/function-result/stop) — Stop agent execution.

### Media

- [`play_background_file()`](/docs/sdks/reference/python/agents/function-result/play-background-file) — Play background audio.
- [`stop_background_file()`](/docs/sdks/reference/python/agents/function-result/stop-background-file) — Stop background audio.
- [`record_call()`](/docs/sdks/reference/python/agents/function-result/record-call) — Start call recording.
- [`stop_record_call()`](/docs/sdks/reference/python/agents/function-result/stop-record-call) — Stop call recording.
- [`tap()`](/docs/sdks/reference/python/agents/function-result/tap) — Stream call audio to an endpoint.
- [`stop_tap()`](/docs/sdks/reference/python/agents/function-result/stop-tap) — Stop a media tap.

### Data

- [`update_global_data()`](/docs/sdks/reference/python/agents/function-result/update-global-data) — Set global session data.
- [`remove_global_data()`](/docs/sdks/reference/python/agents/function-result/remove-global-data) — Remove global session data keys.
- [`set_metadata()`](/docs/sdks/reference/python/agents/function-result/set-metadata) — Set function-scoped metadata.
- [`remove_metadata()`](/docs/sdks/reference/python/agents/function-result/remove-metadata) — Remove function-scoped metadata keys.

### Context Navigation

- [`swml_change_step()`](/docs/sdks/reference/python/agents/function-result/swml-change-step) — Change conversation step.
- [`swml_change_context()`](/docs/sdks/reference/python/agents/function-result/swml-change-context) — Switch conversation context.
- [`switch_context()`](/docs/sdks/reference/python/agents/function-result/switch-context) — Advanced context switch with prompt replacement.

### Events

- [`swml_user_event()`](/docs/sdks/reference/python/agents/function-result/swml-user-event) — Fire a custom user event.
- [`simulate_user_input()`](/docs/sdks/reference/python/agents/function-result/simulate-user-input) — Inject simulated user speech.

### Functions

- [`toggle_functions()`](/docs/sdks/reference/python/agents/function-result/toggle-functions) — Enable or disable SWAIG functions.
- [`enable_functions_on_timeout()`](/docs/sdks/reference/python/agents/function-result/enable-functions-on-timeout) — Allow function calls on speaker timeout.

### Hints

- [`add_dynamic_hints()`](/docs/sdks/reference/python/agents/function-result/add-dynamic-hints) — Add speech recognition hints.
- [`clear_dynamic_hints()`](/docs/sdks/reference/python/agents/function-result/clear-dynamic-hints) — Clear all dynamic hints.

### Settings

- [`set_end_of_speech_timeout()`](/docs/sdks/reference/python/agents/function-result/set-end-of-speech-timeout) — Adjust end-of-speech silence timeout.
- [`set_speech_event_timeout()`](/docs/sdks/reference/python/agents/function-result/set-speech-event-timeout) — Adjust speech event timeout.
- [`update_settings()`](/docs/sdks/reference/python/agents/function-result/update-settings) — Update AI runtime settings.
- [`enable_extensive_data()`](/docs/sdks/reference/python/agents/function-result/enable-extensive-data) — Send full data for one turn.
- [`replace_in_history()`](/docs/sdks/reference/python/agents/function-result/replace-in-history) — Control history appearance.

### SMS

- [`send_sms()`](/docs/sdks/reference/python/agents/function-result/send-sms) — Send an SMS or MMS message.

### Payment

- [`pay()`](/docs/sdks/reference/python/agents/function-result/pay) — Collect a credit card payment.
- [`create_payment_prompt()`](/docs/sdks/reference/python/agents/function-result/create-payment-prompt) — Build a payment prompt object.
- [`create_payment_action()`](/docs/sdks/reference/python/agents/function-result/create-payment-action) — Build a payment prompt action.
- [`create_payment_parameter()`](/docs/sdks/reference/python/agents/function-result/create-payment-parameter) — Build a payment parameter.

### SIP

- [`sip_refer()`](/docs/sdks/reference/python/agents/function-result/sip-refer) — SIP REFER transfer.

### Rooms and Conferences

- [`join_room()`](/docs/sdks/reference/python/agents/function-result/join-room) — Join a RELAY room.
- [`join_conference()`](/docs/sdks/reference/python/agents/function-result/join-conference) — Join an audio conference.

### SWML and RPC

- [`execute_swml()`](/docs/sdks/reference/python/agents/function-result/execute-swml) — Execute raw SWML.
- [`execute_rpc()`](/docs/sdks/reference/python/agents/function-result/execute-rpc) — Execute a generic RPC method.
- [`rpc_dial()`](/docs/sdks/reference/python/agents/function-result/rpc-dial) — Dial out via RPC.
- [`rpc_ai_message()`](/docs/sdks/reference/python/agents/function-result/rpc-ai-message) — Inject a message into another call's AI.
- [`rpc_ai_unhold()`](/docs/sdks/reference/python/agents/function-result/rpc-ai-unhold) — Release another call from hold.
