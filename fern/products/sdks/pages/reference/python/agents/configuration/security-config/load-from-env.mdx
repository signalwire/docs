---
title: "load_from_env"
slug: /reference/python/agents/configuration/security-config/load-from-env
description: Reload all settings from environment variables.
max-toc-depth: 3
---

Reload all settings from environment variables. This is called automatically
during construction but can be called again to pick up runtime changes.

## **Returns**

`None`

## **Example**

```python {7}
import os
from signalwire.core.security_config import SecurityConfig

security = SecurityConfig()

os.environ["SWML_SSL_ENABLED"] = "true"
security.load_from_env()

print(f"SSL enabled: {security.ssl_enabled}")  # True
```
