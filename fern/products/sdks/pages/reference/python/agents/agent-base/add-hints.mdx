---
title: "add_hints"
slug: /reference/python/agents/agent-base/add-hints
description: Add multiple speech recognition hints at once.
max-toc-depth: 3
---

Add multiple speech recognition hints at once.

## **Parameters**

<ParamField path="hints" type="list[str]" required={true} toc={true}>
  A list of words or phrases.
</ParamField>

## **Returns**

`AgentBase` -- Returns self for method chaining.

## **Example**

```python {4}
from signalwire_agents import AgentBase

agent = AgentBase(name="support", route="/support")
agent.add_hints(["SWML", "SWAIG", "FreeSWITCH"])
agent.serve()
```
