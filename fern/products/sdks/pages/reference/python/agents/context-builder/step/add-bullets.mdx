---
title: "add_bullets"
slug: /reference/python/agents/context-builder/step/add-bullets
description: Add a POM section with bullet points to the step.
max-toc-depth: 3
---

Add a POM section with bullet points to the step. Cannot be used with `set_text()`.

## **Parameters**

<ParamField path="title" type="str" required={true} toc={true}>
  Section heading.
</ParamField>

<ParamField path="bullets" type="list[str]" required={true} toc={true}>
  List of bullet point strings.
</ParamField>

## **Returns**

`Step` -- Self for method chaining.

## **Example**

```python
step = ctx.add_step("collect_info")
step.add_section("Task", "Collect the caller's account information.")
step.add_bullets("Required Information", [
    "Full legal name",
    "Account number (10 digits)",
    "Reason for calling"
])
```
