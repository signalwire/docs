---
title: "GetMedia"
slug: /reference/go/rest/compat/faxes/get-media
description: Retrieve a specific media item from a fax.
max-toc-depth: 3
---

Retrieve a specific media item from a fax.

## **Parameters**

<ParamField path="fax_sid" type="string" required={true} toc={true}>
  The SID of the fax.
</ParamField>

<ParamField path="media_sid" type="string" required={true} toc={true}>
  The SID of the media item.
</ParamField>

## **Returns**

`dict` -- The media resource.

## **Example**

```go {9-9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    item := client.compat.faxes.get_media("FX...", "ME...")
}
```
