---
title: "delete"
slug: /reference/go/rest/compat/transcriptions/delete
description: Delete a transcription.
max-toc-depth: 3
---

Delete a transcription.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The transcription SID to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success.

## **Example**

```go {9-9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    client.compat.transcriptions.delete("TR...")
}
```
