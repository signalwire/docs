---
title: "get"
slug: /reference/go/rest/compat/calls/get
description: Retrieve a single call by SID.
max-toc-depth: 3
---

Retrieve a single call by SID.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The call SID.
</ParamField>

## **Returns**

`dict` -- The call resource.

## **Example**

```go {9-9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    call := client.compat.calls[]string{"CA..."}
}
```
