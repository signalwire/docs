---
title: "get"
slug: /reference/go/rest/compat/laml-bins/get
description: Retrieve a single LAML bin by SID.
max-toc-depth: 3
---

Retrieve a single LAML bin by SID.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The LAML bin SID.
</ParamField>

## **Returns**

`dict` -- The LAML bin resource.

## **Example**

```go {9-9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    laml_bin := client.compat.laml_bins[]string{"LB..."}
}
```
