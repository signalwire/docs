---
title: "get"
slug: /reference/go/rest/compat/conferences/get
description: Retrieve a single conference by SID.
max-toc-depth: 3
---

Retrieve a single conference by SID.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The conference SID.
</ParamField>

## **Returns**

`dict` -- The conference resource.

## **Example**

```go {9-9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    conf := client.compat.conferences[]string{"CF..."}
}
```
