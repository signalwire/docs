---
title: "get"
slug: /reference/go/rest/compat/phone-numbers/get
description: Retrieve a single phone number by SID.
max-toc-depth: 3
---

Retrieve a single phone number by SID.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The phone number SID.
</ParamField>

## **Returns**

`dict` -- The phone number resource.

## **Example**

```go {9-9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    number := client.compat.phone_numbers[]string{"PN..."}
}
```
