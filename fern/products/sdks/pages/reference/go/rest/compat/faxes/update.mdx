---
title: "update"
slug: /reference/go/rest/compat/faxes/update
description: Update a fax resource.
max-toc-depth: 3
---

Update a fax resource.

## **Parameters**

<ParamField path="sid" type="string" required={true} toc={true}>
  The fax SID.
</ParamField>

## **Returns**

`dict` -- The updated fax resource.

## **Example**

```go {9-9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    client.compat.faxes.update("FX...")
}
```
