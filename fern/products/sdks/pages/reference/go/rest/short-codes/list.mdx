---
title: "list"
slug: /reference/go/rest/short-codes/list
description: List short codes in the project.
max-toc-depth: 3
---

List short codes in the project.

## **Parameters**

<ParamField path="**params" type="dict" toc={true}>
  Optional query parameters to filter and paginate results.
</ParamField>

## **Returns**

`dict` -- JSON response containing a `data` list of short code objects.

## **Example**

```go {9}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    result := client.short_codes.list()
    for _, code := range result[]string{"data"} {
        fmt.Println(code[]string{"short_code"}, code[]string{"id"})
}
```
