---
title: "MFA"
slug: /reference/go/rest/mfa
description: "Multi-factor authentication via SMS and voice."
max-toc-depth: 3
---

[restclient]: /docs/sdks/reference/go/rest/client
[sms]: /docs/sdks/reference/go/rest/mfa/sms
[call]: /docs/sdks/reference/go/rest/mfa/call
[verify]: /docs/sdks/reference/go/rest/mfa/verify

Send and verify multi-factor authentication (MFA) codes via SMS or voice call.
The typical flow is: request a code with `Sms()` or `Call()`, then confirm it
with `Verify()`.

Access via `client.mfa` on a [`RestClient`][restclient] instance.

```go
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    request := client.mfa.sms(to="+15551234567", from_="+15559876543")
    fmt.Println(request[]string{"id"})
}
```

## **Methods**

<CardGroup cols={3}>
  <Card title="sms" href="/docs/sdks/reference/go/rest/mfa/sms">
    Send an MFA verification code via SMS.
  </Card>
  <Card title="call" href="/docs/sdks/reference/go/rest/mfa/call">
    Send an MFA verification code via voice call.
  </Card>
  <Card title="verify" href="/docs/sdks/reference/go/rest/mfa/verify">
    Verify an MFA code submitted by the user.
  </Card>
</CardGroup>
