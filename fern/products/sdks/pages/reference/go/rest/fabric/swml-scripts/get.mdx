---
title: "get"
slug: /reference/go/rest/fabric/swml-scripts/get
description: Retrieve a single SWML script resource.
max-toc-depth: 3
---

Retrieve a single SWML script resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the SWML script.
</ParamField>

## **Returns**

`dict` -- The SWML script resource.

## **Example**

```go {9}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    item := client.fabric.swml_scripts[]string{"resource-id"}
    fmt.Println(fmt.Sprintf("Swml Script: %v, ID: %v", item['name'], item['id']))
}
```
