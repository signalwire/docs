---
title: "delete"
slug: /reference/go/rest/fabric/cxml-applications/delete
description: Delete a cXML application resource.
max-toc-depth: 3
---

Delete a cXML application resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the cXML application to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success (HTTP 204).

## **Example**

```go {9}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    client.fabric.cxml_applications.delete("app-id")
    fmt.Println("cXML application deleted")
}
```
