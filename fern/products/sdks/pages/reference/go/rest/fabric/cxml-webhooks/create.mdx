---
title: "create"
slug: /reference/go/rest/fabric/cxml-webhooks/create
description: Create a new cXML webhook resource.
max-toc-depth: 3
---

Create a new cXML webhook resource.

## **Parameters**

<ParamField path="name" type="string" required={true} toc={true}>
  Display name of the cXML webhook.
</ParamField>

Additional keyword arguments are passed directly to the API as the request body.

## **Returns**

`dict` -- The created cXML webhook resource.

## **Example**

```go {9}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    result := client.fabric.cxml_webhooks.create(
        name := "my-webhook",
    )
    fmt.Println(fmt.Sprintf("Created: %v", result['id']))
}
```
