---
title: "update"
slug: /reference/go/rest/fabric/subscribers/update
description: Replace a subscriber resource.
max-toc-depth: 3
---

Replace a subscriber resource. Uses PUT, so the full resource body must be provided.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the subscriber.
</ParamField>

Additional keyword arguments are the fields for the replacement resource body.

## **Returns**

`dict` -- The updated subscriber resource.

## **Example**

```go {9}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    updated := client.fabric.subscribers.update("subscriber-id", name="bob")
    fmt.Println(fmt.Sprintf("Updated: %v", updated['name']))
}
```
