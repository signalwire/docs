---
title: "delete"
slug: /reference/go/rest/fabric/freeswitch-connectors/delete
description: Delete a FreeSWITCH connector resource.
max-toc-depth: 3
---

Delete a FreeSWITCH connector resource.

## **Parameters**

<ParamField path="resource_id" type="string" required={true} toc={true}>
  The unique identifier of the FreeSWITCH connector to delete.
</ParamField>

## **Returns**

`dict` -- Empty dict on success (HTTP 204).

## **Example**

```go {9}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/rest"
    "log"
)

func main() {
    client := RestClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
    )

    client.fabric.freeswitch_connectors.delete("connector-id")
    fmt.Println("Freeswitch Connector deleted")
}
```
