---
title: "HasConfig"
slug: /reference/go/agents/configuration/config-loader/has-config
description: Check whether a configuration file was successfully loaded.
max-toc-depth: 3
---

Check whether a configuration file was successfully loaded.

## **Returns**

`bool` -- `true` if a config file was found and loaded.

## **Example**

```go {4}
package main

import "fmt"

func main() {
    loader := ConfigLoader()
    if loader.has_config():
        fmt.Println(fmt.Sprintf("Loaded config from: %v", loader.GetConfigFile()))
    else:
        fmt.Println("No config file found, using defaults")
}
```
