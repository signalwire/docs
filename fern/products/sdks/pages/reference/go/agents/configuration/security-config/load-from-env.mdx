---
title: "LoadFromEnv"
slug: /reference/go/agents/configuration/security-config/load-from-env
description: Reload all settings from environment variables.
max-toc-depth: 3
---

Reload all settings from environment variables. This is called automatically
during construction but can be called again to pick up runtime changes.

## **Returns**

`nil`

## **Example**

```go {7}
package main

import "fmt"

func main() {
    import os

    security := SecurityConfig()

    os.environ[]string{"SWML_SSL_ENABLED"} = "true"
    security.load_from_env()

    fmt.Println(fmt.Sprintf("SSL enabled: %v", security.ssl_enabled))
}
```
