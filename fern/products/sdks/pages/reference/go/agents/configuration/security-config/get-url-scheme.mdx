---
title: "GetUrlScheme"
slug: /reference/go/agents/configuration/security-config/get-url-scheme
description: Get the URL scheme based on SSL configuration.
max-toc-depth: 3
---

Get the URL scheme based on SSL configuration.

## **Returns**

`str` -- `"https"` if SSL is enabled, `"http"` otherwise.

## **Example**

```go {4}
package main

import "fmt"

func main() {
    security := SecurityConfig()
    scheme := security.get_url_scheme()

    fmt.Println(fmt.Sprintf("URL scheme: %v")  # "http" (default", scheme))
}
```
