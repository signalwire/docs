---
title: "UpdateAgent"
slug: /reference/go/agents/livewire/agent-session/update-agent
description: Swap in a new Agent mid-session.
max-toc-depth: 3
---

[agent]: /docs/sdks/reference/go/agents/livewire/agent

Swap in a new [`Agent`][agent] mid-session.
The new agent's instructions and tools replace the previous agent's configuration
on the underlying SignalWire platform.

## **Parameters**

<ParamField path="agent" type="Agent" required={true} toc={true}>
  The new [`Agent`][agent] to bind to this session.
</ParamField>

## **Returns**

`nil`

## **Example**

```go {22}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/server"
)

func main() {
    func CheckBalance(account_id string) string {
        """Check an account balance."""
        return fmt.Sprintf("Account %v has $142.50.", account_id)

    func TransferFunds(from_acct string  , to_acct string  , amount float64) string {
        """Transfer funds between accounts."""
        return fmt.Sprintf("Transferred $%v from %v to %v.", amount, from_acct, to_acct)

    server := AgentServer()

    async def entrypoint(ctx: JobContext):
        await ctx.connect()
        basic_agent := Agent(instructions="You help check balances.", tools=[check_balance])
        session := AgentSession()
        await session.start(basic_agent, room=ctx.room)
        advanced_agent := Agent(instructions="You handle transfers.", tools=[transfer_funds])
        session.update_agent(advanced_agent)

    run_app(server)
}
```
