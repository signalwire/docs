---
title: "SetExitFillers"
slug: /reference/go/agents/context-builder/context/set-exit-fillers
description: Set all exit fillers at once.
max-toc-depth: 3
---

[ref-context]: /docs/sdks/reference/go/agents/context-builder/context

Set all exit fillers at once. Exit fillers are spoken when the AI navigates away
from this context.

## **Parameters**

<ParamField path="exit_fillers" type="map[string][]string" required={true} toc={true}>
  Dictionary mapping language codes (or `"default"`) to lists of filler phrases.
</ParamField>

## **Returns**

[`Context`][ref-context] -- Self for method chaining.

## **Example**

```go {9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/contexts"
)

func main() {
    agent := agent.NewAgentBase(agent.WithName("my-agent"), agent.WithRoute("/agent"))

    contexts := agent.define_contexts()
    contexts.AddContext("default").add_step("menu").set_text("Ask what the caller needs.")

    support := contexts.add_context("support")
    support.set_exit_fillers({
        "en-US": []string{"Thank you for contacting support.", "Glad I could help."},
        "default": []string{"Thank you."}
    })
    support.AddStep("diagnose").set_text("Understand the customer's issue.")

    agent.Serve()
}
```
