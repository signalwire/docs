---
title: "SetConsolidate"
slug: /reference/go/agents/context-builder/context/set-consolidate
description: Set whether to consolidate conversation history when entering this context.
max-toc-depth: 3
---

[ref-context]: /docs/sdks/reference/go/agents/context-builder/context

Set whether to consolidate (summarize) conversation history when entering this
context. Reduces token usage while preserving key information.

## **Parameters**

<ParamField path="consolidate" type="bool" required={true} toc={true}>
  Whether to consolidate previous conversation on entry.
</ParamField>

## **Returns**

[`Context`][ref-context] -- Self for method chaining.

## **Example**

```go {9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/contexts"
)

func main() {
    agent := agent.NewAgentBase(agent.WithName("my-agent"), agent.WithRoute("/agent"))

    contexts := agent.define_contexts()
    contexts.AddContext("default").add_step("menu").set_text("Ask what the caller needs.")

    support := contexts.add_context("support")
    support.SetConsolidate(true)
    support.SetSystemPrompt("You are a patient technical support engineer.")
    support.AddStep("diagnose").set_text("Understand the customer's issue.")

    agent.Serve()
}
```
