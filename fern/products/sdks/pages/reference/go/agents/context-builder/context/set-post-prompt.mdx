---
title: "SetPostPrompt"
slug: /reference/go/agents/context-builder/context/set-post-prompt
description: Override the post-prompt text while this context is active.
max-toc-depth: 3
---

[ref-context]: /docs/sdks/reference/go/agents/context-builder/context

Override the post-prompt text while this context is active.

## **Parameters**

<ParamField path="post_prompt" type="string" required={true} toc={true}>
  Post-prompt text for this context.
</ParamField>

## **Returns**

[`Context`][ref-context] -- Self for method chaining.

## **Example**

```go {9}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/contexts"
)

func main() {
    agent := agent.NewAgentBase(agent.WithName("my-agent"), agent.WithRoute("/agent"))

    contexts := agent.define_contexts()
    contexts.AddContext("default").add_step("menu").set_text("Ask what the caller needs.")

    billing := contexts.add_context("billing")
    billing.SetPostPrompt("Summarize the billing issue and resolution.")
    billing.AddStep("review").set_text("Review the caller's account and recent charges.")

    agent.Serve()
}
```
