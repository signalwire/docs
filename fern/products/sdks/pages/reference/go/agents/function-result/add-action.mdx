---
title: "AddAction"
slug: /reference/go/agents/function-result/add-action
description: Append a raw action to the FunctionResult action list.
max-toc-depth: 3
---

[connect]: /docs/sdks/reference/go/agents/function-result/connect
[hangup]: /docs/sdks/reference/go/agents/function-result/hangup
[say]: /docs/sdks/reference/go/agents/function-result/say
[functionresult]: /docs/sdks/reference/go/agents/function-result

Append a raw action to the action list. Prefer the named convenience methods
([`Connect()`][connect],
[`Hangup()`][hangup],
[`Say()`][say], etc.)
for common operations.

## **Parameters**

<ParamField path="name" type="string" required={true} toc={true}>
  Action type identifier (e.g., `"hold"`, `"hangup"`, `"set_global_data"`).
</ParamField>

<ParamField path="data" type="interface{}" required={true} toc={true}>
  Action payload. Can be a string, boolean, dict, or list depending on the action type.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```go {11-12}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/swaig"
)

func main() {
    agent := agent.NewAgentBase(agent.WithName("my-agent"), agent.WithRoute("/agent"))
    agent.SetPromptText("You are a helpful assistant.")

    agent.DefineTool(agent.ToolDefinition{
        Name:        "custom_action",
        Description: "Process a custom action",
        Handler: func(args map[string]any, rawData map[string]any) *swaig.FunctionResult {
        return (
            FunctionResult("Processing your request.")
            .add_action("set_global_data", {"status": "active"})
            .add_action("hold", 60)
        )

    agent.Serve()
}
```
