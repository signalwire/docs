---
title: "ExecuteSwml"
slug: /reference/go/agents/function-result/execute-swml
description: Execute a raw SWML document as an action.
max-toc-depth: 3
---

[connect]: /docs/sdks/reference/go/agents/function-result/connect
[record-call]: /docs/sdks/reference/go/agents/function-result/record-call
[send-sms]: /docs/sdks/reference/go/agents/function-result/send-sms
[functionresult]: /docs/sdks/reference/go/agents/function-result
[ref-swmlbuilder]: /docs/sdks/reference/go/agents/swml-builder

Execute a raw SWML document as an action. This is the escape hatch for advanced
use cases that are not covered by the named convenience methods.

<Note>
Most use cases are covered by the specific action methods
([`Connect()`][connect],
[`RecordCall()`][record-call],
[`SendSms()`][send-sms], etc.).
Use `ExecuteSwml()` only when you need SWML features not available through
convenience methods.
</Note>

## **Parameters**

<ParamField path="swml_content" type="interface{}" required={true} toc={true}>
  SWML content in one of three formats:
  - `str` — raw SWML JSON text (parsed internally)
  - `dict` — SWML data structure
  - SWML object — any object with a `.to_dict()` method (e.g., a [`SWMLBuilder`][ref-swmlbuilder] instance)
</ParamField>

<ParamField path="transfer" type="bool" default="False" toc={true}>
  When `true`, the call exits the agent after the SWML executes. When `false`,
  the SWML executes inline and the agent continues.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```go {20}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/swaig"
)

func main() {
    agent := agent.NewAgentBase(agent.WithName("my-agent"), agent.WithRoute("/agent"))
    agent.SetPromptText("You are a helpful assistant.")

    agent.DefineTool(agent.ToolDefinition{
        Name:        "play_announcement",
        Description: "Play an announcement",
        Handler: func(args map[string]any, rawData map[string]any) *swaig.FunctionResult {
        swml_doc := {
            "version": "1.0.0",
            "sections": {
                "main": [
                    {"play": {"url": "https://example.com/announcement.mp3"}},
                    {"hangup": {}}
                ]
            }
        }
        return (
            FunctionResult()
            .execute_swml(swml_doc, transfer=false)
        )

    agent.Serve()
}
```
