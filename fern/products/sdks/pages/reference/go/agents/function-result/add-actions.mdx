---
title: "AddActions"
slug: /reference/go/agents/function-result/add-actions
description: Append multiple raw actions to the FunctionResult action list.
max-toc-depth: 3
---

[functionresult]: /docs/sdks/reference/go/agents/function-result

Append multiple raw actions at once. Each action dictionary is added to the
action list in order.

## **Parameters**

<ParamField path="actions" type="[]map[string]interface{}" required={true} toc={true}>
  List of action dictionaries to append. Each dictionary should map a single
  action name to its payload.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```go {11}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/swaig"
)

func main() {
    agent := agent.NewAgentBase(agent.WithName("my-agent"), agent.WithRoute("/agent"))
    agent.SetPromptText("You are a helpful assistant.")

    agent.DefineTool(agent.ToolDefinition{
        Name:        "batch_actions",
        Description: "Process multiple actions at once",
        Handler: func(args map[string]any, rawData map[string]any) *swaig.FunctionResult {
        return (
            FunctionResult("Processing your request.")
            .add_actions([
                {"set_global_data": {"key": "value"}},
                {"hold": 60},
                {"say": "Please wait."}
            ])
        )

    agent.Serve()
}
```
