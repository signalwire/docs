---
title: "RemoveMetadata"
slug: /reference/go/agents/function-result/remove-metadata
description: Remove one or more keys from the current function's metadata store.
max-toc-depth: 3
---

[functionresult]: /docs/sdks/reference/go/agents/function-result

Remove one or more keys from the current function's metadata store.

## **Parameters**

<ParamField path="keys" type="interface{}" required={true} toc={true}>
  A single key string or a list of key strings to remove from metadata.
</ParamField>

## **Returns**

[`FunctionResult`][functionresult] — self, for chaining.

## **Example**

```go {11}
package main

import (
    "github.com/signalwire/signalwire-go/pkg/agent"
    "github.com/signalwire/signalwire-go/pkg/swaig"
)

func main() {
    agent := agent.NewAgentBase(agent.WithName("my-agent"), agent.WithRoute("/agent"))
    agent.SetPromptText("You are a helpful assistant.")

    agent.DefineTool(agent.ToolDefinition{
        Name:        "clear_metadata",
        Description: "Clear function metadata",
        Handler: func(args map[string]any, rawData map[string]any) *swaig.FunctionResult {
        return (
            FunctionResult("Metadata cleared.")
            .remove_metadata([]string{"timestamp", "user_id"})
        )

    agent.Serve()
}
```
