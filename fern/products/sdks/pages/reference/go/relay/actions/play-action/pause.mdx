---
title: "pause"
slug: /reference/go/relay/actions/play-action/pause
description: Pause audio playback.
max-toc-depth: 3
---

Pause audio playback. The playback position is preserved.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```go {18}
package main

import "github.com/signalwire/signalwire-go/pkg/relay"

func main() {
    client := RelayClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
        contexts := []string{"default"},
    )

    async def handle_call(call):
        await call.answer()
        action := await call.play(
            [{"type": "audio", "url": "https://example.com/hold-music.mp3"}],
            loop := 0,
        )

        await action.pause()
        // ... do something ...
        await action.resume()

    client.Run()
}
```
