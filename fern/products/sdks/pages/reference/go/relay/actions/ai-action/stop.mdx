---
title: "stop"
slug: /reference/go/relay/actions/ai-action/stop
description: Stop the AI agent session.
max-toc-depth: 3
---

Stop the AI agent session.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```go {19}
package main

import "github.com/signalwire/signalwire-go/pkg/relay"

func main() {
    client := RelayClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
        contexts := []string{"default"},
    )

    async def handle_call(call):
        await call.answer()
        action := await call.ai(
            prompt := {"text": "You are a helpful assistant."},
            ai_params := {"temperature": 0.7},
        )

        // Stop the AI session
        await action.stop()

    client.Run()
}
```
