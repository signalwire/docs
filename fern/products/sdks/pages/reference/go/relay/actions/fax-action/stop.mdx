---
title: "stop"
slug: /reference/go/relay/actions/fax-action/stop
description: Stop the fax operation.
max-toc-depth: 3
---

Stop the fax operation.

## **Returns**

`dict` -- Server acknowledgment.

## **Example**

```go {19}
package main

import "github.com/signalwire/signalwire-go/pkg/relay"

func main() {
    client := RelayClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
        contexts := []string{"default"},
    )

    async def handle_call(call):
        await call.answer()
        action := await call.send_fax(
            document := "https://example.com/invoice.pdf",
            identity := "+15559876543",
        )

        // Stop the fax operation
        await action.stop()

    client.Run()
}
```
