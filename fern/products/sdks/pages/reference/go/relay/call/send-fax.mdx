---
title: "SendFax"
slug: /reference/go/relay/call/send-fax
description: "Send a fax document on a call."
max-toc-depth: 3
---

[faxaction]: /docs/sdks/reference/go/relay/actions
[calling-call-fax]: /docs/sdks/reference/go/relay/call#events
[call-events]: /docs/sdks/reference/go/relay/call#events

Send a fax document on the call. The document must be accessible via URL.
Returns a [`FaxAction`][faxaction] that you can
use to stop the fax or wait for it to complete.

<Info>
This method emits [`calling.call.fax`][calling-call-fax] events. See [Call Events][call-events] for payload details.
</Info>

## **Parameters**

<ParamField path="document" type="string" required={true} toc={true}>
  URL of the document to fax (PDF or TIFF format).
</ParamField>

<ParamField path="identity" type="*string" toc={true}>
  Caller identity string (TSI) transmitted with the fax.
</ParamField>

<ParamField path="header_info" type="*string" toc={true}>
  Header text printed at the top of each fax page.
</ParamField>

<ParamField path="control_id" type="*string" toc={true}>
  Custom control ID. Auto-generated if not provided.
</ParamField>

<ParamField path="on_completed" type="*func(RelayEvent) interface{}" toc={true}>
  Callback invoked when the fax operation completes.
</ParamField>

## **Returns**

[`FaxAction`][faxaction] -- An action handle with
`Stop()` and `Wait()` methods.

## **Example**

```go {14}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/relay"
)

func main() {
    client := RelayClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
        contexts := []string{"default"},
    )

    async def handle_call(call):
        await call.answer()

        action := await call.send_fax(
            document := "https://example.com/invoice.pdf",
            identity := "+15559876543",
            header_info := "Invoice #12345",
        )
        event := await action.wait()

        fax_result := event.params[]string{"fax"}
        fmt.Println(fmt.Sprintf("Fax sent: %v pages", fax_result.get('pages', 0)))
        await call.hangup()

    client.Run()
}
```
