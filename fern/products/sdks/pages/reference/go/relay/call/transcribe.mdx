---
title: "transcribe"
slug: /reference/go/relay/call/transcribe
description: "Start transcribing call audio."
max-toc-depth: 3
---

[transcribeaction]: /docs/sdks/reference/go/relay/actions
[live-transcribe]: /docs/sdks/reference/go/relay/call/live-transcribe
[calling-call-transcribe]: /docs/sdks/reference/go/relay/call#events
[call-events]: /docs/sdks/reference/go/relay/call#events

Start transcribing call audio. Returns a
[`TranscribeAction`][transcribeaction] that you can use
to stop the transcription or wait for it to complete.

<Info>
For real-time transcription with immediate text output, see
[`LiveTranscribe()`][live-transcribe].
The `Transcribe()` method provides a post-call transcription result.
</Info>

<Info>
This method emits [`calling.call.transcribe`][calling-call-transcribe] events. See [Call Events][call-events] for payload details.
</Info>

## **Parameters**

<ParamField path="control_id" type="*string" toc={true}>
  Custom control ID. Auto-generated if not provided.
</ParamField>

<ParamField path="status_url" type="*string" toc={true}>
  URL to receive transcription status webhooks.
</ParamField>

<ParamField path="on_completed" type="*func(RelayEvent) interface{}" toc={true}>
  Callback invoked when transcription completes. The event contains the
  transcription `url`, `duration`, and `size`.
</ParamField>

## **Returns**

[`TranscribeAction`][transcribeaction] -- An action handle
with `Stop()` and `Wait()` methods.

## **Example**

```go {15}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/relay"
)

func main() {
    client := RelayClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
        contexts := []string{"default"},
    )

    async def handle_call(call):
        await call.answer()

        // Start transcription
        action := await call.transcribe(
            status_url := "https://example.com/transcription-status",
        )

        // Let the call proceed...
        await call.wait_for_ended()

        // The transcription result is available after the call ends
        if action.result:
            url := action.result.params[]string{"url"}
            fmt.Println(fmt.Sprintf("Transcription available at: %v", url))

    client.Run()
}
```
