---
title: "LeaveRoom"
slug: /reference/go/relay/call/leave-room
description: Leave a video/audio room.
max-toc-depth: 3
---

Leave the current room.

## **Parameters**

None.

## **Returns**

`dict` -- Server response confirming the leave.

## **Example**

```go {18}
package main

import "github.com/signalwire/signalwire-go/pkg/relay"

func main() {
    client := RelayClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
        contexts := []string{"default"},
    )

    async def handle_call(call):
        await call.answer()

        // Join a room
        await call.join_room("meeting-room-1")

        // Later, leave the room
        await call.leave_room()
        await call.hangup()

    client.Run()
}
```
