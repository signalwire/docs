---
title: "hold"
slug: /reference/go/relay/call/hold
description: Put a call on hold.
max-toc-depth: 3
---

[unhold]: /docs/sdks/reference/go/relay/call/unhold
[calling-call-hold]: /docs/sdks/reference/go/relay/call#events
[call-events]: /docs/sdks/reference/go/relay/call#events

Put the call on hold. The remote party hears hold music or silence while the
call is held.

<Info>
Use [`Unhold()`][unhold] to release the call from hold.
</Info>

<Info>
This method emits [`calling.call.hold`][calling-call-hold] events. See [Call Events][call-events] for payload details.
</Info>

## **Parameters**

None.

## **Returns**

`dict` -- Server response confirming the hold operation.

## **Example**

```go {15}
package main

import (
    "fmt"
    "github.com/signalwire/signalwire-go/pkg/relay"
)

func main() {
    import asyncio

    client := RelayClient(
        project := "your-project-id",
        token := "your-api-token",
        host := "your-space.signalwire.com",
        contexts := []string{"default"},
    )

    async def handle_call(call):
        await call.answer()
        await call.play([{"type": "tts", "text": "Please hold while I look that up."}])

        result := await call.hold()
        fmt.Println(fmt.Sprintf("Hold started: %v", result))

        // Do some processing...
        await asyncio.sleep(5)

        // Take the call off hold
        await call.unhold()

    client.Run()
}
```
