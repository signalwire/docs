---
id: 2a59d824-9223-4860-8559-607a798d40df
title: Error codes
slug: /error-codes
max-toc-depth: 3
---

When using SignalWire REST APIs, some errors will include error codes. Below, you will find a list of our unique error codes and a short explanation of each to help with error handling and troubleshooting.

<ParamField path="account_limit_exceeded" type="Error" toc={true}>

- The account must be upgraded to perform this action.
</ParamField>

<ParamField path="allowed_address_limit_exceeded" type="Error" toc={true}>

- The maximum number of allowed addresses has been exceeded.
</ParamField>

<ParamField path="already_assigned" type="Error" toc={true}>

- This resource is already assigned.
</ParamField>

<ParamField path="already_verified" type="Error" toc={true}>

- This number is already verified.
</ParamField>

<ParamField path="address_has_no_valid_channels" type="Error" toc={true}>

- The address has no valid channels.
</ParamField>

<ParamField path="auto_top_up_deactivated" type="Error" toc={true}>

- Auto top-up is not active on this account.
</ParamField>

<ParamField path="authblock_is_invalid" type="Error" toc={true}>

- The authblock is invalid or doesn't reference an existing [subscriber](/docs/platform/subscribers).
</ParamField>

<ParamField path="authblock_is_expired" type="Error" toc={true}>

- The expires_at timestamp of the subscriber from the authblock has passed.
</ParamField>

<ParamField path="billing_route_not_found" type="Error" toc={true}>

- Unable to locate a billing route.
</ParamField>

<ParamField path="body_must_be_empty" type="Error" toc={true}>

- The `body` value must be an empty string to redact the message.
</ParamField>

<ParamField path="body_or_media_required" type="Error" toc={true}>

- A `body` must be included if there is no `media`.
</ParamField>

<ParamField path="brand_not_unique" type="Error" toc={true}>

- The CSP Brand Reference must be unique within the Space
</ParamField>

<ParamField path="call_id_is_duplicated" type="Error" toc={true}>

- The provided call ID is already associated with an existing call.
</ParamField>

<ParamField path="call_update_failed" type="Error" toc={true}>

- The call update could not be completed at this time. Please try again, and contact support if issues persist.
</ParamField>

<ParamField path="campaign_inactive" type="Error" toc={true}>

- Numbers cannot be assigned to an inactive campaign
</ParamField>

<ParamField path="campaign_limit_exceeded" type="Error" toc={true}>

- The campaign limit has been exceeded.
</ParamField>

<ParamField path="campaign_not_active" type="Error" toc={true}>

- Campaign must be active.
</ParamField>

<ParamField path="campaign_not_found" type="Error" toc={true}>

- The campaign ID provided could not be found.
</ParamField>

<ParamField path="cannot_cancel_in_progress" type="Error" toc={true}>

- Cannot cancel an in-progress call.
</ParamField>

<ParamField path="cannot_process" type="Error" toc={true}>

- The request cannot be processed right now.
</ParamField>

<ParamField path="cannot_redact_in_progress_message" type="Error" toc={true}>

- Cannot redact a message that is in progress. Messages in `queued` or `initiated` state cannot be redacted; only messages in terminal states (`delivered`, `undelivered`, `failed`) are eligible.
</ParamField>

<ParamField path="cannot_redirect_not_in_progress" type="Error" toc={true}>

- Call is not in-progress. Cannot redirect.
</ParamField>

<ParamField path="cannot_remove_assignment_from_integration_test" type="Error" toc={true}>

- You cannot remove the assignment from an integration test campaign.
</ParamField>

<ParamField path="cannot_update_completed" type="Error" toc={true}>

- Completed calls cannot be updated.
</ParamField>

<ParamField path="channel_has_missing_properties" type="Error" toc={true}>

- Channel must have read or write property.
</ParamField>

<ParamField path="channel_name_is_reserved" type="Error" toc={true}>

- The channel name uses a reserved prefix.
</ParamField>

<ParamField path="channel_name_size_limit_exceeded" type="Error" toc={true}>

- The channel name exceeds the maximum allowed length.
</ParamField>

<ParamField path="channel_size_limit_exceeded" type="Error" toc={true}>

- The number of channels exceeds the allowed limit.
</ParamField>

<ParamField path="character_limit_exceeded" type="Error" toc={true}>

- The message `body` exceeds the provider-specific character limit.
</ParamField>

<ParamField path="chat_token_invalid_for_session" type="Error" toc={true}>

- The provided token is invalid for the provided room session.
</ParamField>

<ParamField path="chat_token_is_invalid" type="Error" toc={true}>

- The provided token is not a valid chat token.
</ParamField>

<ParamField path="contains_external_entities" type="Error" toc={true}>

- XML contains external entity definitions which are not allowed for security reasons.
</ParamField>

<ParamField path="conversation_flow_steps_are_invalid" type="Error" toc={true}>

- This value must be an array. Example: ["Intro", ["Introduce yourself", "Ask caller's name"], "Summarize the conversation and thank the caller"]
</ParamField>

<ParamField path="could_not_purchase" type="Error" toc={true}>

- The resource could not be purchased.
</ParamField>

<ParamField path="destination_number_not_supported" type="Error" toc={true}>

- The destination phone number is not supported.
</ParamField>

<ParamField path="document_version_mismatch" type="Error" toc={true}>

- The document_version does not match the current record's document_version.
</ParamField>

<ParamField path="does_not_exist" type="Error" toc={true}>

- This entry does not exist.
</ParamField>

<ParamField path="domain_has_no_password" type="Error" toc={true}>

- The domain has not been configured with a password.
</ParamField>

<ParamField path="duplicate_skill_type" type="Error" toc={true}>

- Only one skill of this type can exist per AI Agent.
</ParamField>

<ParamField path="exceeds_history_logs_limit" type="Error" toc={true}>

- This value exceeds the allowed date limit.
</ParamField>

<ParamField path="exceeds_platform_stream_limit" type="Error" toc={true}>

- Adding an additional stream would exceed the allowed number of streams per room.
</ParamField>

<ParamField path="feature_not_available" type="Error" toc={true}>

- This feature is not available to this account.
</ParamField>

<ParamField path="file_already_exists" type="Error" toc={true}>

- An asset already exists with this project, path, and filename.
</ParamField>

<ParamField path="file_can_not_be_opened" type="Error" toc={true}>

- An error occurred trying to open the provided file.
</ParamField>

<ParamField path="file_exceeds_size_limit" type="Error" toc={true}>

- The file exceeds the maximum allowed size.
</ParamField>

<ParamField path="file_not_found" type="Error" toc={true}>

- The requested file could not be found (404 Not Found). Please check the URL and try again.
</ParamField>

<ParamField path="from_fabric_address_id_does_not_match_subscriber" type="Error" toc={true}>

- The provided from_fabric_address_id does not match an address for the provided subscriber.
</ParamField>

<ParamField path="inactive_campaign" type="Error" toc={true}>

- The `from` number must belong to an active campaign.
</ParamField>

<ParamField path="incompatible_parameters" type="Error" toc={true}>

- The parameters that were input cannot be specified together.
</ParamField>

<ParamField path="incorrect_keyword_format" type="Error" toc={true}>

- Keywords must be comma separated values with no space.
</ParamField>

<ParamField path="incorrect_number_of_sub_use_cases_low_volume" type="Error" toc={true}>

- Mixed use case campaigns require minimum 2 and maximum 5 sub use cases.
</ParamField>

<ParamField path="incorrect_number_of_sub_use_cases_mixed" type="Error" toc={true}>

- Mixed use case campaigns require minimum 2 and maximum 5 sub use cases.
</ParamField>

<ParamField path="inline_swml_or_swml_webhook_required" type="Error" toc={true}>

- You must pass instructions through inline SWML or a URL to a SWML document.
</ParamField>

<ParamField path="insufficient_balance" type="Error" toc={true}>

- This account has an insufficient balance.
</ParamField>

<ParamField path="integration_test_verified_caller_required" type="Error" toc={true}>

- The `to` number must be the verified caller ID for the trial campaign.
</ParamField>

<ParamField path="invalid_addresses_for_transfer_skill" type="Error" toc={true}>

- Addresses must contain non-empty values for name and destination
</ParamField>

<ParamField path="invalid_auth_token_permissions" type="Error" toc={true}>

- Permissions list contains invalid selections. Valid permissions are calling, chat, fax, management, messaging, numbers, pubsub, storage, tasking, and video.
</ParamField>

<ParamField path="invalid_call_handler_for_external_swml_handler" type="Error" toc={true}>

- Handler must be calling or messaging.
</ParamField>

<ParamField path="invalid_call_handler_for_swml_application" type="Error" toc={true}>

- Handler must be external_url or script.
</ParamField>

<ParamField path="invalid_call_id_passed" type="Error" toc={true}>

- The call ID must not point to a cXML call.
</ParamField>

<ParamField path="invalid_campaign_id" type="Error" toc={true}>

- The campaign ID provided is invalid.
</ParamField>

<ParamField path="invalid_campaign_status" type="Error" toc={true}>

- The campaign status is invalid.
</ParamField>

<ParamField path="invalid_codecs_format" type="Error" toc={true}>

- Codecs must be an array of strings or a comma-separated string.
</ParamField>

<ParamField path="invalid_command_attempted" type="Error" toc={true}>

- The specified command is invalid.
</ParamField>

<ParamField path="invalid_content_type" type="Error" toc={true}>

- Invalid content type.
</ParamField>

<ParamField path="invalid_destination_number" type="Error" toc={true}>

- The destination phone number is invalid.
</ParamField>

<ParamField path="invalid_for_type" type="Error" toc={true}>

- This is not valid for the current type.
</ParamField>

<ParamField path="invalid_for_page_chunking" type="Error" toc={true}>

- Chunking parameters are not allowed for page chunking.
</ParamField>

<ParamField path="invalid_for_paragraph_chunking" type="Error" toc={true}>

- Chunking parameters are not allowed for paragraph chunking.
</ParamField>

<ParamField path="invalid_for_sentence_chunking" type="Error" toc={true}>

- Provided chunking parameters are not valid for sentence chunking. Allowed parameters are max_sentences_per_chunk and split_newlines.
</ParamField>

<ParamField path="invalid_for_sliding_chunking" type="Error" toc={true}>

- Provided chunking parameters are not valid for sliding chunking. Allowed parameters are chunk_size and overlap_size.
</ParamField>

<ParamField path="invalid_from_number" type="Error" toc={true}>

- The `from` value must be a valid purchased phone number on your SignalWire project. Verified caller IDs cannot be used for outbound messaging.
</ParamField>

<ParamField path="invalid_group_configuration" type="Error" toc={true}>

- Fabric addresses must form a valid conversation group (a single room or two non-room subscribers) and group ID must start with 'sw_'.
</ParamField>

<ParamField path="invalid_handler_use" type="Error" toc={true}>

- Handler must be calling or messaging.
</ParamField>

<ParamField path="invalid_http_method" type="Error" toc={true}>

- This request method must be GET or POST.
</ParamField>

<ParamField path="invalid_join_until_date" type="Error" toc={true}>

- Join Until date can't be in the past of Join From date.
</ParamField>

<ParamField path="invalid_list_items" type="Error" toc={true}>

- The list items are invalid.
</ParamField>

<ParamField path="invalid_media_type" type="Error" toc={true}>

- The media type provided is invalid.
</ParamField>

<ParamField path="invalid_media_url" type="Error" toc={true}>

- The media URL provided is invalid.
</ParamField>

<ParamField path="invalid_message_body" type="Error" toc={true}>

- The message body provided is invalid.
</ParamField>

<ParamField path="invalid_mustache_syntax" type="Error" toc={true}>

- The mustache syntax is invalid..
</ParamField>

<ParamField path="invalid_number_format" type="Error" toc={true}>

- The phone number is not in the correct format. Please use E.164 format, i.e., +15558675309.
</ParamField>

<ParamField path="invalid_object" type="Error" toc={true}>

- This object is invalid. Refer to the error message for more details.
</ParamField>

<ParamField path="invalid_parameter" type="Error" toc={true}>

- This list contains an invalid entry.
</ParamField>

<ParamField path="invalid_pstn_destination" type="Error" toc={true}>

- The PSTN destination is either missing or not in E164 format.
</ParamField>

<ParamField path="invalid_relay_connector_parameters" type="Error" toc={true}>

- Send relay_connector_id to link to an existing connector or relay_connector_name and relay_connector_token to create a new one.
</ParamField>

<ParamField path="invalid_resource_for_handler" type="Error" toc={true}>

- This resource is not valid for the specified handler type.
</ParamField>

<ParamField path="invalid_resource_type" type="Error" toc={true}>

- The passed resource type is not a valid resource type.
</ParamField>

<ParamField path="invalid_room_session" type="Error" toc={true}>

- The room session ID does not point to a valid room session.
</ParamField>

<ParamField path="invalid_sip_configuration" type="Error" toc={true}>

- The SIP configuration value is invalid.
</ParamField>

<ParamField path="invalid_sip_destination" type="Error" toc={true}>

- The SIP destination is either missing or could not be parsed.
</ParamField>

<ParamField path="invalid_state" type="Error" toc={true}>

- The object is not in the correct state for this action.
</ParamField>

<ParamField path="invalid_sub_use_cases" type="Error" toc={true}>

- This sub use case is not valid.
</ParamField>

<ParamField path="invalid_subproject_id" type="Error" toc={true}>

- The subproject ID passed could not be found or does not belong to the project being used to authenticate the request.
</ParamField>

<ParamField path="invalid_token_format" type="Error" toc={true}>

- The format of the provided token is invalid.
</ParamField>

<ParamField path="invalid_zendesk_capability" type="Error" toc={true}>

- The Zendesk Capability is invalid.
</ParamField>

<ParamField path="list_contains_invalid_entries" type="Error" toc={true}>

- The list contains an invalid entry.
</ParamField>

<ParamField path="malformed_xml" type="Error" toc={true}>

- The XML is not well formed
</ParamField>

<ParamField path="max_queued_calls_exceeded" type="Error" toc={true}>

- The maximum number of queued calls has been exceeded.
</ParamField>

<ParamField path="max_queued_messages_exceeded" type="Error" toc={true}>

- has exceeded the maximum number of queued messages/calls for this number
</ParamField>

<ParamField path="media_limit_exceeded" type="Error" toc={true}>

- Exceeded the maximum number of media items allowed per message (currently 8).
</ParamField>

<ParamField path="media_size_exceeds_limit" type="Error" toc={true}>

- The media size exceeds the allowed limit.
</ParamField>

<ParamField path="media_url_required" type="Error" toc={true}>

- A media URL is required.
</ParamField>

<ParamField path="message_backlog_limit_exceeded" type="Error" toc={true}>

- The project's messaging backlog limit has been exceeded. Wait before sending more messages.
</ParamField>

<ParamField path="message_body_required" type="Error" toc={true}>

- The message body is required.
</ParamField>

<ParamField path="message_length_exceeds_limit" type="Error" toc={true}>

- The message length exceeds the allowed limit.
</ParamField>

<ParamField path="messaging_provider_invalid" type="Error" toc={true}>

- Messaging provider is invalid.
</ParamField>

<ParamField path="missing_messaging_capability" type="Error" toc={true}>

- The `from` number is not capable of sending the determined message kind (SMS or MMS).
</ParamField>

<ParamField path="missing_required_parameter" type="Error" toc={true}>

- The passed value is blank or empty, but it is required.
</ParamField>

<ParamField path="missing_sub_use_cases" type="Error" toc={true}>

- Sub use cases are required for MIXED and LOW_VOLUME_MIXED campaigns.
</ParamField>

<ParamField path="missing_sip_configuration" type="Error" toc={true}>

- The SIP configuration requires at least one selection.
</ParamField>

<ParamField path="must_belong_to_project" type="Error" toc={true}>

- The requested resource must belong to the project being used to authenticate the request.
</ParamField>

<ParamField path="must_specify_related_parameter" type="Error" toc={true}>

- This parameter must be used in combination with a related parameter.
</ParamField>

<ParamField path="no_channel_specified" type="Error" toc={true}>

- You must specify a channel.
</ParamField>

<ParamField path="no_devices_or_registrations" type="Error" toc={true}>

- The fabric address has no registered devices or current online registrations.
</ParamField>

<ParamField path="no_fabric_addresses" type="Error" toc={true}>

- The resource has no fabric addresses.
</ParamField>

<ParamField path="no_installed_applications" type="Error" toc={true}>

- Fabric Application must be installed in this project.
</ParamField>

<ParamField path="no_registered_devices" type="Error" toc={true}>

- The fabric address has no registered devices.
</ParamField>

<ParamField path="no_steered_resource" type="Error" toc={true}>

- The project has a steering tag set, but no steered resource is available for that tag.
</ParamField>

<ParamField path="no_valid_number_for_random_send_as_selection" type="Error" toc={true}>

- There is no purchased or verified number in the project to select as the send_as.
</ParamField>

<ParamField path="not_a_boolean" type="Error" toc={true}>

- Value must be true or false.
</ParamField>

<ParamField path="not_a_datetime" type="Error" toc={true}>

- Value must be a datetime.
</ParamField>

<ParamField path="not_a_hash" type="Error" toc={true}>

- Value must be a valid hash.
</ParamField>

<ParamField path="not_a_refresh_token" type="Error" toc={true}>

- Invalid token type. You must use a Refresh token.
</ParamField>

<ParamField path="not_a_string" type="Error" toc={true}>

- Value must be formatted as a string.
</ParamField>

<ParamField path="not_a_valid_json" type="Error" toc={true}>

- This value must be valid JSON.
</ParamField>

<ParamField path="not_allowed_on_subscription_plan" type="Error" toc={true}>

- This feature is not allowed on your current subscription plan.
</ParamField>

<ParamField path="not_alphanumeric" type="Error" toc={true}>

- Value must be alphanumeric.
</ParamField>

<ParamField path="not_an_array_of_hashes" type="Error" toc={true}>

- Value must be an array of hashes.
</ParamField>

<ParamField path="not_an_array_of_strings" type="Error" toc={true}>

- Value must be an array of strings.
</ParamField>

<ParamField path="not_an_http_url" type="Error" toc={true}>

- Value must be an HTTP or HTTPS URL.
</ParamField>

<ParamField path="not_available_for_assignment" type="Error" toc={true}>

- This can not be assigned to the current target.
</ParamField>

<ParamField path="not_e164" type="Error" toc={true}>

- The number must be in E.164 format.
</ParamField>

<ParamField path="not_json_or_yaml" type="Error" toc={true}>

- Value must be a valid JSON or YAML document.
</ParamField>

<ParamField path="not_parsable" type="Error" toc={true}>

- Value is not parsable.
</ParamField>

<ParamField path="not_parseable" type="Error" toc={true}>

- Value is not parseable.
</ParamField>

<ParamField path="not_purchased_or_verified" type="Error" toc={true}>

- The phone number must be purchased or verified.
</ParamField>

<ParamField path="not_registered" type="Error" toc={true}>

- The endpoint is not registered.
</ParamField>

<ParamField path="not_routable" type="Error" toc={true}>

- Unable to locate a route to the destination number. Please check geographic permissions and the destination number, and contact Support if issues persist.
</ParamField>

<ParamField path="not_routeable" type="Error" toc={true}>

- The `to` number is not routeable.
</ParamField>

<ParamField path="not_valid_for_caller_id" type="Error" toc={true}>

- The value is not valid (must be an E.164 number, caller ID string or SIP URI)
</ParamField>

<ParamField path="not_voice_capable" type="Error" toc={true}>

- The number must be voice capable.
</ParamField>

<ParamField path="not_unique_within_project" type="Error" toc={true}>

- Value must be unique within project.
</ParamField>

<ParamField path="not_unique_within_space" type="Error" toc={true}>

- Value must be unique within space.
</ParamField>

<ParamField path="number_assignment_cannot_be_removed" type="Error" toc={true}>

- Number assignments can only be removed if they are active, failed, or assigned to a pending campaign.
</ParamField>

<ParamField path="number_blacklisted" type="Error" toc={true}>

- The phone number is blacklisted.
</ParamField>

<ParamField path="number_not_supported" type="Error" toc={true}>

- The phone number is not supported.
</ParamField>

<ParamField path="number_not_verified" type="Error" toc={true}>

- 'From' phone number not verified.
</ParamField>

<ParamField path="number_of_assignments_exceed_limit" type="Error" toc={true}>

- The requested assignment would exceed the allowed number of assigned numbers for the selected campaign.
</ParamField>

<ParamField path="out_of_sync" type="Error" toc={true}>

- The resource is out of sync.
</ParamField>

<ParamField path="page_token_is_invalid" type="Error" toc={true}>

- The provided page token is not valid.
</ParamField>

<ParamField path="page_token_required" type="Error" toc={true}>

- A page token is required if the page_number value is greater than 0.
</ParamField>

<ParamField path="page_token_required_for_next_page" type="Error" toc={true}>

- The page token is required when the page number is passed and greater than 0.
</ParamField>

<ParamField path="pft_daily_limit_exceeded" type="Error" toc={true}>

- The Platform Free Trial campaign daily message cap of 200 messages has been reached.
</ParamField>

<ParamField path="provided_id_is_unrecognized" type="Error" toc={true}>

- The provided ID is unrecognized. Verify that the ID points to a valid resource belonging to the current project.
</ParamField>

<ParamField path="purchased_too_recently" type="Error" toc={true}>

- This number was purchased too recently to release. Please wait 14 days after purchase to release this number or 30 days if your account is in trial mode. Contact Support for further assistance.
</ParamField>

<ParamField path="query_processing_failed" type="Error" toc={true}>

- The query cannot be processed at the moment. Please try again later.
</ParamField>

<ParamField path="rate_limit_exceeded" type="Error" toc={true}>

- The rate limit has been exceeded.
</ParamField>

<ParamField path="resource_assigned_as_handler" type="Error" toc={true}>

- You cannot delete a resource if it is currently assigned as a call or message handler
</ParamField>

<ParamField path="resource_in_different_project" type="Error" toc={true}>

- The fabric resource is in a different project.
</ParamField>

<ParamField path="resource_not_allowed" type="Error" toc={true}>

- Calls to this fabric resource are not allowed with this authorization.
</ParamField>

<ParamField path="resource_not_callable" type="Error" toc={true}>

- The fabric resource is not callable.
</ParamField>

<ParamField path="resource_not_deletable" type="Error" toc={true}>

- Resource cannot be deleted.
</ParamField>

<ParamField path="room_is_not_chat_enabled" type="Error" toc={true}>

- The room for the provided room session does not have chat enabled.
</ParamField>

<ParamField path="room_token_invalid_for_session" type="Error" toc={true}>

- The provided token is invalid for the provided room session.
</ParamField>

<ParamField path="room_token_is_invalid" type="Error" toc={true}>

- The provided token is not a valid room token.
</ParamField>

<ParamField path="sat_must_have_application" type="Error" toc={true}>

- SignalWire Access Token must be associated with a Fabric Application to perform this action.
</ParamField>

<ParamField path="send_as_is_invalid" type="Error" toc={true}>

- The provided send_as number is missing or invalid.
</ParamField>

<ParamField path="sending_restricted_to_verified" type="Error" toc={true}>

- Sending is restricted to verified numbers for this account.
</ParamField>

<ParamField path="sip_gateway_uri_must_be_external" type="Error" toc={true}>

- Only external SIP entities are supported for the SIP Gateway. Do not provide a SignalWire-managed SIP entity.
</ParamField>

<ParamField path="sole_proprietor_daily_limit_exceeded" type="Error" toc={true}>

- The sole proprietor campaign daily message cap of 1,000 messages has been reached.
</ParamField>

<ParamField path="sort_order_validations" type="Error" toc={true}>

- Sort order cannot be specified without sort by.
</ParamField>

<ParamField path="state_size_limit_exceeded" type="Error" toc={true}>

- The state size exceeds the maximum allowed limit.
</ParamField>

<ParamField path="sub_use_cases_present" type="Error" toc={true}>

- Sub use cases are not permitted for non mixed campaign use case.
</ParamField>

<ParamField path="terms_and_conditions_required" type="Error" toc={true}>

- You must agree to the terms and conditions to complete registration.
</ParamField>

<ParamField path="to_destination_unresolvable" type="Error" toc={true}>

- The destination does not resolve to a valid destination.
</ParamField>

<ParamField path="token_expired" type="Error" toc={true}>

- Token has expired.
</ParamField>

<ParamField path="token_has_invalid_prefix" type="Error" toc={true}>

- The token includes an invalid format prefix.
</ParamField>

<ParamField path="token_invalid" type="Error" toc={true}>

- The provided token is invalid.
</ParamField>

<ParamField path="token_invalid_or_unadoptable" type="Error" toc={true}>

- The provided FreeSWITCH token is incorrect or not ready for adoption
</ParamField>

<ParamField path="token_not_associated_with_video_room" type="Error" toc={true}>

- The provided token is not associated with a valid video room.
</ParamField>

<ParamField path="too_many_verification_attempts" type="Error" toc={true}>

- Too many incorrect attempts to verify this number.
</ParamField>

<ParamField path="too_many_verification_requests" type="Error" toc={true}>

- Too many call requests without verifying this number.
</ParamField>

<ParamField path="too_many_verification_requests_in_time_frame" type="Error" toc={true}>

- This account has had too many Verified Caller ID requests within a short timeframe. As a precaution, we have disabled creating more for a short time. Please contact support if you need this restriction lifted sooner.
</ParamField>

<ParamField path="unknown_download_error" type="Error" toc={true}>

- An unexpected error occurred while downloading the file. Please try again later or contact support if the issue persists.
</ParamField>

<ParamField path="unrecognized_fabric_addresses" type="Error" toc={true}>

- One or more of the provided fabric addresses is unrecognized. Check that all IDs are valid and belong to the current project.
</ParamField>

<ParamField path="unsupported_file_extension" type="Error" toc={true}>

- Unsupported file extension.
</ParamField>

<ParamField path="unsupported_mode" type="Error" toc={true}>

- This mode is not supported on this number.
</ParamField>

<ParamField path="unsupported_option" type="Error" toc={true}>

- The specified option is not supported.
</ParamField>

<ParamField path="unsupported_region" type="Error" toc={true}>

- This number must be from an allowed region.
</ParamField>

<ParamField path="uri_does_not_resolve_to_address" type="Error" toc={true}>

- The provided URI does not resolve to a recognized address.
</ParamField>

<ParamField path="uri_does_not_resolve_to_resource" type="Error" toc={true}>

- The provided URI does not resolve to a valid resource.
</ParamField>

<ParamField path="uri_resolves_to_unsupported_resource" type="Error" toc={true}>

- The provided URI resolves to a resource that is unsupported for dialing.
</ParamField>

<ParamField path="upload_error" type="Error" toc={true}>

- There was an error while uploading the file. Please try again.
</ParamField>

<ParamField path="url_failed_to_parse" type="Error" toc={true}>

- An unknown error occurred when parsing the provided URL.
</ParamField>

<ParamField path="url_is_unsafe" type="Error" toc={true}>

- URL must not point to internal/private networks.
</ParamField>

<ParamField path="value_not_unique" type="Error" toc={true}>

- This value **must** be unique.
</ParamField>

<ParamField path="verification_request_too_soon" type="Error" toc={true}>

- This number has received a verification call too recently. Please wait a minute before requesting another call.
</ParamField>

<ParamField path="verified_caller_id_not_permitted" type="Error" toc={true}>

- Verified caller IDs are not permitted for outbound messaging.
</ParamField>