---
tags: ["ai"]
layout: page
---

{/*
  Copy this file to YYYY-MM-DD.mdx and delete these comments.

  DATE      The day the change reached customers — not the day you wrote the entry.
            One file per date. Several changes on one date go in the same file,
            each under its own `##` heading, most important first.

  TAGS      Product areas a reader would filter by. Pick one or two:
            voice · messaging · ai · video · fabric · sdks
            swml · apis · tts · e911 · compliance · security

  TITLE     A short noun phrase naming the thing. Sentence case, no end punctuation.
            Good:  Conversation analytics · Brand Identity Verification
                   Default TTS engine changed to ElevenLabs
            Bad:   New guide for tool calling · Analytics fully documented
                   We're excited to announce analytics!

  BODY      One to three sentences. Second person, present tense, active voice.
            Open on what the reader can now do, or on the fact itself — never on
            the page that describes it. Two short paragraphs are fine when the
            change genuinely carries that much (a deprecation, a compliance gate).

            Never write about the documentation. Banned: "now documented",
            "the docs now cover", "previously undocumented", "fully documented".
            No marketing adjectives, no emoji, no exclamation marks.

            A capability nobody was told about is new to the reader, however long
            it has been in production. Do not hedge it with a ship date.
            Say "Already available — " only when leaving it out would mislead:
            a security control the reader might think just appeared, payload
            fields they might think started arriving, a compliance gate they
            might read as a new requirement.

  LINKS     Inline, in the prose. Never a trailing "Updated pages:" list.
            Copy every URL from the target page's own `slug:` frontmatter,
            prefixed with /docs/<product-slug>/. Never guess a URL.

  ACTION    If there is somewhere to go or something to change, close with it.

  BREAKING  Lead with a titled <Warning>. An untitled callout flattens into plain
            prose in the Markdown export and the reader loses the signal.
*/}

## Short noun phrase naming the thing

What the reader can now do, stated first. One to three sentences. Link the primary
destination [inline](/docs/changelog), not in a list at the bottom.

Close with where to start, if there is somewhere to go.
