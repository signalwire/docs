---
tags: ["ai"]
---

{/*
  Copy this file to YYYY-MM-DD.mdx and delete these comments.

  DATE      The day the change reached customers — not the day you wrote the entry.
            One file per date. Several changes on one date go in the same file,
            each under its own `##` heading, most important first.

  TAGS      Product areas only, alphabetical. They render as chips on the index
            and feed the tag filter, so keep the list short — a tag matching one
            entry filters nothing.
              voice · messaging · ai · video · fabric · sdks
              swml · apis · tts · e911 · compliance · security
            The kind of change does NOT go here. Put it in the title.

  TITLE     Say what was added, changed, removed, or deprecated — in the words
            of the title, since no tag carries it. A reader
            skimming the index should not have to open the entry to get
            the gist.
            Good:  Six new text-to-speech engines
                   `playai-turbo` is now `groq-tts`
                   Default TTS engine changed from Google to ElevenLabs
                   E911 self-service added to the REST API
            Bad:   Tool calling                    (what about it?)
                   One AI agent, many languages    (reads like marketing)
                   AI section improvements         (says nothing)
                   Webhook payloads documented     (documentation is not a change)

  BODY      Lead with the reader's capability, concretely, in the first clause —
            what they can do, or what now behaves differently. That first line is
            the whole entry for anyone skimming. "You can now …" is one way to
            get there, not a required opening; an imperative usually reads better
            and stays out of the way ("Pick Groq, Mistral, or Grok as your TTS
            engine", "Register an emergency address yourself"). Vary it — a page
            of identical openings stops being read.
            One to three sentences, second person, present tense. Two short
            paragraphs are fine when the change genuinely carries that much.

            No marketing adjectives, no emoji, no exclamation marks.

  WHAT DOES NOT GET AN ENTRY
            A new guide, a new reference page, or documentation for something
            that already shipped. Not even for an important feature — the
            changelog records platform changes, not documentation work.
            The narrow exception is a capability customers had no way to
            discover at all, where publishing it is effectively the release.

  LINKS     Inline, in the prose. Never a trailing "Updated pages:" list.
            Copy every URL from the target page's own `slug:` frontmatter,
            prefixed with /docs/<product-slug>/. Never guess a URL.

  BREAKING  Say so in the title, then open with a plain text paragraph and a titled
            <Warning> — Fern previews each entry by its opening content, and a
            callout in that slot renders badly. Title the callout: an untitled
            one flattens into plain prose in the Markdown export.
*/}

## Say what was added, changed, or removed

You can now do the thing, stated first. One to three sentences. Link the primary
destination [inline](/docs/platform/changelog), not in a list at the bottom.

Close with where to start, if there is somewhere to go.
