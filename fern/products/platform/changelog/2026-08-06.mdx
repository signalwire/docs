---
tags: ["e911", "voice"]
---

## E911 self-service added to the REST API

Register an emergency address and enable E911 on a US phone number yourself, instead of filing a support ticket. Create a carrier-validated address, assign it to the number, then poll until `e911_status` reads `active`.

Start with the [E911 guide](/docs/platform/e911).
