---
title: "play_audio"
slug: /reference/python/relay/call/play-audio
description: "Play an audio file from a URL on a call."
max-toc-depth: 3
---

[play]: /docs/server-sdks/reference/python/relay/call/play

Play an audio file from a URL. A typed convenience over [`play()`][play] that
builds the `{"type": "audio", "params": {"url": ...}}` media item for you.

## **Parameters**

<ParamField path="url" type="str" required={true} toc={true}>
  URL of the audio file to play.
</ParamField>

<ParamField path="volume" type="float | None" default="None" toc={true}>
  Volume adjustment in dB, from `-40.0` to `40.0`. Keyword-only.
</ParamField>

<ParamField path="on_completed" type="Callable[[RelayEvent], Any] | None" default="None" toc={true}>
  Callback invoked when the operation reaches a terminal state. Can be a regular
  function or async coroutine. Keyword-only.
</ParamField>

## **Returns**

[`PlayAction`](/docs/server-sdks/reference/python/relay/actions) -- An action handle with `stop()`, `pause()`, `resume()`, `volume()`, and `wait()` methods.

## **Example**

```python {13}
from signalwire.relay import RelayClient

client = RelayClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
    contexts=["default"],
)

@client.on_call
async def handle_call(call):
    await call.answer()
    action = await call.play_audio("https://example.com/hold-music.mp3", volume=-6.0)
    await action.wait()

client.run()
```
