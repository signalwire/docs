---
title: "create_token"
slug: /reference/python/rest/pubsub/create-token
description: Create a PubSub authentication token.
max-toc-depth: 3
---

Create a PubSub authentication token.

<EndpointSchemaSnippet endpoint="POST /api/pubsub/tokens" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /api/pubsub/tokens" />

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

token = client.pubsub.create_token(
    channels=["notifications", "updates"],
    member_id="user-123",
    ttl=3600,
)
print("PubSub token:", token.get("token"))
```
