---
title: "get"
slug: /reference/python/rest/video/rooms/get
description: Retrieve a single video room.
max-toc-depth: 3
---

Retrieve a single video room by its unique identifier.

<EndpointSchemaSnippet endpoint="GET /api/video/rooms/{id}" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/video/rooms/{id}" />

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

room = client.video.rooms.get("room-id")
```
