---
title: "stream"
slug: /reference/python/rest/calling/stream
description: Stream call audio to a WebSocket endpoint via REST.
max-toc-depth: 3
---

[stream-stop]: /docs/server-sdks/reference/python/rest/calling/stream-stop

Start streaming audio from an active call to a WebSocket endpoint. This is
commonly used for real-time speech processing, analytics, or archival. Pass a
`control_id` to name the stream so you can stop it later with
[`stream_stop()`][stream-stop]. The response never carries `control_id` back --
omit it and you have no way to stop this stream afterward.

## **Request**

<Schema type="Calling.CallStreamRequest" api="signalwire-rest" exclude={["command"]} />

## **Response**

<Schema type="Calling.CallResponse" api="signalwire-rest" />

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

client.calling.stream(
    call_id="call-id-xxx",
    control_id="stream-1",
    url="wss://example.com/audio-stream",
)
```
