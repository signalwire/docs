---
title: "get"
slug: /reference/python/rest/compat/messages/get
description: Retrieve a single message by SID.
max-toc-depth: 3
---


Retrieve a single message by SID.

<EndpointSchemaSnippet endpoint="GET /Accounts/{AccountSid}/Messages/{Sid}" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /Accounts/{AccountSid}/Messages/{Sid}" />

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

message = client.compat.messages.get("SM...")
```
