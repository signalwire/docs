---
title: "create"
slug: /reference/python/rest/fabric/swml-webhooks/create
description: Create a new SWML webhook resource.
max-toc-depth: 3
---

Create a new SWML webhook resource.

<EndpointSchemaSnippet endpoint="POST /api/fabric/resources/swml_webhooks" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /api/fabric/resources/swml_webhooks" />

## **Example**

```python {9}
from signalwire.rest import RestClient

client = RestClient(
    project="your-project-id",
    token="your-api-token",
    host="your-space.signalwire.com",
)

result = client.fabric.swml_webhooks.create(
    name="my-webhook", primary_request_url="https://example.com/swml",
)
print(f"Created: {result['id']}")
```
