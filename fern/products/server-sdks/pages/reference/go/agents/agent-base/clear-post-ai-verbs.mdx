---
title: "ClearPostAiVerbs"
slug: /reference/go/agents/agent-base/clear-post-ai-verbs
description: Remove all post-AI verbs from the call flow.
max-toc-depth: 3
---

[ref-agentbase]: /docs/server-sdks/reference/go/agents/agent-base
[add-post-ai-verb]: /docs/server-sdks/reference/go/agents/agent-base/add-post-ai-verb

Remove every verb previously registered with
[`AddPostAiVerb`][add-post-ai-verb]. After this call the post-AI verb list is
empty until new verbs are appended.

## **Parameters**

None.

## **Returns**

[`*AgentBase`][ref-agentbase] — returns the receiver for fluent chaining.

## **Example**

```go {12}
package main

import "github.com/signalwire/signalwire-go/pkg/agent"

func main() {
	a := agent.NewAgentBase(
		agent.WithName("support"),
		agent.WithRoute("/support"),
	)
	a.SetPromptText("You are a helpful assistant.")
	a.AddPostAiVerb("hangup", map[string]any{})
	a.ClearPostAiVerbs()

	_ = a.Serve()
}
```
