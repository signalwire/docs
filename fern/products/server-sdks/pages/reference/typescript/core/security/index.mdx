---
title: "Webhook Signature Validation"
slug: /reference/typescript/core/security
description: "Verify that inbound HTTP requests were genuinely signed by SignalWire."
max-toc-depth: 3
---

[agentbase]: /docs/server-sdks/reference/typescript/agents/agent-base
[validate-webhook-signature]: /docs/server-sdks/reference/typescript/core/security/validate-webhook-signature
[validate-request]: /docs/server-sdks/reference/typescript/core/security/validate-request
[webhook-validation-middleware]: /docs/server-sdks/reference/typescript/core/security/webhook-validation-middleware

SignalWire signs the webhook requests it sends to your endpoints. These
utilities verify those signatures so you can reject forged or tampered requests.
All are imported directly from `@signalwire/sdk`.

```typescript {2-7}
import {
  validateWebhookSignature,
  validateRequest,
  webhookValidationMiddleware,
  SIGNALWIRE_SIGNATURE_HEADER,
  TWILIO_COMPAT_SIGNATURE_HEADER,
} from '@signalwire/sdk';
```

<Tip>
If you are building with [`AgentBase`][agentbase], set the `signingKey`
constructor option instead — the agent auto-mounts
[`webhookValidationMiddleware`][webhook-validation-middleware] on its endpoints
for you.
</Tip>

## **Functions**

<CardGroup cols={3}>
  <Card title="validateWebhookSignature" href="/docs/server-sdks/reference/typescript/core/security/validate-webhook-signature">
    Verify a signature against a raw request body.
  </Card>
  <Card title="validateRequest" href="/docs/server-sdks/reference/typescript/core/security/validate-request">
    Legacy compatibility-api drop-in accepting parsed form params.
  </Card>
  <Card title="webhookValidationMiddleware" href="/docs/server-sdks/reference/typescript/core/security/webhook-validation-middleware">
    Hono middleware that validates and rejects unsigned requests.
  </Card>
</CardGroup>

## **Signature header constants**

<ParamField path="SIGNALWIRE_SIGNATURE_HEADER" type="string" toc={true}>
  The SignalWire signature header name: `'x-signalwire-signature'`.
</ParamField>

<ParamField path="TWILIO_COMPAT_SIGNATURE_HEADER" type="string" toc={true}>
  The Twilio-compatible signature header name: `'x-twilio-signature'`,
  recognized on the compatibility surface.
</ParamField>
