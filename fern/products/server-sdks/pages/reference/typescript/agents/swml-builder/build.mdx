---
title: "build"
slug: /reference/typescript/agents/swml-builder/build
description: "Build and return the SWML document as an object."
max-toc-depth: 3
---

[ref-render]: /docs/server-sdks/reference/typescript/agents/swml-builder/render
[ref-document]: /docs/server-sdks/reference/typescript/agents/swml-builder/document

Return the SWML document as a plain object. The name matches the Python SDK's
`SWMLBuilder.build()`. For a JSON string, use [`render()`][ref-render]. For
property-style access to the same object, use the [`document`][ref-document]
getter.

## **Returns**

`Record<string, unknown>` -- the SWML document with `version` and `sections`
keys.

## **Example**

```typescript {6}
import { SwmlBuilder } from '@signalwire/sdk';

const builder = new SwmlBuilder();
builder.answer();
builder.ai({ prompt: { text: 'You are a helpful assistant.' } });

const doc = builder.build();
console.log(JSON.stringify(doc, null, 2));
```
