---
title: "enableEnvExpansion"
slug: /reference/typescript/agents/data-map/enable-env-expansion
description: "Enable or disable environment variable expansion in URLs, bodies, and outputs."
max-toc-depth: 3
---

[ref-datamap]: /docs/server-sdks/reference/typescript/agents/data-map

Enable `${ENV.*}` variable expansion in URLs, bodies, and outputs.

## **Parameters**

<ParamField path="enabled" type="boolean" default="true" toc={true}>
  Whether to enable expansion. Defaults to `true` if omitted.
</ParamField>

## **Returns**

[`DataMap`][ref-datamap] -- Self for method chaining.

## **Example**

```typescript {4}
import { DataMap } from '@signalwire/sdk';

const dm = new DataMap('get_weather');
dm.enableEnvExpansion();
dm.setAllowedEnvPrefixes(['WEATHER_']);
dm.webhook('GET', 'https://api.weather.com/v1/current?key=${ENV.WEATHER_API_KEY}');
```
