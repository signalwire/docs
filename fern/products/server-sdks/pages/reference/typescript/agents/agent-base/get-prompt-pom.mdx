---
title: "getPromptPom"
slug: /reference/typescript/agents/agent-base/get-prompt-pom
description: Return the current prompt as a POM (Prompt Object Model) array.
max-toc-depth: 3
---

Return the current prompt as a POM (Prompt Object Model) array — the
structured, section-based representation. Returns `null` when the agent is
configured with a raw text prompt (`usePom: false`).

## **Returns**

`Record<string, unknown>[] | null`
