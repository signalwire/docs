---
title: "enableDebugRoutes"
slug: /reference/typescript/agents/agent-base/enable-debug-routes
description: Expose the agent's debug HTTP endpoints.
max-toc-depth: 3
---

Expose the agent's debug HTTP endpoints on the Hono server. Use in
development to inspect live state; avoid in production.

## **Returns**

`this` — the agent for method chaining.
