---
title: "playAudio"
slug: /reference/typescript/relay/call/play-audio
description: "Play an audio file from a URL on a call."
max-toc-depth: 3
---

[playaction]: /docs/server-sdks/reference/typescript/relay/actions
[play]: /docs/server-sdks/reference/typescript/relay/call/play

Play an audio file from a URL on the call. A typed convenience over
[`play()`][play].

## **Parameters**

<ParamField path="url" type="string" required={true} toc={true}>
  URL of the audio file.
</ParamField>

<ParamField path="options" type="object" toc={true}>
  Playback options.
</ParamField>

<Indent>

<ParamField path="options.volume" type="number" toc={true}>
  Volume adjustment in dB, from `-40` to `40`.
</ParamField>

<ParamField path="options.onCompleted" type="(event: RelayEvent) => void | Promise<void>" toc={true}>
  Callback invoked when the operation reaches a terminal state.
</ParamField>

</Indent>

## **Returns**

`Promise<`[`PlayAction`][playaction]`>` -- An action handle with `stop()`, `pause()`, `resume()`, `volume()`, and `wait()` methods.

## **Example**

```typescript {11}
import { RelayClient } from '@signalwire/sdk';

const client = new RelayClient({
  project: process.env.SIGNALWIRE_PROJECT_ID!,
  token: process.env.SIGNALWIRE_API_TOKEN!,
  contexts: ['default']
});

client.onCall(async (call) => {
  await call.answer();
  const action = await call.playAudio('https://example.com/hold-music.mp3', { volume: -6 });
  await action.wait();
});

await client.run();
```
