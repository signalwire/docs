---
title: "waitForEnding"
slug: /reference/typescript/relay/call/wait-for-ending
description: "Wait for a call to reach the ending state."
max-toc-depth: 3
---

[playaction]: /docs/server-sdks/reference/typescript/relay/actions
[collectaction]: /docs/server-sdks/reference/typescript/relay/actions
[detectaction]: /docs/server-sdks/reference/typescript/relay/actions
[relayevent]: /docs/server-sdks/reference/typescript/relay/events
[play]: /docs/server-sdks/reference/typescript/relay/call/play
[play-and-collect]: /docs/server-sdks/reference/typescript/relay/call/play-and-collect
[detect]: /docs/server-sdks/reference/typescript/relay/call/detect
[wait-for]: /docs/server-sdks/reference/typescript/relay/call/wait-for

Wait for the call to reach the `ending` state. Returns immediately if the call
is already ending or past that state. A typed wait over [`waitFor()`][wait-for].

## **Parameters**

<ParamField path="timeout" type="number | undefined" toc={true}>
  Maximum milliseconds to wait. Throws an `Error` if exceeded. `undefined`
  waits indefinitely.
</ParamField>

## **Returns**

`Promise<`[`RelayEvent`][relayevent]`>` -- The state-change event.

## **Example**

```typescript {12}
import { RelayClient } from '@signalwire/sdk';

const client = new RelayClient({
  project: process.env.SIGNALWIRE_PROJECT_ID!,
  token: process.env.SIGNALWIRE_API_TOKEN!,
  contexts: ['default']
});

client.onCall(async (call) => {
  await call.answer();
  await call.hangup();
  await call.waitForEnding();
  console.log(call.state);
});

await client.run();
```
