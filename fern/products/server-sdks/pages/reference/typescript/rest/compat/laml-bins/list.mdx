---
title: "list"
slug: /reference/typescript/rest/compat/laml-bins/list
description: List LAML bins in the account.
max-toc-depth: 3
---


List LAML bins in the account.

<EndpointSchemaSnippet endpoint="GET /Accounts/{AccountSid}/LamlBins" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /Accounts/{AccountSid}/LamlBins" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const bins = await client.compat.lamlBins.list({ PageSize: 20 });
```
