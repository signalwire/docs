---
title: "list"
slug: /reference/typescript/rest/compat/accounts/list
description: List accounts in the project.
max-toc-depth: 3
---


List accounts.

<EndpointSchemaSnippet endpoint="GET /Accounts" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /Accounts" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const accounts = await client.compat.accounts.list();
```
