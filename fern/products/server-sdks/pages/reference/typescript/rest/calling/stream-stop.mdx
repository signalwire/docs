---
title: "streamStop"
slug: /reference/typescript/rest/calling/stream-stop
description: Stop an active audio stream on a call via REST.
max-toc-depth: 3
---

[stream]: /docs/server-sdks/reference/typescript/rest/calling/stream

Stop an active audio stream.

<EndpointSchemaSnippet endpoint="POST /api/calling/calls" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /api/calling/calls" />

## **Example**

```typescript {9-9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.streamStop("call-id-xxx", { control_id: "ctrl-id" });
```
