---
title: "recordResume"
slug: /reference/typescript/rest/calling/record-resume
description: Resume a paused recording on a call via REST.
max-toc-depth: 3
---

[record]: /docs/server-sdks/reference/typescript/rest/calling/record

Resume a paused recording.

<EndpointSchemaSnippet endpoint="POST /api/calling/calls" />

## **Response Example**

<EndpointResponseSnippet endpoint="POST /api/calling/calls" />

## **Example**

```typescript {9-9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.recordResume("call-id-xxx", { control_id: "ctrl-id" });
```
