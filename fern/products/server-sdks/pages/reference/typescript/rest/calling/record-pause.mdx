---
title: "recordPause"
slug: /reference/typescript/rest/calling/record-pause
description: Pause an active recording on a call via REST.
max-toc-depth: 3
---

[record-resume]: /docs/server-sdks/reference/typescript/rest/calling/record-resume
[record]: /docs/server-sdks/reference/typescript/rest/calling/record

Pause an active recording. The recording can be resumed later with
[`recordResume()`][record-resume].

## **Request**

<Schema type="Calling.CallRecordPauseRequest" api="signalwire-rest" exclude={["command"]} />

## **Response**

<Schema type="Calling.CallResponse" api="signalwire-rest" />

## **Example**

```typescript {9-9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

await client.calling.recordPause("call-id-xxx", { control_id: "ctrl-id" });
```
