---
title: "get"
slug: /reference/typescript/rest/fabric/resources/get
description: Retrieve a single Fabric resource.
max-toc-depth: 3
---

Retrieve a single Fabric resource by ID.

<EndpointSchemaSnippet endpoint="GET /api/fabric/resources/{id}" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fabric/resources/{id}" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const resource = await client.fabric.resources.get("resource-id");
console.log(`${resource.type}: ${resource.name}, ID: ${resource.id}`);
```
