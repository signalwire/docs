---
title: "listAddresses"
slug: /reference/typescript/rest/fabric/relay-applications/list-addresses
description: List addresses associated with a RELAY application resource.
max-toc-depth: 3
---

List addresses associated with a RELAY application resource.

<EndpointSchemaSnippet endpoint="GET /api/fabric/resources/relay_applications/{id}/addresses" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fabric/resources/relay_applications/{id}/addresses" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const addresses = await client.fabric.relayApplications.listAddresses("resource-id");
for (const addr of addresses.data ?? []) {
  console.log(`${addr.display_name}: ${addr.id}`);
}
```
