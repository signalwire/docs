---
title: "listVersions"
slug: /reference/typescript/rest/fabric/call-flows/list-versions
description: List all versions of a call flow.
max-toc-depth: 3
---

List all versions of a call flow resource.

<EndpointSchemaSnippet endpoint="GET /api/fabric/resources/call_flow/{id}/versions" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fabric/resources/call_flow/{id}/versions" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const response = await client.fabric.callFlows.listVersions("call-flow-id");
for (const version of response.data ?? []) {
  console.log(`Version ${version.version}: ${version.id}`);
}
```
