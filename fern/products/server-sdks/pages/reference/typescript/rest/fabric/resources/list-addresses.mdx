---
title: "listAddresses"
slug: /reference/typescript/rest/fabric/resources/list-addresses
description: List addresses associated with a Fabric resource.
max-toc-depth: 3
---

List addresses associated with a Fabric resource.

<EndpointSchemaSnippet endpoint="GET /api/fabric/resources/{id}/addresses" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fabric/resources/{id}/addresses" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const addresses = await client.fabric.resources.listAddresses("resource-id");
for (const addr of addresses.data ?? []) {
  console.log(`${addr.display_name}: ${addr.id}`);
}
```
