---
title: "listAddresses"
slug: /reference/typescript/rest/fabric/swml-scripts/list-addresses
description: List addresses associated with a SWML script resource.
max-toc-depth: 3
---

List addresses associated with a SWML script resource.

<EndpointSchemaSnippet endpoint="GET /api/fabric/resources/swml_scripts/{id}/addresses" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/fabric/resources/swml_scripts/{id}/addresses" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const addresses = await client.fabric.swmlScripts.listAddresses("resource-id");
for (const addr of addresses.data ?? []) {
  console.log(`${addr.display_name}: ${addr.id}`);
}
```
