---
title: "update"
slug: /reference/typescript/rest/sip-profile/update
description: "Update the project SIP profile."
max-toc-depth: 3
---

Update the project SIP profile. Uses PUT for full replacement.

<EndpointSchemaSnippet endpoint="PUT /api/relay/rest/sip_profile" />

## **Response Example**

<EndpointResponseSnippet endpoint="PUT /api/relay/rest/sip_profile" />

## **Example**

```typescript {4}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient();
const result = await client.sipProfile.update({ default_codecs: ['PCMU', 'PCMA'] });
console.log(result);
```
