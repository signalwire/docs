---
title: "Room Tokens"
slug: /reference/typescript/rest/video/room-tokens
description: Manage video room tokens via the Video namespace.
max-toc-depth: 3
---

[restclient]: /docs/server-sdks/reference/typescript/rest/client
[create]: /docs/server-sdks/reference/typescript/rest/video/room-tokens/create

Generate room tokens allowing clients to join video rooms.

Access via `client.video.roomTokens` on a [`RestClient`][restclient] instance.

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com",
});

const token = await client.video.roomTokens.create({
  room_name: "my_room",
  user_name: "John Smith",
});
```

## **Methods**

<CardGroup cols={3}>
  <Card title="create" href="/docs/server-sdks/reference/typescript/rest/video/room-tokens/create">
    Create a room token.
  </Card>
</CardGroup>
