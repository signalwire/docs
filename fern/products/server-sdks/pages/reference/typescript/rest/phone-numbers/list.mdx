---
title: "list"
slug: /reference/typescript/rest/phone-numbers/list
description: List phone numbers owned by the project.
max-toc-depth: 3
---

List phone numbers owned by the project.

<EndpointSchemaSnippet endpoint="GET /api/relay/rest/phone_numbers" />

## **Response Example**

<EndpointResponseSnippet endpoint="GET /api/relay/rest/phone_numbers" />

## **Example**

```typescript {9}
import { RestClient } from "@signalwire/sdk";

const client = new RestClient({
  project: "your-project-id",
  token: "your-api-token",
  host: "your-space.signalwire.com"
});

const result = await client.phoneNumbers.list({ page_size: 10 });
for (const number of result.data ?? []) {
    console.log(number.number, number.name);
}
```
