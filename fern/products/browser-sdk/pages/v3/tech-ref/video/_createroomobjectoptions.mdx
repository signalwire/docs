<ParamField path="applyLocalVideoOverlay" type="boolean" default="true">
  Whether to apply the local-overlay on top of your video.
</ParamField>

<ParamField path="audio" type="boolean | MediaTrackConstraints" default="true">
  Audio constraints to use when joining the room.
</ParamField>

<ParamField path="autoJoin" type="boolean">
  Whether to automatically join the room session.
</ParamField>

<ParamField path="iceServers" type="RTCIceServer[]">
  List of ICE servers.
</ParamField>

<ParamField path="logLevel" type="'trace' | 'debug' | 'info' | 'warn' | 'error' | 'silent'">
  Logging level.
</ParamField>

<ParamField path="project" type="string" required={true}>
  SignalWire project id, e.g. `a10d8a9f-2166-4e82-56ff-118bc3a4840f`.
</ParamField>

<ParamField path="rootElementId" type="string">
  Id of the HTML element in which to display the video stream.
</ParamField>

<ParamField path="speakerId" type="string">
  Id of the speaker device to use for audio output. If undefined, picks a default speaker.
</ParamField>

<ParamField path="stopCameraWhileMuted" type="boolean" default="true">
  Whether to stop the camera when the member is muted.
</ParamField>

<ParamField path="stopMicrophoneWhileMuted" type="boolean" default="true">
  Whether to stop the microphone when the member is muted.
</ParamField>

<ParamField path="token" type="string" required={true}>
  SignalWire project token, e.g. `PT9e5660c101cd140a1c93a0197640a369cf5f16975a0079c9`.
</ParamField>

<ParamField path="video" type="boolean | MediaTrackConstraints" default="true">
  Video constraints to use when joining the room.
</ParamField>
