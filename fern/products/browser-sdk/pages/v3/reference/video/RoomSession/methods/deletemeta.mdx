---
id: e503df7c-b9f5-40a5-9ed1-c21cf95e3980
title: "deleteMeta"
slug: /js/reference/video/room-session/delete-meta
description: deleteMeta method for the RoomSession class.
max-toc-depth: 3
---

### deleteMeta

- **deleteMeta**(`keys`): `Promise<void>`

Deletes the specified keys from the metadata for this RoomSession.

#### Parameters

<ParamField path="keys" type="string[]" toc={true}>
  The keys to remove.
</ParamField>

#### Returns

`Promise<void>`

#### Permissions

- `room.set_meta`

You need to specify the permissions when [creating the Video Room Token](/docs/apis/rest/video/room-tokens/create-room-token) on the server side.

#### Example

```js
roomSession.on("room.updated", (e) => {
  // We can set an event listener to log changes to the metadata.
  console.log(e.room.meta);
});

await roomSession.setMeta({ foo: "bar", baz: true });
// The logger will now print `{ foo: "bar", baz: true }`

await roomSession.deleteMeta(["foo"]);
// The logger will now print `{ baz: true }`
```

