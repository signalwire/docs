# `<sw-participant-controls>`

Per-participant control panel. Consumes `callStateContext`.

## Props

| prop | type | description |
|------|------|-------------|
| `participant-id` | `string` | ID of participant to control |
| `show-volume` | `boolean` | Show volume slider |
| `show-pin` | `boolean` | Show pin button |
