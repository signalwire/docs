---
title: "Migrate from v3"
slug: /guides/migrate-from-v3
sidebar-title: "Migrate from v3"
position: 4
max-toc-depth: 3
---

A guided walkthrough for moving an existing v3 (`@signalwire/js@3.x`) integration to v4. Covers package changes, the new client entry point, the shift from event emitters to RxJS observables, auth-token differences, and renamed/removed APIs.

> **TODO:** Stub. Sections to fill in:
>
> - Package & import changes (`Video.RoomSession` → `SignalWire` / `dial`)
> - Auth: room tokens → Subscriber Access Tokens & Embed Tokens
> - Events → Observables migration table (`.on('member.joined')` → `participants$`)
> - Removed namespaces (`Chat`, `PubSub`, `WebRTC` standalone clients)
> - Web Components: v3 had none — point to new `<sw-*>` elements
> - Side-by-side code diffs for: outbound call, inbound call, screen share, recording
