---
title: "setLogLevel"
slug: /reference/functions/set-log-level
description: "Set the log level for the built-in logger."
max-toc-depth: 3
---

```ts
setLogLevel(level): void
```
Set the log level for the built-in logger.
Has no effect when a custom logger is set via `setLogger()`.

## Parameters

<ParamField path="level" type="LogLevel" required>
</ParamField>

## Returns

`void`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/utils/logger.ts#L98)
