---
title: "updateMeta"
slug: /reference/webrtc-call/update-meta
description: "Merges values into the call's custom metadata (unlike setMeta which replaces)."
max-toc-depth: 3
---

```ts
updateMeta(_meta): Promise<void>
```
Merges values into the call's custom metadata (unlike [setMeta](#setmeta) which replaces).

## Parameters

<ParamField path="_meta" type="Record<string, unknown>" required>
  Metadata to merge.
</ParamField>

## Returns

`Promise<void>`

## Throws

Not yet implemented.

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Call.ts#L396)
