---
title: "memberLeft$"
slug: /reference/webrtc-call/member-left$
description: "Observable of member-left events, emitted when a participant leaves the call."
max-toc-depth: 3
---

```ts
get memberLeft$(): Observable<MemberLeftPayload>
```
Observable of member-left events, emitted when a participant leaves the call.

## **Examples**

```ts
call.memberLeft$.subscribe((memberLeft) => {
  console.log('memberLeft:', memberLeft);
});
```
