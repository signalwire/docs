---
title: "executeMethod"
slug: /reference/webrtc-call/execute-method
description: "Executes a Verto RPC method targeting a specific participant."
max-toc-depth: 3
---

```ts
executeMethod<T>(target, method, args): Promise<T>
```
Executes a Verto RPC method targeting a specific participant.

Constructs call context (node_id, call_id, member_id) and sends the RPC request.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` *extends* `JSONRPCResponse` | `JSONRPCResponse` |

## Parameters

<ParamField path="target" type="string | MemberTarget" required>
  Target member ID string, or a MemberTarget object.
</ParamField>

<ParamField path="method" type="string" required>
  Verto method name (e.g. `'call.mute'`, `'call.member.remove'`).
</ParamField>

<ParamField path="args" type="Record<string, unknown>" required>
  Parameters for the RPC method.
</ParamField>

## Returns

`Promise<T>`

The RPC response.

## Throws

If the RPC call returns an error.

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Call.ts#L424)
