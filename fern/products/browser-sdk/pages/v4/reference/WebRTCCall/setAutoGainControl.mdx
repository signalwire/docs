---
title: "setAutoGainControl"
slug: /reference/webrtc-call/set-auto-gain-control
description: "Toggle browser automatic gain control on the local mic at runtime."
max-toc-depth: 3
---

```ts
setAutoGainControl(enabled): Promise<void>
```
Toggle browser automatic gain control on the local mic at runtime.

## **Parameters**

<ParamField path="enabled" type="boolean" required toc={true}>
  Whether automatic gain control should be enabled.
</ParamField>

## **Returns**

`Promise<void>`
