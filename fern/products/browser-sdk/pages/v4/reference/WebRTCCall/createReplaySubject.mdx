---
title: "createReplaySubject"
slug: /reference/webrtc-call/create-replay-subject
description: "Internal helper that creates a ReplaySubject tracked for cleanup on destroy."
max-toc-depth: 3
---

```ts
protected createReplaySubject<T>(bufferSize?, windowTime?): ReplaySubject<T>
```
## **Type Parameters**

| Type Parameter |
| ------ |
| `T` |

## **Parameters**

<ParamField path="bufferSize" type="number" toc={true}>
  Maximum number of past values to replay. Defaults to unbounded.
</ParamField>

<ParamField path="windowTime" type="number" toc={true}>
  Time window in milliseconds for replay. Defaults to unbounded.
</ParamField>

## **Returns**

`ReplaySubject<T>`

#### Inherited from

`Destroyable.createReplaySubject`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/behaviors/Destroyable.ts#L79)
