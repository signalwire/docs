---
title: "OverconstrainedFallbackError"
slug: /reference/errors/overconstrained-fallback-error
description: "Error thrown when getUserMedia fails with OverconstrainedError and all fallback levels have been exhausted."
max-toc-depth: 3
---
Error thrown when getUserMedia fails with OverconstrainedError
and all fallback levels have been exhausted.

## **Extends**

- `Error`

## **Constructors**

### Constructor

```ts
new OverconstrainedFallbackError(deviceKind, originalError?): OverconstrainedFallbackError
```

### Parameters

<ParamField path="deviceKind" type="string" required toc={true}>
  Device kind whose constraints could not be satisfied (`audio` or `video`).
</ParamField>

<ParamField path="originalError" type="unknown" toc={true}>
  Final `OverconstrainedError` returned after all fallback levels were exhausted.
</ParamField>

## **Properties**

<ParamField path="deviceKind" type="string" required toc={true}>
  Device kind whose constraints could not be satisfied (`audio` or `video`).
</ParamField>

<ParamField path="originalError" type="unknown" toc={true}>
  Final `OverconstrainedError` returned after all fallback levels were exhausted.
</ParamField>
