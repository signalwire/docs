---
title: "requestMediaPermissions"
slug: /reference/signalwire/request-media-permissions
description: "Triggers the browser's media permission dialog and captures the user's device selections."
max-toc-depth: 3
---

```ts
requestMediaPermissions(options?): Promise<PermissionResult>
```
Triggers the browser's media permission dialog and captures the user's device selections.

## **Parameters**

<ParamField path="options" type="{ audio?: boolean; video?: boolean; }" toc={true}>
  Which permissions to request.
</ParamField>

## **Returns**

`Promise<PermissionResult>`

The permission result with selected devices.

## **Examples**

```ts
await client.requestMediaPermissions(options);
```
