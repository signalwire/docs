---
title: "enableStudioAudio"
slug: /reference/self-participant/enable-studio-audio
description: "Enables studio audio mode by disabling all audio processing."
max-toc-depth: 3
---

```ts
enableStudioAudio(): Promise<void>
```
Enables studio audio mode by disabling all audio processing.
Sets echoCancellation, noiseSuppression, and autoGainControl to false.

## **Returns**

`Promise<void>`

## **Examples**

```ts
await selfParticipant.enableStudioAudio();
```

## **See**

- [`studioAudio$`](/docs/browser-sdk/v4/reference/self-participant/studio-audio$) — reactive state pair.
- [`disableStudioAudio`](/docs/browser-sdk/v4/reference/self-participant/disable-studio-audio) — restore default processing.
- Per-flag toggles: [`toggleEchoCancellation`](/docs/browser-sdk/v4/reference/participant/toggle-echo-cancellation), [`toggleAudioInputAutoGain`](/docs/browser-sdk/v4/reference/participant/toggle-audio-input-auto-gain), [`toggleNoiseSuppression`](/docs/browser-sdk/v4/reference/participant/toggle-noise-suppression).
