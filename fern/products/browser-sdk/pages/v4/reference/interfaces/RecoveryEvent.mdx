---
title: "RecoveryEvent"
slug: /reference/interfaces/recovery-event
description: "Event emitted when a recovery action is taken on a call."
max-toc-depth: 3
---
Event emitted when a recovery action is taken on a call.

## **Properties**

<ParamField path="action" type="'keyframe_requested' | 'reinvite_started' | 'reinvite_succeeded' | 'reinvite_failed' | 'reinvite_timeout' | 'max_attempts_reached' | 'call_recovering' | 'call_recovered' | 'call_recovery_failed' | 'signal_reconnect' | 'full_reconnect' | 'video_disabled' | 'video_restored'" required toc={true}>
  The recovery action that was taken.
</ParamField>

<ParamField path="attempt" type="number" toc={true}>
  Current attempt number (for multi-attempt recoveries).
</ParamField>

<ParamField path="maxAttempts" type="number" toc={true}>
  Maximum number of attempts allowed.
</ParamField>

<ParamField path="reason" type="string" required toc={true}>
  Human-readable description of why recovery was triggered.
</ParamField>

<ParamField path="timestamp" type="number" required toc={true}>
  Timestamp when the event occurred (epoch ms).
</ParamField>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/resilience.types.ts#L72)
