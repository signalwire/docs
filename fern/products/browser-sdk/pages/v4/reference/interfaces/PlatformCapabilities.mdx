---
title: "PlatformCapabilities"
slug: /reference/interfaces/platform-capabilities
description: "Browser/platform WebRTC capability flags."
max-toc-depth: 3
---
Browser/platform WebRTC capability flags.

## Properties

<ParamField path="audioCodecs" type="readonly string[]" required>
  List of supported audio codecs.
</ParamField>

<ParamField path="audioOutputSelection" type="boolean" required>
  Whether setSinkId (audio output selection) is supported.
</ParamField>

<ParamField path="getDisplayMedia" type="boolean" required>
  Whether getDisplayMedia is available.
</ParamField>

<ParamField path="getUserMedia" type="boolean" required>
  Whether getUserMedia is available.
</ParamField>

<ParamField path="insertableStreams" type="boolean" required>
  Whether insertable streams / encoded transforms are available.
</ParamField>

<ParamField path="screenShare" type="boolean" required>
  Whether screen sharing is supported.
</ParamField>

<ParamField path="screenShareAudio" type="boolean" required>
  Whether screen share can include system audio (Chrome-only).
</ParamField>

<ParamField path="simulcast" type="boolean" required>
  Whether simulcast is supported.
</ParamField>

<ParamField path="videoCodecs" type="readonly string[]" required>
  List of supported video codecs.
</ParamField>

<ParamField path="webrtc" type="boolean" required>
  Whether the browser supports WebRTC at all.
</ParamField>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/resilience.types.ts#L169)
