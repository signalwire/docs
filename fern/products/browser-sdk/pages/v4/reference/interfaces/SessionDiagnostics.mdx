---
title: "SessionDiagnostics"
slug: /reference/interfaces/session-diagnostics
description: "Structured diagnostic bundle for a session."
max-toc-depth: 3
---
Structured diagnostic bundle for a session.

## Properties

<ParamField path="calls" type="readonly CallDiagnosticSummary[]" required>
  Quality summary per call.
</ParamField>

<ParamField path="capabilities" type="PlatformCapabilities" required>
  Platform capabilities detected at construction time.
</ParamField>

<ParamField path="deviceChanges" type="readonly DeviceRecoveryEvent[]" required>
  Device changes that occurred during the session.
</ParamField>

<ParamField path="devices" type="object" required>
  Current device list snapshot.
</ParamField>

<ParamField path="devices.audioInput" type="readonly MediaDeviceInfo[]" required>
</ParamField>

<ParamField path="devices.audioOutput" type="readonly MediaDeviceInfo[]" required>
</ParamField>

<ParamField path="devices.videoInput" type="readonly MediaDeviceInfo[]" required>
</ParamField>

<ParamField path="events" type="readonly DiagnosticEvent[]" required>
  Timeline of significant events during the session.
</ParamField>

<ParamField path="sdkVersion" type="string" required>
  SDK version.
</ParamField>

<ParamField path="userAgent" type="string" required>
  Browser/platform user agent string.
</ParamField>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/resilience.types.ts#L272)
