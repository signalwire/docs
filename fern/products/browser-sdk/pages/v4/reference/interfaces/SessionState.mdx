---
title: "SessionState"
slug: /reference/interfaces/session-state
description: "Extended session interface that adds call management and authentication state on top of the narrow ClientSession contract."
max-toc-depth: 3
---
Extended session interface that adds call management and authentication
state on top of the narrow ClientSession contract.

Accessible via `client.session`. Call and CallFactory continue to depend
only on the narrow ClientSession interface.

## **Extends**

- `ClientSession`

## **Properties**

<ParamField path="authenticated" type="boolean" required toc={true}>
  Current authentication state. Returns `true` if the session is currently authenticated.
</ParamField>

<ParamField path="authenticated$" type="Observable<boolean>" required toc={true}>
  Observable that emits `true` once the session has been authenticated, and `false` after disconnect.
</ParamField>

<ParamField path="calls" type="Call[]" required toc={true}>
  Current snapshot of all active calls.
</ParamField>

<ParamField path="calls$" type="Observable<Call[]>" required toc={true}>
  Observable stream of all currently active calls (both inbound and outbound).
</ParamField>

<ParamField path="incomingCalls" type="Call[]" required toc={true}>
  Current snapshot of active inbound calls.
</ParamField>

<ParamField path="incomingCalls$" type="Observable<Call[]>" required toc={true}>
  Observable stream of currently active inbound calls. Filters `calls$` to only include calls with `direction === 'inbound'`.
</ParamField>

<Accordion title="Inherited from ClientSession">
  <ParamField path="iceServers" type="RTCIceServer[] | undefined" required toc={true}>
    ICE servers configuration for WebRTC peer connections Used by VertoManager to configure RTCPeerConnection
  </ParamField>

  <ParamField path="signalingEvent$" type="Observable<Record<string, unknown>>" required toc={true}>
    Observable stream of incoming signaling events Used by Call to listen for call-related events from the server
  </ParamField>
</Accordion>

## **Methods**

### execute()

```ts
execute<T>(request, options?): Promise<T>
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/interfaces/ClientSession.ts#L16)

Execute an RPC request through the session transport

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` *extends* [`JSONRPCResponse`](/docs/browser-sdk/v4/reference/type-aliases/jsonrpc-response) | [`JSONRPCResponse`](/docs/browser-sdk/v4/reference/type-aliases/jsonrpc-response) |

#### Parameters

<ParamField path="request" type="JSONRPCRequest" required toc={true}>
  The JSON-RPC request to execute
</ParamField>

<ParamField path="options" type="PendingRPCOptions" toc={true}>
  Optional RPC execution options (timeout, etc.)
</ParamField>

#### Returns

`Promise<T>`

Promise resolving to the RPC response

#### Inherited from

`ClientSession.execute`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/interfaces/SessionState.ts#L12)
