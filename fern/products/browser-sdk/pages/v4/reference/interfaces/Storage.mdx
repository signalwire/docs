---
title: "Storage"
slug: /reference/interfaces/storage
description: "Key-value storage interface for persisting SDK preferences and state."
max-toc-depth: 3
---
Key-value storage interface for persisting SDK preferences and state.

## **Methods**

### clear()

```ts
clear(scope): Promise<void>
```

Clears all keys in the given scope. Implementations may scope the clear to SDK keys only.

#### Parameters

<ParamField path="scope" type="StorageScope" required toc={true}>
  Storage scope (e.g. session vs. persistent).
</ParamField>

#### Returns

`Promise<void>`

***

### getItem()

```ts
getItem(key, scope): Promise<string | null>
```

#### Parameters

<ParamField path="key" type="string" required toc={true}>
  Storage key to read or write.
</ParamField>

<ParamField path="scope" type="StorageScope" required toc={true}>
  Storage scope (e.g. session vs. persistent).
</ParamField>

#### Returns

`Promise<string | null>`

***

### removeItem()

```ts
removeItem(key, scope): Promise<void>
```

#### Parameters

<ParamField path="key" type="string" required toc={true}>
  Storage key to read or write.
</ParamField>

<ParamField path="scope" type="StorageScope" required toc={true}>
  Storage scope (e.g. session vs. persistent).
</ParamField>

#### Returns

`Promise<void>`

***

### setItem()

```ts
setItem(key, value, scope): Promise<void>
```

#### Parameters

<ParamField path="key" type="string" required toc={true}>
  Storage key to read or write.
</ParamField>

<ParamField path="value" type="string | null" required toc={true}>
  Value to write, or `null` to clear the key.
</ParamField>

<ParamField path="scope" type="StorageScope" required toc={true}>
  Storage scope (e.g. session vs. persistent).
</ParamField>

#### Returns

`Promise<void>`
