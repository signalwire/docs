---
title: "NodeSocketAdapter"
slug: /reference/interfaces/node-socket-adapter
description: "There's a difference in searchParams between URL from lib and URL from url (node) that makes using the same not possible for us."
max-toc-depth: 3
---
There's a difference in `searchParams` between URL from
`lib` and URL from `url` (node) that makes using the same
not possible for us.

## **Constructors**

### Constructor

```ts
new NodeSocketAdapter(address, options?): NodeSocketClient
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/common.types.ts#L93)

#### Parameters

<ParamField path="address" type="string | URL" required toc={true}>
  WebSocket URL to connect to.
</ParamField>

<ParamField path="options" type="unknown" toc={true}>
  Underlying socket-library options.
</ParamField>

#### Returns

`NodeSocketClient`

### Constructor

```ts
new NodeSocketAdapter(address, protocols?, options?): NodeSocketClient
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/common.types.ts#L94)

#### Parameters

<ParamField path="address" type="string | URL" required toc={true}>
  WebSocket URL to connect to.
</ParamField>

<ParamField path="protocols" type="string | string[]" toc={true}>
  Optional WebSocket sub-protocol(s) to request.
</ParamField>

<ParamField path="options" type="unknown" toc={true}>
  Underlying socket-library options.
</ParamField>

#### Returns

`NodeSocketClient`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/common.types.ts#L92)
