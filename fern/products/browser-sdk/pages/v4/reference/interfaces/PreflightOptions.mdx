---
title: "PreflightOptions"
slug: /reference/interfaces/preflight-options
---
Options for the preflight connectivity test.

## Properties

<ParamField path="audioDevice" type="MediaDeviceInfo">
  Test a specific audio device instead of the currently selected one.
</ParamField>

<ParamField path="duration" type="number">
  How long to run the media test in seconds (default: 10).
</ParamField>

<ParamField path="skipMediaTest" type="boolean">
  Skip the media/bandwidth test, only test signaling + TURN + devices.
</ParamField>

<ParamField path="videoDevice" type="MediaDeviceInfo">
  Test a specific video device instead of the currently selected one.
</ParamField>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/resilience.types.ts#L197)
