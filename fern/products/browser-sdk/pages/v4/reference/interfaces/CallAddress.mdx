---
title: "CallAddress"
slug: /reference/interfaces/call-address
description: "Minimal address interface for call context Avoids circular dependency with full Address class"
max-toc-depth: 3
---
Minimal address interface for call context
Avoids circular dependency with full Address class

## **Properties**

<ParamField path="displayName" type="string" toc={true}>
  Human-readable display name for the address.
</ParamField>

<ParamField path="id" type="string" required toc={true}>
  Server-assigned ID of the address.
</ParamField>

<ParamField path="textMessages$" type="Observable<CallTextMessageCollection | undefined>" required toc={true}>
  Observable of the text-message collection for this address.
</ParamField>

<ParamField path="type" type="string" toc={true}>
  Kind of address (e.g. `sip`, `room`, `subscriber`).
</ParamField>

## **Methods**

### sendText()

```ts
sendText(text): Promise<void>
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/types/call.types.ts#L181)

#### Parameters

<ParamField path="text" type="string" required toc={true}>
  Address as a URI or plain text.
</ParamField>

#### Returns

`Promise<void>`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/types/call.types.ts#L177)
