---
title: "SDKCredential"
slug: /reference/interfaces/sdk-credential
description: "Authentication credentials for the SDK."
max-toc-depth: 3
---
Authentication credentials for the SDK.

At least one of `token` or `authorizationState` must be provided.

## **Properties**

<ParamField path="authorizationState" type="string" toc={true}>
  Pre-authorized session state (alternative to token).
</ParamField>

<ParamField path="expiry_at" type="number" toc={true}>
  Token expiry timestamp in milliseconds since epoch. When set, the SDK attempts credential refresh before expiry.
</ParamField>

<ParamField path="token" type="string" toc={true}>
  JWT subscriber access token (SAT).
</ParamField>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/common.types.ts#L112)
