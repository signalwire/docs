---
title: "CallDiagnosticSummary"
slug: /reference/interfaces/call-diagnostic-summary
description: "Quality summary for a single call in the diagnostic bundle."
max-toc-depth: 3
---
Quality summary for a single call in the diagnostic bundle.

## **Properties**

<ParamField path="avgQualityScore" type="number" required toc={true}>
  Average MOS quality score over the call.
</ParamField>

<ParamField path="callId" type="string" required toc={true}>
  Unique call ID.
</ParamField>

<ParamField path="destination" type="string" toc={true}>
  The destination dialed, if outbound.
</ParamField>

<ParamField path="direction" type="inbound | outbound" required toc={true}>
  Whether the call was inbound or outbound.
</ParamField>

<ParamField path="duration" type="number" required toc={true}>
  Total call duration in seconds.
</ParamField>

<ParamField path="finalMetrics" type="CallNetworkMetrics" required toc={true}>
  Final network metrics snapshot at call end.
</ParamField>

<ParamField path="iceCandidateTypes" type="readonly string[]" required toc={true}>
  ICE candidate types that were used.
</ParamField>

<ParamField path="minQualityScore" type="number" required toc={true}>
  Worst (minimum) MOS quality score during the call.
</ParamField>

<ParamField path="recoveryAttempts" type="number" required toc={true}>
  Number of recovery attempts made during the call.
</ParamField>

<ParamField path="status" type="string" required toc={true}>
  Final call status.
</ParamField>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/resilience.types.ts#L306)
