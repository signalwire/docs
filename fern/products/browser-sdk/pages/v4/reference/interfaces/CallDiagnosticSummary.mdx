---
title: "CallDiagnosticSummary"
slug: /reference/interfaces/call-diagnostic-summary
description: "Quality summary for a single call in the diagnostic bundle."
max-toc-depth: 3
---
Quality summary for a single call in the diagnostic bundle.

## Properties

<ParamField path="avgQualityScore" type="number" required>
  Average MOS quality score over the call.
</ParamField>

<ParamField path="callId" type="string" required>
  Unique call ID.
</ParamField>

<ParamField path="destination" type="string">
  The destination dialed, if outbound.
</ParamField>

<ParamField path="direction" type="inbound | outbound" required>
  Whether the call was inbound or outbound.
</ParamField>

<ParamField path="duration" type="number" required>
  Total call duration in seconds.
</ParamField>

<ParamField path="finalMetrics" type="CallNetworkMetrics" required>
  Final network metrics snapshot at call end.
</ParamField>

<ParamField path="iceCandidateTypes" type="readonly string[]" required>
  ICE candidate types that were used.
</ParamField>

<ParamField path="minQualityScore" type="number" required>
  Worst (minimum) MOS quality score during the call.
</ParamField>

<ParamField path="recoveryAttempts" type="number" required>
  Number of recovery attempts made during the call.
</ParamField>

<ParamField path="status" type="string" required>
  Final call status.
</ParamField>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/types/resilience.types.ts#L306)
