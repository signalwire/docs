---
title: "WebRTCMediaDevices"
slug: /reference/interfaces/webrtc-media-devices
description: "Subset of the MediaDevices interface actually used by the SDK."
max-toc-depth: 3
---
Subset of the `MediaDevices` interface actually used by the SDK.

Implementations only need to provide these methods — the full browser
`MediaDevices` type is intentionally not required so that React Native
and other non-browser environments can conform without polyfilling
unused APIs.

## **Methods**

### addEventListener()

```ts
addEventListener(type, listener): void
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/dependencies/interfaces.ts#L126)

#### Parameters

<ParamField path="type" type="string" required toc={true}>
  Event type to listen for.
</ParamField>

<ParamField path="listener" type="EventListenerOrEventListenerObject" required toc={true}>
  Listener invoked when the event fires.
</ParamField>

#### Returns

`void`

***

### enumerateDevices()

```ts
enumerateDevices(): Promise<MediaDeviceInfo[]>
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/dependencies/interfaces.ts#L125)

#### Returns

`Promise<MediaDeviceInfo[]>`

***

### getDisplayMedia()?

```ts
optional getDisplayMedia(options): Promise<MediaStream>
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/dependencies/interfaces.ts#L124)

#### Parameters

<ParamField path="options" type="DisplayMediaStreamOptions" required toc={true}>
  Options to pass to `getDisplayMedia`.
</ParamField>

#### Returns

`Promise<MediaStream>`

***

### getUserMedia()

```ts
getUserMedia(constraints): Promise<MediaStream>
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/dependencies/interfaces.ts#L123)

#### Parameters

<ParamField path="constraints" type="MediaStreamConstraints" required toc={true}>
  Media-track constraints to pass to `getUserMedia`.
</ParamField>

#### Returns

`Promise<MediaStream>`

***

### removeEventListener()

```ts
removeEventListener(type, listener): void
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/dependencies/interfaces.ts#L127)

#### Parameters

<ParamField path="type" type="string" required toc={true}>
  Event type to listen for.
</ParamField>

<ParamField path="listener" type="EventListenerOrEventListenerObject" required toc={true}>
  Listener invoked when the event fires.
</ParamField>

#### Returns

`void`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/dependencies/interfaces.ts#L122)
