---
title: "Directory"
slug: /reference/interfaces/directory
---
Directory interface for managing addresses

This is the public API contract for address directory functionality.
It provides access to addresses, loading capabilities, and search functionality.

## Extends

- `AddressProvider<Address>` (see [`Address`](../Address/index.mdx))

## Properties

<ParamField path="addresses" type="Address[]">
  Current snapshot of all addresses in the directory
</ParamField>

<ParamField path="addresses$" type="Observable<Address[]>">
  Observable stream of all addresses in the directory Emits a new array whenever addresses are added, removed, or updated
</ParamField>

<ParamField path="hasMore$" type="Observable<boolean>">
  Observable indicating whether more addresses can be loaded from the server
</ParamField>

<ParamField path="loading" type="boolean">
</ParamField>

<ParamField path="loading$" type="Observable<boolean>">
  Observable indicating the current loading state Emits `true` when loading, `false` when idle
</ParamField>

## Methods

### findAddressIdByURI()

```ts
findAddressIdByURI(uri): Promise<string | undefined>
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Directory.ts#L58)

Find an address ID by searching for a name

#### Parameters

<ParamField path="uri" type="string" required>
  The address name to search for
</ParamField>

#### Returns

`Promise<string | undefined>`

Promise resolving to the address ID, or undefined if not found

***

### get()

```ts
get(addressId): Address | undefined
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Directory.ts#L50)

Get a specific address by ID

#### Parameters

<ParamField path="addressId" type="string" required>
  The address ID to retrieve
</ParamField>

#### Returns

[`Address`](../Address/index.mdx) | `undefined`

The address instance, or undefined if not found

***

### get$()

```ts
get$(id): Observable<Address> | undefined
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/interfaces/AddressProvider.ts#L22)

Get an observable stream for a specific address by ID

#### Parameters

<ParamField path="id" type="string" required>
  The address ID to retrieve
</ParamField>

#### Returns

`Observable<Address> | undefined`

Observable of the address, or undefined if not found

#### Inherited from

`AddressProvider.get$`

***

### loadMore()

```ts
loadMore(): void
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Directory.ts#L42)

Load more addresses from the server
Only loads if `hasMore` is true

#### Returns

`void`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Directory.ts#L13)
