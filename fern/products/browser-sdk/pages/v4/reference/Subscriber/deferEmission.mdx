---
title: "deferEmission"
slug: /reference/subscriber/defer-emission
description: "Wraps an observable so emissions are deferred to the microtask queue."
max-toc-depth: 3
---

```ts
protected deferEmission<T>(observable): Observable<T>
```
Wraps an observable so emissions are deferred to the microtask queue.

Use ONLY for public-facing getters that expose a subject via
`.asObservable()` without going through `cachedObservable`.

Do NOT use for observables consumed internally by the SDK.

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `observable` | `Observable<T>` |

## Returns

`Observable<T>`

## Inherited from

`Fetchable.deferEmission`

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/behaviors/Destroyable.ts#L61)
