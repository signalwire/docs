---
title: "Subscriber"
slug: /reference/subscriber
description: "Authenticated subscriber profile."
max-toc-depth: 3
---
Authenticated subscriber profile.

Fetched automatically when a [SignalWire](../SignalWire/index.mdx) connects.
Contains identity, contact, and organization details.

## **Extends**

- `Fetchable<GetSubscriberInfoResponse>`

## **Constructors**

### Constructor

```ts
new Subscriber(http): Subscriber
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Subscriber.ts#L50)

#### Parameters

<ParamField path="http" type="HTTPRequestController" required toc={true}>
  HTTP request controller used for REST calls.
</ParamField>

#### Returns

`Subscriber`

## **Properties**

<ParamField path="addresses" type="GetAddressResponse[]" required toc={true}>
  Fabric addresses associated with this subscriber.
</ParamField>

<ParamField path="appSettings" type="object" toc={true}>
  Application-level settings (display name, permission scopes).
</ParamField>

<ParamField path="appSettings.displayName" type="string" toc={true}>
  Display name advertised to other participants.
</ParamField>

<ParamField path="appSettings.scopes" type="string[]" toc={true}>
  OAuth-style scopes granted to this session.
</ParamField>

<ParamField path="companyName" type="string" toc={true}>
  Company name.
</ParamField>

<ParamField path="country" type="string" toc={true}>
  Country code.
</ParamField>

<ParamField path="displayName" type="string" toc={true}>
  Display name shown to other participants.
</ParamField>

<ParamField path="email" type="string" required toc={true}>
  Subscriber email address.
</ParamField>

<ParamField path="firstName" type="string" toc={true}>
  First name.
</ParamField>

<ParamField path="id" type="string" required toc={true}>
  Unique subscriber identifier.
</ParamField>

<ParamField path="jobTitle" type="string" toc={true}>
  Job title.
</ParamField>

<ParamField path="lastName" type="string" toc={true}>
  Last name.
</ParamField>

<ParamField path="pushNotificationKey" type="string" required toc={true}>
  Push notification key for mobile/web push.
</ParamField>

<ParamField path="region" type="string" toc={true}>
  Region/state.
</ParamField>

<ParamField path="satClaims" type="SATClaims" toc={true}>
  Filtered SAT claims when the token has special capabilities (e.g., refresh scope).
</ParamField>

<ParamField path="timeZone" type="number" toc={true}>
  Time zone offset.
</ParamField>

<Accordion title="Inherited from Fetchable">
  <ParamField path="fetched$" type="Observable<boolean>" required toc={true}>
  Observable that emits `true` once the subscriber profile has been fetched.
</ParamField>

  <ParamField path="fromPath" type="string" required toc={true}>
  Origin path or URI captured at construction time, used for routing inbound calls.
</ParamField>
</Accordion>

<Accordion title="Internal">
  <ParamField path="_destroyed$" type="Subject<void>" required toc={true}>
  Internal subject signalled when the instance is destroyed.
</ParamField>

  <ParamField path="http" type="HTTPRequestController" required toc={true}>
  HTTP request controller used for REST calls.
</ParamField>

  <ParamField path="subjects" type="Subject<unknown>[]" required toc={true}>
  Internal list of RxJS subjects tracked for cleanup on destroy.
</ParamField>

  <ParamField path="subscriptions" type="Subscription[]" required toc={true}>
  Internal list of active subscriptions tracked for cleanup on destroy.
</ParamField>
</Accordion>

## **Accessors**

<CardGroup cols={2}>
  <Card title="destroyed$" href="/docs/browser-sdk/v4/reference/subscriber/destroyed$">
    Observable that emits when the instance is destroyed
  </Card>
</CardGroup>

## **Methods**

<CardGroup cols={2}>
  <Card title="cachedObservable" href="/docs/browser-sdk/v4/reference/subscriber/cached-observable">
    Internal helper: lazily creates and caches an observable keyed by `key`.
  </Card>
  <Card title="createBehaviorSubject" href="/docs/browser-sdk/v4/reference/subscriber/create-behavior-subject">
    Internal helper: creates a `BehaviorSubject` tracked for cleanup on destroy.
  </Card>
  <Card title="createReplaySubject" href="/docs/browser-sdk/v4/reference/subscriber/create-replay-subject">
    Internal helper: creates a `ReplaySubject` tracked for cleanup on destroy.
  </Card>
  <Card title="createSubject" href="/docs/browser-sdk/v4/reference/subscriber/create-subject">
    Internal helper: creates a `Subject` tracked for cleanup on destroy.
  </Card>
  <Card title="deferEmission" href="/docs/browser-sdk/v4/reference/subscriber/defer-emission">
    Wraps an observable so emissions are deferred to the microtask queue.
  </Card>
  <Card title="destroy" href="/docs/browser-sdk/v4/reference/subscriber/destroy">
    Cleans up subscriptions and subjects owned by this instance.
  </Card>
  <Card title="populateInstance" href="/docs/browser-sdk/v4/reference/subscriber/populate-instance">
    Internal helper: hydrates this instance from a server response payload.
  </Card>
  <Card title="publicCachedObservable" href="/docs/browser-sdk/v4/reference/subscriber/public-cached-observable">
    Like `cachedObservable`, but defers emissions to the microtask queue via `observeOn(asapScheduler)`.
  </Card>
  <Card title="subscribeTo" href="/docs/browser-sdk/v4/reference/subscriber/subscribe-to">
    Internal helper: subscribes to an observable and tracks the subscription for cleanup.
  </Card>
</CardGroup>

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Subscriber.ts#L17)
