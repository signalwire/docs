---
title: "receiveAudio"
slug: /reference/client-preferences/receive-audio
description: "Whether to receive remote audio by default."
max-toc-depth: 3
---

```ts
get receiveAudio(): boolean
set receiveAudio(value): void
```
Whether to receive remote audio by default.

## **Parameters**

<ParamField path="value" type="boolean" toc={true}>
  If `true`, the local peer accepts incoming audio.
</ParamField>

## **Examples**

```ts
console.log(client.preferences.receiveAudio);
```
