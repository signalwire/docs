---
title: "connectionTimeout"
slug: /reference/client-preferences/connection-timeout
description: "WebSocket connection timeout in seconds."
max-toc-depth: 3
---

```ts
get connectionTimeout(): number
set connectionTimeout(seconds: number)
```
WebSocket connection timeout in seconds.

## **Parameters**

<ParamField path="seconds" type="number" required toc={true}>
  Connection attempt timeout, in seconds.
</ParamField>

## **Examples**

```ts
// Read
console.log(client.preferences.connectionTimeout);

// Write
client.preferences.connectionTimeout = 1;
```
