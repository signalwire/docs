---
title: "StaticCredentialProvider"
slug: /reference/credential-providers/static-credential-provider
description: "Credential provider that returns a fixed set of credentials."
max-toc-depth: 3
---
Credential provider that returns a fixed set of credentials.

Use when the token is already available (e.g. from a backend endpoint).

## **Examples**

```ts
const provider = new StaticCredentialProvider({ token: 'my-sat-token' });
const client = new SignalWire(provider);
```

## **Implements**

- [`CredentialProvider`](/docs/browser-sdk/v4/reference/interfaces/credential-provider)

## **Constructors**

### Constructor

```ts
new StaticCredentialProvider(credentials): StaticCredentialProvider
```

### Parameters

<ParamField path="credentials" type="SDKCredential" required toc={true}>
  Credentials used to authenticate the SDK session.
</ParamField>

## **Methods**

<CardGroup cols={2}>
  <Card title="authenticate" href="/docs/browser-sdk/v4/reference/credential-providers/static-credential-provider/authenticate">
    Returns the static credentials.
  </Card>
</CardGroup>
