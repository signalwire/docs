---
title: "outputVolume$"
slug: /reference/participant/output-volume$
description: "Observable of the participant's server-side speaker output volume as reported by the mix engine (FreeSWITCH channel write volume)."
max-toc-depth: 3
---

```ts
get outputVolume$(): Observable<number | undefined>
```
Observable of the participant's **server-side** speaker output volume as
reported by the mix engine (FreeSWITCH channel write volume). NOT the
local HTML `<audio>` element volume — set that on your own element.

See [setAudioOutputVolume](#setaudiooutputvolume)

## outputVolume

```ts
get outputVolume(): number | undefined
```

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Participant.ts#L354)

Current **server-side** speaker output volume from the mix engine, or
`undefined` if not set. Not the local `<audio>` element volume.

[Go to Code](https://github.com/signalwire/browser-sdk/blob/main/packages/main/src/core/entities/Participant.ts#L155)
