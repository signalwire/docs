---
title: "updateMeta"
slug: /reference/participant/update-meta
description: "Merges values into custom metadata (unlike setMeta which replaces)."
max-toc-depth: 3
---

```ts
updateMeta(_meta): Promise<void>
```
Merges values into custom metadata (unlike [setMeta](#setmeta) which replaces).

<Warning>
**Not yet implemented in v4.** This method is on the API surface and will throw when called.
</Warning>

## **Parameters**

<ParamField path="_meta" type="Record<string, unknown>" required toc={true}>
  Metadata to merge.
</ParamField>

## **Returns**

`Promise<void>`

## **Throws**

Throws unconditionally — implementation pending.

## **Examples**

```ts
await participant.updateMeta(_meta);
```
