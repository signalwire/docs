---
hide_title: false
slug: /swml/methods/ai/prompt
title: ai.prompt
description: Establish the set of rules and instructions for the AI agent through a prompt.
---

[contexts]: /swml/methods/ai/prompt/contexts
[pom]: /swml/methods/ai/prompt/pom
[prompt-parameters]: #prompt-parameters
[Prompting Best Practices]: /ai/guides/best-practices#crafting-the-initial-prompt-for-the-ai

Defines the AI agent's personality, goals, behaviors, and instructions for handling conversations.
The prompt establishes how the agent should interact with callers, what information it should gather,
and how it should respond to various scenarios.

It is recommended to write prompts using markdown formatting as LLMs better understand structured content.
Additionally it is recommended to read the [Prompting Best Practices][Prompting Best Practices] guide.

**ai.prompt** (type: object)
  An object that contains the [`prompt parameters`][prompt-parameters].

## **prompt Parameters**

The `prompt` property accepts one of the following objects:

<Tabs>
    <Tab title="Regular Prompt">
        <RegularPrompt />
        <SharedPromptParams />
    </Tab>
    <Tab title="POM Prompts">
        <PomPrompts />
        <SharedPromptParams />
    </Tab>
</Tabs>

## **Variable Expansion**

Use the following syntax to expand variables into your prompt.

**${call_direction}** (type: string)
  Inbound or outbound.

**${caller_id_number}** (type: string)
  The caller ID number.

**${local_date}** (type: string)
  The local date.

**${spoken_date}** (type: string)
  The spoken date.

**${local_time}** (type: string)
  The local time.

**${time_of_day}** (type: string)
  The time of day.

**${supported_languages}** (type: string)
  A list of supported languages.

**${default_language}** (type: string)
  The default language.

