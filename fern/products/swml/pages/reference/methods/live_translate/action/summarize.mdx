---
id: c7267ca6-d8ff-4b06-872d-24ce184b18f4
title: summarize
description: Summarize the live translation.
slug: /swml/methods/live_translate/action/summarize
---

Request an on-demand AI summary of the conversation while translation is still active.

This action is designed for use on active calls that have an existing translation session running.
You can send this action via the [Call Commands REST API](/rest/signalwire-rest/endpoints/calling/call-commands),
or include it in a SWML section executed via [`transfer`](/swml/methods/transfer) or [`execute`](/swml/methods/execute) during a call.

<Tip>
Create a summarization at the start of the translation session
If you want automatic summarization when the session ends instead,
use `ai_summary: true` in the [`start`](/swml/methods/live_translate/action/start) action.
</Tip>

<ParamField path="action.summarize" type="object" required={true}>
  An object that accepts the following properties.
</ParamField>

## **Properties**

<ParamField path="summarize.webhook" type="string">
  The webhook URI to be called. Authentication can also be set in the url in the format of `username:password@url`.
</ParamField>

<ParamField path="summarize.prompt" type="string">
  The AI prompt that instructs the AI model how to summarize the conversation.
  This guides the style and content of the generated summary.
</ParamField>

## **Object Example**

<CodeBlocks>
<CodeBlock title="YAML">
```yaml
live_translate:
  action:
    summarize:
      webhook: 'https://example.com/webhook'
      prompt: Summarize the key points of this conversation.
```
</CodeBlock>
<CodeBlock title="JSON">
```json
{
  "live_translate": {
    "action": {
      "summarize": {
        "webhook": "https://example.com/webhook",
        "prompt": "Summarize the key points of this conversation."
      }
    }
  }
}
```
</CodeBlock>
</CodeBlocks>

## **Default Summarization**

If the `summarize` action is called with a empty object, the default summarization prompt and webhook will be used.

<CodeBlocks>
<CodeBlock title="YAML">
```yaml
live_translate:
  action:
    summarize: {}
```
</CodeBlock>
<CodeBlock title="JSON">
```json
{
  "live_translate": {
    "action": {
      "summarize": {}
    }
  }
}
```
</CodeBlock>
</CodeBlocks>

