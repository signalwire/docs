---
title: Schema Component Test
subtitle: Testing auto-generated SWML documentation from the schema
slug: /swml/schema-test
---

This page demonstrates using the Fern `<Schema>` component to auto-generate 
SWML documentation from the TypeSpec-defined SWML schema.

## SWMLObject

The top-level SWML document object:

<Schema type="SWMLObject" />

## AI Method

The `ai` method configuration:

<Schema type="AI" />

## AIObject

The full AI object with all nested properties:

<Schema type="AIObject" />

## Answer Method

The `answer` method:

<Schema type="Answer" />

## Connect Method

The `connect` method:

<Schema type="Connect" />

## Play Method

The `play` method:

<Schema type="Play" />

## Prompt Method

The `prompt` method:

<Schema type="Prompt" />
