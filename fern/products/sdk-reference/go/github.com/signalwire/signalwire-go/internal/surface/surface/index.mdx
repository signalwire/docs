---
auto_generated: true
kind: "module"
language: "go"
qualified_name: "github.com/signalwire/signalwire-go/internal/surface"
module: "github.com.signalwire.signalwire-go.internal.surface"
slug: "/sdk-reference/go/github.com/signalwire/signalwire-go/internal/surface/surface"
title: "surface"
sdk_label: "Go SDK"
icon: "golang"
---
# `surface`

Package surface holds the lookup tables that map Go SDK types and functions to their Python-canonical reference names. Both cmd/enumerate-surface (names-only) and cmd/enumerate-signatures (full signatures) consume these tables.

Package surface holds the lookup tables that map Go SDK types and
functions to their Python-canonical reference names. Both
cmd/enumerate-surface (names-only) and cmd/enumerate-signatures
(full signatures) consume these tables.

## Signature

```go
package surface
```

## Constants

<ParamField path="FactoryInit" type="FactoryInit">
  factoryInit maps a Go factory function (not a ``New<Struct>`` that matches
its struct name) to the class whose ``__init__`` it satisfies.  Example:
``datamap.New`` constructs ``DataMap`` — lift it into the __init__ slot.
</ParamField>
<ParamField path="FreeFnTable" type="FreeFnTable">
  freeFnTable maps a Go ``<shortPkg>.<FuncName>`` to a Python module-level
function.  Only used for genuinely free-standing functions — factory
constructors (``New<Struct>``) that should become ``__init__`` are
declared via ``factoryInit`` instead.
</ParamField>
<ParamField path="StructTable" type="StructTable">
  structTable maps a Go ``<shortPkg>.<StructName>`` to one or more Python
class targets.  Unmapped structs are treated as port-only extensions
(they must appear in PORT_ADDITIONS.md to avoid drift, but because we
simply don't emit them they are silently dropped).
</ParamField>

## Classes


<CardGroup cols={2}>
  <Card title="ClassTarget" href="/sdk-reference/go/github.com/signalwire/signalwire-go/internal/surface/surface/class-target">
    classTarget is one Python class destination for a Go struct's methods. Each (pyModule, pyClass) pair is created exactly once; methods accumulate across multiple mappings that share the same target.
  </Card>
</CardGroup>
