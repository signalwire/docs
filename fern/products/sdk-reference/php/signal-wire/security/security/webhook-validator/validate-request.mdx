---
auto_generated: true
kind: "method"
language: "php"
qualified_name: "SignalWire\\Security\\WebhookValidator::validateRequest"
parent: "SignalWire\\Security\\WebhookValidator"
module: "SignalWire.Security"
slug: "/reference/php/signal-wire/security/security/webhook-validator/validate-request"
source_url: "https://github.com/signalwire/signalwire-php/blob/main/src/SignalWire/Security/WebhookValidator.php"
visibility: "public"
title: "validateRequest"
sdk_label: "PHP SDK"
icon: "php"
---
# `validateRequest`

Legacy @signalwire/compatibility-api drop-in entry point.

If $paramsOrRawBody is a string, delegates to validateWebhookSignature
(Scheme A then Scheme B with parsed form). If it is an array, treats
it as pre-parsed form params and runs Scheme B directly (with URL
port normalisation).

**Throws:**
- `InvalidArgumentException` — When $signingKey is missing or the
                                 4th argument has an invalid type.

## Signature

```php
static public function validateRequest(
    string $signingKey,
    string $signature,
    string $url,
    mixed $paramsOrRawBody
): bool
```

## Parameters

<ParamField path="signingKey" type="string" required>
  Customer's Signing Key.
</ParamField>
<ParamField path="signature" type="string" required>
  Header value. Empty returns false.
</ParamField>
<ParamField path="url" type="string" required>
  Full URL SignalWire POSTed to.
</ParamField>
<ParamField path="paramsOrRawBody" type="mixed" required>
  Raw body string OR
                           pre-parsed form params (associative or list-of-
                           [key, value] pairs).
</ParamField>

## Returns

`bool` — True on match, false otherwise.

## Source

[`src/SignalWire/Security/WebhookValidator.php`](https://github.com/signalwire/signalwire-php/blob/main/src/SignalWire/Security/WebhookValidator.php)

Line 118.
