---
auto_generated: true
kind: "method"
language: "ruby"
qualified_name: "SignalWire::Security::WebhookValidator._candidate_urls"
parent: "SignalWire::Security::WebhookValidator"
module: "SignalWire.Security.WebhookValidator"
slug: "/sdk-reference/ruby/signal-wire/security/webhook-validator/candidate-urls"
source_url: "https://github.com/signalwire/signalwire-ruby/blob/main/lib/signalwire/security/webhook_validator.rb"
visibility: "public"
title: "_candidate_urls"
sdk_label: "Ruby SDK"
icon: "ruby"
---
# `_candidate_urls`

Return the URL variants to try for Scheme B port normalization.

- If the URL already has a non-standard port: just the input URL.
- If https + no port: input URL AND url with ``:443``.
- If http + no port: input URL AND url with ``:80``.
- If https + ``:443`` / http + ``:80``: input URL AND url without port.
- Otherwise (any explicit non-standard port): just the input URL.

## Signature

```ruby
def _candidate_urls(url)
```

## Parameters

<ParamField path="url" required></ParamField>

## Source

[`lib/signalwire/security/webhook_validator.rb`](https://github.com/signalwire/signalwire-ruby/blob/main/lib/signalwire/security/webhook_validator.rb)

Line 235.
