---
auto_generated: true
kind: "method"
language: "java"
qualified_name: "com.signalwire.sdk.agent.AgentBase.detectServerlessBaseUrl"
parent: "com.signalwire.sdk.agent.AgentBase"
module: "com.signalwire.sdk.agent.AgentBase"
slug: "/reference/java/com/signalwire/sdk/agent/agent/agent-base/detect-serverless-base-url__2"
source_url: "https://github.com/signalwire/signalwire-java/blob/main/src/main/java/com/signalwire/sdk/agent/AgentBase.java"
visibility: "public"
title: "detectServerlessBaseUrl"
sdk_label: "Java SDK"
icon: "java"
---
# `detectServerlessBaseUrl`

Resolve a base URL from the supplied `EnvProvider` (proxy or
 serverless platform).

Returns `null` if no suitable env vars
 are set.

 &lt;p>The `env`-aware overload is required by tools that cannot
 mutate the real process environment (such as the --simulate-serverless harness in the CLI). Those callers build a
 layered `EnvProvider` that overlays simulated values on top
 of the real environment and pass it through here so the serverless
 URL detection matches what would happen at runtime on the target
 platform.

 &lt;p>If the instance has a `proxyUrlBase` set (from the real
 env at build time or an explicit `#manualSetProxyUrl(String)`
 call), that wins \u2014 the injected env is only consulted for detection
 and Lambda URL synthesis.

## Signature

```java
public java.lang.String detectServerlessBaseUrl(
    com.signalwire.sdk.runtime.EnvProvider env
)
```

## Parameters

<ParamField path="env" type="com.signalwire.sdk.runtime.EnvProvider" required>
  environment variable source.
</ParamField>

## Returns

`java.lang.String` — base URL, or `null`.

## Source

[`src/main/java/com/signalwire/sdk/agent/AgentBase.java`](https://github.com/signalwire/signalwire-java/blob/main/src/main/java/com/signalwire/sdk/agent/AgentBase.java)

Line 1851.
