---
slug: "/reference/java/agents"
title: "agents"
sdk_label: "Java SDK"
icon: "java"
lustri:
  auto_generated: true
  kind: "module"
  language: "java"
  qualified_name: "agents"
---
# `agents`

## Classes

<CardGroup cols={2}>
  <Card title="agent" href="/docs/sdk-reference/reference/java/agents/agent">
    Base class for all SignalWire AI agents. Composes prompt management, tool registration, AI config, HTTP serving, skills integration, and SWML rendering. Use the builder pattern: &lt;pre> var agent = AgentBase.builder() .name("my-agent") .route("/") .port(3000) .build(); &lt;/pre>
  </Card>

  <Card title="cli" href="/docs/sdk-reference/reference/java/agents/cli">
    CLI tool for testing SWAIG functions against an agent. &lt;p> Uses only JDK classes (java.net.http.HttpClient) -- no external dependencies. &lt;p> Two modes: &lt;ul> &lt;li>&lt;b>URL mode&lt;/b> — hit a running agent HTTP server.&lt;/li> &lt;li>&lt;b>Simulation mode&lt;/b> — \<platform> loads the agent class directly and routes invocation through the matching serverless adapter (e.g.
  </Card>

  <Card title="datamap" href="/docs/sdk-reference/reference/java/agents/datamap">
    Fluent builder for creating SWAIG data\_map configurations. &lt;p> DataMap tools execute on SignalWire servers without requiring webhook endpoints. Supports variable expansion: `${args.param`\}, `${response.field`\}, `${global_data.key`\}, `${foreach.item`\}. &lt;p> All methods return `this` for fluent chaining.
  </Card>

  <Card title="runtime" href="/docs/sdk-reference/reference/java/agents/runtime">
    Resolve the base URL of a SignalWire agent running as an AWS Lambda function, from standard Lambda environment variables. &lt;p>Precedence — highest first: &lt;ol> &lt;li>`AWS_LAMBDA_FUNCTION_URL` — the Function URL explicitly assigned to this Lambda (the only variant that is guaranteed correct; synthetic fallbacks below assume the Function URL service was used and the function name matches the public subdomain, which is typically true but not guaranteed).&lt;/li> &lt;li>Synthesised `https://{AWS_LAMBDA_FUNCTION_NAME`.lambda-url.\{AWS\_REGION\}.on.aws\} — built when the Function URL env var is not present but the standard Lambda identity variables are.&lt;/li> &lt;/ol> &lt;p>This class returns a bare origin (scheme + host).
  </Card>

  <Card title="server" href="/docs/sdk-reference/reference/java/agents/server">
    Multi-agent hosting server. Registers multiple agents on different routes and dispatches requests accordingly.
  </Card>
</CardGroup>

## Modules

<CardGroup cols={2}>
  <Card title="contexts" href="/docs/sdk-reference/reference/java/agents/contexts" />

  <Card title="pom" href="/docs/sdk-reference/reference/java/agents/pom" />

  <Card title="prefabs" href="/docs/sdk-reference/reference/java/agents/prefabs" />

  <Card title="skills" href="/docs/sdk-reference/reference/java/agents/skills" />

  <Card title="swaig" href="/docs/sdk-reference/reference/java/agents/swaig" />

  <Card title="swml" href="/docs/sdk-reference/reference/java/agents/swml" />

  <Card title="tools" href="/docs/sdk-reference/reference/java/agents/tools" />
</CardGroup>
