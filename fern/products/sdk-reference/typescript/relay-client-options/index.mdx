---
auto_generated: true
kind: "interface"
language: "typescript"
qualified_name: "RelayClientOptions"
slug: "/reference/typescript/relay-client-options"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/types.ts"
title: "RelayClientOptions"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `RelayClientOptions`

Options for constructing a RelayClient.

## Signature

```typescript
interface RelayClientOptions
```

## Properties

<ParamField path="contexts" type="string[]">
  Contexts (topics) to receive inbound calls/messages on.
</ParamField>
<ParamField path="host" type="string">
  RELAY host. Defaults to relay.signalwire.com.
</ParamField>
<ParamField path="jwtToken" type="string">
  JWT token for authentication. Defaults to env SIGNALWIRE_JWT_TOKEN.
</ParamField>
<ParamField path="maxActiveCalls" type="number">
  Maximum number of concurrent active calls. Defaults to env RELAY_MAX_ACTIVE_CALLS or 1000.
</ParamField>
<ParamField path="project" type="string">
  SignalWire project ID. Defaults to env SIGNALWIRE_PROJECT_ID.
</ParamField>
<ParamField path="scheme" type="&quot;ws&quot; | &quot;wss&quot;">
  WebSocket scheme — `'wss'` (production, the default) or `'ws'`
(loopback fixtures and local audit harnesses). Reads from
`SIGNALWIRE_RELAY_SCHEME` if not explicitly set; defaults to `'wss'`.

Production deployments should never pass `'ws'` — the value exists
solely so the porting-sdk's `audit_relay_handshake.py` can drive the
real client against a plain-WS loopback fixture without standing up
TLS termination.
</ParamField>
<ParamField path="token" type="string">
  SignalWire API token. Defaults to env SIGNALWIRE_API_TOKEN.
</ParamField>

## Source

[`src/relay/types.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/types.ts)

Line 10.
