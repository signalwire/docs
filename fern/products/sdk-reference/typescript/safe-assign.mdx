---
auto_generated: true
kind: "function"
language: "typescript"
qualified_name: "safeAssign"
slug: "/reference/typescript/safe-assign"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/SecurityUtils.ts"
title: "safeAssign"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `safeAssign`

Copy properties from `source` to `target`, filtering out prototype-pollution keys.
Drop-in replacement for `Object.assign(target, source)` where `source` is untrusted.

## Signature

```typescript
safeAssign<T>(target: T, source: Record<string, unknown>): T
```

## Type Parameters

<ParamField path="T" type="Record<string, unknown>" required></ParamField>

## Parameters

<ParamField path="target" type="T" required>
  The object to assign into.
</ParamField>
<ParamField path="source" type="Record<string, unknown>" required>
  The object to copy properties from.
</ParamField>

## Returns

`T` — The target object.

## Source

[`src/SecurityUtils.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/SecurityUtils.ts)

Line 24.
