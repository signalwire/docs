---
auto_generated: true
kind: "interface"
language: "typescript"
qualified_name: "SwmlBuilderOptions"
slug: "/sdk-reference/typescript/swml-builder-options"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/SwmlBuilder.ts"
title: "SwmlBuilderOptions"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `SwmlBuilderOptions`

Options for constructing a SwmlBuilder.

## Signature

```typescript
interface SwmlBuilderOptions
```

## Properties

<ParamField path="enableValidation" type="boolean">
  When false, disables verb schema validation.
Defaults to true unless `SWML_SKIP_SCHEMA_VALIDATION=true` is set in the environment.
</ParamField>
<ParamField path="initialDocument" type="{ sections?: Record<string, unknown[]>; version?: string }">
  An initial SWML document to seed the builder with, enabling document injection.
When provided, the builder uses this document instead of creating an empty one.
This mirrors the Python SDK's pattern of injecting an SWMLService instance.
</ParamField>
<ParamField path="schemaPath" type="string">
  Optional path to a custom SWML schema JSON file. When set, the builder uses
a per-instance SchemaUtils loaded from this path instead of the bundled schema.
Mirrors Python's `schema_path` constructor parameter on `SWMLService`/`AgentBase`.
</ParamField>

## Source

[`src/SwmlBuilder.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/SwmlBuilder.ts)

Line 18.
