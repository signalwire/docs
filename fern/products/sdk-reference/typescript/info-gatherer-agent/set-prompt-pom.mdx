---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "InfoGathererAgent.setPromptPom"
parent: "InfoGathererAgent"
slug: "/sdk-reference/typescript/info-gatherer-agent/set-prompt-pom"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/AgentBase.ts"
title: "setPromptPom"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `setPromptPom`

Set the prompt as a POM (Prompt Object Model) dictionary.

Replaces the current POM sections with the provided structured data.
Each entry should have `title`, and optionally `body`, `bullets`,
`numbered`, `numberedBullets`, and `subsections`.

**Throws:**
- Error if POM mode is not enabled (`usePom: false`).

## Signature

```typescript
setPromptPom(pom: Record<string, unknown>[]): this
```

## Parameters

<ParamField path="pom" type="Record<string, unknown>[]" required>
  Array of POM section dictionaries.
</ParamField>

## Returns

`this` — This agent instance for chaining.

## Source

[`src/AgentBase.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/AgentBase.ts)

Line 576.
