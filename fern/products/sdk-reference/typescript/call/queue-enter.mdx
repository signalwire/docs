---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.queueEnter"
parent: "Call"
slug: "/reference/typescript/call/queue-enter"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "queueEnter"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `queueEnter`

Place the call into a named queue.

**Throws:**
- When the queue.enter command is rejected.

## Signature

```typescript
queueEnter(
  queueName: string,
  options: { ...2 fields } = {}
): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="queueName" type="string" required>
  Name of the queue to enter.
</ParamField>
<ParamField path="options" type="object" required default="{}">
  Queue-entry behaviour.
</ParamField>
<Indent>
  <ParamField path="options.controlId" type="string">
    Explicit control ID. Auto-generated when omitted.
  </ParamField>
  <ParamField path="options.statusUrl" type="string">
    Webhook URL for queue status events.
  </ParamField>
</Indent>

## Returns

`Promise<Record<string, unknown>>` — The platform's queue.enter response.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 1234.
