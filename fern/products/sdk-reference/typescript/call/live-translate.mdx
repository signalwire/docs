---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.liveTranslate"
parent: "Call"
slug: "/reference/typescript/call/live-translate"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "liveTranslate"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `liveTranslate`

Start or stop live translation on the call.

**Throws:**
- When the live_translate command is rejected.

## Signature

```typescript
liveTranslate(
  action: Record<string, unknown>,
  options: { ...1 fields } = {}
): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="action" type="Record<string, unknown>" required>
  Platform-shaped action block (`start` or `stop` plus
source/target languages).
</ParamField>
<ParamField path="options" type="object" required default="{}">
  Live-translate behaviour.
</ParamField>
<Indent>
  <ParamField path="options.statusUrl" type="string">
    Webhook URL for translation status events.
  </ParamField>
</Indent>

## Returns

`Promise<Record<string, unknown>>` — The platform's live_translate response.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 1037.
