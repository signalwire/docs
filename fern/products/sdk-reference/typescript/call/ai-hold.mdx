---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.aiHold"
parent: "Call"
slug: "/sdk-reference/typescript/call/ai-hold"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "aiHold"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `aiHold`

Put the AI agent session on hold (pause turn-taking).

**Throws:**
- When the ai_hold command is rejected.

## Signature

```typescript
aiHold(options: { ...2 fields } = {}): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="options" type="object" required default="{}">
  Hold behaviour.
</ParamField>
<Indent>
  <ParamField path="options.prompt" type="string">
    Prompt played to the caller during the hold.
  </ParamField>
  <ParamField path="options.timeout" type="string">
    Maximum hold duration.
  </ParamField>
</Indent>

## Returns

`Promise<Record<string, unknown>>` — The platform's ai_hold response.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 1185.
