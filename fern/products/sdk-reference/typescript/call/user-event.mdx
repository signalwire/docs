---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.userEvent"
parent: "Call"
slug: "/reference/typescript/call/user-event"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "userEvent"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `userEvent`

Emit a custom user-defined event on the call for your webhooks.

**Throws:**
- When the user_event command is rejected.

## Signature

```typescript
userEvent(
  options: { event?: string } & Record<string, unknown> = {}
): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="options" type="{ event?: string } & Record<string, unknown>" required default="{}">
  Freeform event payload. Set `options.event` for the
event name and include any additional fields your webhook expects.
</ParamField>

## Returns

`Promise<Record<string, unknown>>` — The platform's user_event response.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 1216.
