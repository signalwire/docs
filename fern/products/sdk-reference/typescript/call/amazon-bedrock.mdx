---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.amazonBedrock"
parent: "Call"
slug: "/reference/typescript/call/amazon-bedrock"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "amazonBedrock"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `amazonBedrock`

Connect the call to an Amazon Bedrock AI agent.

## Signature

```typescript
amazonBedrock(options: { ...6 fields } = {}): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="options" type="object" required default="{}">
  Bedrock agent configuration.
</ParamField>
<Indent>
  <ParamField path="options.aiParams" type="Record<string, unknown>">
    AI engine parameters.
  </ParamField>
  <ParamField path="options.globalData" type="Record<string, unknown>">
    Global-data object available to SWAIG tools.
  </ParamField>
  <ParamField path="options.postPrompt" type="Record<string, unknown>">
    Post-prompt configuration.
  </ParamField>
  <ParamField path="options.postPromptUrl" type="string">
    URL to POST the final summary to.
  </ParamField>
  <ParamField path="options.prompt" type="unknown">
    Prompt to send to the Bedrock agent.
  </ParamField>
  <ParamField path="options.SWAIG" type="Record<string, unknown>">
    SWAIG configuration (functions, includes, etc.).
  </ParamField>
</Indent>

## Returns

`Promise<Record<string, unknown>>` — The platform's amazon_bedrock response.

## Throws

- When the amazon_bedrock command is rejected.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 1133.
