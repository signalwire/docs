---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.liveTranscribe"
parent: "Call"
slug: "/reference/typescript/call/live-transcribe"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "liveTranscribe"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `liveTranscribe`

Start or stop live transcription on the call.

## Signature

```typescript
liveTranscribe(
  action: Record<string, unknown>,
  extra?: Record<string, unknown>
): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="action" type="Record<string, unknown>" required>
  Platform-shaped action block (`start` or `stop` plus
configuration).
</ParamField>
<ParamField path="extra" type="Record<string, unknown>">
  Optional additional params merged into the request.
</ParamField>

## Returns

`Promise<Record<string, unknown>>` — The platform's live_transcribe response.

## Throws

- When the live_transcribe command is rejected.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 1023.
