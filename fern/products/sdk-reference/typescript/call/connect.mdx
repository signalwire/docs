---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.connect"
parent: "Call"
slug: "/sdk-reference/typescript/call/connect"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "connect"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `connect`

Bridge the call to one or more destinations.

**Throws:**
- When the connect command is rejected.

## Signature

```typescript
connect(
  devices: Record<string, unknown>[][],
  options: { ...5 fields } = {}
): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="devices" type="Record<string, unknown>[][]" required>
  Serial/parallel dial plan — outer array of serial groups,
inner arrays dialled in parallel.
</ParamField>
<ParamField path="options" type="object" required default="{}">
  Connect behaviour.
</ParamField>
<Indent>
  <ParamField path="options.maxDuration" type="number">
    Maximum connect duration in seconds.
  </ParamField>
  <ParamField path="options.maxPricePerMinute" type="number">
    Price cap per minute.
  </ParamField>
  <ParamField path="options.ringback" type="Record<string, unknown>[]">
    Ringback media played to the originating party.
  </ParamField>
  <ParamField path="options.statusUrl" type="string">
    Webhook URL for status events.
  </ParamField>
  <ParamField path="options.tag" type="string">
    Tag for event correlation.
  </ParamField>
</Indent>

## Returns

`Promise<Record<string, unknown>>` — The platform's connect response.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 509.
