---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.stream"
parent: "Call"
slug: "/reference/typescript/call/stream"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "stream"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `stream`

Start streaming call audio to a WebSocket endpoint.

## Signature

```typescript
stream(url: string, options: { ...9 fields } = {}): Promise<StreamAction>
```

## Parameters

<ParamField path="url" type="string" required>
  WebSocket URL to stream audio to.
</ParamField>
<ParamField path="options" type="object" required default="{}">
  Stream behaviour.
</ParamField>
<Indent>
  <ParamField path="options.authorizationBearerToken" type="string">
    Bearer token sent to the stream endpoint.
  </ParamField>
  <ParamField path="options.codec" type="string">
    Audio codec (e.g. `"PCMU"`, `"PCMA"`).
  </ParamField>
  <ParamField path="options.controlId" type="string">
    Explicit control ID. Auto-generated when omitted.
  </ParamField>
  <ParamField path="options.customParameters" type="Record<string, unknown>">
    Extra parameters forwarded to the stream endpoint.
  </ParamField>
  <ParamField path="options.name" type="string">
    Friendly name for the stream.
  </ParamField>
  <ParamField path="options.onCompleted" type="CompletedCallback">
    Callback fired when the stream completes.
  </ParamField>
  <ParamField path="options.statusUrl" type="string">
    Webhook URL for stream status events.
  </ParamField>
  <ParamField path="options.statusUrlMethod" type="string">
    HTTP method for `statusUrl` requests.
  </ParamField>
  <ParamField path="options.track" type="string">
    Which track to send: `"inbound"`, `"outbound"`, or
`"both"`.
  </ParamField>
</Indent>

## Returns

`Promise<StreamAction>` — A [StreamAction](/reference/typescript/stream-action) for control and completion tracking.

## Throws

- When the stream command is rejected.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 766.
