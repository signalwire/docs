---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.record"
parent: "Call"
slug: "/sdk-reference/typescript/call/record"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "record"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `record`

Record audio from the call.

**Throws:**
- When the record command is rejected.

## Signature

```typescript
record(
  audio?: Record<string, unknown>,
  options: { ...2 fields } = {}
): Promise<RecordAction>
```

## Parameters

<ParamField path="audio" type="Record<string, unknown>">
  Platform-shaped audio recording options (direction, codec,
timeouts, etc.). Defaults to `{}`.
</ParamField>
<ParamField path="options" type="object" required default="{}">
  Recording behaviour.
</ParamField>
<Indent>
  <ParamField path="options.controlId" type="string">
    Explicit control ID. Auto-generated when omitted.
  </ParamField>
  <ParamField path="options.onCompleted" type="CompletedCallback">
    Callback fired when the recording reaches a
terminal state.
  </ParamField>
</Indent>

## Returns

`Promise<RecordAction>` — A [RecordAction](/sdk-reference/typescript/record-action) for control and completion tracking.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 403.
