---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.transfer"
parent: "Call"
slug: "/reference/typescript/call/transfer"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "transfer"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `transfer`

Transfer call control to another RELAY app or SWML script.

**Throws:**
- When the transfer command is rejected.

## Signature

```typescript
transfer(
  dest: string,
  extra?: Record<string, unknown>
): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="dest" type="string" required>
  Destination identifier (RELAY context, SWML URL, etc.).
</ParamField>
<ParamField path="extra" type="Record<string, unknown>">
  Optional additional params merged into the transfer request.
</ParamField>

## Returns

`Promise<Record<string, unknown>>` — The platform's transfer response.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 803.
