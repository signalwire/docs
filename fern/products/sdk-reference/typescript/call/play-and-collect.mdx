---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.playAndCollect"
parent: "Call"
slug: "/reference/typescript/call/play-and-collect"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "playAndCollect"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `playAndCollect`

Play audio and collect digit / speech input in a single operation.

Convenient for IVR prompts — playback stops as soon as input is received.

**Throws:**
- When the play_and_collect command is rejected.

## Signature

```typescript
playAndCollect(
  media: Record<string, unknown>[],
  collect: Record<string, unknown>,
  options: { ...3 fields } = {}
): Promise<CollectAction>
```

## Parameters

<ParamField path="media" type="Record<string, unknown>[]" required>
  Audio items to play while collecting.
</ParamField>
<ParamField path="collect" type="Record<string, unknown>" required>
  Platform-shaped collect config (`digits`, `speech`, etc.).
</ParamField>
<ParamField path="options" type="object" required default="{}">
  Playback and collection behaviour.
</ParamField>
<Indent>
  <ParamField path="options.controlId" type="string">
    Explicit control ID. Auto-generated when omitted.
  </ParamField>
  <ParamField path="options.onCompleted" type="CompletedCallback">
    Callback fired when collect completes.
  </ParamField>
  <ParamField path="options.volume" type="number">
    Playback volume adjustment in dB.
  </ParamField>
</Indent>

## Returns

`Promise<CollectAction>` — A [CollectAction](/reference/typescript/collect-action) for the combined play-and-collect flow.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 433.
