---
auto_generated: true
kind: "method"
language: "typescript"
qualified_name: "Call.aiMessage"
parent: "Call"
slug: "/sdk-reference/typescript/call/ai-message"
source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
title: "aiMessage"
sdk_label: "TypeScript SDK"
icon: "typescript"
---
# `aiMessage`

Send a message into an active AI agent session.

**Throws:**
- When the ai_message command is rejected.

## Signature

```typescript
aiMessage(options: { ...4 fields } = {}): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="options" type="object" required default="{}">
  Message parameters.
</ParamField>
<Indent>
  <ParamField path="options.globalData" type="Record<string, unknown>">
    Global-data updates to merge.
  </ParamField>
  <ParamField path="options.messageText" type="string">
    Text content to inject into the conversation.
  </ParamField>
  <ParamField path="options.reset" type="Record<string, unknown>">
    Reset directives for the conversation state.
  </ParamField>
  <ParamField path="options.role" type="string">
    Speaker role (`"system"`, `"user"`, or `"assistant"`).
  </ParamField>
</Indent>

## Returns

`Promise<Record<string, unknown>>` — The platform's ai_message response.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 1162.
