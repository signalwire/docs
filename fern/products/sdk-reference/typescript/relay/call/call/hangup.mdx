---
slug: "/reference/typescript/relay/call/call/hangup"
title: "hangup"
sdk_label: "TypeScript SDK"
icon: "typescript"
lustri:
  auto_generated: true
  kind: "method"
  language: "typescript"
  qualified_name: "relay.Call.Call.hangup"
  parent: "relay.Call.Call"
  module: "relay.Call"
  source_url: "https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts"
---
# `hangup`

End / hang up the call.

## Signature

```typescript
hangup(reason: string = 'hangup'): Promise<Record<string, unknown>>
```

## Parameters

<ParamField path="reason" type="string" required default="'hangup'">
  Hangup reason delivered to the platform. Defaults to `"hangup"`.
</ParamField>

## Returns

`Promise<Record<string, unknown>>` — The platform's end response.

## Throws

- When the end command is rejected.

## Source

[`src/relay/Call.ts`](https://github.com/signalwire/signalwire-typescript/blob/main/src/relay/Call.ts)

Line 330.
