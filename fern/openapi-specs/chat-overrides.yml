x-fern-base-path: /chat

components:
  securitySchemes:
    BasicAuth:
      type: http
      scheme: basic
      x-fern-auth:
        scheme: basic
        username:
          name: project_id
          env: SIGNALWIRE_PROJECT_ID
        password:
          name: token
          env: SIGNALWIRE_API_TOKEN

security:
  - BasicAuth: []

paths:
  /tokens:
    post:
      x-fern-sdk-group-name:
        - chatTokens
      x-fern-sdk-method-name: create_token
