fern/docs/specs
temp
node_modules
dist
.link-check
.serena
.claude/*
!.claude/rules/
!.claude/docs/
.vscode
.docs-review
.env
.env.*
!.env.example

.docs-review

# Internal process artifacts (superpowers / brainstorming specs)
docs/superpowers/
.superpowers/
.superdesign/

# Changelog pipeline: the collected PR data is large (~170KB/week) and fully
# regenerable with `yarn changelog:collect`. prompt.md and classified.json stay
# tracked — they are the audit trail for how each entry got its tier.
.github/changelog-state/batches/*/input.json
.github/changelog-state/cache/
