---
slug: /swml/methods/unset
title: unset
description: Unset specified variables.
tags: ['swml']
---

import APIField from "@site/src/components/APIField";

Unset specified variables. The variables have been set either using the [`set`](./set.mdx) command
or as a byproduct of some other statements or methods (like [`record`](/swml/methods/record#variables))

<APIField
  name="unset"
  type="string | string[]"
  required={true}
>
  The name of the variable to unset (as a string) or an array of variable names to unset.
</APIField>

## **Variable**

Any variable can be unset by this method.

## **Examples**

### Unset a single variable

```yaml andJson
version: 1.0.0
sections:
  main:
    - set:
        num_var: 1
    - play:
        url: 'say: The value of num_var is: ${num_var}.'
    - unset: num_var
    - play:
        url: 'say: The value of num_var is ${num_var}.'
```

### Unset multiple variables

```yaml andJson
version: 1.0.0
sections:
  main:
    - set:
        systems:
          hr:
            - tracy
            - luke
          engineering:
            john: absent
        name: john
    - play:
        url: 'say: ${systems.hr}'
    - unset:
        - systems
        - name
    # this play statement emits an error because `systems` is undefined
    # at this point so there's nothing for `play` to say.
    - play:
        url: 'say: ${systems}'
```

## **See Also**

- **[Variables and Expressions](/swml/variables)**: Complete reference for SWML variables, scopes, and syntax
- **[set](/swml/methods/set)**: Set variables in the script
