{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "SWAIGResponse.json",
    "type": "object",
    "properties": {
        "response": {
            "type": "string",
            "examples": [
                "Order 12345 shipped Monday and arrives Thursday."
            ],
            "description": "Text that will be added to the AI agent's context. Markdown is supported."
        },
        "action": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {},
                "unevaluatedProperties": {}
            },
            "description": "A list of SWML-compatible objects that are executed when the function returns, such as\n`set_meta_data`, `toggle_functions`, `transfer`, or an inline `SWML` document."
        }
    },
    "required": [
        "response"
    ],
    "unevaluatedProperties": {},
    "description": "The JSON document your server returns from a SWAIG function. `response` is added to the AI agent's\ncontext; `action` carries SWML-compatible objects that change what the call does next. The\naccepted action objects are documented in the\n[tool calling guide](/docs/platform/ai/tool-calling).",
    "title": "SWAIG webhook response"
}