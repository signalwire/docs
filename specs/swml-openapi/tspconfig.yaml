# SWML OpenAPI - generates OpenAPI spec from SWML types for Fern <Schema> component
extends: ../tspconfig.yaml
emit:
  - "@typespec/openapi3"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{cwd}/../../fern/apis/swml-schema"
    output-file: openapi.yaml
