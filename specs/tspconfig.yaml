# Root configuration with emitter options
# Child configs will extend this and specify which emitter to use
output-dir: "{project-root}/tsp-output"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{output-dir}/@typespec/openapi3"
    # Note: seal-object-schemas lives in each child config because TypeSpec's
    # `extends` shallow-merges the top-level `options` key — a child that
    # defines `options."@typespec/openapi3"` fully replaces the parent's,
    # so a setting here would be silently dropped.

  "@typespec/json-schema":
    emitter-output-dir: "{output-dir}/@typespec/json-schema"
    file-type: "json"
    seal-object-schemas: true
