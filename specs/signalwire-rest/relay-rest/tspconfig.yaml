# Relay REST API - outputs to fern/apis/relay-rest
extends: ../../tspconfig.yaml
emit:
  - "@typespec/openapi3"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{cwd}/../../../fern/apis/relay-rest"
    output-file: openapi.yaml
    openapi-versions:
      - "3.1.0"
