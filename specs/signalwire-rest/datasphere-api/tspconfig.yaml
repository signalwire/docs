# Datasphere API - outputs to fern/apis/datasphere
extends: ../../tspconfig.yaml
emit:
  - "@typespec/openapi3"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{cwd}/../../../fern/apis/datasphere"
    output-file: openapi.yaml
    openapi-versions:
      - "3.1.0"
