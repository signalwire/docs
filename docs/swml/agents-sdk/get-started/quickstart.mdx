---
slug: /sdks/agents-sdk/quickstart
---

import VenvScript from '/docs/_common/python-venv.mdx'

# Quickstart

<Subtitle>Agents SDK</Subtitle><br/><br/>

Get up and running quickly with the SignalWire AI Agents SDK. 
This section covers installation, basic setup, and your first AI agent implementation.

<Steps>

## Setup virtual environment

```bash
python -m venv .venv

# On macOS and some other Unix systems, use python3 for this step.
# After activating, `python` alone can be used.
```

Run the activation script corresponding to your shell:

<VenvScript/>

## Install the SDK
```bash
pip install signalwire-agents
```

## Create your first agent
```python title="app.py"
from signalwire_agents import AgentBase

# Create an agent
agent = AgentBase("My Assistant", route="/assistant")

# Add some basic capabilities
agent.add_skill("datetime")     # Current date/time info
agent.add_skill("math")         # Mathematical calculations

# Start the agent
agent.serve()
```

</Steps>

## What's next?

Now that you have a basic agent running, explore these advanced topics:

- **[Configuration guide](../configuration)**: Set up JSON configuration files and environment variable substitution
- **[Security guide](../security)**: Configure HTTPS, authentication, and production security
- **[CLI tools](../cli)**: Test and debug your agents with the command-line interface
