```js
import { SignalWire } from "@signalwire/realtime-api";

const client = await SignalWire({ project: "ProjectID Here", token: "Token Here" })

const chatClient = client.chat;

await chatClient.listen({
  channels: ["channel1"],
  onMessageReceived: async (message) => {
    console.log(message.content);
  }
});

await chatClient.publish({
        channel: "channel1",
        content: `Hello from SignalWire!`
    })
```

import UseCaseLinks from "@site/src/components/HomepageFeatures/UseCaseLinks"

<UseCaseLinks links={[{title:"📚 RELAY docs", link:"/sdks/reference/realtime-sdk/relay-v4"}, {title:"View the full guide →", link:"/guides"}]}/>