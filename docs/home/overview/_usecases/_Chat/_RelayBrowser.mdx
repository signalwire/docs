```js
import { Chat } from "@signalwire/js";

const chatClient = new Chat.Client({
  token: "<your_chat_token>", // get this from the REST APIs
});

await chatClient.subscribe(["mychannel1", "mychannel2"]);

chatClient.on("message", (message) => {
  console.log("Received", message.content,
              "on", message.channel,
              "at", message.publishedAt);
});

await chatClient.publish({
  channel: "mychannel1",
  content: `Hello from SignalWire!`,
});
```

import UseCaseLinks from "@site/src/components/HomepageFeatures/UseCaseLinks"

<UseCaseLinks links={[{title:"📚 RELAY docs", link:"/sdks/reference/browser-sdk/"}, {title:"View the full guide →", link:"/guides"}]}/>